---
source: tests/read_verbs.rs
expression: run.stdout
---
Member `45-governance` (spec) — everything that holds it in place:

Requirements it satisfies:
  • `governance` — "the home for the governance model"
      It means: "the corpus declares a governance model an architecture doc must satisfy".
      It is required — at least one member must fill it.

Governing floor: its `spec` kind binds the `spec` floor, whose clauses check it.

Edges out: it points at no member (it declares no resolved reference).
Edges in: no member points at it.
