---
source: tests/read_verbs.rs
expression: run.stdout
---
Member `dev-standards` (skill) — everything that holds it in place:

Requirements it satisfies:
  • `engineering-standards` — "the home for engineering-standards enforcement"
      It means: "the harness maintains development standards".
      It is required — at least one member must fill it.

Governing floor: its `skill` kind binds the `skill` floor, whose clauses check it.

Edges out: it points at no member (it declares no resolved reference).
Edges in (the resolved references that point at it):
  • `collaboration` (rule) points at it via its `routes_to` field
