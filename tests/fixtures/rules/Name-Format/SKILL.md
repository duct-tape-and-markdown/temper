---
name: Name-Format
description: Use when importing a harness; not for single files.
---
# Name format

The name carries uppercase letters, outside [a-z0-9-]. The directory is named
to match the name, so only skill.name-format fires.
