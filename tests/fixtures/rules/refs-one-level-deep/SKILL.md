---
name: refs-one-level-deep
description: Use when importing a harness; not for single files.
---
# Refs one level deep

Run the helper at `scripts/deep/tool.sh` first. The file exists, so it resolves,
but it sits two levels below SKILL.md.
