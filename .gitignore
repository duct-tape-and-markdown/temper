# Rust
/target
**/*.rs.bk

# Node (flume harness control plane only)
/node_modules

# Flume harness-managed state (not authored, not committed)
/.flume/worktrees/
/.flume/sessions/
/.flume/awake/
/.flume/prior-attempts/

# temper's own surface — generated by `import` from temper.toml + .claude/ + specs/.
# A derived mirror (the recursive dogfood); regenerate with
# `temper import . --into .temper`. Not committed until apply/surface-authority make
# the surface canonical (would otherwise duplicate .claude/ + specs/).
/.temper/
