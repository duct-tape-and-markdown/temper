{
  "name": "temper-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "The temper authoring face — typed module authoring for harness members, kinds, packages, and assemblies; emit compiles to the inert manifest the gate reads. (Publish name + scope pending the PACKAGING-CHANNELS ruling; private until then.)",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test \"dist/test/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
