/**
 * The one authored home of facts more than one surface states. A projection
 * may repeat a fact; authorship may not — every member that speaks one of
 * these interpolates the constant, so cross-surface contradiction is
 * unrepresentable rather than reviewed for.
 */

/** How the governed toy is exercised end to end. */
export const VERIFY_COMMAND = "node src/main.js TODO.md";

/** What the toy prints when TODO.md carries its committed contents. */
export const VERIFY_EXPECTED = "2/3 done";

/**
 * The structural gate over this corpus. The workspace argument is the
 * `.temper/` program directory — `temper check .` at the harness root
 * resolves no lock declarations and silently checks built-in kinds alone.
 */
export const GATE_COMMAND = "temper check .temper";
