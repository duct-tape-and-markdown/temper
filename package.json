{
  "name": "author-flume-harness",
  "version": "0.0.0",
  "private": true,
  "description": "flume control plane for building author. The product is Rust (see Cargo.toml); this manifest exists only to run the build harness.",
  "type": "module",
  "scripts": {
    "flume": "flume",
    "typecheck:chain": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@dtmd/flume": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
