# Plan state

- Spec derived through: 13455d4a — unchanged, copied forward (not this tick's job).
- Audited through: 53852595 — window 8feb4617..53852595 (10 src/tests/sdk-touching commits) reconciled. Every commit maps to a tag the dispatcher already dequeued (REQUIREMENT-KIND-CONSTRAINT-ENFORCEMENT, IDENTITY-MAP-REFUSING-CONSTRUCTOR, GUARD-SETTINGS-JSON-EMIT-OWNED-COVERAGE, PATH-ROOT-NEWTYPE-LOCK-SOURCE-PATH, BUILTIN-CONTRACT-MATRIX-SNAPSHOT, GUARD-POSTTOOLUSE-BASH-DRIFT-CHECK, EMIT-DISCOVERY-MEMBER-SET-ROUND-TRIP, GRAPH-SCOPE-WITNESS-GLOB-VOCABULARY) plus two already-swept fixes (e77766c5 AT-LOCUS, 017e59e0 skill mentionReachable); all verified present on disk. Nothing left to drop. `.flume/metrics.jsonl` is gitignored and absent from this worktree — glance skipped, not a blocker.
- Residue swept through: 53852595 — same window, no orphaned symbols/vocabulary. One live hypothesis chased to ground: b284dd80 (IDENTITY-MAP-REFUSING-CONSTRUCTOR) made `sdk/src/emit.ts`'s `memberTable` a refusing `uniqueMap` keyed by `kind:name`; this repo's own `.temper/lock.toml` carries two `hook`/`PostToolUse` registration rows, the exact shape that sank the earlier duplicate-key refusal (e4ff7bc6, parked as HOOK-COLLECTION-ADDRESS-DUPLICATE-REFUSAL). Read `sdk/src/declarations.ts`'s `tapHookRows`: it synthesizes registration rows only, never inserted into `harness.members`, so `memberTable` never sees the tap-hook side of the pair — confirmed empirically, `temper emit --dry-run` off a built SDK: 12 unchanged, 0 errors. No gap; nothing filed. (`.temper/package-lock.json` was touched by the `npm install` needed to run this check and restored via `git checkout --`.)
- Posture swept through: 22f8064c — unchanged, mid-rotation continues. Frozen frontier (armed at 7d695577, fixed regardless of later HEAD movement per the posture-sweep rule): src/read.rs (covered), src/telemetry.rs (covered), tests/read_verbs.rs (covered), src/admissibility.rs (covered), src/gate.rs (covered), src/graph.rs (covered this tick), tests/graph.rs (covered this tick), src/tap.rs, tests/tap.rs, tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts, tests/emit.rs, src/install.rs, tests/install.rs, src/compose.rs, src/drift.rs, src/glob.rs, src/placement.rs.
- This tick: posture sweep, one neighborhood — src/graph.rs (frontier) read in full together with tests/graph.rs (also frontier, its test file). Found: `uncontained`/`is_scope_contained_in_gate` (graph.rs:469-503, mention-reachable's subset-containment check) drive their witness synthesis through a ~250-line hand-rolled glob-syntax parser — `representative_paths_for_glob`/`process_glob_segment`/`expand_braces`/`concretize_segment`/`concretize_char_class`/`is_in_char_class` (graph.rs:514-745) — parsing braces and character classes independently of `globset`, which `src/glob.rs`'s own doc comment names as "the one glob-matching surface every caller shares" (CLAUDE.md's tech-stack pin). Traced provenance before filing: the subset-containment semantics itself is a deliberate, human-ruled change (GH #33, MENTION-REACHABLE-SUBSET-CONTAINMENT + GRAPH-SCOPE-WITNESS-GLOB-VOCABULARY, both plan-filed and shipped clean this window) — not drift — so only the parsing duplication is the finding, and it needs a design call (does `globset` expose enough to consolidate, or is this the engineering.md "pinned semantics" exception) rather than a mechanical fix. Filed `.flume/refactor/plan-graph-glob-witness-hand-roll.md` (observed at e8c160c3). Rest of graph.rs/tests/graph.rs read clean against engineering.md and architecture.md. Remaining frontier (uncovered): src/tap.rs, tests/tap.rs, tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts, tests/emit.rs, src/install.rs, tests/install.rs, src/compose.rs, src/drift.rs, src/glob.rs, src/placement.rs.
- Queue: 6 pending — 1 open, 3 parked, 2 deferred. Open forks: 5 (unchanged). Friction: 0. Amendments: 0. Inbox: 0. Refactor: 1 (plan-graph-glob-witness-hand-roll.md, filed this tick).

Plan continues: after-build — EMIT-BANNER-OWNERSHIP-MOVE is pickable now; the posture sweep's open rotation resumes once the wave hands back.
