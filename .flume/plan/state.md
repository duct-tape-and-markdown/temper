# Plan state

- Spec derived through: fcf719f4 — unchanged, copied forward; the spec delta
  is drained.
- Audited through: 24420d45 — unchanged; 24420d45..e0d0123b (two ship waves,
  four build commits) is unreconciled and is next tick's job.
- Residue swept through: 24420d45 — unchanged, same window.
- Posture swept through: tests/hook_kind.rs next — mid-rotation continues.
  Frozen frontier (armed at 7d695577): covered — src/read.rs,
  src/telemetry.rs, tests/read_verbs.rs, src/admissibility.rs, src/gate.rs,
  src/graph.rs, tests/graph.rs, src/tap.rs, tests/tap.rs; open —
  tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts,
  tests/emit.rs, src/install.rs, tests/install.rs, src/compose.rs,
  src/drift.rs, src/glob.rs, src/placement.rs.
- This tick: drained the one refactor capture — the two-writers claim over
  `.claude/settings.json` reproduced at HEAD, filed as
  INSTALL-GATE-HOOKS-RIDE-THE-PROGRAM, with the lift entry serialized behind
  it and its stale discovery-count premise re-measured.
- Queue: 18 pending — 4 open, 6 blockedBy, 3 deferred, 5 parked. Pickable: 4.
  Open forks: 14. Friction: 1 (human channel). Amendments: 0. Refactor: 0.
  Inbox: 0.

Plan continues: yes — the 24420d45..e0d0123b post-ship window is unreconciled.
