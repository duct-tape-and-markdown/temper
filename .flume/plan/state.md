# Plan state

- Spec derived through: abe5d5d
- Audited through: f369531
- Residue swept through: f369531
- This tick: RECONCILED the 420da04..f369531 window (6605bf5, a28e2c6,
  a7a8cc1), both motions, on disk — never the log. The three entries the
  window shipped were already dropped from pending by f369531; each was
  verified on disk anyway, because a dropped entry is a claim like any other:
  the root export face closes over `render`-hook and `kind()` types
  (`index.ts` 51-77), `bodyless` fences the four body-shaped predicates at
  admissibility, and `supporting-doc` ships as `skill`'s file-child layer
  (`builtins.ts:153`, `builtin_lock.toml:92`). **Both stale gates re-tested
  and both moved.** SUPPORTING-DOC-REACH-CLAUSE is **OPEN**: its last blocker
  (SKILL-NESTED-REFERENCE-DOCS) shipped, and a7a8cc1's own body names this
  entry as the owner of the one clause it declined to invent — the restraint
  worked as designed. EMBEDDED-EDGE-DEGREE-SEAM is **OPEN** and re-verified
  line by line: `declarations.ts` and `graph.rs` were untouched in the window,
  so all six cited anchors are live. SDK-FIXTURE-WIRING-ONE-HOME does **not**
  open — its gate re-points rather than lifts: `tests/builtin_lock_frozen.rs`
  is still contended, now by SUPPORTING-DOC-REACH-CLAUSE, which rewrites the
  frozen bytes. One ground, tested, not restated.
  **The sweep's whole yield is subtraction, and it is the point.** Three
  riders were named in the three shipped entries' own descriptions, and all
  three discharged — verified by `rg`, not by reading commit bodies:
  `builtins.ts`'s two dead `packages/{rule,memory}.anthropic/PACKAGE.md` cites
  are cut; `engine.rs` carries no `Facet` vocabulary; `builtin_kind.rs`'s
  cites are **re-verified and bumped** to 2026-07-16, not blind-bumped — the
  distinction IMPORT-HOP-CAP-CITE exists to enforce. So the **"Rust engine
  narration cites lag the SDK clause re-fetch" record is DELETED**:
  `builtin_kind.rs` was its one surviving surface, `graph.rs` had already left
  the class, and a record whose every surface is discharged is pure per-tick
  tax. Three records shrank with it (the `kinds/`+`packages/` debt is down to
  its `session_start.rs` half; the pack-is-a-skill condition is now stated
  against a landed built-in). Net: the fork board is ~2.4k chars lighter and
  every line in it is live. Three entries have now proven the same law —
  **name the rider in the entry, or it does not discharge** — and nine entries
  opened `builtins.ts` without doing so before one did.
  Closing checklist: queue disjoint over the two open entries
  (EMBEDDED-EDGE-DEGREE-SEAM: declarations.ts/graph.rs/emit.test.ts/
  tests/graph.rs; SUPPORTING-DOC-REACH-CLAUSE: builtins.ts/builtin_lock.toml/
  builtin_lock_frozen.rs/builtins.test.ts — no shared path); IMPORT-HOP-CAP-CITE
  shares graph.rs with the former but is parked, so no wave can pick both;
  both parks re-tested against disk (no version tag, crate 0.1.0 vs npm 0.0.7,
  release.yml:7-9 still defers darwin); line cites corrected where the window
  moved them (`builtin_lock_frozen.rs` 61→68, `gate_fail_loud.rs` 279→284);
  field lengths validated. Spec cursor copied forward verbatim — this tick
  derived no spec.
- Queue: 2 pickable (EMBEDDED-EDGE-DEGREE-SEAM, SUPPORTING-DOC-REACH-CLAUSE);
  1 gated behind them (SDK-FIXTURE-WIRING-ONE-HOME, on builtin_lock_frozen.rs
  contention); 2 parked on human acts (IMPORT-HOP-CAP-CITE: a hop-depth probe.
  PACKAGING-CHANNELS-REMAINDER: Apple notarizing + the v0.1 tag). No gate is
  stale — every one was tested this tick.

Plan continues: no — the window is reconciled on both motions, the inbox is at
its header, and the spec delta is empty. Build takes over on two pickable
entries. The two parked entries need John, and IMPORT-HOP-CAP-CITE is still
the one worth surfacing: a live gate constant whose own cited source
contradicts it.
