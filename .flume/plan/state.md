# Plan state

- Spec derived through: 6a04322
- Audited through: 5f88258
- Residue swept through: ec3d112
- This tick: Residue sweep (job 4) — swept the 0019 recut (6a04322)
  against code; the commit gap was vacuous (zero src/tests/sdk commits in
  5f88258..ec3d112). Retired vocabulary greps clean: "prose shape" has
  zero code hits under any spelling; old wedge-demo and positioning
  phrasings zero; every "never mined" comment is composed-host-scoped
  (0019 kept those true). One residue class found and routed: the
  pre-0019 "every governed artifact is a projection" universal in
  src/install.rs:18 + tests/install.rs:15,382 — comment staleness, rides
  the exit clause: the install.rs fix is now named in LAYOUT-READER's
  file description; tests/install.rs rides its next opener
  (open-questions bullet). Standing kinds/packages debts re-verified
  vacuously at ec3d112.
- Queue: REQUIREMENT-PROSE-PERSISTS, SKILL-CONTRACT-RECITE (both open,
  disjoint) → FRONTMATTER-MALFORMED-LOUD → KIND-CONTENT-FACT →
  LAYOUT-READER → LAYOUT-PROSE-IMPORT (one linear blockedBy chain over the
  shared main.rs/drift.rs/declarations.ts surfaces); PACKAGING-CHANNELS
  (parked, carried verbatim).

Plan continues: yes — quiet pass (job 5) is now the live input: all four
inputs above it are current after this sweep; the closing pass re-verifies
queue disjointness and every gate reason.
