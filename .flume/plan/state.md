# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: 3ba4f40 — window fce471f..3ba4f40 reconciled.
- Residue swept through: 3ba4f40 — same window.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs, src/frontmatter.rs, src/gate.rs, src/glob.rs, src/graph.rs, src/hash.rs covered, mid-rotation. src/import.rs is the tree-order candidate next — unchanged, not this tick's job.
- This tick: POST-SHIP RECONCILIATION, window fce471f..3ba4f40 (100ee02 build + 3ba4f40 ship, HASH-READ-UTF8-ERROR-MAP-DEDUP). Audit: verified on disk (not the log alone) — `src/frontmatter.rs:130-137`, `src/json_manifest.rs:307-314`, `src/toml_document.rs:159-166` each now carry a `From<crate::hash::ReadUtf8Error>` impl reproducing the prior field-for-field Io/NotUtf8 mapping exactly, and all three call sites (`toml_document::read` 33, `frontmatter::from_source_rooted` 180, `json_manifest::read_to_string` 319) collapsed their `.map_err(|err| match err {...})?` to plain `?`. `cargo build`/`cargo clippy --all-targets` both clean. `pending.json` already had the entry dropped by the ship commit (32 lines removed) — nothing left to prune. Metrics glance: 37 turns, no revert, in-band with this rotation's other dedup ships (21-38 turns). Sweep: `rg` for `ReadUtf8Error`/`read_utf8` across `src/` found no remaining hand-matched call site and no new duplication introduced by the fix — window closed, no residue.
- Queue: 2 pending, 0 open, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — jobs 1-3 are quiet (empty inbox, no spec delta, reconciliation window now closed at 3ba4f40); next tick's live input is the open posture rotation, neighborhood src/import.rs, with no pickable entries in queue to hand off to build first.
