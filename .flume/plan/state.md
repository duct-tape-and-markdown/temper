# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: 2a6e488 — window 385cf3e..2a6e488 reconciled.
- Residue swept through: 2a6e488 — same window.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs covered, mid-rotation. src/frontmatter.rs is the tree-order candidate next — unchanged, not this tick's job.
- This tick: POST-SHIP RECONCILIATION, window 385cf3e..2a6e488 (2a6e488 the only src/tests/sdk-touching commit). Audit: EXTRACT-BODY-HEADINGS-COLLECT-HEADS-DEDUP verified shipped on disk — `body_headings` (615-623) now collects lines and delegates to `collect_heads`, mapping its output to text instead of re-running the fence/ATX scan; the orphaned `manifest_members` doc fragment is excised and `number_kind` carries only its own doc comment. `cargo test` full suite green (`track_fence`/`atx_heading` still each have live callers — no dead code left behind); entry correctly dropped from pending.json by the ship commit. Sweep: no fresh residue — `body_sections`, `body_heading_tree`, and now `body_headings` all delegate to `collect_heads`; `body_at_imports`'s own `track_fence` call is a distinct import-scan use, already cleared. Open-questions' ride-only-orphan record updated: the extract.rs `manifest_members` orphan discharged (its own paragraph replaced with a discharge note beside the roster.rs precedent); its companion `src/json_manifest.rs` `Manifest::read` cite (line 347) was NOT touched by this ship and still rides whichever entry next opens that file — live-orphan count dropped from eight to seven.
- Queue: 2 pending, 0 open, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — no pickable entry remains (both pending entries are human-parked), so the open posture rotation (next neighborhood src/frontmatter.rs) is plan's own to drive next tick.
