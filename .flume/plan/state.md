# Plan state

- Spec derived through: 087b90a — unchanged; `git log 087b90a..HEAD -- specs/` empty.
- Audited through: a00e14a — CONTRACT-FORMAT-PLACES-EDGES-ZERO-CONSUMER shipped clean (8c3015d: sdk/test/contract.test.ts gains `formatPlacesEdges composes a format-verification predicate with no field`, asserting the bare `{ key: "format-places-edges" }` value and its unchanged round-trip through `skillClauseRow`, matching sibling predicate tests); the ship commit (a00e14a) already dropped the entry from pending.json. `pnpm --dir sdk test` re-run clean: 139/139 pass. Metrics clean (21-turn build tick, no reverts). Both parks re-tested and hold: `git diff ac40c72..HEAD -- src/graph.rs tests/graph.rs .github/` shows only an unrelated `any_clause_of` extraction in graph.rs (lines 222-261/377, a `degree`/`mention_reachable` dedup) — outside the cited 71-75/548/647-648/672-675 region; tests/graph.rs and .github/ untouched.
- Residue swept through: a00e14a — same window (1cf7f11..HEAD); the only src/tests/sdk commit in it is 8c3015d, a clean 6-line test addition introducing no residue of its own. Nothing to file. (9b17a34 also lands in the window but touches only .claude/rules/.temper/ — harness territory, outside this job's src/tests/sdk fence.)
- Posture swept through: sdk/src/declarations.ts (+ its immediate imports assembly.ts/kind.ts/contract.ts/prose.ts/builtins.ts, read for context per the posture-sweep rule) covered; sdk/src/dial.ts next in rotation (tree order after declarations.ts; the phrase delta at 2e2b32a still owes sdk/src/ the remainder — dial.ts, emit.ts, index.ts, kind.ts, needs.ts, prose.ts — then the tests/ tree).
- This tick: POSTURE SWEEP sdk/src/declarations.ts. Two findings: `TELEMETRY_EVENT_HOOKS` (743-765) hardcodes Claude Code hook event/matcher literals cited to code.claude.com/docs/en/hooks — a documented provider fact living outside the provider face (`builtins.ts` already owns `DOCUMENTED_HOOK_EVENTS`), architecture.md "The provider face is data"; filed DECLARATIONS-TELEMETRY-HOOK-PROVIDER-FACE-MOVE (open). `mentionScope` (873) and `SEAM_VERSION` (915) are exported with zero callers anywhere in sdk/src or sdk/test (grep-verified, each used only inside its own defining file), engineering.md "An export earns its consumer"; filed DECLARATIONS-ZERO-CONSUMER-EXPORTS-PRUNE, blockedBy the first entry (both touch declarations.ts — pending-entry rule, "Disjoint, or serialized"). No other posture-page violation found in the neighborhood.
- Queue: 4 pending — 1 open (DECLARATIONS-TELEMETRY-HOOK-PROVIDER-FACE-MOVE), 1 blockedBy (DECLARATIONS-ZERO-CONSUMER-EXPORTS-PRUNE), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER). Open forks: 2 (multi-harness-projection, lazy-grounds). Friction: 0. Amendments: 0. Inbox: 0 notes.

Plan continues: after-build — the posture rotation is the only remaining live job and a pickable entry now exists (DECLARATIONS-TELEMETRY-HOOK-PROVIDER-FACE-MOVE), so the wave ships it before the sweep resumes.
