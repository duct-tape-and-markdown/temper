# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: ac2ff61 — unchanged; no commits since touch src/, tests/, or sdk/ (`git log ac2ff61..HEAD -- src/ tests/ sdk/` empty — the two commits since, b8f29cd and b30dc3f, are both `plan:` commits).
- Residue swept through: ac2ff61 — unchanged, same reason.
- Posture swept through: src/builtin.rs..src/json_splice.rs (prior rotation), plus src/kind.rs, src/layout.rs, src/lib.rs, src/main.rs covered — mid-rotation. src/main.rs's neighborhood: a cohesion violation. main.rs's own module doc (1-6) and `.claude/rules/rust.md` both state main.rs is "a thin `clap` dispatch… all logic lives in the library so `tests/` can drive it," but its entire `#[cfg(test)] mod tests` (632-912) tests `compose::` internals — not one test drives `Cli::parse()` or the `Command` match. `repo_file_set_stays_raw_disk_including_gitignored_targets` (647-662) calls `compose::repo_file_set` directly; three tests (676-798) pin `compose::resolve_kind_units_count`/`compose::overlay_builtin_kind_count` deltas over a `gate::gate` run, the same shape `tests/check_cost.rs` already uses for `import::walk_count`; three more (851-911) unit-test `compose::edge_fields_by_kind`/`compose::embedded_member_features` via hand-built `drift::Declarations` fixtures, no CLI involved. Traced to source: fe58bc2 ("move corpus-assembly functions to compose.rs") moved the implementation out of main.rs two months ago but left every test behind — compose.rs has carried its own `#[cfg(test)] mod tests` (1274) the whole time, orphaned from these. Filed MAIN-TEST-MODULE-COMPOSE-RELOCATE (mechanical relocation, precedent for both destinations already on disk — compose.rs's test module, tests/check_cost.rs's count-pin pattern), open. No dead plumbing, no embedded-provider-knowledge finding beyond the already-documented `BUILTIN_DEFAULT_CONTRACT_KINDS` (its own doc comment states the CLI-specific curation; the bare "skill"/"rule" kind-name literal is pervasive project-wide — engine.rs, drift.rs doc comments — not novel residue local to this file). Rotation continues to src/placement.rs next.
- This tick: POSTURE SWEEP, neighborhood src/main.rs — cohesion finding (test module tests compose.rs, not the CLI), MAIN-TEST-MODULE-COMPOSE-RELOCATE filed open, cursor advanced, rotation continues to src/placement.rs. `cargo clippy --all-targets -- -D warnings` and `cargo fmt --all --check` both clean.
- Queue: 3 pending, 1 open (MAIN-TEST-MODULE-COMPOSE-RELOCATE), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — the only remaining live job is the posture sweep (next candidate src/placement.rs), and a pickable entry (MAIN-TEST-MODULE-COMPOSE-RELOCATE) now exists; the sweep resumes once the wave hands back.
