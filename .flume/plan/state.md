# Plan state

- Spec derived through: 13455d4a — unchanged, copied forward (not this tick's job).
- Audited through: 8f20132a — window 53852595..8f20132a (bccf42c0, 10d1d332, 42c3cca8 — the EMIT-BANNER-OWNERSHIP-MOVE landing) reconciled. The tag was already dequeued (dropped in 10d1d332 itself); its one surfaced product question (banner-in-body) was already routed to READ-BODY-EXCLUDES-PLACEMENT-BANNER inside the same window (8f20132a). Bootstrapped this worktree's missing sdk/node_modules + .temper deps (absent, not a regression) and reran everything at HEAD: cargo test green (310+ lib tests plus every integration binary, install.rs's banner-convergence and emit.rs's banner-carrying tests included), clippy -D warnings clean, fmt clean, cargo doc --no-deps --document-private-items exits 0 (two pre-existing advisory private-link warnings in roster.rs/graph.rs, unrelated to this window).
- Residue swept through: 8f20132a — same window, no orphaned symbols/vocabulary. One stale-prose lead: src/placement.rs:9/15's parentheticals still name the pre-rename `project_note`/`project_banner` symbols (`project_banner` became `converge_banner_wording` in bccf42c0) — this is the "prose staleness rides the next entry that opens the file" exception (open-questions.md's stale-cites bullet), not a fileable gap: READ-BODY-EXCLUDES-PLACEMENT-BANNER already opens this file next. `pub mod placement` (lib.rs, widened by bccf42c0) earns its four real test consumers (tests/emit.rs, install.rs, memory_contract.rs, prose_include.rs, json_document_format.rs).
- Posture swept through: 22f8064c — unchanged, mid-rotation continues (not this tick's job). Frozen frontier (armed at 7d695577): src/read.rs (covered), src/telemetry.rs (covered), tests/read_verbs.rs (covered), src/admissibility.rs (covered), src/gate.rs (covered), src/graph.rs (covered), tests/graph.rs (covered), src/tap.rs, tests/tap.rs, tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts, tests/emit.rs, src/install.rs, tests/install.rs, src/compose.rs, src/drift.rs, src/glob.rs, src/placement.rs.
- This tick: post-ship reconciliation over 53852595..8f20132a — clean, no gap; queue unchanged (0 dropped, 0 filed).
- Queue: 7 pending — 2 open, 3 parked, 2 deferred. Open forks: 5 (unchanged). Friction: 0. Amendments: 0. Inbox: 0. Refactor: 0.

Plan continues: after-build — READ-BODY-EXCLUDES-PLACEMENT-BANNER and GRAPH-GLOB-WITNESS-HAND-ROLL-DOC-NOTE are pickable now; the posture sweep's open rotation resumes once the wave hands back.
