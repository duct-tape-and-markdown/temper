# Plan state

- Spec derived through: b645125 — unchanged, not this tick's job.
- Audited through: 1e5aae2 — advanced from fee46ef. Window fee46ef..1e5aae2 reconciled: one code-touching commit, 01c3650 (build: widen the SDK-derived-lock round-trip test to decode all 14 kind rows).
- Residue swept through: 1e5aae2 — advanced, same window; no residue found.
- Posture swept through: 77c0790 — unchanged; re-armed for next tick: 3533e0b (sdk/src/contract.ts, sdk/src/declarations.ts) plus, new this window, 01c3650 (tests/builtin_lock_frozen.rs) — all posture-eligible.
- This tick: POST-SHIP RECONCILIATION. Audited 01c3650 live on disk: tests/builtin_lock_frozen.rs now asserts `declarations.kinds.len() == 14` and loops every kind row through `CustomKind::from_kind_fact_row`, the installed-plugin Enablement regression pin kept as a targeted assertion beneath it — `cargo test --test builtin_lock_frozen` reruns green (2 passed), confirming BUILTIN-LOCK-ROUND-TRIP-ALL-KIND-ROWS's acceptance was actually met before the ship commit (1e5aae2) dropped it from pending.json. Swept for residue: the window's only other code-adjacent diff is sdk/package-lock.json's 0.0.9→0.0.10 optionalDependencies bump, lockstep with aa67454's ledger note, not a residue class. Metrics glanced (31 turns, 192.6s build tick, clean merge, no revert) — unremarkable sizing. Both parked entries' blockers re-verified untouched: src/graph.rs, tests/graph.rs (IMPORT-HOP-CAP-CITE) and .github/, Cargo.toml still 0.1.0 with no v0.1 tag (PACKAGING-CHANNELS-REMAINDER). The six ride-only stale-cite files (json_splice.rs, drift.rs, document.rs, install.rs, extract.rs, json_manifest.rs) also untouched this window — no rider record needs updating.
- Queue: 2 pending, 0 open, 0 blockedBy, 2 parked. Refactor: 0 live. Friction: 0 live. Inbox: 0 notes.

Plan continues: yes — posture sweep re-arms next tick (window now touches sdk/src/contract.ts, sdk/src/declarations.ts, and tests/builtin_lock_frozen.rs), and no pickable entries exist to hand build meanwhile (both queue entries are parked), so the next tick runs the sweep directly rather than idling.
