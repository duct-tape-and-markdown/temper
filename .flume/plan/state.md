# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: 2a6e488 — window 385cf3e..2a6e488 reconciled.
- Residue swept through: 2a6e488 — same window.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs, src/frontmatter.rs covered, mid-rotation. src/gate.rs is the tree-order candidate next — unchanged, not this tick's job.
- This tick: POSTURE SWEEP, neighborhood src/frontmatter.rs (immediate imports document.rs/kind.rs/hash.rs/extract.rs read for context, not re-swept — already covered or not yet due). No violation under engineering.md's sections or architecture.md's choices, nor the cohesion/dead-plumbing/embedded-provider-knowledge lenses: all exports consumer-checked (fold_file_id, closing_delimiter, field/has_field all called outside their module), the hand-rolled `split_frontmatter`/`closing_delimiter` is the declared pinned-byte-fidelity exception to "libraries before hand-rolls," `FrontmatterError`'s Io/NotUtf8 shape mirrors the shared `hash::ReadUtf8Error`-wrapping pattern every format uses (not residue), and no UnitShape-driven id logic hardcodes a provider fact outside `kind.rs`'s own declarations. Quiet-on-clean; rotation continues to src/gate.rs next tick. (Noted, not filed: the module header and two doc comments narrate the retired `src/skill.rs`/`src/rule.rs` IRs — rust.md's era-narration comment rule, not an engineering.md/architecture.md posture-sweep lens, so it rides whichever entry next opens the file per rust.md's own exit clause.)
- Queue: 2 pending, 0 open, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — no pickable entry remains (both pending entries are human-parked), so the open posture rotation (next neighborhood src/gate.rs) is plan's own to drive next tick.
