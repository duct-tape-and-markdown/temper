# Plan state

- Spec derived through: 054addd — unchanged; 087b90a (distribution.md's cite-delegation sentence) sits past it, undrained until job 2 runs next tick.
- Audited through: 863149f — unchanged this tick (job 1 was live and served; 087b90a touched only `docs/ledger.md`/`specs/distribution.md`, no `src/`/`tests/`/`sdk/` ripple).
- Residue swept through: 863149f — unchanged this tick, same reason.
- Posture swept through: full src/ list covered through the last rotation; sdk/src/contract.ts next.
- This tick: INBOX. Routed all five notes. (1) `when` guard/body admissibility-fence bypass, confirmed on disk at engine.rs:153-511 → WHEN-BODY-ADMISSIBILITY-FENCE (blockedBy the next item — same seam). (2) `when` body-evaluates-at-member-root: re-verified and ESCALATED past the note's own low-severity/design-undetermined framing — an empirical probe (added, run, reverted, never committed) proved the shipped `marketplace` kind's source-specific required-field checks (github/url/git-subdir/npm) already silently never fire; contract.md's `clause` section already settles the semantic → WHEN-BODY-ELEMENT-SCOPE-EVALUATE (open); no `(when-body-element-relative-addressing)` fork is left standing — resolved, not parked. (3) reporter.rs/install.rs cite-bar gaps (distribution.md's new delegation sentence, 087b90a) → REPORTER-EXTERNAL-FACT-CITE + GUARD-HOOK-SHAPE-CITE (open, disjoint files); the yaml-language-server half of the same note was already resolved separately (MODELINE-MARKER-CITE, 8396d34), re-verified on disk (src/placement.rs:20-22). (4) directive-backing path-domain mismatch, confirmed on disk (compose.rs's `repo_file_set` strips the root, graph.rs's index/target resolution keeps it, coinciding only when root is `.`) → DIRECTIVE-BACKING-PATH-DOMAIN (open), scoped as the mechanism fix — third instance of the class — since `repo_file_set`'s only two callers (gate.rs variable-root, read.rs hardcoded `.`) mean the fix is contained to compose.rs. (5) compose.rs's two lean ride-alongs (`read_dial`'s dead raw-definition fallback; `overlay_builtin_kind`'s doc overstating "always") — accepted as debt per the note's own framing ("ride the next entry that opens the file, never standalone"); no pending entry currently opens compose.rs for this reason, so neither is filed. Inbox drained to empty.
- Queue: 7 pending — 5 open/blockedBy (WHEN-BODY-ELEMENT-SCOPE-EVALUATE, WHEN-BODY-ADMISSIBILITY-FENCE blockedBy it, REPORTER-EXTERNAL-FACT-CITE, GUARD-HOOK-SHAPE-CITE, DIRECTIVE-BACKING-PATH-DOMAIN), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER, untouched). Open forks: 2 (multi-harness-projection, lazy-grounds). Friction: 1 live (human's to read). Amendments: 0. Inbox: 0 notes.

Plan continues: yes — 087b90a sits past the spec-derived cursor undrained (job 2 next tick); it likely resolves as "already routed" by this tick's cite entries, the same shape as cf8e05a, but that check is job 2's to run, not job 1's.
