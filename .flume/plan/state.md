# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: d61b87b — unchanged, not this tick's job (no src/tests/sdk commits land past it).
- Residue swept through: d61b87b — unchanged, not this tick's job.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs, src/frontmatter.rs, src/gate.rs, src/glob.rs, src/graph.rs, src/hash.rs, src/import.rs, src/install.rs covered, mid-rotation. src/json_manifest.rs is the tree-order candidate next.
- This tick: POSTURE SWEEP src/install.rs (read with its immediate imports). Filed FRONTMATTER-MATTER-STRIP-DEDUP, per engineering.md "One job, one home": `project_modeline`/`project_note` (src/install.rs:1607-1618/1637-1653) and `placement_lines` (src/placement.rs:23-32) each independently re-derive `source.strip_prefix("---\n")` + `frontmatter::closing_delimiter(rest)` in a differently-styled two-line stanza — `closing_delimiter`'s own doc comment (src/frontmatter.rs:344-347) already states the three-site-sharing intent this residual wrapper duplication defeats. No other posture-sweep lens (cohesion, dead plumbing, embedded-provider-knowledge, export-consumer, vacuity) tripped on install.rs: every `InstallError` variant is constructed, every `pub` export has a real consumer outside the file (`npm_program`, `SESSION_START_COMMAND`, `GuardedManifest`, `manifest_write_findings`, etc. all ≥1 external use), and `member_dir`'s literal skill/rule/memory match is temper's own `.temper/` scaffold-naming convention (not a cited Claude Code provider fact, unlike `governs.root`) with a safe `_ => "members"` fallback — considered against "The fix lands at the mechanism" and judged the right depth, not residue.
- Queue: 3 pending, 1 open (FRONTMATTER-MATTER-STRIP-DEDUP), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — FRONTMATTER-MATTER-STRIP-DEDUP is pickable and ships first; the posture rotation (next neighborhood: src/json_manifest.rs) resumes once the wave hands back.
