# Plan state

- **Phase:** reconcile. HEAD 2b6abc0.
- **Last shipped:** CONTEXT-VERB (`build` 52d0047 / `chore` 5baf2ca) — the
  addressable-corpus floor engine chain has fully drained. Since then, three
  human ceremonies landed: the corpus genre package (`chore(harness)` 5f56fda),
  the SDK authoring-face scaffold (`sdk:` a13ad53 — ask (a) discharged), and the
  rung de-codification intent (`specs:` d4a6999).
- **This tick:** drained a 5-line inbox and filed the gap. Two pickable Rust
  entries are now **open** and disjoint (parallel-safe): **RUNG-DECODIFY-CODE**
  (rework the genre-ordinal + carriage strings onto the ratified fact language;
  `rg -i rung src/ tests/` → empty) and **INSTALL-NOTE-CONTENT-DRIFT** (the
  managed-by note re-places on a body change, not just on absence). Filed the
  altitude authoring wave as the serialized **SDK-EMIT-BYTE-PARITY →
  SDK-BODY-RESOLUTION → SDK-PROJECTION-LOCK** chain (parked on the new
  `(sdk-build-gate)` fork — build's gates are cargo-only, can't validate TS).
  Confirmed the scaffold's manifest schema matches compose.rs. Raised two forks:
  `(sdk-build-gate)` and `(display-rule-emit-face)` (the rung-3 pilot's blocker,
  entangled with a custom-kind emit face). The three prior held entries
  (EXTRACTION-VOCAB-GAPS deferred, AGENT-KIND deferred, PACKAGING-CHANNELS parked)
  hold on their own terms, unchanged.
- **Pickable now:** RUNG-DECODIFY-CODE and INSTALL-NOTE-CONTENT-DRIFT — both
  `open`, both cargo-validated, no shared file. Build runs the wave.
- **What's next:** after the two open entries ship, the queue is deferred/parked
  again — the SDK wave waits on a human chain.ts gate (`(sdk-build-gate)`), the
  display rule on a human scope call (`(display-rule-emit-face)`), packaging on
  human creds. Accepted debt (out of fence): temper's own `temper.toml`+lock
  predate MANIFEST-EMIT — a human `chore(harness)` `emit` regen; the dogfood
  self-gate stays unwired until the wave-end confirmation pass (entity ruling,
  90-spec-system).

Plan continues: no — inbox drained, queue reconciled, two disjoint `open`
entries filed and pickable. Building is how the queue drains; hand to build.
