# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: fce471f — window d4ec7da..fce471f reconciled.
- Residue swept through: fce471f — same window.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs, src/frontmatter.rs, src/gate.rs, src/glob.rs, src/graph.rs covered, mid-rotation. src/hash.rs is the tree-order candidate next — unchanged, not this tick's job.
- This tick: POST-SHIP RECONCILIATION, window d4ec7da..fce471f (7bf87e4 build + fce471f ship of GRAPH-CLAUSE-GUARD-DEDUP, the only src/-touching commits since the last audit). Audit: `any_clause_of(selections, matches)` (222-231) extracted and is now the sole guard-check shape in graph.rs — `degree` (264-266) and `mention_reachable` (380-382) both delegate to it, `rg` over the file finds no remaining `clauses.iter().any` duplicate; `cargo test --lib graph` 27/27 green; the pending entry self-drained in the ship commit (fce471f, -33 lines, nothing left to drop); metrics.jsonl shows one build tick (21 turns) and a clean merge (no revert, no re-pick) — no oversized-split signal. Sweep: the window's only src/ diff is the extraction itself — no retirement named, no new duplicate surface introduced. Re-tested both parked entries' conditions against this window: `MAX_IMPORT_HOPS`/its cite (now further shifted, unread this tick) and the darwin/channel-3/version-lockstep gates for PACKAGING-CHANNELS-REMAINDER sit outside the touched region/tree — neither's park condition moved.
- Queue: 2 pending, 0 open, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — no pickable entry exists (both remaining are parked on human action), so the open posture rotation (next neighborhood src/hash.rs) is next tick's live job.
