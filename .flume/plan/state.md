# Plan state

- Spec derived through: 5945405
- Audited through: b1cecfe
- Residue swept through: 45aa0db
- This tick: Residue sweep 8aeb64e..HEAD (the four build commits' src work).
  Filed BUNDLE-SKILL-INSTALL-VERB — the bundled operate-the-gate skill +
  PLUGIN_DESCRIPTION (src/bundle.rs) still teach the retired `import` verb
  (CLI has no Import subcommand; `install` is the on-ramp), unowned by any
  pending entry. Other residue classes routed: read.rs `import` suggestion ->
  EXPLAIN-RESOLVER (operationalized); sdk/src/emit.ts + src/kind.rs
  specs/architecture/* cites/fixtures -> citation staleness riding
  MENTION-EDGE-LANDS / GUARD-OWNPATH (dfba26f left non-comment strings to
  owning entries); session_start.rs +++/kinds/packages fixtures -> accepted
  debt (open-questions "Kept on purpose"). Cursor to HEAD.
- Queue: 10 — 6 open/disjoint (HELP-TEXT-RECUT main.rs, BUNDLE-SKILL-INSTALL-VERB
  bundle.rs, COVERAGE-KIND-AWARE coverage_note.rs, DISCOVERY-SKIPS-SURFACE
  import.rs, SCAFFOLD-OUTPUT-VALID install.rs, EXPLAIN-RESOLVER read.rs), 3
  blocked (PATH-SEP-NORMALIZE on SCAFFOLD, GUARD-OWNPATH on PATH-SEP —
  install.rs chain; MENTION-EDGE-LANDS on HELP-TEXT-RECUT for shared main.rs),
  PACKAGING-CHANNELS parked.

Plan continues: no — all inputs current (inbox empty, spec delta empty, ship
audit current: only 45aa0db past b1cecfe is plan-only, residue swept to HEAD).
Six pickable open entries await build.
