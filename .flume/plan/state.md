# Plan state

- **Phase:** drained the front-door demolition inbox and filed the serialized
  S1–S7 chain; retired the parked GATE-READ-LOCK-DEMOLITION.
- **Last shipped:** SDK-RECUT-SUBPATH-LAYOUT (build 56dce9b / chore ea5346a) —
  the SDK recut to the one-package `@dtmd/temper` subpath layout.
- **This tick:** PR #6 (0c11135) merged the front door; John's go, cooling
  waived, clean slate. Re-verified the demolition blast-radius symbols on disk
  (import::run 143, write_rollup 866, scratch_surface 1076, the compose
  `[[member]]` codec 1512/1738, carry_representation 318, drift governs columns
  724, SDK lock/toml/project writers) and filed the ceremony's chain as seven
  serialized entries — EMIT-PAYLOAD-SEAM (open head) → CHECK-READS-LOCK-GOVERNS →
  FIXTURES-OFF-IMPORT → SCRATCH-RETIRE → CODEC-RETIRE → INSTALL-FRONT-DOOR →
  TEMPER-TOML-ZERO, each `blockedBy` its predecessor (shared main.rs/import.rs/
  compose.rs/drift.rs spine). Marked `(carriage-aware-placements)` RESOLVED (its
  code fix is S6). Routed the cascade genre-pilot into `(genre-fence-format)` as
  a DATUM — the fence grammar is undesigned until the workshop, sequences after
  S1, not a pending entry yet. PACKAGING-CHANNELS unchanged (parked, human creds).
- **In flight:** EMIT-PAYLOAD-SEAM is the one pickable `open` head; S2–S7 wait
  behind it, one tick at a time.
- **What's next:** build drains the chain head; a ship unblocks the next link on
  the following reconcile. Human owns the release creds (PACKAGING-CHANNELS), the
  USPTO name screen, and the genre-fence-format workshop.

Plan continues: no — the inbox is drained, the chain is filed with a pickable
`open` head (EMIT-PAYLOAD-SEAM), and the rest is serialized behind it. Building
is how the queue drains from here; re-planning the same queue would spin.
