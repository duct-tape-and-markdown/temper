# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: ac2ff61 — window b9233dd..ac2ff61 audited: the one src/tests-touching commit in it, d0e0797 (build: test LayoutError::MissingKey), verified on disk against LAYOUT-MISSING-KEY-UNTESTED's own acceptance — `a_collection_member_missing_its_declared_explicit_key_fires_loud` (tests/layout_kind.rs:127-143) drives `src/layout.rs:236-240`'s `LayoutError::MissingKey` construction exactly as filed, asserting the error carries both the heading ("Loud or nothing") and the missing key ("id"); `cargo test --test layout_kind` green (10 passed), `cargo clippy --all-targets -- -D warnings` and `cargo fmt --all --check` both clean. pending.json unchanged (2 entries, both parked; LAYOUT-MISSING-KEY-UNTESTED already drained by the ac2ff61 ship commit). Metrics glance: the LAYOUT-MISSING-KEY-UNTESTED build tick ran 19 turns/122s, in line with the window's other build ticks — no sizing anomaly.
- Residue swept through: ac2ff61 — same window. The commit is a pure test addition (tests/layout_kind.rs only, no src/ change) — no retirement or demolition named, no new zero-consumer export introduced. Both parked entries re-tested: IMPORT-HOP-CAP-CITE (`git diff b9233dd..HEAD -- src/graph.rs tests/graph.rs` empty, park holds) and PACKAGING-CHANNELS-REMAINDER (`git diff b9233dd..HEAD -- .github/` empty, park holds). open-questions.md's orphan list is untouched by this window — the sole file the window changed, tests/layout_kind.rs, isn't among any cited orphan's file (including the ninth, extract.rs, surfaced last tick but not opened by this window's commit).
- Posture swept through: src/builtin.rs..src/json_splice.rs (prior rotation), plus src/kind.rs, src/layout.rs, src/lib.rs covered — mid-rotation. src/lib.rs's neighborhood is itself alone: the module carries no `use` statement (`grep -n '^use\|^pub use' src/lib.rs` empty), only three crate-root constants (`VERSION`, `WORKSPACE_DIR`, `LOCK_FILENAME`) and 38 `pub`/`pub(crate) mod` declarations. All three constants have live consumers outside the file (`rg` — `VERSION` in src/reporter.rs:235, src/bundle.rs:249/420, tests/bundle.rs:268; `WORKSPACE_DIR` across src/coverage_note.rs, src/builtin_kind.rs, src/import.rs, src/install.rs, src/main.rs; `LOCK_FILENAME` in tests/check_cost.rs); `LOCK_FILENAME` and `VERSION` also read fine standalone. The `mod` list matches `src/*.rs` on disk exactly (diff of `rg -oP '(?<=mod )\w+' src/lib.rs` against the file tree minus lib.rs/main.rs: empty). No cohesion issue (crate-root constants + module wiring is lib.rs's one job), no dead plumbing, no embedded provider knowledge (`.temper`/`lock.toml` are temper's own workspace convention, not a Claude Code provider fact) — clean, no entry filed. src/main.rs is the tree-order candidate next.
- This tick: POSTURE SWEEP, neighborhood src/lib.rs — clean, cursor advanced, rotation continues to src/main.rs. `cargo clippy --all-targets -- -D warnings` and `cargo fmt --all --check` both clean.
- Queue: 2 pending, 0 open, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — the posture sweep rotation is still open (next candidate src/main.rs) with no pickable entries in queue; it resumes next tick.
