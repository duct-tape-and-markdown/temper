# Plan state

- Spec derived through: 087b90a — unchanged; `git log 087b90a..HEAD -- specs/` empty, cursor still caught up to HEAD's spec state.
- Audited through: 491c695 — window 863149f..HEAD touched src/ (ff6ec84, 0d40461, 0ba6ffe, 10461c5, f21551f). All five shipped tags (WHEN-BODY-ELEMENT-SCOPE-EVALUATE, REPORTER-EXTERNAL-FACT-CITE, GUARD-HOOK-SHAPE-CITE, DIRECTIVE-BACKING-PATH-DOMAIN, WHEN-BODY-ADMISSIBILITY-FENCE) already absent from pending.json — nothing to drop. Both parks re-tested: IMPORT-HOP-CAP-CITE (src/graph.rs, tests/graph.rs untouched in window, holds), PACKAGING-CHANNELS-REMAINDER (.github/ untouched, holds). `.flume/metrics.jsonl` tail glanced: no reverts/bails, tick sizes unremarkable (26-105 turns).
- Residue swept through: 491c695 — same window. `git show ff6ec84 -- src/engine.rs` read in full: `element_address_for_guard`/`parent_json_of` (1277-1417) hand-roll address-string slicing and JSON-tree walking that `src/address.rs`'s `FieldPath` already owns (module header: "Nothing here walks a JSON tree by hand"), and `parent_address_of` (1316, `#[allow(dead_code)]`) is genuinely uncalled (`rg` confirmed). Filed WHEN-ELEMENT-ADDRESS-DEDUP (engineering.md, "One job, one home"). reporter.rs/install.rs/compose.rs diffs (0d40461, 0ba6ffe, 10461c5) read clean — no residue.
- Posture swept through: full src/ list covered through the last rotation; sdk/src/contract.ts next.
- This tick: POST-SHIP RECONCILIATION. Audit motion: re-verified both parks hold, confirmed the window's five shipped entries already left the queue, glanced metrics for bails/oversizing (none). Sweep motion: found and filed one residue entry (WHEN-ELEMENT-ADDRESS-DEDUP) — engine.rs's when-body element scoping duplicates address.rs's FieldPath and carries dead code. Advanced both audit/residue cursors to 491c695.
- Queue: 3 pending — 1 open (WHEN-ELEMENT-ADDRESS-DEDUP), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER). Open forks: 2 (multi-harness-projection, lazy-grounds). Friction: 1 live (human's to read). Amendments: 0. Inbox: 0 notes.

Plan continues: after-build — inbox is empty, spec delta is drained, and post-ship reconciliation just caught both cursors up to HEAD, so the only remaining live job is the posture sweep (sdk/src/contract.ts next); WHEN-ELEMENT-ADDRESS-DEDUP sits pickable in the queue right now — ready work ships first, the sweep resumes when the wave hands back.
