# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: c9d8b24 — window c9d6cea..c9d8b24 reconciled.
- Residue swept through: c9d8b24 — same window.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs covered, mid-rotation. src/extract.rs is the tree-order candidate next — unchanged, not this tick's job.
- This tick: POST-SHIP RECONCILIATION, window c9d6cea..c9d8b24 (8a47d84 the only src/tests/sdk-touching commit). Audit: ENGINE-WHEN-GUARD-MEMBERSHIP-DEDUP verified shipped on disk — `guard_membership_fails` extracted (870-883), `Predicate::When`'s guard match calls it (1237), the three new direct runtime tests (2713/2761/2809-ish) pass (`cargo test when_guard`); entry correctly dropped from pending.json by the ship commit. Sweep: the ship commit's own message claims "the Type/Enum arms call it for violations" — false on disk (per collaboration.md, read the disk, not the log): `Predicate::Type`'s decide arm (935-944) and `Predicate::Enum`'s (972-980) still hand-write the identical boolean `guard_membership_fails` computes, so the consolidation landed a third home instead of one. Filed ENGINE-DECIDE-TYPE-ENUM-ARMS-GUARD-HELPER-REUSE (per engineering.md, "One job, one home"), no new tests needed — existing Type/Enum decide tests (1458, 2267) already pin the behavior.
- Queue: 3 pending, 1 open (ENGINE-DECIDE-TYPE-ENUM-ARMS-GUARD-HELPER-REUSE), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — a pickable entry now exists (ENGINE-DECIDE-TYPE-ENUM-ARMS-GUARD-HELPER-REUSE) and it is the only live job besides the still-open posture rotation (next neighborhood src/extract.rs); build ships the entry first, the sweep resumes when the wave hands back.
