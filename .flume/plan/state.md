# Plan state

- Spec derived through: f4189c3
- Audited through: 2b82ebd
- Residue swept through: 2b82ebd
- This tick: Spec delta f4189c3 fully routed — final 0011 slice (the file-member
  built-in kinds command + agent) registered as open fork `(command-agent-kinds)`.
  builtins.md "The named expansion" names them forward work; direction ratified,
  kind design under-specified (field schema, default-contract stance, and the
  registration-value model — "user-invoked" for command; whether registration
  becomes a first-class `kind` attribute touches representation.md). Today
  `.claude/commands` / `.claude/agents` are only KNOWN_SURFACES coverage
  placeholders, no modeled kind — a feature addition mirroring
  `(agents-md-builtin-kind)`, John's to shape. Embedded peers (hook, MCP) already
  route via `(json-projection-format)`. Spec cursor 5945405 -> f4189c3. Rider
  pipeline.md:50 embed->include is spec-internal wording (0001-rename survivor) —
  any stale "embed" vocab in code is residue-sweep work, not new derivable intent.
  Audited/Residue cursors copied forward verbatim (not serviced this tick).
- Queue: 7 unchanged — EMIT-REAP-ORPHANS open (drift.rs), EMIT-LF-NORMALIZE
  blockedBy it (same drift.rs+emit.rs), UNCLAIMED-ENTRY-ADVISORY open
  (coverage_note.rs, independent), GUARD-OWNPATH blockedBy PATH-SEP-NORMALIZE
  (blocker SHIPPED 2efd00b — ship audit unblocks it), SETTINGS-FORMAT-PRESERVING
  blockedBy GUARD-OWNPATH, MODULE-RELATIVE-PATHS blockedBy it (install.rs chain
  tail; also SDK), PACKAGING-CHANNELS parked (human release creds).

Plan continues: yes — ship audit trails (PATH-SEP-NORMALIZE shipped 2efd00b past
Audited-through 2b82ebd; unblocks GUARD-OWNPATH, work derivable next tick).
