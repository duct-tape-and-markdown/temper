# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: fce471f — window d4ec7da..fce471f reconciled.
- Residue swept through: fce471f — same window.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs, src/frontmatter.rs, src/gate.rs, src/glob.rs, src/graph.rs, src/hash.rs covered, mid-rotation. src/import.rs is the tree-order candidate next — unchanged, not this tick's job.
- This tick: POSTURE SWEEP, neighborhood src/hash.rs (no internal imports of its own, so the neighborhood is the module alone; its three callers — toml_document.rs, frontmatter.rs, json_manifest.rs — read for the duplication check per the "One job, one home" lens, not recorded covered). Found: `toml_document::read` (33-38), `frontmatter::from_source_rooted` (171-177), and `json_manifest::read_to_string` (310-315) each `.map_err(|err| match err { ReadUtf8Error::Io{..} => XError::Io{..}, ReadUtf8Error::NotUtf8{..} => XError::NotUtf8{..} })?` — a field-for-field rename repeated three times; each target error already carries the identical Io/NotUtf8 shape (verified on disk: TomlDocumentError 107-128, FrontmatterError 69-90, JsonManifestError 237-258). The "two matchers" residue example named in engineering.md, "One job, one home" — fixed by a `From<ReadUtf8Error>` impl per format, the idiomatic `?`-conversion mechanism, never filed before (`rg` over pending.json confirmed no existing entry touches these three sites). Filed HASH-READ-UTF8-ERROR-MAP-DEDUP. No cohesion violation in hash.rs itself: the module's two jobs (sha256_hex, read_utf8) were deliberately consolidated at be8c8e6 because every caller uses both back-to-back (read the bytes, then hash them) — not a parallel-structure smell, the module's own doc header states the pairing. No dead plumbing: `rg` confirms both `sha256_hex` and `read_utf8`/`ReadUtf8Error` have real cross-file consumers (drift.rs; toml_document.rs/frontmatter.rs/json_manifest.rs respectively). `fs::read` sites elsewhere (drift.rs, 7 call sites) are pure byte-reads with distinct not-found semantics per site (Option/dangling-import/reap/skip), never decoding UTF-8 — not the same job as `read_utf8`, so not residue against it.
- Queue: 3 pending, 1 open (HASH-READ-UTF8-ERROR-MAP-DEDUP), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — HASH-READ-UTF8-ERROR-MAP-DEDUP is pickable and ships next; the open posture rotation (next neighborhood src/import.rs) resumes once the wave hands back.
