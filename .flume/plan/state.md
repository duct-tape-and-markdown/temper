# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: 3ba4f40 — unchanged; no src/tests/sdk commits since (log 3ba4f40..HEAD empty).
- Residue swept through: 3ba4f40 — same.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs, src/frontmatter.rs, src/gate.rs, src/glob.rs, src/graph.rs, src/hash.rs, src/import.rs covered, mid-rotation. src/install.rs is the tree-order candidate next — unchanged, not this tick's job.
- This tick: POSTURE SWEEP, neighborhood src/import.rs (+ immediate imports src/kind.rs, src/glob.rs read alongside). Found: `discover_kind_files` (288-304) tests `if kind.name == "skill"` to push a harness-root `SKILL.md` — a kind-generic discovery function branching on one instance's literal name, the exact shape engineering.md's "The fix lands at the mechanism" names, and a documented Claude Code fact (root-is-a-skill) embedded as a literal outside `builtin_kind.rs`, the provider face architecture.md assigns it to. `rg` confirmed it's the only `kind.name ==` literal in import.rs (builtin_lock.rs's two are lock-row lookups, a different class) and that no citation for the bare-root convention exists anywhere in the tree. Filed IMPORT-BARE-ROOT-SKILL-DATA-DRIVE (open): generalize via a new `CustomKind` field builtin_kind.rs declares and cites, import.rs reads generically. src/glob.rs's per-segment matching (checked as a candidate hand-roll) is not residue — its own header already names import's per-segment walk as a documented consumer of the shared `compile_glob`, each segment still globset-matched.
- Queue: 3 pending, 1 open (IMPORT-BARE-ROOT-SKILL-DATA-DRIVE), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — the posture rotation stays open (src/install.rs next) but a pickable entry now exists (IMPORT-BARE-ROOT-SKILL-DATA-DRIVE, open); build takes it and the sweep resumes when the wave hands back.
