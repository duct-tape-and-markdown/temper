# Plan state

- Spec derived through: fcf719f4 — unchanged, copied forward; the spec delta
  is drained.
- Audited through: db387262 — advanced; 24420d45..e0d0123b (two ship waves,
  four build commits) reconciled this tick, both motions.
- Residue swept through: db387262 — advanced, same window.
- Posture swept through: tests/hook_kind.rs next — mid-rotation continues.
  Frozen frontier (armed at 7d695577): covered — src/read.rs,
  src/telemetry.rs, tests/read_verbs.rs, src/admissibility.rs, src/gate.rs,
  src/graph.rs, tests/graph.rs, src/tap.rs, tests/tap.rs; open —
  tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts,
  tests/emit.rs, src/install.rs, tests/install.rs, src/compose.rs,
  src/drift.rs, src/glob.rs, src/placement.rs.
- This tick: reconciled the post-ship window — filed the two named corner
  cuts (reachable's dropped guidance, residue shadowing a typed field),
  folded 4b1508eb's deferred admissibility half into the root entry, and
  re-measured the reached-from anchors against the shipped seam.
- Queue: 20 pending — 6 open, 6 blockedBy, 3 deferred, 5 parked. Pickable: 6.
  Open forks: 14. Friction: 1 (human channel). Amendments: 0. Refactor: 0.
  Inbox: 0.

Plan continues: after-build — the posture rotation is open (tests/hook_kind.rs
next) and six pickable entries ship first.
