# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: b829216 — unchanged, not this tick's job.
- Residue swept through: b829216 — unchanged, not this tick's job.
- Posture swept through: 6f74784, mid-rotation — phrase delta 8911c38 (specs/process/engineering.md, "A seam gate reads what the real writer wrote") re-armed the sweep with the whole module list in the frontier (0522eff cut architecture.md's named-subsystem roster, so the frontier is every module in src/, sdk/src/, tests/ — no roster left to skip forward on). src/address.rs and src/admissibility.rs both covered. Next: any remaining frontier module, judged fresh next tick (src/builtin.rs is the tree-order candidate).
- This tick: POSTURE SWEEP. Nothing above it is live (inbox empty, spec delta empty, job 3 stays quiet — no window commit past b829216 touches src/, tests/, or sdk/). Read src/admissibility.rs in full (342 lines, seven admissibility judges) against every engineering.md section and architecture.md's two, together with its seven crate-internal imports (check, compose, contract, drift, engine, extract, kind) as the neighborhood context. Found a real "Cost scale is hoisted" violation: `compose::overlay_builtin_kind` is independently re-derived per builtin kind at six sites in one `gate()` run — two of them this module's own `governs_collision_diagnostics`/`local_locus_admissibility`, called back-to-back from gate.rs with identical args — though `declarations.kinds` (its only input) is provably unchanged across every site, and `resolve_kind_units`'s own internal re-overlay is dead weight on both its current callers. The codebase already carries the identical precedent fix one layer up (`RESOLVE_KIND_UNITS_COUNT`, src/compose.rs:38-46, pinning a past "called twice per kind" bug) — that pin doesn't reach this one. Fixing it needs a design call (the natural hoist point, `assemble_lock_family`, is called at gate.rs:63 before any of its five sibling call sites exist, and has a second caller at read.rs:1554), so routed as a `.flume/refactor/` capture (`plan-overlay-builtin-kind-dedup.md`, observed at 29c5baf) rather than a pending entry. Module otherwise clean against every section (7 judges match the doc header's own count, no duplicate matcher, no provider-fact literal, every pub fn has a consumer). `cargo clippy --all-targets -- -D warnings` clean tree-wide. pending.json untouched this tick.
- Queue: 2 pending, 0 open, 0 blockedBy, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — unchanged, not this tick's job). Refactor: 1 live (plan-overlay-builtin-kind-dedup.md, filed this tick). Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — the refactor capture filed this tick is job 1's live input next tick (drain, verify at HEAD, file the scoped pending entry, delete the capture), outranking the posture sweep's own still-open rotation (frontier still holds every module past admissibility.rs); no pickable entries exist to hand build meanwhile (both queue entries are parked).
