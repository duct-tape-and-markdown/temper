# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: bfe147c — advanced from a54f3c3; window a54f3c3..bfe147c (c4e0998, 749f06f) reconciled this tick.
- Residue swept through: bfe147c — advanced from a54f3c3, same window.
- Posture swept through: full src/ list covered; sdk/src/assembly.ts, sdk/src/builtins.ts, and sdk/src/claude-code.ts covered too — mid-rotation, unchanged; sdk/src/contract.ts is the tree-order candidate next.
- This tick: POST-SHIP RECONCILIATION. Audit: c4e0998's empty diff verified correct on disk — BUILTINS-SETTINGS-MANIFEST-EXPORT-PRUNE's "zero consumer" premise was false (declarations.ts:19,797,814 import/use SETTINGS_MANIFEST; a real NUL byte at that exact spot since 7b26b4e, invisible to grep/rg's binary-skip, hid it from the scoping `rg`), so build correctly emptied the entry rather than pruning a live export; cost 25 build attempts (metrics.jsonl, next-highest entry: 6) before landing empty. Sweep: 749f06f deleted the NUL (real corruption, msg accurate) but left tapHookRows' dedup Map key (`declarations.ts:785`) as bare `event+matcher` concatenation with no separator at all — collision-prone by construction though today's fixed 4-entry table doesn't collide; filed TAP-HOOK-DEDUP-KEY-SEPARATOR-RESTORE. Also filed a friction capture on the grep/NUL-byte blind spot and its 25-attempt cost. Noted for the human: 749f06f shipped under a non-conforming `fix:` prefix with no pending entry, outside the flume gate chain — not plan's to route, flagged in chat.
- Queue: 5 pending, 3 open (READ-DIAL-OVERLAY-MIGRATE, GUARD-MEMBERSHIP-FALLTHROUGH-RESTORE, TAP-HOOK-DEDUP-KEY-SEPARATOR-RESTORE), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — unaffected, unchecked this tick). Refactor: 0 live. Friction: 1 live (plan-null-byte-grep-blindspot.md). Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — the only remaining live job is the posture-sweep rotation (sdk/src/contract.ts next) and 3 pickable entries exist; ready work ships first, the sweep resumes when the wave hands back.
