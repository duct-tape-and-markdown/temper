# Plan state

- Spec derived through: 054addd — routed, zero fresh entries this tick. Not a ratified Decision (no `specs/decisions/` file accompanies it — a "chore(spec)" commit like 787d424, so no Consequences checklist applies); the diff is one hunk (representation.md's `locus` § "embedded" bullet, 4 insertions/1 deletion). Already fully routed last tick: the inbox note (same commit) was derived into KIND-REGISTRATION-LOCUS-FAULT (open) before this tick began. Re-verified the routing's still sound before advancing: the section's other two bullets (`file`, `nested file`) carry no analogous change — `nested file`'s "the child kind governs no glob of its own" already matches `governs.is_none()`, the same predicate the filed entry keys on, so both governs-None loci (embedded + nested-file) are covered by the one entry, not two; disk re-check holds (`local_locus_fault` src/kind.rs:520-533, `local_locus_admissibility`/`LOCAL_LOCUS_RULE` src/admissibility.rs:204-231, gate.rs call site 212-216, `claude_code_supporting_doc` src/builtin_kind.rs:201 still the sole `governs: None` built-in). Cursor advance is the routing act; no new entry filed.
- Audited through: 0bf526a — window a4926ca..0bf526a (bc0d8c0) audited: the shipped fix (IMPORT-NESTED-FILE-DOT-ROOT-STRIP-PREFIX, `discover_nested_file`'s root normalized before `strip_prefix`) re-verified on disk against the shipped diff and proven green (`cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all --check`); no `sdk/**` touch in-window so the SDK gate wasn't re-run. Sizing normal (metrics: 37 turns, no bails, no reverts). pending.json already reflected it drained.
- Residue swept through: 0bf526a — same window: one residue found and filed. That entry's own notes had already flagged `compose.rs:640`'s `base = disc.harness().join(&governs.root)` as sharing the identical unnormalized-join shape, repro'd a bare-stem-id degrade there, but left it out of scope as unverified. Re-confirmed this tick (standalone `strip_prefix` mechanics + the built-in `memory` kind's shape, root ".", glob "**/CLAUDE.md") and filed COMPOSE-GOVERNED-ROOT-DOT-STRIP-PREFIX (open): under `temper check .`, a nested-placement member's id silently collapses to the bare stem, colliding with a sibling member's id.
- Posture swept through: full src/ list covered; sdk/src/assembly.ts, sdk/src/builtins.ts, and sdk/src/claude-code.ts covered too — mid-rotation, unchanged; sdk/src/contract.ts is the tree-order candidate next.
- This tick: SPEC DELTA. Advanced the cursor past 054addd — its sole hunk was already derived last tick (KIND-REGISTRATION-LOCUS-FAULT, filed from the inbox note citing the same commit); re-verified the routing covers the full `locus` § change (both governs-None loci, embedded and nested-file, share the one predicate and the one entry) and that the entry's disk cites still hold. No fresh entry; pending.json unchanged.
- Queue: 4 pending, 2 open (COMPOSE-GOVERNED-ROOT-DOT-STRIP-PREFIX, KIND-REGISTRATION-LOCUS-FAULT), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 1 live (plan-null-byte-grep-blindspot.md, human's to read). Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — no queue-shaping input remains live (inbox drained, spec delta routed through 054addd, no new commits past Audited/Residue-swept cursors, posture rotation open but pickable entries exist: COMPOSE-GOVERNED-ROOT-DOT-STRIP-PREFIX and KIND-REGISTRATION-LOCUS-FAULT are both `open`) — ready work ships first; the posture sweep resumes when the wave hands back.
