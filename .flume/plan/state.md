# Plan state

- Spec derived through: 13455d4a — pipeline.md Telemetry gains the tap-log-homes-in-primary-checkout doctrine (6 lines): "the workspace of record is the primary checkout's ... where the primary cannot be resolved, the log homes where the tap runs." Routed as verified-already-moot, not a gap: src/tap.rs::log_path (171-213) already resolves the git-linked-worktree chain (.git file → gitdir → commondir) to the primary checkout's `.temper/` and falls back to `workspace_dir.join(LOG_FILENAME)` — literally "where the tap runs" — precisely when resolution fails, matching the new text clause for clause. The commit's own body says as much: "Encodes the doctrine behind the 0.0.15 worktree-safe tap (d021b5b6)." Editorial re-cut, no ratified-intent change, no entries filed.
- Audited through: 21f61463 — window 965fcf69..21f61463 reconciled: TAP-INSTRUCTIONS-LOADED-MATCHER-WIDEN shipped (1bc3fe02: sdk/src/builtins.ts's InstructionsLoaded matcher widened to `.*`, registering all five documented load_reason values, verified against sdk/test/emit.test.ts's updated assertions) and COLLECTION-ADDRESS-DUPLICATE-REJECT shipped (21f61463: `collection_address_collision_diagnostics` added at src/admissibility.rs:373, wired at src/gate.rs:213, regression green in `tests/hook_kind.rs::two_kinds_at_the_same_collection_address_trip_collision_loud`) — both dropped from the queue. GUARD-SUFFIX-PATH-BOUNDARY/CHECK-EMBEDDED-MENTION-TARGET/TAP-CWD-PROJECT-DIR-ANCHOR were already dropped pre-window (4d36ce97). COVERAGE-EMBEDDED-COUNT-MARKER's blockedBy condition (COLLECTION-ADDRESS-DUPLICATE-REJECT) is now satisfied — reopened, its src/gate.rs line cite refreshed (~477-484 → ~487, shifted by the collision-diagnostics insert). TAP-RECORD-SCHEMA-V2 not yet shipped (src/tap.rs::TAP_RECORD_VERSION still 1) — untouched.
- Residue swept through: 21f61463 — same window; no residue (no retirement named in any commit body; grepped `path_glob_match` across src/sdk/tests — only a specs/decisions historical mention and builtins.ts's own updated doc comment remain, no stale test/code reference to the narrowed matcher).
- Posture swept through: 22f8064c — unchanged, still mid-rotation: window 22f8064c..HEAD (f1401cc4, 5d078a88, e4f59ffa, 1bc3fe02, 21f61463, 24b22045) keeps touching src/; not this tick's job.
- This tick: post-ship reconciliation — audited+swept 965fcf69..21f61463, both cursors advanced; two shipped entries dropped, one reopened (blocker cleared, line cite refreshed); no new residue.
- Queue: 5 pending — 2 open (TAP-RECORD-SCHEMA-V2, COVERAGE-EMBEDDED-COUNT-MARKER), 1 parked, 2 deferred. Open forks: 3, unchanged. Friction: 0. Amendments: 0. Inbox: 0. Refactor: 0.

Plan continues: after-build — inbox/spec-delta/reconciliation are all clear, but the posture rotation (frontier 22f8064c) is still open past this window, so it's live next; 2 pickable open entries (TAP-RECORD-SCHEMA-V2, COVERAGE-EMBEDDED-COUNT-MARKER) ship first; the sweep resumes when the wave hands back.
