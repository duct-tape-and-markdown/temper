# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: f88e96d — window c2dafe0..f88e96d audited: the one src-touching commit in it, c27b411 (build: pin manifest_read_count to gate/check/explain manifest-cache path), verified on disk against MANIFEST-CACHE-READ-COUNT-PIN's own acceptance — `gate_manifest_cache_read_is_hoisted_across_governing_kinds` (tests/check_cost.rs:640-771) builds two kinds (hook, installed-plugin) both governing the same settings.json via distinct collection addresses, calls `compose::build_manifest_cache`, and asserts the manifest is read exactly once, not twice; `cargo test --test check_cost gate_manifest_cache_read_is_hoisted_across_governing_kinds` reruns green on disk. `json_manifest.rs`'s `manifest_read_count` doc comment (40-43) now names the real pinning test instead of the prior false "Pinned by count-tests" claim with no test — the zero-consumer export the last posture sweep found now has a consumer. `.flume/metrics.jsonl`'s build tick ran 55 turns (vs. this window's own plan tick's 36) — the fixture-heavy test (132 lines, two hand-built `CustomKind`s + `KindFactRow`s) explains the size; noted for visibility only, not a sizing flag. pending.json unchanged (2 entries, both parked; MANIFEST-CACHE-READ-COUNT-PIN already drained by the ship commit).
- Residue swept through: f88e96d — same window. No retirement or demolition named beyond the entry shipped. Both parked entries re-tested: IMPORT-HOP-CAP-CITE (`git diff c2dafe0..HEAD -- src/graph.rs tests/graph.rs` empty, park holds) and PACKAGING-CHANNELS-REMAINDER (`git diff c2dafe0..HEAD -- .github/` empty, park holds). `rg manifest_read_count src/ tests/` shows both `json_manifest::manifest_read_count` and `drift::manifest_read_count` now with real test consumers — no new zero-consumer export introduced. The json_manifest.rs `Manifest::read` doc comment orphan (open-questions.md) shifted 347→352 in this window's diff; re-read and updated in place — the shipped entry touched only the unrelated `manifest_read_count` doc comment (40-43), so the orphan still rides whichever entry next names it.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs, src/frontmatter.rs, src/gate.rs, src/glob.rs, src/graph.rs, src/hash.rs, src/import.rs, src/install.rs, src/json_manifest.rs, src/json_splice.rs covered, mid-rotation. src/kind.rs is the tree-order candidate next.
- This tick: POSTURE SWEEP, src/json_splice.rs — neighborhood read whole (its only import is `serde_json`; no local-module imports to widen the read), swept clean against engineering.md and architecture.md. Export-earns-its-consumer (grep-verified): every `pub(crate)` surface has a live caller outside the module — `apply_edits`/`object_shape`/`array_shape`/`insert_member`/`append_element` all called from `install.rs`'s settings-merge path, `object_shape`/`insert_member`/`apply_edits` also from `json_manifest.rs`'s test, `pretty_at` from both `insert_member`/`append_element` in-module and `json_manifest.rs::render_object` (517) — no zero-consumer export. Libraries-before-hand-rolls: the module's own header (1-8) states its hand-rolled byte scanning exists to leave "every other byte (a human's key order, spacing, indentation) untouched" — the pinned-semantics exception the section itself names, grounded in `specs/model/pipeline.md` "Install" (223-224, 235): "The settings write is format-preserving — existing keys, order, and formatting survive the insertion; install never re-serializes a file it does not own." No sanctioned crate in CLAUDE.md's Tech stack covers a format-preserving JSON editor (unlike `toml_edit` for TOML), so the hand-roll is the sanctioned exception, not residue. One-job-one-home: `json_manifest.rs`'s own doc comment (464-467) already asserts `pretty_at` is consolidated as the single shared pretty-printer ("never a second encoder") and `render_object` (505-521) calls it rather than re-implementing — read and confirmed, no second encoder exists anywhere in `src/`. Embedded-provider-knowledge: the module is fully generic JSON text mechanics with no Claude-Code literal (no kind name, locus, or payload-schema string) — the provider-specific structure (`"hooks"`, settings.json shape) lives in `install.rs`'s call sites, correctly layered per architecture.md "The provider face is data." Cohesion: single job (locate spans, splice edits, pretty-print at indent), no split-worthy second concern. Dead plumbing: no enums, no `Result` — none to check. The module header's stale "install's … the sole consumer" claim (6-8, `json_manifest.rs` also consumes it since 404b73a-adjacent work) is already the open-questions.md ride-only orphan; unchanged this tick, no entry opens the file so it still rides forward. Nothing fileable — posture cursor advances alone.
- Queue: 2 pending, 0 open, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick, not re-tested this tick since post-ship reconciliation wasn't this tick's job). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — the posture sweep rotation is still open (next candidate src/kind.rs) with no pickable entries in queue; it resumes next tick.
