# Plan state

- Spec derived through: 13455d4a — unchanged, copied forward (not this tick's job).
- Audited through: 9b967326 — advanced from 2fa5efd7. Verified on disk: normalize_instructions_loaded_identity (src/telemetry.rs) now looks up the lock's path_to_id map (built in src/read.rs's explain_target via drift::emit_owned_targets) before falling back to the bare-stem heuristic; field/requirement_field/explain/requirement_detail thread the map through exactly as the entry's files[].description specified. Regression test added (tests/read_verbs.rs) passes (`cargo test --test read_verbs a_nested_instructions_loaded_record_joins_to_its_placement_folded_member_id`: ok); 29/30 other read_verbs tests pass, the one failure (sdk-round-trip test) is a pre-existing environment gap (sdk/dist unbuilt in this worktree), unrelated to this fix. Dropped TELEMETRY-INSTRUCTIONS-LOADED-NESTED-JOIN from the queue.
- Residue swept through: 9b967326 — advanced, same window (2fa5efd7..9b967326). No retirement/demolition named in either commit body; 3237033f only added a refactor-capture file (already drained last tick, not a src/tests/sdk touch) and carries no residue of its own.
- Posture swept through: 22f8064c — unchanged, mid-rotation continues. This tick's window (22f8064c..HEAD, code delta — no touch to specs/process/engineering.md or specs/process/architecture.md) armed a frontier of every module the four intervening build commits touched: src/read.rs, src/telemetry.rs, tests/read_verbs.rs, src/tap.rs, tests/tap.rs, src/admissibility.rs, src/gate.rs, tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts, tests/emit.rs, src/graph.rs, tests/graph.rs, src/install.rs, tests/install.rs, src/compose.rs, src/drift.rs, src/glob.rs, src/placement.rs.
- This tick: posture sweep, one neighborhood — src/gate.rs (frontier) read with its immediate import src/admissibility.rs (also frontier, both touched by 21f61463 "refuse collision when two kinds declare same collectionAddress"). Found and filed: admissibility.rs's new `collection_key_path_to_string` (412-419) hand-copies the same four `CollectionKeyPath` wire-label literals ("hooks.<Event>", "mcpServers.*", "enabledPlugins.*", "extraKnownMarketplaces.*") that src/kind.rs's `collection_key_path_from_label` (971) already owns as the closed vocabulary's one home — a second encoder of one concept, per "A shared concept is one type". Filed COLLECTION-KEY-PATH-LABEL-CONSOLIDATE (mechanical, open). Rest of gate.rs/admissibility.rs read clean against the other engineering.md sections and architecture.md. Covered this tick: src/gate.rs, src/admissibility.rs. Remaining frontier (uncovered): src/read.rs, src/telemetry.rs, tests/read_verbs.rs, src/tap.rs, tests/tap.rs, tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts, tests/emit.rs, src/graph.rs, tests/graph.rs, src/install.rs, tests/install.rs, src/compose.rs, src/drift.rs, src/glob.rs, src/placement.rs.
- Queue: 5 pending — 2 open (COVERAGE-EMBEDDED-COUNT-MARKER, COLLECTION-KEY-PATH-LABEL-CONSOLIDATE), 1 parked, 2 deferred. Open forks: 3, unchanged. Friction: 0. Amendments: 0. Inbox: 0. Refactor: 0.

Plan continues: yes — an armed rotation is a live plan job until its frontier empties (posture-sweep rule); the remaining frontier modules above still need their neighborhoods swept next tick, resuming without waiting on a forced wake.
