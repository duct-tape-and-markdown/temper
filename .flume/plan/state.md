# Plan state

- Spec derived through: 13455d4a — unchanged, copied forward (not this tick's job).
- Audited through: 9b967326 — unchanged, copied forward (not this tick's job).
- Residue swept through: 9b967326 — unchanged, copied forward (not this tick's job).
- Posture swept through: 22f8064c — unchanged, mid-rotation continues. This tick's window (22f8064c..HEAD, code delta) frontier unchanged from last tick's derivation: src/read.rs, src/telemetry.rs, tests/read_verbs.rs, src/tap.rs, tests/tap.rs, src/admissibility.rs (covered), src/gate.rs (covered), tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts, tests/emit.rs, src/graph.rs, tests/graph.rs, src/install.rs, tests/install.rs, src/compose.rs, src/drift.rs, src/glob.rs, src/placement.rs.
- This tick: posture sweep, one neighborhood — src/read.rs (frontier, its `explain`/`telemetry::field`/`telemetry::requirement_field`/`explain_target` paths read in full) together with its immediate import src/telemetry.rs (also frontier) and tests/read_verbs.rs (also frontier, the pair's test file), all three touched by 9b967326 "normalize InstructionsLoaded identities via lock's path-to-id mapping". Found and filed: telemetry.rs's `field()` (89-93) and `requirement_field()` (202-206) each hand-copy an identical `if record.event == TapEvent::InstructionsLoaded { normalize_instructions_loaded_identity(...) } else { record.identity.clone() }` dispatch block — two normalizers of one job, per "One job, one home". Filed TELEMETRY-IDENTITY-DISPATCH-DEDUP (mechanical, open). read.rs's threading of path_to_id (built once in explain_target at 1780, passed through explain/requirements/requirement_detail) is hoisted correctly, no cost-scale issue; read_verbs.rs's local test builders (feature/req/tap_record/tap_readout) are file-scoped single-purpose helpers, not duplicated scaffolding. Rest of the neighborhood read clean against engineering.md and architecture.md. Covered this tick: src/read.rs, src/telemetry.rs, tests/read_verbs.rs. Remaining frontier (uncovered): src/tap.rs, tests/tap.rs, tests/hook_kind.rs, sdk/src/builtins.ts, sdk/src/declarations.ts, tests/emit.rs, src/graph.rs, tests/graph.rs, src/install.rs, tests/install.rs, src/compose.rs, src/drift.rs, src/glob.rs, src/placement.rs.
- Queue: 6 pending — 3 open (COVERAGE-EMBEDDED-COUNT-MARKER, COLLECTION-KEY-PATH-LABEL-CONSOLIDATE, TELEMETRY-IDENTITY-DISPATCH-DEDUP), 1 parked, 2 deferred. Open forks: 3, unchanged. Friction: 0. Amendments: 0. Inbox: 0. Refactor: 0.

Plan continues: after-build — 3 pickable open entries exist and the posture sweep is the only other remaining live job; ready work ships first, the sweep resumes over the remaining frontier when the wave hands back.
