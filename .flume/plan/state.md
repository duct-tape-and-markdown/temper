# Plan state

- Spec derived through: 13455d4a — pipeline.md Telemetry gains the tap-log-homes-in-primary-checkout doctrine (6 lines): "the workspace of record is the primary checkout's ... where the primary cannot be resolved, the log homes where the tap runs." Routed as verified-already-moot, not a gap: src/tap.rs::log_path (171-213) already resolves the git-linked-worktree chain (.git file → gitdir → commondir) to the primary checkout's `.temper/` and falls back to `workspace_dir.join(LOG_FILENAME)` — literally "where the tap runs" — precisely when resolution fails, matching the new text clause for clause. The commit's own body says as much: "Encodes the doctrine behind the 0.0.15 worktree-safe tap (d021b5b6)." Editorial re-cut, no ratified-intent change, no entries filed.
- Audited through: 2fa5efd7 — window 21f61463..2fa5efd7 reconciled: TAP-RECORD-SCHEMA-V2 shipped (4bd13394: src/tap.rs::TAP_RECORD_VERSION now 2, TapRecord gained ts + raw_path, identity relativized; 2fa5efd7 fixed a same-window off-by-one-parent bug in that relativization) and was already dropped from the queue by its own ship commit (7d1b50b9) — verified on disk, nothing left to drop this tick. No other pending entry's blocker moved in this window.
- Residue swept through: 2fa5efd7 — same window, one finding: TAP-RECORD-SCHEMA-V2's own entry text named reusing `drift::emit_owned_targets`'s path-to-member-id rows (src/drift.rs:2968) for telemetry.rs's read-side identity join; the shipped code instead hand-rolled `normalize_instructions_loaded_identity` (src/telemetry.rs:17-27), a bare `file_stem` strip. That heuristic silently mis-joins any nested CLAUDE.md member, whose id `fold_file_id` (src/frontmatter.rs:389) folds directory placement into (by design, per that fn's own doc comment on same-named-file nesting) — a second, wrong implementation of the "join through the lock's own declarations" job pipeline.md's Telemetry section states. No test caught it: grepped tests/read_verbs.rs for InstructionsLoaded field-strand assertions — zero. Filed as TELEMETRY-INSTRUCTIONS-LOADED-NESTED-JOIN, cited to pipeline.md Telemetry.
- Posture swept through: 22f8064c — unchanged, still mid-rotation: window 22f8064c..HEAD keeps touching src/ (now also 4bd13394, 2fa5efd7); not this tick's job.
- This tick: post-ship reconciliation — audited+swept 21f61463..2fa5efd7; TAP-RECORD-SCHEMA-V2 confirmed already dropped, one residue finding filed (TELEMETRY-INSTRUCTIONS-LOADED-NESTED-JOIN, a mis-joining heuristic the ship commit substituted for the entry's own named reuse).
- Queue: 5 pending — 2 open (TELEMETRY-INSTRUCTIONS-LOADED-NESTED-JOIN, COVERAGE-EMBEDDED-COUNT-MARKER), 1 parked, 2 deferred. Open forks: 3, unchanged. Friction: 0. Amendments: 0. Inbox: 0. Refactor: 0.

Plan continues: after-build — inbox/spec-delta/reconciliation are all clear, but the posture rotation (frontier 22f8064c) is still open past this window, so it's live next; 2 pickable open entries (TELEMETRY-INSTRUCTIONS-LOADED-NESTED-JOIN, COVERAGE-EMBEDDED-COUNT-MARKER) ship first; the sweep resumes when the wave hands back.
