# Plan state

- Spec derived through: b645125 — unchanged, not this tick's job.
- Audited through: 2bfcccd — unchanged; 2bfcccd..HEAD touches no src/tests/sdk (only the two prior plan commits 8baf50a/00b880d landed since, verified this tick).
- Residue swept through: 2bfcccd — unchanged, same reasoning.
- Posture swept through: 00b880d — rotation closes. Verbs neighborhood swept this tick: main.rs read whole (the only verbs module touched since d01c0a2, via e25f62f) together with its immediate imports (drift.rs, kind.rs, install.rs consulted for the finding below); foundation/model/formats/judges/provider stay skipped, untouched this cycle.
- This tick: POSTURE SWEEP, verbs neighborhood. main.rs's `manifest_path` (522-529) inlines the exact `.`-root branch drift.rs's private `join_locus` (827-833, 3 existing call sites) already performs instead of calling it — a second implementation of one job (engineering.md, "One job, one home"). Read together with it: the doc comment directly above `guarded_manifests` (463-468) is dead prose orphaned by 6807d59 (`build: extract explain's dispatch body to read.rs`), which moved the free `fn explain` this comment used to describe out to `read::explain_target` but left the comment glued — no blank line — above `guarded_manifests`'s own doc. Both fixes land in one entry, MAIN-MANIFEST-PATH-JOIN-LOCUS-STALE-DOC-PRUNE (open, no blockers) — the stale doc rides it per the ride-only rule rather than filing standalone, since the entry already opens main.rs. The frontier (main.rs, the only touched verbs module) is now fully read and covered, so the rotation closes: no untouched module holds a live frontier.
- Queue: 4 pending, 2 open, 0 blockedBy, 2 parked. Refactor: 0 live. Friction: 0 live. Inbox: 0 notes.

Plan continues: no — the posture rotation closed (frontier empty) and the queue's two open entries are pickable build work, not a plan-shaping input; next plan tick re-arms on a fresh inbox/spec-delta/reconciliation window or a commit past 00b880d touching src/, sdk/src/, tests/, or the posture pages.
