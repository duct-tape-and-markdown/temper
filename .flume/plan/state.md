# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: c9d6cea — unchanged, not this tick's job.
- Residue swept through: c9d6cea — unchanged, not this tick's job.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs covered, mid-rotation. src/extract.rs is the tree-order candidate next.
- This tick: POSTURE SWEEP, neighborhood src/engine.rs (read together with contract.rs's `Predicate`/`Selector`/`declared_keys` definitions for context). Read the whole non-test body (1-1318) against engineering.md/architecture.md: no cohesion, dead-plumbing, embedded-provider-knowledge, or zero-consumer findings — every `pub` item has a live external caller (`rg` confirmed). One residue finding: `decide`'s `Predicate::When` arm re-implements the Enum/Type membership test inline (lines 1225/1229) that its own `Predicate::Type`/`Predicate::Enum` arms already compute (918/955) — the identical two boolean expressions, hand-written twice with no shared helper. Filed ENGINE-WHEN-GUARD-MEMBERSHIP-DEDUP (per engineering.md, "One job, one home"), scoped to also add the first direct runtime test for `Predicate::When` (none exists today — only a lock round-trip and an unrelated exhaustive-match fixture touch `when`). Posture cursor advanced: engine.rs now covered, extract.rs next.
- Queue: 3 pending, 1 open (ENGINE-WHEN-GUARD-MEMBERSHIP-DEDUP), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — a pickable entry now exists (ENGINE-WHEN-GUARD-MEMBERSHIP-DEDUP) and it is the only live job besides the still-open posture rotation (next neighborhood src/extract.rs); build ships the entry first, the sweep resumes when the wave hands back.
