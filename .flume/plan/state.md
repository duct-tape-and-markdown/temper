# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: 9a92763 — window ac40c72..9a92763 (b0470a4 the sole src/tests/sdk commit) audited: TELEMETRY-READ-PLURAL-CONSOLIDATE shipped as scoped (src/telemetry.rs:85 and src/read.rs:1357 now call `crate::display::plural`, both hand-rolled branches gone), already off the queue. `cargo test --lib` (274 passed) and `cargo clippy --all-targets` clean. metrics.jsonl shows the build tick at 27 turns, merge shipped clean, no revert — sizing fine. Both parks (IMPORT-HOP-CAP-CITE: src/graph.rs, tests/graph.rs untouched; PACKAGING-CHANNELS-REMAINDER: .github/ untouched) re-tested, still holding.
- Residue swept through: 9a92763 — same window. `rg` for other hand-rolled singular/plural suffix branches found none: the remaining irregular noun/verb pairs (src/read.rs:895 document/documents+carries/carry, src/read.rs:1453 single-satisfier narrative, src/coverage_note.rs:370 is/are) are a different job — verb agreement and threshold narrative, not the "n"+"s" suffix the shared `plural` helper encodes — not duplicates of the consolidated surface.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs covered, mid-rotation. src/engine.rs is the tree-order candidate next.
- This tick: POSTURE SWEEP, neighborhood src/drift.rs + its crate imports read for context (compose, contract, extract::host_address, graph, hash::sha256_hex, kind's row-lifters, layout). Three findings filed, blockedBy-chained since all touch drift.rs: DRIFT-COLLECTION-ADDRESS-ENTRY-SHAPE-DEDUP (open — `entry_shape_of`/1143-1150 hand-reparse the collection-address wire label `kind::collection_address_from_row` already resolves loud, its own doc admitting the mirror; the shared call already used at drift.rs:1754), DRIFT-CONFIG-STALE-FROM-DOC-DELEGATE (blockedBy the first — `config_stale` duplicates `config_stale_from_doc`'s loop verbatim instead of delegating, unlike every other path/doc sibling pair in the file), DRIFT-WRITE-ROLLUP-CUSTOM-PARAM-PRUNE (blockedBy the second — `write_rollup`'s `custom` param is always passed `&BTreeMap::new()` at its sole call site, dead since emit's own rollup already merges every kind into one map). A fourth issue — `source_dep_stale_from_doc`'s doc comment (2867-2886) glued to `source_dep_stale`'s contract, the undocumented sibling — didn't reach entry scope (comment staleness rides the file per the ride-only rule; no entry here reaches 2867-2941); recorded as the eighth ride-only orphan in open-questions.md.
- Queue: 5 pending, 1 open, 2 blockedBy (chained behind the open one, same file), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — the only remaining live job is the posture-sweep rotation (mid-rotation, next neighborhood src/engine.rs), and a pickable entry now exists (DRIFT-COLLECTION-ADDRESS-ENTRY-SHAPE-DEDUP); the sweep resumes once the wave hands back.
