# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: d4ec7da — window 2a6e488..d4ec7da reconciled.
- Residue swept through: d4ec7da — same window.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs, src/engine.rs, src/extract.rs, src/frontmatter.rs, src/gate.rs, src/glob.rs, src/graph.rs covered, mid-rotation. src/hash.rs is the tree-order candidate next — unchanged, not this tick's job.
- This tick: POSTURE SWEEP, src/graph.rs (neighborhood = the module plus its immediate imports check.rs/compose.rs/contract.rs/engine.rs/extract.rs/kind.rs, read for context only — all but kind.rs already covered, so only graph.rs itself is recorded covered). Found: `degree` (247-260) and `mention_reachable` (365-380) each open with a byte-identical opt-in guard — `selections.iter().any(|selection| selection.clauses.iter().any(|clause| matches!(clause.predicate, Predicate::X { .. })))` — differing only in the `Predicate` variant, the same residue class GATE-TWO-GREENS-DISPATCH-DEDUP (175d8d5) just fixed in gate.rs. Filed GRAPH-CLAUSE-GUARD-DEDUP. `MAX_IMPORT_HOPS` (75) and its stale cite (71-74) re-verified on disk, unchanged — already the parked IMPORT-HOP-CAP-CITE, not re-filed. No dead plumbing (`rg` confirmed every pub symbol — `resolved_edges_count`, `reachability_orphaned`, `DirectiveMember`/`DirectiveClassing`, both `ImportDeclaration`/`MentionDeclaration` families, `REQUIREMENT_KIND`, `extract_globs` — has a real consumer outside the file). No cohesion violation: the module's five graph checks share one resolved-edge enumeration by its own header's design, not sprawl.
- Queue: 3 pending, 1 open (GRAPH-CLAUSE-GUARD-DEDUP), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — GRAPH-CLAUSE-GUARD-DEDUP is pickable and ships next; the open posture rotation (next neighborhood src/hash.rs) resumes once the wave hands back.
