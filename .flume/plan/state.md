# Plan state

- Spec derived through: 8911c38 — "A seam gate reads what the real writer wrote" (specs/process/engineering.md) fully routed: surveyed every `tests/*.rs` file matching `agree|byte.pin|round.trip|frozen` for the anti-pattern (writer-vs-writer self-agreement, or a real reader driven over hand-authored counterpart rows, on an *agreement* claim). Two live findings filed — MENTION-REACHABLE-GATE-SDK-AGREEMENT, CUSTOM-KIND-CONTENT-SDK-AGREEMENT — both `tests/lock_declaration_rows.rs` comments overclaiming SDK-emit coverage three compiler-tier tests never exercise, verified against `src/builtin_lock.toml`, `src/contract.rs`, `src/kind.rs`, `sdk/src/kind.ts` on disk. `tests/tap.rs`'s legacy-version test checked and ruled exempt (refusal-test analogy: today's writer cannot produce a stale-schema row). Everything else checked compliant or non-applicable (full list in the plan commit body).
- Audited through: b829216 — window 1fe148c..b829216 (62a7dfb, cfc9516 touched `tests/`) audited: both entries filed off 8911c38 shipped (90cf7a5 MENTION-REACHABLE-GATE-SDK-AGREEMENT, b829216 CUSTOM-KIND-CONTENT-SDK-AGREEMENT), already dropped from pending.json by their own ship commits. Verified on disk, not just the log: `tests/builtin_lock_frozen.rs`'s `the_sdk_derived_installed_plugin_kind_round_trips_through_the_engine_reader` (166-183) decodes the real SDK-derived `rule.mention-reachable` clause row through `contract::predicate_from_row` over a live `wire_sdk_harness`/`drift::emit_program` run — no hand-built row; `tests/emit.rs`'s `emit_program_emits_a_custom_kinds_layout_content_and_fields_shape_end_to_end` (1888-1979) drives a real SDK `kind()` fixture (`CUSTOM_KIND_CONTENT_PROGRAM`) through the same live emit path and decodes both the layout-content and fields-only+collectionAddress kinds through `CustomKind::from_kind_fact_row`. `tests/lock_declaration_rows.rs`'s three comments (556, 1152-1159, 1228-1236) now honestly scope themselves as hand-built compiler-tier round-trips and point at the two new tests for the SDK-agreement claim. `cargo test` full-suite green (Rust + `pnpm --dir sdk test`). Metrics glance: 33 turns/186s and 69 turns/383s, unremarkable. Both queued parks (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER) re-checked against this window's diff — neither's condition touched (no `src/graph.rs`, no `.github/`) — still hold.
- Residue swept through: b829216 — same window: no retirement, demolition, or unsanctioned symbol found; the only src-tree change is the two verified test additions plus their comment rewording. None of the six ride-only stale-cite orphans' files (`json_splice.rs`, `drift.rs`, `document.rs`, `install.rs`, `extract.rs`, `json_manifest.rs`) touched, so none discharge.
- Posture swept through: 6f74784 — still re-armed, unchanged this tick (not this job's turn): forward window now also carries 62a7dfb and cfc9516 (`tests/`) alongside 8911c38 (`specs/process/engineering.md`); rotation resumes next tick.
- This tick: POST-SHIP RECONCILIATION — window 1fe148c..b829216 audited and swept clean; both cursors advanced to b829216.
- Queue: 2 pending, 0 open, 0 blockedBy, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — unchanged, not this tick's job). Refactor: 0 live. Friction: 0 live. Amendments: 0 live (0044's first amendment ratified at 00c3a22, record deleted). Inbox: 0 notes.

Plan continues: no — queue holds only the two human-parked entries, nothing pickable for build to run concurrently; the posture sweep is next tick's live job (nothing above it will be live), but with no ready work to ship alongside it that's `after-build`'s condition failing, not this cycle's to chain into. The loop quiets until the next invocation.
