# Plan state

- Spec derived through: 087b90a — unchanged; two commits still await routing (049ae18 invariant-8, caf29fa 0035's embedded-locus amendment). caf29fa's own ask ("Plan derives the embedded-span entry") is very likely moot on disk already — see this tick's inbox finding below — so next tick's spec-delta job can verify-and-close rather than derive fresh.
- Audited through: a00e14a — unchanged; 19258b7 (ship DECLARATIONS-TELEMETRY-HOOK-PROVIDER-FACE-MOVE, build a6db2b5) touched sdk/src/declarations.ts + sdk/src/builtins.ts since — a00e14a..HEAD is now a live post-ship reconciliation window, third in line behind spec delta.
- Residue swept through: a00e14a — same open window as above; unreconciled.
- Posture swept through: sdk/src/declarations.ts (+ its immediate imports assembly.ts/kind.ts/contract.ts/prose.ts/builtins.ts, read for context per the posture-sweep rule) covered; sdk/src/dial.ts next in rotation (tree order after declarations.ts; the phrase delta at 2e2b32a still owes sdk/src/ the remainder — dial.ts, emit.ts, index.ts, kind.ts, needs.ts, prose.ts — then the tests/ tree). Unchanged this tick.
- This tick: INBOX. One note ("`extent` ranges over the embedded locus", per 0035's Amendment, observed at 049ae18) re-verified against HEAD and found already shipped: EMBEDDED-EXTENT-CAPTURE (e8e7a3b, 2026-07-17 — three days before the amendment it cites and the note's own observed-at commit) added `Locus::Embedded` to `Predicate::Extent`'s exemption from `bodyless` (src/engine.rs ~286-291), threaded `renderedExtents` from `emit.ts`'s `renderMemberBlock` through `NestedMemberRow`/`Features.rendered_lines`/`rendered_chars` (`Option<usize>`), and `tests/extent.rs::an_extent_clause_bound_to_an_embedded_kind_judges_the_captured_span` already covers exactly the requested case (each-grain over/under budget, both units, whole-grain summation) alongside `each_grain_fires_past_the_bound_and_holds_within_it_in_both_units` proving file-locus behavior unchanged. Nothing to file — would duplicate shipped work. Drained the note.
- Queue: 3 pending — 1 open (DECLARATIONS-ZERO-CONSUMER-EXPORTS-PRUNE, unblocked since 19258b7 shipped its blocker), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER). Open forks: 2 (multi-harness-projection, lazy-grounds). Friction: 0. Amendments: 0. Inbox: 0 notes.

Plan continues: yes — spec delta (049ae18, caf29fa) is the next live input, ahead of the a00e14a..HEAD post-ship reconciliation window and the open dial.ts posture rotation.
