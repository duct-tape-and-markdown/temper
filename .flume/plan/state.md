# Plan state

- Spec derived through: b645125 — unchanged, not this tick's job.
- Audited through: 1e5aae2 — unchanged; no commit since touched src/, tests/, or sdk/.
- Residue swept through: 1e5aae2 — unchanged, same reason.
- Posture swept through: 77c0790, mid-rotation — code delta 77c0790..HEAD touched sdk/src/contract.ts, sdk/src/declarations.ts (3533e0b) and tests/builtin_lock_frozen.rs (01c3650); frontier = those three modules. sdk/src/contract.ts + kind.ts covered prior tick (1 finding). sdk/src/declarations.ts + its immediate non-generated imports assembly.ts and prose.ts covered this tick (kind.ts/contract.ts already read last tick). Next: tests/builtin_lock_frozen.rs.
- This tick: POSTURE SWEEP. Read sdk/src/declarations.ts + assembly.ts + prose.ts against engineering.md and architecture.md. assembly.ts and prose.ts clean. declarations.ts's `SETTINGS_MANIFEST` constant (806-811) independently re-declares and re-cites (`code.claude.com/docs/en/settings`, retrieved 2026-07-10) the `"settings.json"` manifest fact `builtins.ts` already carries as kind data (`hook`/`installedPlugin`/`knownMarketplace`'s locus/collectionAddress, its own settings.json cites at 1208/1238/1244/1250, retrieved 2026-07-16) — architecture.md, "The provider face is data" names `builtins`+`claude-code` the SDK's provider face; `declarations.ts` is the seam's row compiler, not that face. Filed SETTINGS-MANIFEST-PROVIDER-FACT-LEAK (open, mechanical: export the constant from builtins.ts, import it in declarations.ts; no cycle). Rotation stays open — tests/builtin_lock_frozen.rs remains in the frontier.
- Queue: 4 pending, 2 open, 0 blockedBy, 2 parked. Refactor: 0 live. Friction: 0 live. Amendments: 1 live (plan-posture-sweep-marker-drift.md — posture-sweep.md's unconditional "closing marker is yes while frontier non-empty" collides with plan-state.md's `after-build` carve-out for exactly this tick's shape; followed plan-state.md, filed the drift for a human to ratify). Inbox: 0 notes.

Plan continues: after-build — plan-state.md: "`after-build` iff the only remaining live job is the posture sweep AND pickable entries exist." Two pickable open entries (TELEMETRY-DOC-PROVIDER-FACT-LEAK, SETTINGS-MANIFEST-PROVIDER-FACT-LEAK) ship first; the sweep resumes over tests/builtin_lock_frozen.rs when the wave hands back.
