# Plan state

- Spec derived through: 13455d4a — unchanged, copied forward (not this tick's job).
- Audited through: 53852595 — window 8feb4617..53852595 (10 src/tests/sdk-touching commits) reconciled. Every commit maps to a tag the dispatcher already dequeued (REQUIREMENT-KIND-CONSTRAINT-ENFORCEMENT, IDENTITY-MAP-REFUSING-CONSTRUCTOR, GUARD-SETTINGS-JSON-EMIT-OWNED-COVERAGE, PATH-ROOT-NEWTYPE-LOCK-SOURCE-PATH, BUILTIN-CONTRACT-MATRIX-SNAPSHOT, GUARD-POSTTOOLUSE-BASH-DRIFT-CHECK, EMIT-DISCOVERY-MEMBER-SET-ROUND-TRIP, GRAPH-SCOPE-WITNESS-GLOB-VOCABULARY) plus two already-swept fixes (e77766c5 AT-LOCUS, 017e59e0 skill mentionReachable); all verified present on disk. Nothing left to drop. `.flume/metrics.jsonl` is gitignored and absent from this worktree — glance skipped, not a blocker.
- Residue swept through: 53852595 — same window, no orphaned symbols/vocabulary. One live hypothesis chased to ground: b284dd80 (IDENTITY-MAP-REFUSING-CONSTRUCTOR) made `sdk/src/emit.ts`'s `memberTable` a refusing `uniqueMap` keyed by `kind:name`; this repo's own `.temper/lock.toml` carries two `hook`/`PostToolUse` registration rows, the exact shape that sank the earlier duplicate-key refusal (e4ff7bc6, parked as HOOK-COLLECTION-ADDRESS-DUPLICATE-REFUSAL). Read `sdk/src/declarations.ts`'s `tapHookRows`: it synthesizes registration rows only, never inserted into `harness.members`, so `memberTable` never sees the tap-hook side of the pair — confirmed empirically, `temper emit --dry-run` off a built SDK: 12 unchanged, 0 errors. No gap; nothing filed. (`.temper/package-lock.json` was touched by the `npm install` needed to run this check and restored via `git checkout --`.)
- Posture swept through: 22f8064c — unchanged, mid-rotation continues (not this tick's job).
- This tick: post-ship reconciliation, window 8feb4617..53852595. Re-tested both stale-gate conditions still in the queue: EMIT-BANNER-OWNERSHIP-MOVE's cited `install.rs`/`drift.rs` symbols (`NOTE_COMMENT`, `NOTE_BANNER`, `project_banner`, `project_bytes`, `emit()`) all still resolve after two intervening `install.rs` edits (1c80c77b, cf67f291) shifted line numbers by ~150-200 — the entry's citations are `~`-approximate already, so no rewrite owed. HOOK-COLLECTION-ADDRESS-DUPLICATE-REFUSAL's park condition (the `(hook-member-identity)` fork) is untouched by this window — confirmed above the window's identity-map work sits on the member-identity axis, not the fork's collection-address-collapse axis. Queue unchanged: 0 dropped, 0 filed.
- Queue: 6 pending — 1 open, 3 parked, 2 deferred. Open forks: 5 (unchanged). Friction: 0. Amendments: 0. Inbox: 0. Refactor: 0.

Plan continues: after-build — EMIT-BANNER-OWNERSHIP-MOVE is pickable now; the posture sweep's open rotation resumes once the wave hands back.
