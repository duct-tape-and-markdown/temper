# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: bfe147c — unchanged; window bfe147c..HEAD (feef754, 5009090, d3cb30c, b5f7d7c) still live, deferred this tick — inbox outranked it.
- Residue swept through: bfe147c — unchanged, same deferred window.
- Posture swept through: full src/ list covered; sdk/src/assembly.ts, sdk/src/builtins.ts, and sdk/src/claude-code.ts covered too — mid-rotation, unchanged; sdk/src/contract.ts is the tree-order candidate next.
- This tick: INBOX. A note appeared on `.flume/inbox.md` mid-tick (after the session's orientation digest had already read it empty) — a centercode field report (PR #23): `temper check .` silently drops every nested-file member. Re-verified rather than transcribed: read `unit_dir`/`discover_nested_file` (src/import.rs) and `normalize_path` (src/address.rs) on disk, then reproduced directly with the built binary on a synthetic `.claude/skills/x/{SKILL.md,reference.md}` fixture — `temper check $(pwd)` reports `supporting-doc (1)`, `temper check .` on the identical tree reports `supporting-doc (0)`, no error. Root-caused past the note's own citation: the precise defect is `discover_nested_file`'s `root` construction (import.rs:234), built via a raw `disc.harness().join(&governs.root)` with no `crate::address::normalize_path` — unlike the sibling `scan_locus` (import.rs:351), which already normalizes for the identical reason its own comment states. Confirmed CI blindness: every existing `discover_builtin`/`discover_nested_file` test (import.rs's own `mod tests`, tests/nested_member.rs) constructs `Discovery` over an absolute `tempfile` dir, never a `.`-rooted one. Checked a sibling site sharing the same unnormalized-join pattern, `compose.rs:640` (feeds `frontmatter::fold_file_id`'s `strip_prefix`) — reproduced no symptom there (a nested-`CLAUDE.md` fixture showed identical member counts relative vs. absolute; that path degrades to a bare-stem id on a miss rather than dropping the member), so left out of this entry's scope rather than claimed as broken on an unverified premise. Filed IMPORT-NESTED-FILE-DOT-ROOT-STRIP-PREFIX (open, per specs/model/representation.md "nesting") and drained the inbox line.
- Queue: 3 pending, 1 open (IMPORT-NESTED-FILE-DOT-ROOT-STRIP-PREFIX), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 1 live (plan-null-byte-grep-blindspot.md, human's to read). Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — the deferred post-ship reconciliation window (bfe147c..HEAD) is still unreconciled and outranks both the pickable entry and the open posture rotation.
