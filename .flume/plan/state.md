# Plan state

- Spec derived through: 8911c38 — unchanged, not this tick's job.
- Audited through: c9d6cea — window 9a92763..c9d6cea (a96d0b0, f0880ce, c52a1db the three src commits, shipping the three drift.rs findings filed last tick) audited on disk: entry_shape_of deleted (drift.rs:1140-1148 now wired to `collection_address_from_row(facts)?.map(|addr| addr.entry_shape)` directly, the 54-line hand-parse gone); config_stale now delegates to config_stale_from_doc via read_lock_document_for_emit, its own duplicate loop gone; write_rollup collapsed to one `rollups` param, the dead `custom: &BTreeMap::new()` call-site argument and loop gone. All three off pending.json already (build's own removal on ship). `cargo test --lib` (274 passed), `cargo clippy --all-targets`, `cargo fmt --all --check` all clean; `rg` confirms no dangling `entry_shape_of` or two-map `write_rollup(` references anywhere in src/tests/sdk. metrics.jsonl: three build ticks (37/58/25 turns), three merges, no reverts — sizing fine, the 58-turn outlier still shipped clean. Both parks re-tested (`git diff 9a92763..c9d6cea -- src/graph.rs tests/graph.rs .github/` empty), still holding.
- Residue swept through: c9d6cea — same window. All three commits are pure deletions/consolidations onto an already-shared surface; no new duplicate introduced. The eighth ride-only orphan (`source_dep_stale_from_doc`'s glued doc comment) re-read and re-cited at its shifted lines (now 2787-2861, shifted -80 by this window) in open-questions.md — none of the three shipped commits touched it, and no entry is currently open on drift.rs at all, so it still rides whichever one next does.
- Posture swept through: src/builtin.rs, src/builtin_kind.rs, src/builtin_lock.rs, src/bundle.rs, src/check.rs, src/compose.rs, src/contract.rs, src/coverage.rs, src/coverage_note.rs, src/dial.rs, src/display.rs, src/document.rs, src/drift.rs covered, mid-rotation. src/engine.rs is the tree-order candidate next.
- This tick: POST-SHIP RECONCILIATION, window 9a92763..c9d6cea. Audit + sweep both clean; both cursors advanced to c9d6cea.
- Queue: 2 pending, 0 open, 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 0 live. Amendments: 0 live. Inbox: 0 notes.

Plan continues: yes — no pickable entries exist (both queue entries parked) and the posture-sweep rotation is still open (next neighborhood src/engine.rs); plan drives the sweep itself next tick.
