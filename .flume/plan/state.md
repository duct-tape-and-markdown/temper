# Plan state

- Spec derived through: 787d424 — routed with zero entries. The commit is a "lean subtraction" (no accompanying `specs/decisions/` file, so no Consequences checklist applies), four cuts, each verified moot on disk: (1) local-member and (2) dial dedupes only moved each fact's sole-owning section (spec-system.md's one-noun-one-section rule) — text unchanged, still asserted in representation.md/contract.md, `src/dial.rs`'s "deletion is unspellable" comment still matches; (3) the dropped "user < project < local < invocation" spelled order has no code owner anywhere (`rg` for the phrase and for locus/layer precedence data in sdk/src/claude-code.ts and src/engine.rs's `Locus` enum both come up empty) — still the documented-but-`ungoverned` gap the prose already named, no regression; (4) the probe correction (three declared species → two shipped, probe named deferred per 0037) matches `src/compose.rs`'s `Verifier` enum (`Script`/`Telemetry` only, its own doc comment already reads "Two species this slice; a probe stays a documented pattern until a consumer types its transcript surface") and every mirror (`sdk/src/contract.ts`, `sdk/src/generated/Verifier.ts`, `src/tap.rs`) — spec was catching up to already-shipped code, not the reverse.
- Audited through: 0bf526a — window a4926ca..0bf526a (bc0d8c0) audited: the shipped fix (IMPORT-NESTED-FILE-DOT-ROOT-STRIP-PREFIX, `discover_nested_file`'s root normalized before `strip_prefix`) re-verified on disk against the shipped diff and proven green (`cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all --check`); no `sdk/**` touch in-window so the SDK gate wasn't re-run. Sizing normal (metrics: 37 turns, no bails, no reverts). pending.json already reflected it drained.
- Residue swept through: 0bf526a — same window: one residue found and filed. That entry's own notes had already flagged `compose.rs:640`'s `base = disc.harness().join(&governs.root)` as sharing the identical unnormalized-join shape, repro'd a bare-stem-id degrade there, but left it out of scope as unverified. Re-confirmed this tick (standalone `strip_prefix` mechanics + the built-in `memory` kind's shape, root ".", glob "**/CLAUDE.md") and filed COMPOSE-GOVERNED-ROOT-DOT-STRIP-PREFIX (open): under `temper check .`, a nested-placement member's id silently collapses to the bare stem, colliding with a sibling member's id.
- Posture swept through: full src/ list covered; sdk/src/assembly.ts, sdk/src/builtins.ts, and sdk/src/claude-code.ts covered too — mid-rotation, unchanged; sdk/src/contract.ts is the tree-order candidate next.
- This tick: SPEC DELTA. Routed 787d424 (lean subtraction, no Decision file) — all four cuts verified moot on disk, zero entries derived, cursor advanced.
- Queue: 3 pending, 1 open (COMPOSE-GOVERNED-ROOT-DOT-STRIP-PREFIX), 2 parked (IMPORT-HOP-CAP-CITE, PACKAGING-CHANNELS-REMAINDER — untouched this tick). Refactor: 0 live. Friction: 1 live (plan-null-byte-grep-blindspot.md, human's to read). Amendments: 0 live. Inbox: 0 notes.

Plan continues: after-build — the sole remaining plan job is the posture-sweep rotation (sdk/src/contract.ts next candidate), but COMPOSE-GOVERNED-ROOT-DOT-STRIP-PREFIX is pickable (open); ready work ships first, the sweep resumes when the wave hands back.
