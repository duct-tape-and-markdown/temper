[
  {
    "tag": "MEMORY-PROJECTION-SDK",
    "summary": "Project a module-carried memory in SDK emit — its frontmatterless body to the root `<name>.md` locus, with a lock row — so the dogfood's memories carry onto the SDK authoring face",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "Member carriage — module, document, in-place"
    },
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/project.ts",
          "description": "Make `memory` a projected kind: extend `isProjectedKind` and `projectionPath` so a `claude-code.memory`/`agents-md.memory` member projects to `<name>.md` at the harness root (`CLAUDE.md`, `AGENTS.md`) — the memory KIND.md declares no frontmatter (kinds/claude-code/memory/KIND.md), so the whole file is the body and `projectMember` emits body-alone (no `---` block). Update the doc comments that name only `rule`/`skill` as projected. NB nested/placement-folded memory loci stay out of scope — a module-carried member is locus-less (no `source_dir`), so this slice projects the root memory only; a nested-locus declaration mechanism is an unfiled follow-on."
        },
        {
          "path": "sdk/src/emit.ts",
          "description": "`emit`'s `projected` filter and its `lockRow` map now include memory members (via the extended `isProjectedKind`), so a memory both projects a root `CLAUDE.md`/`AGENTS.md` and stamps a `source_hash`/`emit_hash` lock row. Update the `EmitResult`/`emit` doc comments that assert 'only rule/skill carry a projection and a lock row'. No manifest-shape or byte-parity change — memory already serializes into `[[member]]`; this adds only its projection + lock output."
        },
        {
          "path": "sdk/test/emit.test.ts",
          "description": "Add a case: a `memory({ name: 'CLAUDE', body: md`...` })` member emits a projection at `CLAUDE.md` whose bytes are the body alone (no frontmatter block) and a lock row keyed on that path; assert double-emit stability holds with the memory included. Keep the existing rule/skill byte-parity cases untouched."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/emit.test.ts",
        "asserts": "a module-carried memory projects its frontmatterless body to `CLAUDE.md` (body-alone bytes) and stamps a lock row; emit stays double-emit stable with the memory present."
      }
    ],
    "acceptance": "`emit` on a harness carrying a `memory()` member returns a projection at the root `<name>.md` with body-alone bytes and a lock row for it (today it lands in the manifest but projects nowhere); `writeEmit` writes that CLAUDE.md; the rule/skill byte-parity fixtures still pass; `pnpm --dir sdk test` green.",
    "notes": "John's ruling (inbox 07-04): the dogfood migrates onto the SDK and memory projection is the wanted next SDK slice. Scoped to the root memory locus — sufficient for the dogfood (CLAUDE.md + AGENTS.md both at root). Rust emit does NOT project memory (BUILTIN_KINDS=skill/rule), so this is an SDK-ahead capability under TS-primary, not a byte-parity port — no Rust projector to pin against. Shares emit.ts+project.ts with the SDK seams held as forks; single open SDK head this tick."
  },
  {
    "tag": "EXTRACTION-VOCAB-GAPS",
    "summary": "Make `Primitive::Field` honor the nested key-path its doc-comment promises — held deferred until a nested-key kind (a JSON-manifest kind) consumes it",
    "per": {
      "path": "specs/architecture/15-kinds.md",
      "section": "The extraction algebra — the soundness boundary, as data"
    },
    "gate": {
      "kind": "deferred",
      "reason": "no consumer — the Fenced half graduated to FENCED-PRIMITIVE (its genre-fence consumer arrived); the remaining key-path half has none. `Primitive::Field` reads a flat `fields.get(key)`, so a nested `a.b.c` path can't be read — but no current kind (skill/rule/memory) declares a nested key-path field. Revive when a kind demands it (a JSON-manifest kind's nested key-path field)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Make `Primitive::Field` honor the key-path its own doc-comment promises: the impl does a flat `unit.frontmatter.get(key)` in `apply`, so a nested `a.b.c` path can't be read. No new vocabulary member — this fixes the existing `field` primitive."
        },
        {
          "path": "src/extract.rs",
          "description": "Key-path traversal for the field extractor: walk nested TOML/JSON tables to the leaf before `json_to_feature`, kind-preserving per the `type` lattice (`specs/architecture/10-contracts.md`). Absent (not errored) when the path doesn't resolve."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/extract_equivalence.rs",
        "asserts": "a key-path `field` reads a nested value and is absent (not errored) when the path doesn't resolve; it preserves the source scalar kind."
      }
    ],
    "acceptance": "A nested-key-path `Primitive::Field` reads `a.b.c` over a `Unit` and is absent when unresolved; a KIND.md declaring it reads the intended feature; cargo fmt/clippy -D warnings/test green. (Deferred: not built until a kind consumes it.)",
    "notes": "Narrowed 07-03: the Fenced half graduated to FENCED-PRIMITIVE (consumer arrived). Only the key-path `field` half remains, still no consumer. `Primitive::Field { key }` still flat-reads on disk (kind.rs:836). Shares kind.rs/extract.rs with AGENT-KIND — serialize the two if both un-defer."
  },
  {
    "tag": "AGENT-KIND",
    "summary": "Add the `agent` built-in harness kind — `.claude/agents/*.md` as a member document, read through the generic loader and checked by the embedded `agent.anthropic` package",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "Decision: package binding is by artifact kind"
    },
    "gate": {
      "kind": "deferred",
      "reason": "more built-in kinds is the wrong direction under the requirements/satisfies reframe; and post-adapter-wave the agent kind is mostly curated data (a KIND.md + PACKAGE.md, both outside build's fence) with near-zero engine code — revive only if a story demands the agent kind"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "kinds/claude-code/agent/KIND.md",
          "description": "The embedded built-in `agent` KIND.md (curated — humans author it; outside build's fence), under its provider beside kinds/claude-code/skill and kinds/claude-code/rule with a `provider = \"claude-code\"` line: `governs = { root = '.claude/agents', glob = '*.md' }`, the declared `yaml-frontmatter` projection format + unit shape, composed extraction over the `name`/`description`/optional `tools`/`model` fields plus markdown structure. build.rs auto-embeds it — no engine extractor."
        },
        {
          "path": "packages/agent.anthropic/PACKAGE.md",
          "description": "The agent kind's built-in package `agent.anthropic` (curated product territory, outside build's fence), embedded with the std-lib by build.rs. Clauses: `name` required + `allowed_chars` `[a-z0-9-]`; `description` required; `forbidden_keys` for the Cursor `.mdc` keys Claude Code ignores; an advisory `max_lines`. Per-clause `source` citations to the Claude Code subagent docs."
        },
        {
          "path": "tests/agent_contract.rs",
          "description": "Fixture-driven proof mirroring tests/rule_contract.rs: one broken agent per clause proving it fires, a clean agent that trips nothing, and an import round-trip insta snapshot proving idempotence — all through the generic adapter + loader, no per-kind engine code."
        }
      ],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Add `\"agent\"` to the `BUILTIN_KINDS` name-list so a `[kind.agent]` binding reads as a built-in layer, not a custom kind."
        },
        {
          "path": "src/builtin.rs",
          "description": "Add the `AGENT_PACKAGE = \"agent.anthropic\"` const beside SKILL_PACKAGE/RULE_PACKAGE and register its kind->package binding. build.rs auto-embeds the PACKAGE.md; no manual embed edit."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/agent_contract.rs",
        "asserts": "each decidable clause of the agent package fires on its broken fixture and stays silent on a clean agent; import of an unchanged agent is idempotent (insta snapshot)."
      }
    ],
    "acceptance": "`temper check` dispatches `.claude/agents/*.md` to the `agent.anthropic` package (conformance) and that package passes admissibility; `temper import` projects agents as one-document members idempotently via the generic frontmatter adapter; cargo fmt/clippy -D warnings/test all green.",
    "notes": "Declared-adapter architecture: no src/agent.rs IR — the generic adapter projects it. Curated kinds/claude-code/agent/KIND.md + packages/agent.anthropic/PACKAGE.md sit outside build's fence; engine wiring is near-zero. Stays deferred: wrong direction under requirements/satisfies. Shares src/kind.rs with EXTRACTION-VOCAB-GAPS — serialize the two if both un-defer."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship acquisition channels — npm optionalDependencies binary wrapper, Homebrew, cargo install — auto-detected, fail-loud on a missing platform binary",
    "per": {
      "path": "specs/architecture/50-distribution.md",
      "section": "Decision: acquisition rides the ecosystem's package managers"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs human release setup (npm org, Homebrew tap, signing/publish creds) before the mechanical wrappers can be validated end-to-end"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages, GitHub release assets, and the Homebrew tap; channel auto-detected. Distinct from the existing `.github/workflows/temper.yml` CI job."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`, confirmed on disk). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages (Biome's pattern) plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, `specs/architecture/50-distribution.md`). Reconcile with the flume manifest (a non-private publish entry, or a split package dir) rather than clobbering the harness manifest. Scoped/prefixed names per the resolved (project-name) fork. NB distinct from sdk/package.json (the authoring-face manifest)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger can acquire the prebuilt binary via npm (platform-specific optionalDependencies), Homebrew, standalone release assets, or `cargo install`; a missing platform binary is an install error, never a silent skip.",
    "notes": "Release infra, mostly outside `src/`; the cargo gates can't validate npm/Homebrew publish, and channels need human creds (npm org, tap, signing). NB root `package.json` is the *private* flume manifest (≠ sdk/package.json) — reconcile or split, never blindly create a second. Only `.github/workflows/temper.yml` (CI) exists — no release.yml yet. Parked until a human sets creds up."
  }
]
