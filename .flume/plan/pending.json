[
  {
    "tag": "EMBEDDED-EDGE-DEGREE-SEAM",
    "summary": "An embedded kind's declared edge fields never reach the lock's assembly `edge` facts, so an embedded edge resolves to nothing and every `degree({incoming})` clause over it reads 0",
    "per": {
      "path": "specs/model/contract.md",
      "section": "edge"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/declarations.ts",
          "description": "The root cause, re-verified on disk at f369531 (the file is untouched since scoping; every line below is live). `compileDeclarations` (695) computes ONE kind list at 696 — `unitKindsInPlay(kindsInPlay(harness))` — and spends it twice: on `kinds.map(kindFactRow)` (705), where the embedded filter is CORRECT (an embedded kind takes no kind-fact row; it reaches the lock through its host's `templates` column alone), and on `assemblyFactRows(harness, kinds)` (708), where it is not. `unitKindsInPlay` (278-282) drops every `locus.kind === \"embedded\"` kind, and `assemblyFactRows` (323-331) writes one `{fact: \"edge\", from, field, to}` row per `kind.edgeFields` — so an embedded kind's declared edge fields silently produce NO assembly edge fact. Split the two lists: the fact rows keep `unitKindsInPlay`, the assembly edge facts range over `kindsInPlay` (every kind in play, at any locus — the comment at 258 already says that is what the set means). The edge is a declared relationship at any grain, so the row is owed whatever the source's locus."
        },
        {
          "path": "src/graph.rs",
          "description": "The reader half of the same seam, re-verified live at f369531 (untouched in the window). With the edge fact present, `resolved_edges` (792-820) still resolves nothing: it collects `targets` as the bare `features.id` of every member of `edge.to` (801-807) and compares them against whatever `edge_targets` (1081-1087) reads out of the source's field. An embedded member's edge leaf is a FULL `kind:name` address, not a bare id — `sdk/src/kind.ts:370` pins it (\"The target's `kind:name` address — what the edge field's leaf authored\") and `sdk/src/emit.ts:248-274` resolves it that way — so `targets.contains(\"skill:data-access\")` is false and the edge drops, silently, before any degree count. Normalize the spelling at read time: accept a bare identity OR a `kind:name` address whose kind component is `edge.to`, and resolve by identity within the target kind either way — one resolution path, the spec's own words, and the read-time-normalization precedent decision 0024 set for the lock's own labels (`pipeline.md`, \"The lock\"). A `kind:` prefix naming any kind but `edge.to` stays dangling and keeps its existing finding."
        },
        {
          "path": "sdk/test/emit.test.ts",
          "description": "The assembly-row assertion home — `declarations.assembly` is asserted at 260/275/279/285 (unmoved; 6605bf5 touched only the line-37 import), so the new row proof belongs beside them rather than in a second home (engineering.md, \"One job, one home\"). Assert an embedded kind declaring an edge field writes its assembly `edge` fact, and that the kind still takes no kind-fact row — the two halves 696 conflates, pinned apart so a future filter cannot silently re-merge them."
        },
        {
          "path": "tests/graph.rs",
          "description": "The end-to-end reference-graph acceptance home (its own header: route resolution against a lock-declared edge field, driving the real binary). Add the embedded case: a host member whose embedded child carries an address-spelled edge leaf at a lock-declared edge fact resolves, and a `degree({incoming})` bound over the target counts it — the reach clause the field report watched read 0."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none — no built-in declares `edgeFields` (`sdk/src/builtins.ts` carries none) and `src/builtin_lock.toml` holds no assembly `edge` row, so the embedded lock does not re-derive.",
    "tests": [
      {
        "path": "sdk/test/emit.test.ts",
        "asserts": "an embedded kind's declared edge field writes an assembly `edge` fact row, while the kind itself still takes no kind-fact row"
      },
      {
        "path": "tests/graph.rs",
        "asserts": "an embedded member's address-spelled edge leaf resolves to its target and counts toward that target's incoming degree"
      }
    ],
    "acceptance": "A field-declared embedded edge resolves on the one path every other edge resolves on and counts for degree exactly as a frontmatter field's does (contract.md, \"edge\": a field edge is declared at any grain — a frontmatter field, an embedded member's edge field — and one resolution path and one degree semantics hold). A consumer's reach clauses judge embedded citations without a leaf-mention shim. `cargo test` and `pnpm --dir sdk test` green.",
    "notes": "Routes the 07-16 centercode regression (filed 3fdebb8), diffed forward from `observed at 8b43293`: unmoved through f369531. Root cause is narrower than the report's \"0025 hasn't landed\": one kind list reused for two jobs at declarations.ts:696. 5c34ced's fence test hand-writes the embedded edge fact (gate_fail_loud.rs:284), pinning the engine and never this seam — `format-places-edges` is unreachable through real emit for the same reason. scoped at 3fdebb8, opened at f369531."
  },
  {
    "tag": "SUPPORTING-DOC-REACH-CLAUSE",
    "summary": "`supporting-doc`'s reach clause: a supporting file is invisible unless its host skill references it — the by-kind universal binding, each-grain incidence, advisory",
    "per": {
      "path": "specs/builtins.md",
      "section": "Default contracts"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/builtins.ts",
          "description": "`supportingDocDefaultContract` (557) — the empty exported clause array a7a8cc1 shipped, empty because its one real clause was inadmissible then — gains that clause: an incoming-degree bound over the kind's members, cited to code.claude.com/docs/en/skills \"Add supporting files\" (retrieved 2026-07-16), the vendor fact that every supporting file is invisible unless SKILL.md references it. **Universal, not existential**: the fact holds of every supporting document, so the spelling is the by-kind binding with each-grain incidence (`contract.md`, \"selection\"), never a requirement — a requirement is the opt-in selector and would make a vendor fact consumer ceremony. **Reach is locus-agnostic**: any resolved edge from the host skill counts, post-0025's one edge noun and one degree semantics — a mention included, this clause being the \"a contract may\" that mention's obligation-free default reserves (`contract.md`, \"edge\"). Severity **advisory**, per `intent.md` invariant 5: a shipped coverage clause enters advisory, and escalation is the corpus's declared act. The contract's own doc (546-556) already states the vendor fact as prose; the clause is what enforces it, so the doc's \"empty\" framing is rewritten with it."
        },
        {
          "path": "src/builtin_lock.toml",
          "description": "The embedded built-in lock re-derives: `supporting-doc`'s kind row (92) gains its one clause row. The file's header commentary (13, 27) enumerates the built-in kinds and calls out `supporting-doc` as the one nested-file-locus row; neither line's claim changes."
        },
        {
          "path": "tests/builtin_lock_frozen.rs",
          "description": "The frozen gate re-derives the built-in lock through the real SDK and byte-compares; it moves with the grown clause row."
        },
        {
          "path": "sdk/test/builtins.test.ts",
          "description": "`supportingDocDefaultContract ships empty — the format documents no schema to gate` (183-184) is the assertion this entry retires: it pins today's empty array and is the one test that must flip. Replace it with the shipped-clause bar (`builtins.md`, \"The clauses live in code\"): the contract carries exactly one clause, at advisory severity, cited and guided. The kind's own shape assertions (168-181) are untouched — the clause is a contract fact, not a kind fact."
        }
      ],
      "retire": []
    },
    "schemaDelta": "`src/builtin_lock.toml` re-derives: `supporting-doc`'s kind row gains one clause row (the incoming-degree reach bound, advisory).",
    "tests": [
      {
        "path": "sdk/test/builtins.test.ts",
        "asserts": "`supporting-doc`'s default contract is one advisory reach clause, cited to the vendor fact and carrying its guidance"
      },
      {
        "path": "tests/builtin_lock_frozen.rs",
        "asserts": "the embedded built-in lock re-derives byte-identically with the clause row"
      }
    ],
    "acceptance": "A supporting document no edge from its host skill reaches is an advisory finding naming the vendor fact (builtins.md, \"Default contracts\"; the clause is decidable, severity-declared, guided, cited and dated) — and reach counts any resolved edge, locus-agnostic. `cargo test` and `pnpm --dir sdk test` green.",
    "notes": "Routes the 07-16 ruling's clause itself (25e9317 resolved the fork). Both blockers are gone, both verified on disk this tick: PREDICATE-SELECTION-ALGEBRA (28ad61f) made `graph::degree` take `&[Selection]`, so a by-kind degree clause is admissible outside a requirement; SKILL-NESTED-REFERENCE-DOCS (a7a8cc1) minted the kind and its empty contract, and its own commit body names this entry as the owner of the clause it declined to invent. scoped at d572827, opened at f369531."
  },
  {
    "tag": "SDK-FIXTURE-WIRING-ONE-HOME",
    "summary": "The fixture-harness wiring builder belongs in `tests/common`, not copy-pasted per suite: three copies of tmpdir → `.temper` → `harness.ts` → `vendor_sdk` collapse into one home",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "SUPPORTING-DOC-REACH-CLAUSE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/common/mod.rs",
          "description": "The declared one home for test scaffolding — its own header (1-4) names \"the SDK vendoring used by tests that drive a real `node` subprocess\" as the copy-per-suite it exists to end. It carries every ingredient (`tmpdir` 31, `scaffold` 44, `vendor_sdk` 96) and not the builder that composes them into a fixture harness, which is exactly the surface the three copies below each re-derive. Add that builder: a label plus the fixture program text, returning `(harness, into)`. `tests/install.rs`'s `vendor_sdk` calls are a different job — the install verb scaffolds its own `harness.ts` under test, rather than wiring an authored fixture program — and are out of scope."
        },
        {
          "path": "tests/emit.rs",
          "description": "`wire_sdk_harness_program` (1223) is the original, and the surface the other two copies' doc comments cite by name as the pattern they follow; `wire_sdk_harness` (1217) binds it to this suite's `HARNESS_PROGRAM`. Retire the local body onto the common home — the `HARNESS_PROGRAM` binding stays suite-local, being this suite's fixture rather than scaffolding."
        },
        {
          "path": "tests/builtin_lock_frozen.rs",
          "description": "`wire_memberless_harness` (68-78, shifted +7 by a7a8cc1's supporting-doc rows): a verbatim copy of emit.rs's builder — tmpdir, `.temper`, write `harness.ts`, `vendor_sdk` — whose own doc (65-67) still cites `tests/emit.rs`'s `wire_sdk_harness` as the pattern it follows. Call the common builder with `MEMBERLESS_BUILTIN_PROGRAM`."
        },
        {
          "path": "tests/projection_path_seam.rs",
          "description": "`wire_waypoint_harness` (141), the third copy (e76ec85), carrying the same doc cite back to emit.rs's builder. Call the common builder with `WAYPOINT_PROGRAM`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "One builder composes tmpdir → `.temper` → `harness.ts` → `vendor_sdk`, in `tests/common`, and no suite re-derives it (engineering.md, \"One job, one home\": shared fixtures and builders live in one home, never copy-pasted per file). Behavior is unchanged, so the three suites' existing tests are the coverage; `cargo test` green.",
    "notes": "Found sweeping 74f4e62..ca4e866: e76ec85 added the third copy, citing emit.rs's builder as the pattern it followed rather than the home it should have extended. **Gate re-pointed at f369531**: SKILL-NESTED-REFERENCE-DOCS shipped, but builtin_lock_frozen.rs stays contended — SUPPORTING-DOC-REACH-CLAUSE rewrites its frozen bytes — so that entry now serializes this one, on the one surviving ground. All three copies re-verified live; only builtin_lock_frozen.rs moved (+7). rescoped at b745415."
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics — the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (⇒ 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (⇒ 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence — never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. `MAX_IMPORT_HOPS = 5` (62) sits under a doc (58-62) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' — a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (484-523) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds, one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts — hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it — 382-383 and 481-483, both 'as the format documents' / 'the `at-import` recursion depth the format documents' — carry the same claim and move with it."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1126) already pins the boundary mechanically rather than in a comment — `r1..r5` rescued from a live seed, `r6` dead — so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1129-1130) spells 'the format's five-hop import recursion' and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper — the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Found sweeping b745415..420da04. 28ad61f carried a rider to re-verify graph.rs's two memory cites, bumped the verified one (689 → 677) and declined this rather than flip a live gate inside an unrelated entry — the right call, captured in `.flume/friction/`. NOT citation staleness, so it does not ride: staleness rides because the fact holds; here the constant may be wrong. graph.rs untouched through f369531; every cited line re-verified live. scoped at 420da04."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Supersedes retired PACKAGING-CHANNELS (John, 07-11): both its file claims were dead — release.yml now exists (its `new` claim fails the refs gate) and the root-package.json launcher was ruled out (the launcher + optionalDependencies live on the SDK, distribution.md 'What ships'). Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested at f369531: no version tag, crate 0.1.0 vs npm 0.0.7, release.yml:7-9 still defers darwin. scoped at b676656."
  }
]
