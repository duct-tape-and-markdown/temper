[
  {
    "tag": "RENDER-HOOK-LEAF-RESOLUTION",
    "summary": "Resolve embedded-kind Text leaves (mention-checked, loud on dangling) before invoking a kind's render() hook, so the refusing clause holds regardless of hook presence",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "Emit"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/emit.ts",
          "description": "renderMemberFence (line ~107) calls value.render(value) with the raw EmbeddedMemberValue when a kind declares render(); resolve each Text leaf via resolveLeaf (mention-checked, loud on a dangling address, same as renderMemberToml's own leaf loop) into a plain-string-leaved value before passing it to render(), so both paths share one resolution point."
        },
        {
          "path": "sdk/src/kind.ts",
          "description": "render hook's declared parameter type (`(value: EmbeddedMemberValue) => string`, lines 138/160/224) and doc comments should reflect that leaves are already resolved to plain strings post-fix, so hook authors never need to handle a raw Text leaf themselves."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/emit.test.ts",
        "asserts": "a kind with a render() hook and a Text leaf carrying a dangling mention refuses with the same error the hook-less default TOML view produces; a resolvable mention's rendered text reaches the hook as a plain string, not a Text object"
      }
    ],
    "acceptance": "Refusing on a dangling embedded-kind leaf mention no longer depends on whether the kind declares render(); sdk/test/emit.test.ts covers both paths with matching refusal behavior.",
    "notes": "Routed from friction capture build-embedded-leaf-text-render-hook-gap (drained by 965e1dc). EMBEDDED-KIND-RENDER-HOOK (3c6f50b) shipped render() unresolved; EMBEDDED-LEAF-TEXT (18d3406, later) resolved leaves in the default view only -- pipeline.md's Refusing clause binds unconditionally, so the hook path's silent `[object Object]` case must fix too. Rejected: hooks own resolution via exported resolveLeaf -- unenforced per-hook. No Rust-side change. scoped at 965e1dc."
  },
  {
    "tag": "REQUIREMENT-KIND-SDK-TYPE",
    "summary": "Retype SDK Requirement.kind to KindDefinition<never> so kind: skill/agent/command type-check, completing 0003's stranded SDK-side half",
    "per": {
      "path": "specs/model/representation.md",
      "section": "kind"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/contract.ts",
          "description": "Retype `Requirement.kind?: KindDefinition<object>` (line 154) to `KindDefinition<never>`. `KindDefinition<T>`'s sole call signature `(init: MemberInit<T>): Member` (sdk/src/kind.ts:127-128) sits contravariantly in T, so `never` is the universal supertype -- every `KindDefinition<T>` is assignable regardless of T's required fields -- today only kind: rule / kind: memory type-check since KindDefinition<object> rejects a KindDefinition<Skill|Agent|Command> on that same contravariance. A structural `{ readonly key: string }` was considered and rejected: it compiles but admits a hand-written `{ key: \"skil\" }` literal, i.e. identity-by-string, which representation.md's kind section rules out (\"Kind identity travels by import, never by string\")."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/refusals.test.ts",
        "asserts": "requirement kind: skill, kind: agent, and kind: command type-check and behave like the existing kind: rule cases (currently TS2322 on all three); kind: rule/kind: memory cases keep passing unchanged"
      }
    ],
    "acceptance": "sdk/src/contract.ts's Requirement.kind field accepts every shipped kind (skill, agent, command, rule, memory) with no TS2322; sdk/test/refusals.test.ts gains coverage for at least one required-field kind alongside the existing kind: rule case.",
    "notes": "SDK-side half of Decision 0003, stranded since 9dc9162 (2026-07-04), three days before ratification. Rust-engine half shipped in SATISFIER-KIND-CLAUSE (94ac5f1/2746b11); that entry's own commit body scoped out sdk/. Sole SDK read site is declarations.ts:326 (`requirement.kind?.key`), never a constructor call -- no Rust-side change, no lock/SEAM_VERSION bump. scoped at 965e1dc."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper`, single package, `(sdk-package-layout)`, still at 0.0.5 pending publish — SDK-VERSION-LOCKSTEP); channel 1's SDK foundation shipped. Re-verified at 6d6ae89 (ship audit, EMBEDDED-LEAF-TEXT confirmed shipped): still no `.github/workflows/release.yml` (only `temper.yml`, a check job); root package.json is still the private flume manifest (`temper-flume-harness`)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm `@dtmd/temper`; ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs range-correctness shipped in SCAFFOLD-OUTPUT-VALID (^0.0.4); release owns only future re-pins."
  }
]
