[
  {
    "tag": "EMBEDDED-LEAF-TEXT",
    "summary": "An embedded member's leaf may be a Text (template + mentions), not just a bare string; its mentions lift into the host's mention rows, its resolved display becomes the stored leaf (0018 slice 3/3)",
    "per": {
      "path": "specs/model/contract.md",
      "section": "edge"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "EMBEDDED-KIND-RENDER-HOOK"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "`EmbeddedMemberValue.leaves`, and a collection entry's leaves, widen from `Readonly<Record<string,string>>` to accept a `Text` (imported from `prose.ts`) per leaf alongside a bare string; `embeddedMemberValue()`'s init type follows."
        },
        {
          "path": "sdk/src/emit.ts",
          "description": "`renderMemberToml`'s leaf loop (top-level and the nested collection-entry loop, ~72-81) resolves a `Text` leaf the way `resolveBody` resolves member-level `Text` — mention resolution-checked against `options.mentionable` (loud on a dangling address, contract.md 'edge'), rendered by the one display rule — before TOML-string-quoting it; a bare-string leaf is unchanged."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "`mentionRows()` (~354-383, today walks only top-level member prose) also walks each `blocks()`-declared `EmbeddedMemberValue`'s `Text` leaves, emitting a `MentionRow` per mention keyed to the leaf's own address — the existing `<member>/<kind>/<key>/<child-path>` leaf-address grammar `src/read.rs`'s `parse_leaf_address` already resolves, not a new one."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/emit.test.ts",
        "asserts": "grows a case asserting a Text-valued leaf's mention resolves and renders inline, and an unresolved mention inside a leaf is a loud emit error, mirroring 'a resolved mention renders to its declared value's display form' (~442) / 'an unresolved mention is a loud emit error' (~454) at leaf grain."
      },
      {
        "path": "tests/nested_member.rs",
        "asserts": "'leaf_addresses_are_structural_member_kind_key_child_path' (~118, shifted by NESTED-MEMBER-COLLECTIONS-ORDERED's ordered-collections rework) grows a case where the addressed leaf itself carries a resolved mention, asserting explain/impact still read its plain resolved string."
      }
    ],
    "acceptance": "An embedded member's leaf authored as a text-tagged template with a mention emits with the mention resolved and displayed, contributes a mention row addressed to that leaf, and an unresolved leaf mention refuses emit exactly like a member-level one; a bare-string leaf is unchanged.",
    "notes": "Slice 3 of 3 for 0018's carried inbox scope (filed 6bed388, cite Consequences). Depends on nothing from slices 1/2 semantically but shares sdk/src/kind.ts + emit.ts + declarations.ts, so blockedBy slice 2 per entry-discipline file-disjointness, matching the inbox's stated do-last order. Scoped at 6bed388."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper`, single package, `(sdk-package-layout)`, still at 0.0.5 pending publish — SDK-VERSION-LOCKSTEP); channel 1's SDK foundation shipped. Re-verified at 43b3471 (quiet closing pass): still no `.github/workflows/release.yml` (only `temper.yml`, a check job); root package.json is still the private flume manifest (`temper-flume-harness`)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm `@dtmd/temper`; ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs range-correctness shipped in SCAFFOLD-OUTPUT-VALID (^0.0.4); release owns only future re-pins."
  }
]
