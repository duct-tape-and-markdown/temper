[
  {
    "tag": "IMPACT-VERB",
    "summary": "Add the `temper impact <member>` read verb — deterministic blast-radius traversal (unfilled requirements, dangling satisfies, unbacked directive edges, dead reachability), narrated like why",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "CLI surface"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/read.rs",
          "description": "Add an `impact` traversal beside `why`/`requirements` — read-only, over the composed graph `check` already computes (joins, activation, directive edges). Answers 'what strands if <member> is removed or renamed': the requirements it satisfies that would fall below coverage, the `satisfies` edges left dangling, the directive edges (`at-import`) that lose their backing, and the members whose reachability dies with it. Deterministic tier-1 traversal, narrated in full sentences in the corpus vocabulary (the teaching-surface bar, 50-distribution.md 'the gate teaches'); a projection, never a gate — no non-zero exit on findings. Reuse the shared edge/coverage derivation the read family already calls (READ-EDGE-UNIFY), not a second graph build."
        },
        {
          "path": "src/main.rs",
          "description": "Add the `impact <member>` subcommand to the clap enum and a thin dispatch arm mapping `read::impact(...)` to stdout + exit 0, beside the existing `why`/`requirements` arms. Logic stays in the library (`.claude/rules/rust.md`)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "`impact <member>` names the requirements left unfilled, the `satisfies` left dangling, the directive edges left unbacked, and the members whose reachability dies if the member is removed; it exits 0 (a read, never a gate); an unknown member is a clean error."
      }
    ],
    "acceptance": "`temper impact <member>` prints the deterministic blast radius (unfilled requirements, dangling satisfies, unbacked directive edges, dead reachability) narrated like why/requirements, exit 0 always; logic in src/read.rs, thin dispatch in main.rs; cargo fmt/clippy -D warnings/test green.",
    "notes": "Ratified CLI bullet ec479ee (20-surface.md 'CLI surface'); graduated from horizons. Read-only over graph data check already carries — no new engine semantics; reuse resolved_edges (READ-EDGE-UNIFY, read.rs:163), not a 2nd build. UNBLOCKED: WEDGE-FACT-FLOOR shipped 3cdd3e6. Disjoint from INSTALL-GUARD (read.rs/main.rs vs install.rs) — parallel-safe. CI-comment placement is a later slice, not this one."
  },
  {
    "tag": "INSTALL-GUARD-ARTIFACTS",
    "summary": "`install` wires the posture's enforcement artifacts per authority — a managed-by note on non-memory projections and a PreToolUse guard hook (warn under shared, block under surface)",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "Decision: surface authority is a declared posture, never a baked stance"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "Extend `install` (today: SessionStart hook, CI job, schema modelines — src/install.rs) with the two authority-enforcement artifacts, wired per the composed layer's `authority()` posture (read the assembly; Shared when absent). (1) A managed-by NOTE on projections whose format tolerates cost-free metadata — riding the modeline machinery, SKIPPING memory projections (a CLAUDE.md comment costs context every session) and NEVER stamped by `apply` (law 5 keeps projections content-faithful). (2) A PreToolUse guard hook at the provider's write boundary: warn-and-route under `shared`, block under `surface`; degree maps onto note=information / warn=advisory / block=required so temper never escalates on its own determination. Both enumerated in `gate_installed`'s audit (src/install.rs:167) beside the hook/CI/modeline tally, install-wired + reversible (the gate-installed pattern). The guard hook's message states the limit verbatim: other tools' writes are not bound by it."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "under `authority = \"surface\"` the guard hook is authored with block semantics and the managed-by note lands on non-memory projections (never a memory one); under `shared` the hook warns-and-routes; `install` is idempotent and `gate_installed` audits both new placements; the note is never written by `apply`."
      }
    ],
    "acceptance": "`temper install` projects the posture's enforcement artifacts — a managed-by note on non-memory projections (never via apply) and a PreToolUse guard hook that warns under shared / blocks under surface — enumerated in gate-installed's audit and reversible; cargo fmt/clippy -D warnings/test green.",
    "notes": "Ratified Decision ec479ee. UNBLOCKED: AUTHORITY-POSTURE-PARSE shipped fae7292 — `layer.authority()` lands at compose.rs:761. Edits src/install.rs + tests/install.rs only — disjoint from IMPACT-VERB (read.rs/main.rs), parallel-safe. Memory projections skip the note (law 5); the hook binds one provider's writes — the stated, unsolved limit."
  },
  {
    "tag": "WALK-IGNORE-DISCIPLINE",
    "summary": "Discovery walks honor ignore rules (`.gitignore` + always-exclude `.git`) so a `**` governs glob can't import a vendored dep's CLAUDE.md as a member — the directive-backing set stays raw disk",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "Decision: the workspace is per-project"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs a human ratification: the discovery-ignore-discipline intent isn't yet a written spec Decision (the per-project boundary is the closest owner but doesn't name .gitignore), and correct .gitignore parsing needs a sanctioned-crate call (the `ignore` crate) — build can't add an unsanctioned dep or invent the intent"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "The `**` discovery walk (`collect_glob`, src/import.rs, recurses any-depth) descends everything — so flipping the memory kinds' `governs` to `**/CLAUDE.md`/`**/AGENTS.md` would import a vendored dependency's CLAUDE.md out of node_modules as a member (a different project's harness, outside the per-project boundary). Prune the DISCOVERY walk to honor ignore rules: always exclude `.git`, and skip paths a `.gitignore` excludes — a member is first-party authored content. CRITICAL: do NOT touch `repo_file_set` (src/main.rs, the directive-BACKING set) — the harness loads an `@import` target regardless of gitignore, so pruning the backing set would forge unbacked findings (law 3). Two different sets, kept distinct."
        },
        {
          "path": "Cargo.toml",
          "description": "Add the `ignore` crate (ripgrep's gitignore engine) to the sanctioned dependency set — correct .gitignore semantics (nested files, negation, precedence) are a footgun to hand-roll against the quality bar. Flagged for human sanction (the sanctioned set is human territory); excluding `.git` alone is free, but honoring `.gitignore` needs the dep."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/readd.rs",
        "asserts": "a `**`-governed discovery over a harness with a gitignored `node_modules/dep/CLAUDE.md` and a tracked root `CLAUDE.md` imports only the tracked file; `.git` contents are never discovered; the directive-backing set still sees the gitignored file (unbacked findings are not forged)."
      }
    ],
    "acceptance": "Discovery walks respect `.gitignore` and always exclude `.git`, so a `**` governs glob imports only first-party members and never a vendored dep's file; the directive-backing set (`repo_file_set`) stays raw disk; cargo fmt/clippy -D warnings/test green. (Parked: needs a ratified spec Decision + the `ignore` crate sanctioned.)",
    "notes": "Inbox 2026-07-03 (WALK-IGNORE-DISCIPLINE), blocks the memory tree flip. After it drains, the human flips both memory kinds' `governs` to the any-depth glob (curated embeds, human territory). Parked: the cite (per-project workspace) is the closest owner but the ignore-discipline sentence isn't yet spec'd, and the `ignore` crate needs a human sanction. tests/readd.rs chosen as the discovery-behavior home; swap if a better fixture exists."
  },
  {
    "tag": "EXTRACTION-VOCAB-GAPS",
    "summary": "Extend the extraction algebra with the two primitives 15-kinds lists but code lacks — a `fenced` block extractor and nested key-path `field` lookup — held until a consumer kind needs them",
    "per": {
      "path": "specs/architecture/15-kinds.md",
      "section": "The extraction algebra — the soundness boundary, as data"
    },
    "gate": {
      "kind": "deferred",
      "reason": "no consumer — the extraction vocabulary is harvested from the built-in kinds and extended deliberately (15-kinds.md Decision: extraction is a closed algebra); no current kind (skill/rule/memory) reads a fenced block or a nested key-path, so adding either now is speculative scope with no kind to exercise it. Revive when a kind demands it — a JSON-manifest kind's nested key-path field, or a spec/class kind's fenced-block extraction. (NB `at-import` is the DIRECTIVES wave's directive primitive, a separate edge primitive — not one of these.)"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Two closed-vocabulary additions to `Primitive` (`specs/architecture/15-kinds.md`): (1) a `Fenced` primitive extracting fenced code-block content — the markdown-structure extractor the spec lists (`a fenced block`) but the enum omits (today: field/headings/sections/line_count/placement); (2) make `Primitive::Field` honor the key-path its own doc-comment promises — the impl currently does a flat `unit.frontmatter.get(key)` so a nested `a.b.c` path can't be read. Both extend the closed-vocab load-guard's accepted primitive set (parse_primitive). This entry owns both edits so they don't collide in parallel worktrees."
        },
        {
          "path": "src/extract.rs",
          "description": "The extractor helpers behind the two primitives: a `body_fenced_blocks`-style reader over the markdown body (mirroring `body_headings`/`body_sections`), and key-path traversal for the field extractor (walk nested TOML/JSON tables to the leaf before `json_to_feature`, kind-preserving per the `type` lattice, `specs/architecture/10-contracts.md`)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/extract_equivalence.rs",
        "asserts": "a `fenced` primitive yields the fenced block's content and skips prose; a key-path `field` reads a nested value and is absent (not errored) when the path doesn't resolve; both preserve the source scalar kind."
      }
    ],
    "acceptance": "`Primitive::Fenced` and a nested-key-path `Primitive::Field` are in the closed vocabulary, load-guarded, and extract over a `Unit`; a KIND.md declaring either reads the intended feature; cargo fmt/clippy -D warnings/test green. (Deferred: not built until a kind consumes one.)",
    "notes": "Disk re-confirmed both gaps: `Primitive` (now incl. `Directives`, shipped) still lacks `Fenced`; `Field` reads flat `frontmatter.get(key)`, no key-path; guard is parse_primitive. Neither breaks anything today (no kind uses them). ONE entry — both touch src/kind.rs+extract.rs. NOT the DIRECTIVES wave's `at-import` (a distinct directive primitive). If un-deferred while AGENT-KIND is open, serialize — shared src/kind.rs."
  },
  {
    "tag": "AGENT-KIND",
    "summary": "Add the `agent` built-in harness kind — `.claude/agents/*.md` as a member document, read through the generic loader and checked by the embedded `agent.anthropic` package",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "Artifact kinds & package binding"
    },
    "gate": {
      "kind": "deferred",
      "reason": "more built-in kinds is the wrong direction under the requirements/satisfies reframe; and post-adapter-wave the agent kind is mostly curated data (a KIND.md + PACKAGE.md, both outside build's fence) with near-zero engine code — revive only if a story demands the agent kind"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "kinds/claude-code/agent/KIND.md",
          "description": "The embedded built-in `agent` KIND.md (curated — humans author it; outside build's fence), under its provider beside kinds/claude-code/skill and kinds/claude-code/rule with a `provider = \"claude-code\"` line: `governs = { root = '.claude/agents', glob = '*.md' }`, the declared `yaml-frontmatter` projection format + unit shape, composed extraction over the `name`/`description`/optional `tools`/`model` fields plus markdown structure. build.rs auto-embeds it — no engine extractor."
        },
        {
          "path": "packages/agent.anthropic/PACKAGE.md",
          "description": "The agent kind's built-in package `agent.anthropic` (curated product territory, outside build's fence), embedded with the std-lib by build.rs. Clauses: `name` required + `allowed_chars` `[a-z0-9-]`; `description` required; `forbidden_keys` for the Cursor `.mdc` keys Claude Code ignores; an advisory `max_lines`. Per-clause `source` citations to the Claude Code subagent docs."
        },
        {
          "path": "tests/agent_contract.rs",
          "description": "Fixture-driven proof mirroring tests/rule_contract.rs: one broken agent per clause proving it fires, a clean agent that trips nothing, and an import round-trip insta snapshot proving idempotence — all through the generic adapter + loader, no per-kind engine code."
        }
      ],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Add `\"agent\"` to the `BUILTIN_KINDS` name-list so a `[kind.agent]` binding reads as a built-in layer, not a custom kind."
        },
        {
          "path": "src/builtin.rs",
          "description": "Add the `AGENT_PACKAGE = \"agent.anthropic\"` const beside SKILL_PACKAGE/RULE_PACKAGE and register its kind->package binding. build.rs auto-embeds the PACKAGE.md; no manual embed edit."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/agent_contract.rs",
        "asserts": "each decidable clause of the agent package fires on its broken fixture and stays silent on a clean agent; import of an unchanged agent is idempotent (insta snapshot)."
      }
    ],
    "acceptance": "`temper check` dispatches `.claude/agents/*.md` to the `agent.anthropic` package (conformance) and that package passes admissibility; `temper import` projects agents as one-document members idempotently via the generic frontmatter adapter; cargo fmt/clippy -D warnings/test all green.",
    "notes": "Declared-adapter architecture: no src/agent.rs IR — the generic adapter projects it. Curated kinds/claude-code/agent/KIND.md + packages/agent.anthropic/PACKAGE.md sit outside build's fence; engine wiring is near-zero (add \"agent\" to BUILTIN_KINDS + an AGENT_PACKAGE const/binding in builtin.rs; build.rs auto-embeds). Stays deferred: wrong direction under requirements/satisfies. NB shares src/kind.rs with EXTRACTION-VOCAB-GAPS and src/builtin.rs — serialize if un-deferred while either is open."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship acquisition channels — npm optionalDependencies binary wrapper, Homebrew, cargo install — auto-detected, fail-loud on a missing platform binary",
    "per": {
      "path": "specs/architecture/50-distribution.md",
      "section": "Decision: acquisition rides the ecosystem's package managers"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs human release setup (npm org, Homebrew tap, signing/publish creds) before the mechanical wrappers can be validated end-to-end"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages, GitHub release assets, and the Homebrew tap; channel auto-detected. Distinct from the existing `.github/workflows/temper.yml` CI job."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`, confirmed on disk). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages (Biome's pattern) plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, `specs/architecture/50-distribution.md`). Reconcile with the flume manifest (a non-private publish entry, or a split package dir) rather than clobbering the harness manifest. Scoped/prefixed names per the resolved (project-name) fork."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger can acquire the prebuilt binary via npm (platform-specific optionalDependencies), Homebrew, standalone release assets, or `cargo install`; a missing platform binary is an install error, never a silent skip.",
    "notes": "Release infra, mostly outside `src/`; the cargo gates can't validate npm/Homebrew publish, and channels need human creds (npm org, tap, signing). NB: root `package.json` already exists as the *private* flume manifest — reconcile or split, never blindly create a second. Parked until a human sets creds up."
  },
  {
    "tag": "COMMUNITY-DOCS",
    "summary": "Add CONTRIBUTING.md and SECURITY.md — the two missing launch community docs (two-sided AI-authorship disclosure; vuln-reporting evidence bar)",
    "per": {
      "path": "specs/intent/55-offering.md",
      "section": "The community surface — small, honest, gated"
    },
    "gate": {
      "kind": "parked",
      "reason": "build's writable fence (.flume/chain.ts BUILD_WRITABLE_PATHS) lists only README/CHANGELOG/AGENTS/LICENSE among root docs — no root CONTRIBUTING.md/SECURITY.md glob — so build would bail; a human must widen the fence (chain.ts is human territory) and enable private vulnerability reporting (a repo setting) before build can author these"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "CONTRIBUTING.md",
          "description": "Small and honest per the Decision: how to file a good issue, 'search first', PRs by prior discussion, and the two-sided AI-authorship policy (inbound AI-assisted contributions welcome WITH disclosure; the contributor must understand and defend the change without the assistant). Expectation-setting, not an invitation to co-maintain."
        },
        {
          "path": "SECURITY.md",
          "description": "Vulnerability-reporting policy with an explicit evidence bar — a report must demonstrate, not speculate (the 'unverified AI-generated vuln reports are the new spam' lesson). Points at private vulnerability reporting (the GitHub repo setting a human enables)."
        }
      ],
      "edit": [],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "CONTRIBUTING.md and SECURITY.md exist at root, matching specs/intent/55-offering.md's launch-set Decisions (two-sided disclosure; demonstrate-not-speculate); the fence has been widened so build's entry-fence preflight admits them.",
    "notes": "Launch set (specs/intent/55-offering.md). Already on disk: README, AGENTS.md, CHANGELOG.md, LICENSE-MIT/APACHE, issue forms (.github/ISSUE_TEMPLATE/{bug,feature,config}.yml). Missing only CONTRIBUTING.md + SECURITY.md. Both root-level per the spec but outside build's root-docs fence — parked on a human fence-widen (chain.ts) + enabling private reporting. Parked entries are skipped by the entry-fence gate, so this files cleanly."
  }
]
