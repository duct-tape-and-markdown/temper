[
  {
    "tag": "KIND-PACKAGE-PARSE-RETIRE",
    "summary": "Retire KIND.md / PACKAGE.md parsing — floors and kinds become the compiled default program's declaration rows; no kind/package file format",
    "per": {
      "path": "specs/architecture/15-kinds.md",
      "section": "Decision: field typing lives in the SDK — there is no kind file format"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Retire `CustomKind::load` and the `KIND.md` header grammar — the `Format`/`UnitShape`/`Activation` header keys and string-name identity resolution (15-kinds Decision: 'no KIND.md, no header grammar of format/unit_shape/activation keys'; 'built-ins are a module, and identity is an import' rejects string-name identity + provider qualification). A kind's runtime residue is the five facts read as declaration rows, not a parsed file."
        },
        {
          "path": "src/compose.rs",
          "description": "Retire the `PackageResolver`'s `<packages_dir>/<name>/PACKAGE.md` loading (compose.rs:350/352, 983/993) and the `layer_over`/`effective` package-file resolution, plus the `kinds_dir` `CustomKind::load` dispatch. Floors and kind facts arrive as the compiled default program's declaration rows, not resolved from on-disk KIND.md/PACKAGE.md."
        },
        {
          "path": "src/contract.rs",
          "description": "Retire `load_package`/`parse_package` — the `PACKAGE.md` fenced-document clause parse (contract.rs:549-594, 1221-1228). A floor is an exported clause array delivered as compiled predicate declaration rows, not parsed from a package file (10-contracts, 'clause arrays and two quantifiers, not a package noun')."
        },
        {
          "path": "src/builtin.rs",
          "description": "The built-in floor stops embedding/parsing `packages/<name>/PACKAGE.md` (`BUILTIN_PACKAGES` + `parse_package`, builtin.rs:28/107-121); the floor becomes the compiled default program of clause rows the engine carries for SDK-less checking (50-distribution 'the compiled default program embedded at build time')."
        },
        {
          "path": "src/builtin_kind.rs",
          "description": "The embedded built-in kind set becomes the compiled default program's kind declaration rows, not `definitions()` parsed over the walked `kinds/<provider>/<name>/KIND.md` tree."
        },
        {
          "path": "src/check.rs",
          "description": "`Workspace::load_kinds` (check.rs:63, 556) reconciles to the declaration-row kind facts — no `builtin_kind::definitions()` KIND.md parse feeding the workspace kind map."
        },
        {
          "path": "src/import.rs",
          "description": "The `CustomKind::load` / built-in-kinds-program reads off `<harness>/.temper/kinds/<name>/KIND.md` (import.rs:239-244, 492-497) retire; kinds are declaration rows, not parsed from the authored KIND.md tree."
        },
        {
          "path": "src/main.rs",
          "description": "Retire the gate's `packages_dir`/`kinds_dir` resolution and its `PackageResolver::new` / `compose::effective` / `custom_kinds_and_edges` / `CustomKind::load` call sites (main.rs:329/346/353/508/513/570/599/667/977). The gate reads floors + kind facts from the compiled default program (already the lock-declaration source for the assembly), never from on-disk KIND.md/PACKAGE.md."
        },
        {
          "path": "src/document.rs",
          "description": "The doc-comment references to `KIND.md`/`PACKAGE.md` as document roles retire; the generic fenced-document `Document` medium survives as the member document carriage (20-surface, 'three carriages, one feature shape'). No kind/package-file header grammar remains."
        },
        {
          "path": "build.rs",
          "description": "The build-time embed no longer walks the `kinds/`/`packages/` `*.md` trees to `include_str!` KIND.md/PACKAGE.md; it embeds the compiled default program (declaration rows) instead."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/contract_fixtures.rs",
        "asserts": "reconciled — clauses come from compiled declaration rows, not a parsed PACKAGE.md"
      },
      {
        "path": "tests/contract_template.rs",
        "asserts": "reconciled or retired — no KIND.md/PACKAGE.md template parse remains"
      },
      {
        "path": "tests/extract_equivalence.rs",
        "asserts": "generic extraction driven by the five facts, no per-kind/file-format parse"
      },
      {
        "path": "tests/memory_contract.rs",
        "asserts": "reconciled — the built-in memory floor/kind come from declaration rows, not the embedded KIND.md/PACKAGE.md fixtures"
      }
    ],
    "acceptance": "The engine parses no KIND.md or PACKAGE.md; floors and kinds are the compiled default program's declaration rows (or SDK-authored); the gate resolves no packages_dir/kinds_dir off disk; extraction is the generic algebra driven by the five facts.",
    "notes": "Terminal demolition of the file-format vehicle; now the pickable head (MANIFEST-MACHINERY-RETIRE shipped). Blast radius spans the whole floor/kind parse path — compose.rs, main.rs, check.rs, import.rs, contract.rs, builtin*/build.rs — and is atomic: the parse can't be half-retired in Rust. Curated kinds/·packages/ trees are fence-excluded human territory; the SESSION migrates them after emit lands, this entry retires only the engine parse."
  },
  {
    "tag": "EXPLAIN-UNIFY",
    "summary": "Collapse the four read traversals into one read verb — temper explain <target>",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "Decision: one read verb — `explain`"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "KIND-PACKAGE-PARSE-RETIRE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "Add `Command::Explain { target }` — one read verb over the graph `check` computes (20-surface Decision: one read verb). Resolve one positional `<target>` across the four namespaces (member / requirement / leaf-grain address / neighborhood) by the ruled mechanism (John 2026-07-04, inbox): exactly one hit ⇒ explain it; multiple hits ⇒ ERROR listing each match's qualified spelling (`member:foo`, `requirement:foo`, …) for the retry; a qualified prefix is always accepted as an explicit spelling. Never guesses, never silently prefers a namespace. Projections, never gates; exits zero; discloses coverage (names what it cannot see)."
        },
        {
          "path": "src/read.rs",
          "description": "Re-home the four traversals (why/requirements/impact/context) under `explain`, dispatched by the resolved target species. Reuse the graph-walk helpers left in place by CLI-COLLAPSE."
        },
        {
          "path": "tests/read_verbs.rs",
          "description": "Reframe as explain tests: each target species walks its corner of the one graph; a member-vs-requirement name collision errors with both qualified spellings; a qualified prefix resolves directly; coverage disclosure asserted."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "explain <target> resolves member/requirement/address/neighborhood; a name collision errors listing qualified spellings; qualified prefixes accepted; never gates; discloses coverage"
      }
    ],
    "acceptance": "`temper explain <target>` answers all four read questions over data check computes, exits zero, discloses coverage; an ambiguous bare target errors with each match's qualified spelling; a qualified prefix resolves directly; the four separate verbs are gone.",
    "notes": "Fork resolved (John 2026-07-04, inbox): bare target + collision error — cheap in the common ~95% case, explicit on ambiguity (git's ref-disambiguation ethos). Never guesses, never silently prefers. Terminal leaf; blocks nothing. An optional one-line corpus encode of the collision mechanism into the 20-surface 'one read verb' Decision is a human delta, not plan's to write."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm packages, the engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/architecture/50-distribution.md",
      "section": "Three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs human release setup (npm org + @temper scope, marketplace publish, signing/publish creds) before the mechanical wrappers can be validated end-to-end; channel 1 (SDK publish) also rides the SDK-primary foundation"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (50-distribution, 'install is two placements')."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, 50-distribution 'Fail-loud delivery'). Reconcile with the flume manifest (a non-private publish entry, or a split package dir) rather than clobbering it. Scoped `@temper` names per the resolved `(project-name)` fork. NB distinct from sdk/package.json (the SDK authoring-face manifest — channel 1's `temper` core + `@temper/claude-code` provider module)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (50-distribution 'Fail-loud delivery').",
    "notes": "Release infra, mostly outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm (`temper` + `@temper/claude-code`, floors as exported clause arrays); ch2 = engine binary pinned via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human creds."
  }
]
