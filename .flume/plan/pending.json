[
  {
    "tag": "KIND-EDGE-RELATIONSHIPS",
    "summary": "Reconcile the top-level `[[edge]]` construct into `[kind.<name>.relationships]` so a reference is a kind capability, not a separate construct.",
    "per": {
      "path": "specs/15-kinds.md",
      "section": "The entity graph is a kind capability"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "KIND-RETIRE-BUILTIN-SPEC"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "Move edge parsing from the standalone top-level `[[edge]]` array to `[kind.<name>.relationships]` under the owning kind (`specs/40-composition.md`, the `.relationships` surface) — the `<name>` is the edge source (the implicit `from`), and each relationship names its reference `field` and target `to` kind. Parse it for both a built-in-layer `[kind.<k>]` and a full custom-kind `[kind.<name>]` (relationships are orthogonal to the custom-vs-layer split). Preserve the parsed `Edge { field, from, to }` shape the graph consumes; drop `parse_edges`/`parse_edge`, `edge_str`, the `[[edge]]` reader, and `EdgeRootNotArray`/`BadEdge`. Update the compose in-module edge tests to the new shape."
        },
        {
          "path": "src/graph.rs",
          "description": "Read the declared relationships off the owning kinds rather than a free-standing edge list; route resolution + admissibility behavior unchanged (still over `Edge { field, from, to }`)."
        },
        {
          "path": "src/main.rs",
          "description": "Assemble the harness graph from the kinds' relationships instead of `AuthorLayer::edges()`."
        },
        {
          "path": "tests/graph.rs",
          "description": "Update the edge-construction fixture `ROUTES_TO_EDGE` and its `[[edge]]`-shaped assertions to the `[kind.<name>.relationships]` shape."
        },
        {
          "path": "tests/temper_toml.rs",
          "description": "Assert relationships parse under `[kind.<name>.relationships]` (a new parse assertion alongside the existing `[kind.spec]` custom-kind ones)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "Edges declared under `[kind.<name>.relationships]` assemble the harness graph and drive route resolution + admissibility exactly as the standalone `[[edge]]` construct did."
      }
    ],
    "acceptance": "A reference is declared under its owning kind's `[kind.<name>.relationships]` (over that kind's declared reference syntax) rather than a standalone `[[edge]]` table; the harness graph, route resolution, and edge-admissibility behavior are preserved.",
    "notes": "Note (d) of the reconciliation. The graph foundation shipped soundly (`src/graph.rs`) over `AuthorLayer::edges`; this aligns the edge-declaration *shape* with the specced kind capability (15-kinds 'The entity graph is a kind capability'; 40-composition's `.relationships` surface). File-disjoint from the retirement but serialized behind it for review clarity. `degree`/`acyclic` and the spec kind's references-resolve clause read the same relationships and follow as later entries."
  }
]
