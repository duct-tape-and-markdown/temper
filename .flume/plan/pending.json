[
  {
    "tag": "LAYOUT-COLLECTION-MEMBER-OWN-SPAN-LEAF",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [
      "layout-own-span-leaf"
    ],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/layout.rs",
          "description": "read_collection_member (222) takes a LayoutMember's leaves from its immediate sub-headings only, keyed by slug; the paragraph directly under the member's own heading lands nowhere. Land it under whichever reserved leaf key `(layout-own-span-leaf)` rules — composed leaves are author-named (NestedMemberRow.leaves keyed by field name, no reserved own-span key exists there; the SDK's `FRAMEWORK_KEYS`/kind.ts:245 `prose` is a member-level value, not a leaf-name reservation). Hazards the fork must rule on: node.body includes sub-heading text (the own span must cut at the first child heading), and a sub-heading slugged to the reserved name would collide with it."
        },
        {
          "path": "tests/layout_kind.rs",
          "description": "Regression: a layout collection member whose body is `### Title` + a paragraph + `**Test.** ...` (no sub-headings) materializes a non-empty leaves map carrying the paragraph under the ruled key."
        },
        {
          "path": "sdk/src/kind.ts",
          "description": "FRAMEWORK_KEYS (~245) reserves `prose` as a member-level value only; once `(layout-own-span-leaf)` rules a reserved own-span leaf key, an author's sub-heading slugging to that same name needs a collision guard (or explicit precedence rule) — the exact key and whether it joins FRAMEWORK_KEYS stays whatever the fork rules, not invented here."
        }
      ],
      "retire": []
    },
    "summary": "A layout collection member's own paragraph span lands nowhere — not a leaf, not the lock, not explain — while its sub-headings' spans do.",
    "per": {
      "path": "specs/intent.md",
      "section": "Invariants"
    },
    "tests": [
      {
        "path": "tests/layout_kind.rs",
        "asserts": "a layout collection member's own span (the text directly under its heading) lands in its leaves, under the ruled reserved key"
      }
    ],
    "acceptance": "A layout collection member's own paragraph (not just its sub-headings) reaches a leaf, addressable and visible to leaf predicates, leaf addresses, and explain, matching the composed half's full-leaves behavior. Per 0048's conservation bar, the regression pins non-vacuity: it asserts the captured own-span text is non-empty before checking it resolves as a leaf.",
    "notes": "observed at ad10e8cb (F11, GH #43 leaves gap); corrected at bf4b4168: 'the composed half already names it prose' is false — no reserved own-span leaf name exists in the corpus today. Reserving one is a Decision, tracked by fork `layout-own-span-leaf`, which f07d4f84's `layout_prose` family has since given a third candidate; this entry waits on the ruling either way. Serialized behind KINDS-IN-PLAY and LAYOUT-UNDECLARED-FINDING for tests/layout_kind.rs. scoped at bf2e3eba"
  },
  {
    "tag": "KIND-DECLARED-CITABLE-HANDLE",
    "gate": {
      "kind": "deferred",
      "reason": "An idea, not a defect (the raw note's own framing) — a `handle` fact on a target kind has no named consumer today, and the containment-selection family (open question) is the higher-priority extension to this same render-branching surface. Revive when a render hook wants a spelling the closed target-fact set (name, address, kind, projection path, repo-rooted path) can't already express cleanly."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "Anticipated: a `handle` fact on KindFacts, read by a render hook branching on target.kind, alongside the existing closed engine-derived target-fact set."
        }
      ],
      "retire": []
    },
    "summary": "A kind-declared citable handle ('how a reference to a member of this kind reads') — a lower-priority idea, not a defect, with no consumer yet.",
    "per": {
      "path": "specs/model/representation.md",
      "section": "kind"
    },
    "tests": [],
    "acceptance": "A kind declaring `handle` lets a reference render hook spell a member of that kind without hand-rolling the branch on target.kind — provable once a real render hook wants one.",
    "notes": "observed at ad10e8cb (cascade, idea). Compatible with the closed-set rule as a fifth engine-derived fact read off the target kind's declaration; the raw note itself calls this lower priority and not a defect. scoped at e1b26728"
  },
  {
    "tag": "TAP-ISO8601-DATETIME-CRATE",
    "gate": {
      "kind": "parked",
      "reason": "Adopting a new dependency is the human sanctioned-set call CLAUDE.md's Tech stack section reserves to itself, not plan- or build-derivable; needs either a `time` crate addition to the sanctioned set or a ruled pinned-semantics exception at the site."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/tap.rs",
          "description": "iso8601_utc_timestamp (313-368) and is_leap_year (370-372) hand-roll epoch-day Gregorian calendar arithmetic off SystemTime/UNIX_EPOCH to format an RFC 3339 UTC timestamp; replace with a sanctioned date/time crate (`time`, a narrower fit than `chrono` for one stamp) once added to CLAUDE.md's Tech stack, or mark the site a deliberate pinned-semantics exception (rust.md's comment taxonomy) if the human declines the dependency."
        }
      ],
      "retire": []
    },
    "summary": "src/tap.rs hand-rolls ISO-8601 calendar math for a timestamp; no sanctioned crate does date/time, so adopting one needs a human sanctioned-set call.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Libraries before hand-rolls"
    },
    "tests": [],
    "acceptance": "src/tap.rs's timestamp formatting runs through a sanctioned date/time crate instead of hand-rolled calendar math, or the site carries a ruled comment stating why it stays a hand-roll.",
    "notes": "drained from .flume/refactor/plan-tap-iso8601-hand-roll.md (filed at cd35a551); re-verified live at HEAD — no crate in the sanctioned set (CLAUDE.md, Tech stack) does date/time and this remains the sole iso8601 site (rg -n \"iso8601|SystemTime::now\" src/ tests/). scoped at e1b26728"
  },
  {
    "tag": "HOOK-COLLECTION-ADDRESS-DUPLICATE-REFUSAL",
    "gate": {
      "kind": "parked",
      "reason": "Human re-ruling 09-03 (interactive): the first ruling — refuse a duplicate member address within a kind — was built (e4ff7bc6) and failed temper's own self-host test at afterMerge: Claude Code allows N matcher groups per event and this repo's own harness carries two PostToolUse groups, so a duplicate-event refusal rejects every real harness. The defect (silent last-writer-wins on edge resolution to `hook:<Event>`) stands; the remedy is a hook identity discriminator, which is a model fork, not build's to invent."
    },
    "dependsOnForks": [
      "hook-member-identity"
    ],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/admissibility.rs",
          "description": "New sibling check beside `collection_address_collision_diagnostics` (which 21f61463 added for two *kinds* sharing one collectionAddress): a *single* kind's own members registering on the same key within that address (e.g. three `hook` members on `SessionStart`, which collapse to one entry in the `kind:name` composition map — silent last-writer-wins) is a declaration-time refusal with a named error, not a silent collapse."
        },
        {
          "path": "src/gate.rs",
          "description": "Wire the new diagnostic beside the existing `collection_address_collision_diagnostics` call (~gate.rs:200s)."
        },
        {
          "path": "tests/hook_kind.rs",
          "description": "Regression: three hook members declared on the same event refuse loud, naming all colliding members — mirroring 21f61463's kind-collision test shape."
        }
      ],
      "retire": []
    },
    "summary": "Refuse a duplicate member registration key within one kind's collection address (three hooks on SessionStart collapse to one) with a named error, not last-writer-wins.",
    "per": {
      "path": "specs/model/representation.md",
      "section": "Reach"
    },
    "tests": [
      {
        "path": "tests/hook_kind.rs",
        "asserts": "duplicate hook members on the same event name are refused loud, naming both"
      }
    ],
    "acceptance": "Two `hook` members declared on the same `event` refuse at declaration time with a named error rather than silently composing to whichever was last.",
    "notes": "GH #32. First pick built the duplicate-key refusal; gate-reverted by tests/cli.rs self-host test — temper's own settings.json has two PostToolUse groups. The revert itself isn't a resolvable commit (worktree-local, reverted pre-merge); see 080311b9 for the park record (its predecessor citation, e4ff7bc6, doesn't resolve at HEAD). Parked on `(hook-member-identity)`; on resolution, rewrite around the ruled discriminator (files[] will change). scoped at bf4b4168."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) is now proven live, not merely wired: the v0.0.11 tag fired it and its publish smoke gate went green (b6dc0a3's commit body)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "summary": "Packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing) and the plugin/bundle + marketplace.json channel (channel 3).",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json) — distribution.md's v0.1 launch gate: prebuilt binaries install on all three OSes.",
    "notes": "Park re-tested on disk at 01ba369: release.yml still states the darwin + channel-3 deferral verbatim (lines 7-9). The lockstep clause carried here since ac40c72/b676656 is resolved, dropped: da77c60 walked Cargo.toml 0.1.0→0.0.11 to match npm's cadence directly (no v0.1-tag flip), b6dc0a3 fixed the lockfile mismatch, and tag v0.0.11→da77c60 fired release.yml with a green smoke gate per its own commit body — channel 2 is proven live, not just wired. scoped 01ba369."
  },
  {
    "tag": "GAUNTLET-EXTERNAL-HARNESS-FIXTURES",
    "gate": {
      "kind": "parked",
      "reason": "Fixture choice is human territory (licensing): docs/ledger.md's 07-22 external-yield probe ran check over 9 real external harnesses and found the temper bugs the self-mirror can't (roots of GH #26/#30/#33/#38). Pick 2-3 of those 9 to vendor, license-check, and trim to their .claude/ trees before this unparks."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/fixtures/gauntlet_external/",
          "description": "Anticipated: the human-chosen 2-3 external harnesses' trimmed .claude/ trees, each vendored with its license noted (path/name unset until the human picks)."
        }
      ],
      "edit": [
        {
          "path": "tests/gauntlet.rs",
          "description": "Anticipated: one check+emit snapshot test per vendored external fixture, alongside the existing in-repo kitchen-sink corpus — the standing gate the 07-22 probe wasn't."
        }
      ],
      "retire": []
    },
    "summary": "Extend the gauntlet with 2-3 real external harness fixtures so the July external-yield probe (9 foreign repos, 4 temper bugs found) becomes a standing gate, not a one-off.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "The gauntlet corpus"
    },
    "tests": [
      {
        "path": "tests/gauntlet.rs",
        "asserts": "check/emit run over each vendored external fixture and snapshot-pin its findings"
      }
    ],
    "acceptance": "temper check/emit run clean (or with real, human-verified findings) over 2-3 vendored external harness .claude/ trees, snapshot-tested in tests/gauntlet.rs, so the July external-yield probe is a standing gate rather than a one-time manual run.",
    "notes": "observed at e77766c5, cross-cutting note — every defect in the 09-03 batch came from a foreign harness, none from the self-mirror; docs/ledger.md's 07-22 probe (9 real harnesses) is the candidate pool. Human picks 2-3 (licensing); test wiring is build's once chosen. scoped at 89d96cca."
  },
  {
    "tag": "GUIDANCE-FIELD-DECLARATION-CHANNEL",
    "gate": {
      "kind": "deferred",
      "reason": "0045's Consequences name field-declaration guidance beside kind-level, but the SDK's only per-field metadata today is a clause's `field:` target (predicate-bound) or `EdgeField` (edge-typed only) — no 'field declaration' list a bare advisory could attach to. None of the six promoted notes (GUIDANCE-BUILTIN-NOTES-PROMOTE) target a single field, inventing the mechanism now is speculative scope with no kind to exercise it ('An export earns its consumer', engineering.md). Revive when a field wants counsel no clause carries."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "Anticipated: a field-name-keyed guidance fact on `KindFacts`, analogous to `EdgeField`'s field-name-keyed list (196) but for arbitrary fields — shape unset; no field in any built-in kind wants one yet."
        },
        {
          "path": "src/drift.rs",
          "description": "Anticipated: a `KindFactRow` column (or sibling row) carrying per-field guidance/cite, following the `to_table`/`from_table` pattern GUIDANCE-KIND-SDK-LOCK-FACT establishes for the kind-level pair."
        },
        {
          "path": "src/schema.rs",
          "description": "Anticipated: `emit()`'s per-property `description` (156-172) folds field-declaration guidance in alongside a clause's, the same 'join into one description' discipline `push_description` (316-329) already runs for several guided clauses on one field."
        },
        {
          "path": "src/read.rs",
          "description": "Anticipated: field-level narration extends `narrate_governing_contract` (374-402) or a sibling to print a field's declared guidance."
        }
      ],
      "retire": []
    },
    "summary": "0045 also frees guidance onto a field declaration, no predicate — deferred: none of the six promoted built-in notes targets a single field, so there is no consumer yet",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "tests": [],
    "acceptance": "A field declaring `guidance` (no predicate) surfaces it via `schema` hover and `explain`, never gating — provable once a real kind needs one.",
    "notes": "0045 Consequences name field-declaration guidance beside kind-level; this waits for a driver, the same 'no consumer' bar prior deferred entries in this queue's history have held to. scoped at 946e303."
  },
  {
    "tag": "SCHEMA-KEYSTROKE-JSON-TOML-WIRING",
    "gate": {
      "kind": "deferred",
      "reason": "project_modeline (src/install.rs:1922) reads YAML frontmatter only (frontmatter::frontmatter_matter); a JSON document's native $schema key or a TOML equivalent needs its own design before schema/install extend to those kinds. Ruled (interactive, 2026-07-23) explicitly NOT build's to invent — surface only, never half-invent it. Revive when a JSON/TOML-kind keystroke gap has a real driver."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "Anticipated: `project_modeline`/`schema_ref` (~1594-1623) grow a JSON-document and/or TOML-document variant once a design exists — a JSON `$schema` top-level key insertion (JSON has no comment channel, so no `MODELINE_MARKER` prefix), a TOML equivalent is unprecedented and needs its own ruling."
        }
      ],
      "retire": []
    },
    "summary": "Keystroke JSON Schema wiring for JSON/TOML-document kinds (settings-local, plugin-manifest, marketplace, dial) has no modeline mechanism — deferred out of the YAML-frontmatter widening.",
    "per": {
      "path": "specs/distribution.md",
      "section": "The placements and their enforcement modes"
    },
    "tests": [],
    "acceptance": "A JSON- or TOML-document kind (settings-local, plugin-manifest, marketplace, dial, or a declared kind sharing one of those formats) gets keystroke schema validation wired by `install`, once a modeline-equivalent mechanism is designed for its format.",
    "notes": "Surfaced by inbox #13 (observed at fc77716) alongside SCHEMA-KIND-DOMAIN-WIDEN; deliberately split so the YAML-frontmatter widening isn't blocked on this open design question. scoped at 162e1cce."
  },
  {
    "tag": "LOCAL-LAYOUT-PROSE-REACHES-NO-READ-TIME-ROW",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "`local_document_rows` (:851) calls `read_layout_document`, which already returns the captured spans on `LayoutDocumentRows.prose` (src/drift.rs:1955), and then extends only `rows.nested` and `rows.satisfies` (:883-884) — the prose is read and dropped. Carry it: extend `rows.prose` too, and in `assemble_lock_family` (:1054-1055) collect the local kinds' prose rows into a new `local_layout_prose: Vec<drift::LayoutProseRow>` field on `LockFamily` (:405), beside `local_members` (:424) and for the same stated reason — the documents are uncommitted, so no consumer below can find them again short of a second walk that could disagree with this read. It does NOT go on `drift::Declarations` (:3331): that is the SDK→engine seam payload with a generated TS twin, and `layout_prose` is emit-derived output the seam has never carried."
        },
        {
          "path": "src/gate.rs",
          "description": "`gate_diagnostics` destructures `compose::LockFamily` exhaustively at :167-174 (no `..` arm), so the new `local_layout_prose` field refuses to compile until this site names it — bind it `..`-free like its siblings and leave it unread here (the gate has no prose consumer; `explain` is the reader). Ripple-reconciled: the only other destructure, src/read.rs:2065, already carries `..`."
        },
        {
          "path": "src/read.rs",
          "description": "`explain_target` destructures `LockFamily` at :2038 (taking `declarations` and `overlaid_builtin_kinds`) and its prose strand reads `drift::layout_prose_from_doc(&lock_doc)` at :2189 — the committed rows only. Take `local_layout_prose` off the same destructure and union it with the lock-document rows before `prose_strand` (:1989), so one narration covers both faces. Update the strand's sited comment above :2185, which today says the rows are 'a lock row keyed by the host's address' — true of the committed half only once local rows join it."
        },
        {
          "path": "tests/local_locus.rs",
          "description": "Regression: a local-locus layout kind whose document carries a preamble under a verbatim prose region — `explain <kind>:<id>` narrates the captured span, the same strand a committed layout member gets. Pins the read-time half of f07d4f84, which shipped the committed half and named this one as unwired."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "VERIFY-ONLY — expect no diff. Its `layout_prose` assertions read the committed emit-side rows, which this entry does not touch; in the fence only so a wave-mate's green base cannot be mistaken for coverage of the local face. The same holds for tests/layout_kind.rs, tests/check_cost.rs and tests/snapshots/gauntlet__projection_tree.snap, which the ripple names off the shared `layout_prose`/`overlaid_builtin_kinds`/`local_members` symbols: none reads `LockFamily`'s shape, so none moves."
        }
      ],
      "retire": []
    },
    "summary": "A local-locus layout member's captured prose is read and dropped at read time, so `explain` narrates nothing where the committed half now narrates the span.",
    "per": {
      "path": "specs/intent.md",
      "section": "Invariants"
    },
    "tests": [
      {
        "path": "tests/local_locus.rs",
        "asserts": "`explain` on a local-locus layout member narrates its captured prose region, matching the committed half's strand"
      }
    ],
    "acceptance": "`cargo test` green with the local_locus regression. A local-locus layout member's verbatim prose region reaches a read-time row and `explain`'s prose strand, so no layout document's authored span is read and dropped on either face (invariant 6). Non-vacuity per 0048's bar: the regression asserts the narrated span carries the document's actual preamble text, never merely that a strand appeared.",
    "notes": "Swept 09-07 from f07d4f84's own stated remainder ('the local read path is compose's to wire'). Verified on disk: `LayoutDocumentRows.prose` is already populated by `read_layout_document` and discarded by `local_document_rows` at src/compose.rs:883-884, so the fix is wiring, not derivation. Chained on EXPLAIN for src/read.rs. Ripple-reconciled at bb246cc2: src/gate.rs destructures LockFamily exhaustively. scoped at bf2e3eba"
  },
  {
    "tag": "FEATURES-FIXTURE-ONE-HOME(src)",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "LOCAL-LAYOUT-PROSE-REACHES-NO-READ-TIME-ROW"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/test_support.rs",
          "description": "The home: the module's own opening line already declares it \"Shared scaffolding for in-src `#[cfg(test)] mod tests` blocks\" (:1), and it is `#[cfg(test)]`-gated at src/lib.rs:73, so nothing here reaches a release build. Add one base beside `tmpdir` (:9) / `skill_kind` (:22) / `surface_unit` (:70): `pub(crate) fn features(id: &str) -> extract::Features` — the inert member every fixture below starts from, `fields` empty, `body_lines` 0, `rendered_lines`/`rendered_chars` `Some(0)` (NOT `None`: every existing copy spells `Some(0)`, and the extent predicates read the Some), `headings`/`sections`/`directives`/`fenced_blocks`/`nested_members`/`satisfies` empty, `source_dir` None, `edge_placements` None. Each site keeps only what it varies, via struct update: `Features { body_lines: 1, ..test_support::features(id) }`. Two alternatives considered and rejected, both to be named in the commit body: deriving `Default` on `extract::Features` (src/extract.rs:291) — a production API addition whose only consumer would be tests, and whose `rendered_lines: None` contradicts the `Some(0)` every fixture wants; and routing every fixture through the production choke point `builtin_kind::features` (src/builtin_kind.rs:676) the way tests/nested_member.rs:806 does — right where the subject IS extraction, wrong here, since several sites construct feature states extraction never produces."
        },
        {
          "path": "src/admissibility.rs",
          "description": "`member(id)` (:738) spells all thirteen fields to carry an id and nothing else — the base verbatim. Route it through `test_support::features`; nothing varies, so the local fixture either becomes a one-line forwarder or retires into its call sites."
        },
        {
          "path": "src/coverage.rs",
          "description": "`artifact(id, satisfies)` (:126) varies `satisfies` alone; the other twelve fields are the base."
        },
        {
          "path": "src/dial.rs",
          "description": "`dial_member(entries)` (:146) varies `id` (the literal `workstation`) and `fields` (the `ENTRIES_KEY` entry); the rest is the base."
        },
        {
          "path": "src/engine.rs",
          "description": "`features(id, fields, body_lines, source_dir)` (:1410) varies four columns plus `rendered_lines: Some(body_lines)` — keep those five over the base. `features_with_headings` (:1439) already builds on it and is untouched. `scoped_element_features` (:1306) is production code spelling the same tail (:1312-1325) and stays: it derives a scoped Features from a real one, not a fixture."
        },
        {
          "path": "src/graph.rs",
          "description": "Two copies in one module: `node(name, routes_to)` (:1995) varies `fields`/`body_lines`/`source_dir`, and the inline `let member = Features {…}` at :2664 spells the whole tail a second time. Both take the base. `satisfying` (:2469) already mutates a passed-in Features and stays."
        },
        {
          "path": "src/read.rs",
          "description": "`feature(id, satisfies, description)` (:2240) varies `fields`/`body_lines`/`source_dir`/`satisfies`. `nested_member` (:2431) already rides `feature` and stays. Test-mod only — no narration path is touched, which is why this entry waits behind the two entries that edit this file's `explain` surface rather than racing them."
        },
        {
          "path": "src/roster.rs",
          "description": "`features(name, satisfies)` (:213) varies `body_lines`/`source_dir`/`satisfies`; `skill_with_model` (:407) and `skill_satisfying` (:413) already ride it and stay. Its local name collides with the base's — either import the base qualified (`test_support::features`) or keep the local wrapper, whichever reads cleaner at the call sites."
        }
      ],
      "retire": []
    },
    "summary": "Seven in-src test mods each hand-spell the same thirteen-field `Features` literal; `src/test_support.rs` is the fixture home that exists for exactly this.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "tests": [
      {
        "path": "src/graph.rs",
        "asserts": "the module's two copies (the `node` fixture and the inline member) both build off the shared base, with every existing assertion unmoved and green"
      },
      {
        "path": "src/engine.rs",
        "asserts": "the four-column fixture and its `features_with_headings` derivative still produce the exact feature states the predicate tests judge"
      }
    ],
    "acceptance": "`cargo test` and `cargo clippy --all-targets -- -D warnings` green with no assertion changed — this is an extraction, so every test's subject and expectation is byte-identical. Oracle: `rg -n 'edge_placements: ' src/` hits only the three production constructors (src/kind.rs:1269, src/engine.rs:1325, src/compose.rs:1250) and the one base in src/test_support.rs; no `#[cfg(test)]` module spells the field tail.",
    "notes": "Filed by the post-ship sweep of c2415400..HEAD. feb0c21f's own body named its new in-src test mod's fixtures as repeats of compose.rs's; the `Features` half is the wider one — eight copies over seven modules, diverging on `body_lines` (0 vs 1) and `source_dir` (None vs Some) alone. engineering.md names test scaffolding a surface with one home; `tests/common` is unreachable from a src module, and `src/test_support.rs` is that home crate-side. scoped at dd1d4311"
  },
  {
    "tag": "FEATURES-FIXTURE-ONE-HOME(tests)",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "FEATURES-FIXTURE-ONE-HOME(src)"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/common/mod.rs",
          "description": "The integration home, which already carries the row half of this job — `kind_facts` (:653), `clause` (:709), `raw_unit` (:518) — and no `Features` builder at all. Add `pub fn features(id: &str) -> Features` in the shape FEATURES-FIXTURE-ONE-HOME(src) lands crate-side, so the two homes spell one base: `fields` empty, `body_lines` 0, `rendered_lines`/`rendered_chars` `Some(0)`, every body-derived vector empty, `source_dir`/`edge_placements` None. `Features` is `pub` on `extract` (src/extract.rs:291), so the whole struct is reachable from an integration test — the copies below are habit, not necessity."
        },
        {
          "path": "tests/extent.rs",
          "description": "`member(id, lines, chars, body_lines)` (:20) varies `body_lines`/`rendered_lines`/`rendered_chars` — the render/source divergence the file exists to drive, so those three stay explicit over the base."
        },
        {
          "path": "tests/check_cost.rs",
          "description": "Two inline literals in one test body, `let skill` (:431) and `let rule` (:447), varying `fields`/`body_lines` only."
        },
        {
          "path": "tests/layout_edge_slot.rs",
          "description": "`guide_features(fills)` (:88) — the fixture's varying columns stay, the tail comes from the base."
        },
        {
          "path": "tests/field_addressing.rs",
          "description": "`member(fields)` (:26) varies `id` (`acme-tools`) and `fields` (the retained JSON parse)."
        },
        {
          "path": "tests/shape_predicate.rs",
          "description": "`skill(name, description)` (:20) varies `id` and `fields`."
        },
        {
          "path": "tests/contract_template.rs",
          "description": "`cited(placements)` (:343) and `formatted(placements)` (:348) — the pair varies `edge_placements`, which is exactly the column this file judges, so it stays explicit."
        },
        {
          "path": "tests/layout_prose_import.rs",
          "description": "`feature(id)` (:258) varies `id` alone — the base verbatim."
        },
        {
          "path": "tests/prose_include.rs",
          "description": "`feature(id)` (:194) varies `id` alone — the base verbatim."
        },
        {
          "path": "tests/read_verbs.rs",
          "description": "`feature(id, satisfies)` (:27) carries a doc comment claiming the copy is forced — \"duplicated here since this file, being outside the crate, can only build `Features` through its public fields\" (:24-26). The public fields are what `tests/common` builds from too, so the claim retires with the copy; reword it to name the shared home. This file is also EXPLAIN-KIND-REGISTRATION-LOCUS-AND-ADDRESS-FORM's, which is why that entry is a named parent."
        },
        {
          "path": "tests/closed_keys.rs",
          "description": "`member(fields)` (:22) varies `id`/`fields`."
        },
        {
          "path": "tests/graph.rs",
          "description": "`member(id, field)` (:1650) varies `fields`/`body_lines`/`source_dir`."
        },
        {
          "path": "tests/rule_contract.rs",
          "description": "`rule_features(paths)` (:175) varies `fields` and imports `Features` locally inside the fn (:177) — the import moves with the literal."
        }
      ],
      "retire": []
    },
    "summary": "Thirteen integration tests each hand-spell the `Features` literal `tests/common` exists to home — one of them documenting the copy as unavoidable, which it is not.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "the local fixture builds off `common::features` and every existing explain/impact assertion is unmoved and green"
      },
      {
        "path": "tests/extent.rs",
        "asserts": "the render/source divergence the file drives survives the base — the three varied columns stay explicit and the extent cases judge the same feature states"
      }
    ],
    "acceptance": "`cargo test` green with no assertion changed. Oracle: `rg -n 'edge_placements: ' tests/` hits only tests/common/mod.rs — every integration fixture takes its inert tail from the one home, and a fourteenth field on `Features` is a one-site edit rather than a fourteen-site one.",
    "notes": "Post-ship sweep of c2415400..HEAD, the integration half of the same class. Behind (src) so the base's name, signature and inert shape are decided once and copied, not invented twice; behind EXPLAIN for tests/common/mod.rs and tests/read_verbs.rs, both in that entry's fence. tests/nested_member.rs:806 and tests/type_predicate.rs:62 are excluded on purpose — both already build through a production constructor. scoped at dd1d4311"
  },
  {
    "tag": "LOCK-ROW-FIXTURE-ONE-HOME(src)",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "FEATURES-FIXTURE-ONE-HOME(src)",
        "LOCAL-LAYOUT-PROSE-REACHES-NO-READ-TIME-ROW"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/test_support.rs",
          "description": "Two more bases beside the `Features` one FEATURES-FIXTURE-ONE-HOME(src) lands: `pub(crate) fn kind_fact_row(name: &str) -> drift::KindFactRow` (name set, the other thirteen columns absent — `provider`/`governs_root`/`governs_glob`/`commitment`/`format`/`unit_shape`/`content`/`shape`/`collection_address`/`guidance`/`cite` None, `registration`/`templates` empty) and `pub(crate) fn clause_row(predicate: &str, severity: &str) -> drift::ClauseRow` (the two named columns set, the other twenty-one absent). The integration home already proves both shapes — `tests/common/mod.rs`'s `kind_facts` (:653) and `clause` (:709) — so this is the same builder in the home a src module can reach, not a new design."
        },
        {
          "path": "src/kind.rs",
          "description": "The widest copy: `spec_row()` (:1498) plus ten inline `KindFactRow` literals in the lift tests (:1544, :1595, :1623, :1650, :1683, :1709, :1724, :1775, :1808, :1832), each spelling all fourteen columns to vary one or two. Each becomes `KindFactRow { <the columns this case lifts>, ..test_support::kind_fact_row(\"spec\") }`; `spec_row` itself either forwards or retires."
        },
        {
          "path": "src/admissibility.rs",
          "description": "`kind_row(name)` (:718) is the base verbatim; five inline literals (:853, :871, :888, :908, :931) vary one collision column each; `clause_row(kind, label, severity)` (:688) spells all twenty-three `ClauseRow` columns to set three. All seven route through the two bases. This file is FEATURES-FIXTURE-ONE-HOME(src)'s too, which is why that entry is a named parent."
        },
        {
          "path": "src/compose.rs",
          "description": "`clause_row(severity)` (:1420) spells the twenty-three columns to set `label`/`predicate`/`severity`; five more inline literals (:1454, :1482, :1535, :1549, :1563) do the same for the lift-failure cases. Its signature differs from admissibility.rs's twin — same job, two homes, two shapes — and both collapse onto the one base. The file is LOCAL-LAYOUT-PROSE-REACHES-NO-READ-TIME-ROW's, hence the second parent."
        },
        {
          "path": "src/drift.rs",
          "description": "Two `KindFactRow` literals in the round-trip tests (:5672, :5697), each spelling the tail to vary the columns the write/read pair asserts on."
        },
        {
          "path": "src/coverage_note.rs",
          "description": "One inline `crate::drift::KindFactRow` literal (:609) inside a `Declarations` fixture."
        }
      ],
      "retire": []
    },
    "summary": "Five in-src test mods spell `KindFactRow`'s fourteen columns and `ClauseRow`'s twenty-three by hand — including one fixture name living in two homes with two shapes.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "tests": [
      {
        "path": "src/kind.rs",
        "asserts": "every lift test still judges the exact row it built — the varied columns stay explicit over the base and no assertion moves"
      },
      {
        "path": "src/compose.rs",
        "asserts": "the default-contract lift and its three refusal cases keep their clause rows byte-equivalent through the shared base"
      }
    ],
    "acceptance": "`cargo test` and `cargo clippy --all-targets -- -D warnings` green with no assertion changed. Oracle: `rg -n 'collection_address: None' src/` hits only src/test_support.rs, and neither `spec_row`/`kind_row` nor the two divergent `clause_row` builders survives as a per-module copy of the tail.",
    "notes": "Post-ship sweep of c2415400..HEAD, the lock-row half of the same class — feb0c21f named it in its own commit body (\"its ClauseRow/KindFactRow fixtures repeat what compose.rs keeps\"). Filed as its own entry, not folded into the `Features` one: together they would be twelve files and ~50 sites. Behind (src) for src/test_support.rs and src/admissibility.rs, behind LOCAL-LAYOUT for src/compose.rs. scoped at dd1d4311"
  }
]
