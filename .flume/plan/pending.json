[
  {
    "tag": "EXTENT-PREDICATE",
    "summary": "Derive 0035's render-side `extent` predicate (lines|characters, each+whole grain) and retire `max_lines` into it — the shipped line budgets re-spell as extent over the same selections (0035 amended)",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/extent.rs",
          "description": "The new predicate's fixture home (one per rule, `tests/rust.md`): each-grain (a member/embedded value whose rendered extent exceeds the bound fires; within-bound passes) and whole-grain (a selection's summed rendered extent over the bound fires) at both units (lines, characters). Render-side: a member with a resolved reference/include whose projection exceeds the bound fires even though its source body does not — the axis 0035 rejected `max_lines` for. Plus the retirement's loud edge: a committed lock still carrying the retired `max_lines` predicate refuses at load (the closed vocabulary's ordinary unknown-predicate reject, contract.md 'clause')."
        }
      ],
      "edit": [
        {
          "path": "src/contract.rs",
          "description": "Add `extent` to the closed vocabulary and RETIRE `max_lines` (0035 amended: extent subsumes it). New `Predicate::Extent` carrying its declared unit (lines | characters) and bound; its parse arm in the label->predicate table (where `max_lines` sat, ~462) — an unknown unit rejected at load is the unit's admissibility — and its label/field arms (label ~571, field ~625/639/669/682). Remove `Predicate::MaxLines` entirely: the variant + doc (~247-248), parse arm (~462), label arm (~571), field arms (~639/682). `extent` is a **node-scope** predicate like `max_len`: it names no field and measures the selected item's own rendered size."
        },
        {
          "path": "src/engine.rs",
          "description": "Evaluate `extent` at both grains render-side and remove the `max_lines` eval. Each-grain (per selected item's rendered extent <= bound) and whole-grain (the selection's summed rendered extent <= bound — the ambient-context budget the grain axis gives for free, 0035). Measurement is render-side — the bytes the item contributes to its projection as emit resolved them, decidable off the committed artifacts the gate holds — NOT `features.body_lines` (that was `max_lines`'s source-side count). A file member's extent is its projection; an embedded value's is its rendered span in the host artifact (the load-bearing case — centercode's posture kinds are embedded). Remove `Predicate::MaxLines` (eval ~895/896, the fixed-answer doc ~256, whole-grain arm ~219) and migrate/drop its inline test fixtures (~1547, ~1910, ~2077, ~2315, ~2368). NOTE for build: `features.body_lines` and its `LineCount` layout primitive (kind.rs:1260/1328) may orphan once their sole predicate consumer is gone — confirm on disk; 0035 names no `LineCount` retirement, so do not invent one."
        },
        {
          "path": "src/drift.rs",
          "description": "The `ClauseRow` struct (~2704) gains the optional `unit` column (snake_case wire key, `#[derive(Deserialize)]`-read), lifted into `Predicate::Extent` at load; remove the `max_lines` reconstruction arm with the variant, and update the bound-doc comments naming `max_lines` (~2691, ~2778, ~2823/2831) to `extent`. Two-sided by construction (`sdk.md`, 'The engine seam')."
        },
        {
          "path": "src/builtin_lock.toml",
          "description": "The committed built-in lock re-derives once the shipped defaults re-spell: its four `max_lines` clause rows — `command.max_lines` (~300), `memory.max_lines` (~496), `rule.max_lines` (~831), `skill.max_lines` (~966) — become `extent` rows (unit=lines, same bounds). Regenerated data (see the file header), never hand-edited; `tests/builtin_lock_frozen.rs` byte-compares the re-emission and fails loud if it drifts. (`src/builtin_lock.rs` is the pure loader and carries no `max_lines` — untouched.)"
        },
        {
          "path": "sdk/src/contract.ts",
          "description": "Add an `extent(unit, bound)` constructor where `maxLines` sat (~131), unit typed `'lines' | 'characters'`, returning `{ key: 'extent', ... }`; REMOVE the `maxLines` constructor — extent subsumes it. Update the clause-key doc list (~25) that enumerates `required`/`max_len`/`max_lines`."
        },
        {
          "path": "sdk/src/builtins.ts",
          "description": "Re-spell the shipped `maxLines` adoptions as extent budgets over the SAME selections carrying the EXISTING bounds (0035 amended: the subsumption carries the existing cited budgets across, no NEW budget opinion enters): `skillDefaultContract`'s `maxLines(500)` (~1138) -> `extent('lines', 500)`, `rule` `maxLines(200)` (~1272) and `memory` `maxLines(200)` (~1313) -> `extent('lines', 200)`, each keeping its advisory severity and cite, its guidance reworded to name render-side measurement. `commandDefaultContract` reuses `skillDefaultContract` by `.filter(...)` (~1191), so re-spelling the skill clause flips both `skill` and `command` lock rows — three source edits, four lock rows. Drop the `maxLines` import (~26)."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "`clauseRow` (~64) lowers `extent`: its bound rides `nodeScopeBoundArgs` (~89/~119) — swap the retired `max_lines` key in the guard there (~120) for `extent`, drop `max_lines` from the doc lists (~58/~116) — and its `unit` needs a new optional column emitted onto the `ClauseRow`. (ADMISSION-JOINS-FILE-TEMPLATE shipped at 631bc83 and no longer contends this file — its declarations.ts change was to `templatesFor`, not clause lowering.)"
        },
        {
          "path": "sdk/src/index.ts",
          "description": "Swap the `maxLines` re-export (~38) for `extent` — the root barrel's constructor surface follows the vocabulary change."
        },
        {
          "path": "sdk/src/generated/ClauseRow.ts",
          "description": "Regenerated TS mirror of the `ClauseRow` struct once its `unit` column lands — the generated seam, not hand-authored; run the SDK generate step and commit the diff."
        },
        {
          "path": "sdk/test/contract.test.ts",
          "description": "SDK-side: the `extent` constructor produces the expected clause value and its declaration row carries the unit + bound columns; a round-trip proves the wire key matches the Rust reader. Drop the `maxLines` coverage with the constructor."
        },
        {
          "path": "sdk/test/emit.test.ts",
          "description": "Migrate the `maxLines` uses to `extent`: the import (~30), the `maxLines(300)` clause fixture (~180), and the `\"max_lines\"` predicate-key assertion (~218) -> `extent('lines', 300)` / `\"extent\"`."
        },
        {
          "path": "sdk/test/builtins.test.ts",
          "description": "The shipped-default assertions naming `max_lines` re-point to `extent`: the predicate-key list (~86, ~117) and the memory-contract clause-key assertion (~132)."
        },
        {
          "path": "tests/rule_contract.rs",
          "description": "Default-contract test: the shipped clause's label flips `*.max_lines` -> `*.extent` and its diagnostic reworded render-side; fixtures asserting on it re-point and any snapshot regenerates. Mechanical migration following the label, not new behavior."
        },
        {
          "path": "tests/memory_contract.rs",
          "description": "Same label flip `memory.max_lines` -> `memory.extent` in the memory default-contract fixtures; re-point assertions, regenerate snapshots. Mechanical."
        },
        {
          "path": "tests/memory_gate.rs",
          "description": "The memory-gate fixtures asserting on the `max_lines` clause label/diagnostic re-point to `extent`. Mechanical migration."
        },
        {
          "path": "tests/contract_template.rs",
          "description": "Contract-template fixtures naming `max_lines` re-point to `extent`. Mechanical migration following the label."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "Lock-declaration-row fixtures asserting the emitted `max_lines` clause rows re-point to `extent` rows (with the new `unit` column). Mechanical."
        },
        {
          "path": "tests/dial_kind.rs",
          "description": "Dial-kind fixtures dialing a clause addressed `*.max_lines` re-point to `*.extent`. Mechanical migration following the label."
        },
        {
          "path": "tests/reporters.rs",
          "description": "Reporter fixtures rendering a `max_lines` finding re-point to the `extent` label/diagnostic. Mechanical."
        },
        {
          "path": "tests/acceptance.rs",
          "description": "Acceptance fixtures naming `max_lines` re-point to `extent`; the associated snapshot regenerates (see the snapshot edit below). Mechanical migration."
        },
        {
          "path": "tests/snapshots/acceptance__rules_check_diagnostics.snap",
          "description": "The `insta` snapshot carrying a `max_lines` diagnostic line regenerates to the `extent` label/wording once the shipped clause re-spells. Regenerated fixture, `cargo insta accept`."
        },
        {
          "path": "tests/cli.rs",
          "description": "CLI fixtures naming `max_lines` (a finding or explain line) re-point to `extent`. Mechanical."
        },
        {
          "path": "tests/gate_fail_loud.rs",
          "description": "Fail-loud fixtures referencing the `max_lines` clause re-point to `extent`. Mechanical migration following the label."
        },
        {
          "path": "tests/session_start.rs",
          "description": "Session-start reporter fixtures naming `max_lines` re-point to `extent`. Mechanical."
        },
        {
          "path": "tests/manifest_schema_oracle.rs",
          "description": "The predicate-exhaustive match (~173) listing `Predicate::MaxLines { .. }` follows the vocabulary: drop the `MaxLines` arm, add `Extent { .. }`. This match is the schema oracle's completeness pin, so it must track the enum."
        },
        {
          "path": "tests/closed_keys.rs",
          "description": "The closed-vocabulary fixture constructing `Predicate::MaxLines { max: 9 }` (~213) re-points to `Predicate::Extent { .. }`. Mechanical migration following the enum."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/extent.rs",
        "asserts": "each-grain and whole-grain `extent` fire on rendered-extent overruns at both units (lines, characters) and pass within bound; render-side measurement catches an overrun a source-side count would miss; a lock still carrying the retired `max_lines` predicate refuses at load"
      },
      {
        "path": "tests/manifest_schema_oracle.rs",
        "asserts": "the predicate-exhaustive oracle tracks the enum: `Extent` present, `MaxLines` gone"
      }
    ],
    "acceptance": "`extent(unit, bound)` is a closed-vocabulary predicate: each-grain bounds a selected item's rendered extent, whole-grain bounds the selection's summed rendered extent, unit is lines or characters, measurement is render-side off committed artifacts, and an unknown unit is refused at load (contract.md, 'clause': the predicate vocabulary is closed, an unknown predicate is rejected at load). `max_lines` is gone from the vocabulary and a lock carrying it refuses at load; the three shipped line budgets (skill/command 500, rule 200, memory 200) re-spell as `extent('lines', N)` over the same selections with no new budget opinion, and the built-in lock re-derives to match. `pnpm --dir sdk test` + `cargo test` green.",
    "notes": "Routes 0035's amendment (725188e): extent SUBSUMES max_lines; the 3 cited budgets carry across, no NEW opinion. command reuses skillDefaultContract (builtins.ts:1191), so 3 source edits flip 4 lock rows. body_lines + its LineCount primitive may orphan once max_lines' eval goes — confirm at build, don't invent its retirement (0035 names none). ADMISSION shipped (631bc83): the blockedBy is stale, unblocked to open. scoped at b804e98."
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics — the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (⇒ 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (⇒ 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence — never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. `MAX_IMPORT_HOPS = 5` (59) sits under a doc (55-58) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' — a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (621) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds (648), one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts — hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it — 519 ('hop-capped at [`MAX_IMPORT_HOPS`]') and 618-620 ('capped at [`MAX_IMPORT_HOPS`] hops (the `at-import` recursion depth the format documents)') — carry the same claim and move with it, as does the propagation comment at 643-646 ('capped at the format's import recursion depth'). The file is untouched across 0c3cbcb..b85df4a — re-verified this tick — so every address carries."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1349) already pins the boundary mechanically rather than in a comment — `r1..r5` rescued from a live seed, `r6` dead as the sole finding — so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1350-1354) spells 'capped at the format's five-hop import recursion' (1352) and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number. The file is untouched across 0c3cbcb..b85df4a — re-verified this tick — so the addresses carry."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper — the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Park re-tested at b85df4a and holds — nothing ruled the hop semantics. The window touched neither file, so every address carries. The constant is still 5 on disk (`graph.rs`:59, re-read this tick), so the park's subject is untouched — and may still be wrong. A build-side friction capture records the same disagreement; it is the human's to read, not plan's to drain. rescoped 4a33911; park re-tested b85df4a."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested on disk at b85df4a, holding on every clause: `git tag -l` carries only the four era tags and no version tag, crate 0.1.0 vs npm 0.0.7, and release.yml:7-9 still states the darwin + channel-3 deferral verbatim. `git diff 8fc5e21..b85df4a -- .github/` is empty. scoped b676656."
  }
]
