[
  {
    "tag": "TEST-HELPER-DUPES-CONSOLIDATE(rawunit)",
    "summary": "Consolidate 5 near-identical raw-Unit-literal test constructors (5 copies across 4 files) into one parameterized tests/common helper",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "TEST-HELPER-DUPES-CONSOLIDATE(lockunitbuilders)"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/common/mod.rs",
          "description": "Add `pub fn raw_unit(id: &str, frontmatter: BTreeMap<String, serde_json::Value>, body: &str, source_path: &str) -> Unit`, building `Unit { id: id.to_string(), frontmatter, body: body.to_string(), source_path: PathBuf::from(source_path), satisfies: Vec::new(), satisfies_clauses: Vec::new(), published_requirements: Vec::new() }` — the shape all 5 call sites below share, varying only which of id/frontmatter/body/source_path is fixed vs a parameter."
        },
        {
          "path": "tests/section_contains.rs",
          "description": "Delete local `spec_unit(body: &str) -> Unit` (line 27, id `\"10-contracts\"`, empty frontmatter, source_path `specs/model/contract.md`); call sites use `common::raw_unit(\"10-contracts\", BTreeMap::new(), body, \"specs/model/contract.md\")`."
        },
        {
          "path": "tests/nested_member.rs",
          "description": "Delete local `decision_unit(body: &str) -> Unit` (line 39, id `\"05-surface-authority\"`, empty frontmatter, source_path `docs/decisions/05-surface-authority.md`); call sites use `common::raw_unit`."
        },
        {
          "path": "tests/extract_equivalence.rs",
          "description": "Delete local `memory_unit(body: &str) -> Unit` (line 78, id `\"CLAUDE\"`, empty frontmatter, source_path `CLAUDE.md`) and local `frontmatter_unit(frontmatter: serde_json::Map<...>) -> Unit` (line 170, id `\"settings\"`, empty body, source_path `settings/settings.md`); all call sites of both (several `#[test]` fns in this file) switch to `common::raw_unit` directly with the same fixed id/source_path constants."
        },
        {
          "path": "tests/agent_kind.rs",
          "description": "Delete local `unit_from_member(member: &Member) -> Unit` (line 60, the fully-parameterized shape — id/frontmatter/body/source_path all derived from `member`); call site uses `common::raw_unit(&member.id, member.fields.iter().cloned().collect(), &member.body, member.provenance.source_path.to_str().unwrap())`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/extract_equivalence.rs",
        "asserts": "skill_features/directives/fenced/field-primitive scenarios driven by memory_unit/frontmatter_unit are unchanged after switching to common::raw_unit"
      },
      {
        "path": "tests/nested_member.rs",
        "asserts": "the member-fence nested-member fold scenarios driven by decision_unit are unchanged"
      }
    ],
    "acceptance": "tests/common exports one raw_unit(id, frontmatter, body, source_path) -> Unit; the 5 duplicate raw-Unit-literal constructors (spec_unit, decision_unit, memory_unit, frontmatter_unit, unit_from_member) are gone from their 4 files, each call site wrapping common::raw_unit with its own fixed/derived values; cargo test green.",
    "notes": "Filed in job 4's residue sweep at 18a110a. src/kind.rs:918 carries the same raw-Unit shape (spec_unit) but is the sole in-src copy on the crate-private side of the integration-test boundary (same split as src/test_support::tmpdir vs tests/common::tmpdir) — not residue, left alone. Shares tests/common/mod.rs, tests/agent_kind.rs, tests/extract_equivalence.rs with lockunitbuilders, hence blockedBy not open. scoped at 18a110a"
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper`, single package, `(sdk-package-layout)`, now at 0.0.5 pending publish — SDK-VERSION-LOCKSTEP); channel 1's SDK foundation shipped. Re-verified at ff09cee: still no `.github/workflows/release.yml` (only `temper.yml`, a check job); root package.json is still the private flume manifest (`temper-flume-harness`)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm `@dtmd/temper`; ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs range-correctness shipped in SCAFFOLD-OUTPUT-VALID (^0.0.4); release owns only future re-pins."
  }
]
