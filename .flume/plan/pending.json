[
  {
    "tag": "DRIFT-COMPARE-EOL-CANONICALIZE",
    "summary": "Canonicalize EOL before hashing in drift.rs's 4 fingerprint/baseline comparisons (config_stale, source_dep_stale, reap classify, place baseline) so a CRLF checkout reads clean, per pipeline.md Drift.",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "Drift"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "Canonicalize on-disk bytes' CRLF/lone-CR to LF before hashing/comparing, at exactly 4 sites: classify_orphan's reap comparison (2226, Reaped vs OrphanDrift), place's drifted_from_baseline (2539, install's placement-conflict detection), config_stale_from_doc's committed-projection freshness fact (2620), source_dep_stale_from_doc's layout-import/include freshness fact (2824). The stored emit_hash/import_hash/baseline and any freshly-rendered `desired` bytes stay LF-exact, untouched. Do NOT touch the Unchanged-vs-Emitted write-skip comparisons (emit_one 2331, emit_manifest 1661, place's initial `real == desired.as_bytes()` at 2532) — those are write-time idempotency checks the ratified clause explicitly leaves byte-exact ('emit still writes LF')."
        },
        {
          "path": "src/hash.rs",
          "description": "Likely home for a shared byte-level EOL-canonicalizing helper (its own doc comment already frames this file as the drift engine's single hashing home) — one helper the 4 drift.rs sites share, not four inline copies. Operate on raw &[u8] (no UTF-8 decode required for a CRLF/lone-CR collapse), distinct from drift.rs's existing str-based `normalize_lf` (2239, a write-side helper for authored body content, unrelated to this compare-side fix). Implementer's call whether to add here or reuse/adapt normalize_lf for bytes."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/emit.rs",
        "asserts": "a CRLF-rewritten-but-content-identical committed projection reports drift::config_stale empty, and an orphaned CRLF-rewritten-but-content-identical projection classifies EmitOutcome::Reaped (not OrphanDrift); a genuine content edit under CRLF still flags config_stale and OrphanDrift respectively."
      },
      {
        "path": "tests/layout_prose_import.rs",
        "asserts": "a CRLF-rewritten-but-content-identical layout-import target reports source_dep_stale empty; a genuine content edit under CRLF still flags it (tests/prose_include.rs may fit better for the include family — implementer's call, cite whichever family's fixture the test exercises)."
      },
      {
        "path": "src/drift.rs",
        "asserts": "inline mod tests: place() does not report ApplyOutcome::Conflicted for a CRLF-only rewrite of the target against a baseline hashed from LF content; still reports Conflicted for a genuine (non-EOL) edit — sibling case to the existing place_conflicts_only_against_a_recorded_baseline test."
      }
    ],
    "acceptance": "check's config_stale and source_dep_stale report no finding for an EOL-only (CRLF vs LF) diff between disk and the lock's fingerprint; emit's reap classifies an EOL-only-differing orphan Reaped, not OrphanDrift; install's place() does not report Conflicted for an EOL-only diff against a recorded baseline; each site still flags a genuine non-EOL diff; emit's own write path keeps producing byte-exact LF output unchanged (a_crlf_or_lone_cr_body_emits_an_lf_only_projection and the hash-baseline test stay green).",
    "notes": "scoped at aee005d. Disposition in .flume/inbox.md item 10 (spec ratified aee005d). Re-verified: the disposition's 4 sha256_hex sites (2620/2226/2824/2539) are exactly the fingerprint/baseline comparisons the clause governs. 3 other raw byte-== sites in the same file (emit_one 2331, emit_manifest 1661, place's initial check 2532) are the Unchanged-vs-Emitted write-skip decision the clause leaves alone (\"emit still writes LF\") — excluded on purpose, not an oversight."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "Packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing) and the plugin/bundle + marketplace.json channel (channel 3).",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) is now proven live, not merely wired: the v0.0.11 tag fired it and its publish smoke gate went green (b6dc0a3's commit body)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json) — distribution.md's v0.1 launch gate: prebuilt binaries install on all three OSes.",
    "notes": "Park re-tested on disk at 01ba369: release.yml still states the darwin + channel-3 deferral verbatim (lines 7-9). The lockstep clause carried here since ac40c72/b676656 is resolved, dropped: da77c60 walked Cargo.toml 0.1.0→0.0.11 to match npm's cadence directly (no v0.1-tag flip), b6dc0a3 fixed the lockfile mismatch, and tag v0.0.11→da77c60 fired release.yml with a green smoke gate per its own commit body — channel 2 is proven live, not just wired. scoped 01ba369."
  },
  {
    "tag": "GUIDANCE-FIELD-DECLARATION-CHANNEL",
    "summary": "0045 also frees guidance onto a field declaration, no predicate — deferred: none of the six promoted built-in notes targets a single field, so there is no consumer yet",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "deferred",
      "reason": "0045's Consequences name field-declaration guidance beside kind-level, but the SDK's only per-field metadata today is a clause's `field:` target (predicate-bound) or `EdgeField` (edge-typed only) — no 'field declaration' list a bare advisory could attach to. None of the six promoted notes (GUIDANCE-BUILTIN-NOTES-PROMOTE) target a single field, inventing the mechanism now is speculative scope with no kind to exercise it ('An export earns its consumer', engineering.md). Revive when a field wants counsel no clause carries."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "Anticipated: a field-name-keyed guidance fact on `KindFacts`, analogous to `EdgeField`'s field-name-keyed list (196) but for arbitrary fields — shape unset; no field in any built-in kind wants one yet."
        },
        {
          "path": "src/drift.rs",
          "description": "Anticipated: a `KindFactRow` column (or sibling row) carrying per-field guidance/cite, following the `to_table`/`from_table` pattern GUIDANCE-KIND-SDK-LOCK-FACT establishes for the kind-level pair."
        },
        {
          "path": "src/schema.rs",
          "description": "Anticipated: `emit()`'s per-property `description` (156-172) folds field-declaration guidance in alongside a clause's, the same 'join into one description' discipline `push_description` (316-329) already runs for several guided clauses on one field."
        },
        {
          "path": "src/read.rs",
          "description": "Anticipated: field-level narration extends `narrate_governing_contract` (374-402) or a sibling to print a field's declared guidance."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A field declaring `guidance` (no predicate) surfaces it via `schema` hover and `explain`, never gating — provable once a real kind needs one.",
    "notes": "0045 Consequences name field-declaration guidance beside kind-level; this waits for a driver, the same 'no consumer' bar prior deferred entries in this queue's history have held to. scoped at 946e303."
  }
]
