[
  {
    "tag": "INSTALL-HOOK-APPEND-COVERAGE",
    "summary": "Add an install.rs fixture exercising json_splice::append_element's populated-array branch — a sibling tool's pre-existing hook entry, appended-after path, currently untested",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/install.rs",
          "description": "Add a fixture settings.json whose `hooks.SessionStart` (or `hooks.PreToolUse`) array already carries one entry belonging to a different, non-temper tool (not our own hook, so `hook_present`/`guard_present` is false but the key already exists) before `install::run`. Assert temper's own group is appended after the sibling entry, byte-for-byte, without disturbing it. This reaches `splice_hooks`'s `Some(member) =>` arms (src/install.rs:862 for SessionStart, :883 for PreToolUse) which call `json_splice::append_element` (src/json_splice.rs:220-234) — specifically its `Some(&(_, last_end))` populated-array arm (:224-227). Every current fixture (`NON_CANONICAL_SETTINGS_WITH_HOOK` et al.) either starts with no prior hooks key (hitting `insert_member`'s fresh-key path instead) or with temper's own already-matching entry (skipping the append branch entirely), so the populated-array append is exercised nowhere today."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "installing onto a settings.json with a pre-existing sibling hook array (SessionStart or PreToolUse) appends temper's own group after the sibling entry, preserving it, and converges (unchanged) on a second run"
      }
    ],
    "acceptance": "A new tests/install.rs case drives json_splice::append_element's populated-array arm end to end via install::run; cargo test is green.",
    "notes": "Drained from .flume/refactor/session-install-append-to-existing-untested.md (observed a32a45f); reverified at HEAD cb18a56 — no commits touched install.rs, json_splice.rs, or tests/install.rs since, so the gap is unchanged. Capture file deleted this tick per its own draining protocol. Coverage is a check (CLAUDE.md), not a gate; this entry's own bar is the cargo test gate, not a coverage threshold."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper`, single package, `(sdk-package-layout)`, still at 0.0.5 pending publish — SDK-VERSION-LOCKSTEP); channel 1's SDK foundation shipped. Re-verified at dd7517a (ship audit): still no `.github/workflows/release.yml` (only `temper.yml`, a check job); root package.json is still the private flume manifest (`temper-flume-harness`)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm `@dtmd/temper`; ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs range-correctness shipped in SCAFFOLD-OUTPUT-VALID (^0.0.4); release owns only future re-pins."
  }
]
