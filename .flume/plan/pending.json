[
  {
    "tag": "SESSION-START-GATE",
    "summary": "add the advisory session-start gate — a one-shot check over a harness path emitting the claude-session-start reporter payload",
    "per": {
      "path": "specs/50-distribution.md",
      "section": "Decision: the session-start gate is advisory, not blocking"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "RE-ADD"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/reporter.rs",
          "description": "The `claude-session-start` reporter: render the merged diagnostic set to the SessionStart JSON payload — a failing contract emits `additionalContext` carrying the verdict plus a notify-and-approve instruction; a clean harness emits a quiet payload. Capped to Claude Code's 10k additionalContext limit."
        },
        {
          "path": "tests/session_start.rs",
          "description": "Acceptance over the reporter + the one-shot verb: a failing contract yields a payload with the verdict + notify-and-approve instruction; a clean harness yields a quiet payload; output is valid JSON under the 10k cap; the run exits 0 (advisory, never blocks)."
        }
      ],
      "edit": [
        {
          "path": "src/lib.rs",
          "description": "Add `pub mod reporter;` (the foundation line for the new module — this is the only entry that touches lib.rs)."
        },
        {
          "path": "src/main.rs",
          "description": "Add a session-start check verb that takes a harness path, imports it internally (one-shot, not the two-step author check), runs the same by-kind gate `check` runs, and emits the reporter payload on stdout; advisory — exit 0 regardless. The hook IS the binary (exec-form), so no shell wrapper."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/session_start.rs",
        "asserts": "The emitted SessionStart payload is valid JSON ≤10k, carries the verdict + notify-and-approve instruction on a failing harness and stays quiet on a clean one, and the process exits 0 (advisory)."
      }
    ],
    "acceptance": "`temper` produces the SessionStart hook payload for a harness path via the claude-session-start reporter; the gate is advisory (never blocks).",
    "notes": "50-distribution.md: the hook is the temper binary itself, one-shot over a harness path (import-internally). Fail-loud (a hook that cannot run temper) is a separate concern. SARIF + GitHub-annotation reporters are sibling follow-ons on the same reporter family. Serialized behind RE-ADD only because both touch main.rs."
  }
]
