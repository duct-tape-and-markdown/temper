[
  {
    "tag": "RULE-IMPORT",
    "summary": "Teach `import` to scan `<harness>/.claude/rules/*.md` and write the rule surface (header + byte-faithful body + `[[rule]]` roll-up); `Workspace` gains `rules` and reconstructs them on load.",
    "per": {
      "path": "specs/20-surface.md",
      "section": "Artifact kinds & contract selection"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "Extend discovery + write to the rule kind. Scan `<harness>/.claude/rules/*.md` (today `discover_skill_dirs` scans only `<harness>/skills/`, import.rs:97), and for each rule write the surface mirroring skills (`run` import.rs:78, `import_skill` import.rs:131): a format-preserving header (optional `paths` + `[provenance]`, via `Rule::to_meta_document`) and the byte-faithful body under a `rules/<name>/` tree, plus a `[[rule]]` row in `author.toml` beside `[[skill]]` (`write_rollup` import.rs:170). Keep the keystone idempotence invariant (import.rs:12)."
        },
        {
          "path": "src/check.rs",
          "description": "`Workspace` gains `rules: Vec<Rule>` (struct check.rs:34) and `Workspace::load` reconstructs them from `<workspace>/rules/*/` via `Rule::from_surface_dir`, name-sorted, mirroring the skill load (check.rs:47-74). Update the `Workspace { skills: Vec::new() }` test literal (check.rs:334) for the new field."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/import.rs",
        "asserts": "a harness with `.claude/rules/*.md` writes a rule surface + a `[[rule]]` roll-up row; re-import is byte-identical (idempotent); skills and rules coexist in one import"
      },
      {
        "path": "src/check.rs",
        "asserts": "`Workspace::load` reconstructs rules (name-sorted) from the written surface alongside skills"
      }
    ],
    "acceptance": "`temper import` over a harness carrying rules produces a round-trippable rule surface + roll-up, and `Workspace::load` reads it back; `cargo test`/`clippy -D warnings` green.",
    "notes": "Rules scan at `<harness>/.claude/rules/*.md` per the spec literal — note the root asymmetry with skills (`<harness>/skills/`); self-host (RULE-CHECK) therefore points `import` at the repo root, where `.claude/rules/` lives. Surface topology (the `rules/<name>/` tree, body filename) is code's faithful generalization of the skill layout under law 5."
  },
  {
    "tag": "RULE-CHECK",
    "summary": "Dispatch `check` by artifact kind — embed `contracts/rule.toml`, validate `ws.rules` against it, merge with skill diagnostics; prove self-host on `temper`'s own `.claude/rules/`.",
    "per": {
      "path": "specs/20-surface.md",
      "section": "Decision: contract selection is by artifact kind"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "RULE-IMPORT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "Dispatch `check` by kind. Embed the rule contract beside the skill one: `const BUILTIN_RULE_CONTRACT: &str = include_str!(\"../contracts/rule.toml\");` (next to `BUILTIN_SKILL_CONTRACT` main.rs:30). In the `Check` arm (main.rs:77-82), project `ws.rules` via `extract::rule_features`, validate them against the rule contract parsed by `Contract::parse`, and merge the rule diagnostics with the skill diagnostics before `check::render` / the `any_error` + `--deny-advisories` gate (main.rs:84-94) — both contracts run, each artifact judged by the contract for its kind."
        },
        {
          "path": "tests/cli.rs",
          "description": "Add a rule-kind case: a harness whose `.claude/rules/` holds a clean rule (e.g. `paths:`-only) checks clean, and one holding a forbidden Cursor key (`description`/`globs`/`alwaysApply`) exits non-zero. Add the self-host case: `temper import <repo-root>` (off `CARGO_MANIFEST_DIR`) then `temper check` over `temper`'s OWN `.claude/rules/*.md` (rust.md carries `paths:`, collaboration.md none) exits zero — the bootstrap proof."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.rs",
        "asserts": "import->check over a harness with rules exits zero on a clean rule, non-zero on a forbidden-key rule; self-host: import the repo root then check exits zero over temper's own `.claude/rules/`"
      }
    ],
    "acceptance": "`temper check` validates skills against the skill contract and rules against the rule contract in one run; `temper check` runs green on temper's own `.claude/` (`specs/00-intent.md` self-hosting proof); `cargo test`/`clippy -D warnings` green.",
    "notes": "Closes the inbox NEW SLICE. The merged-diagnostics gate keeps `--deny-advisories`/`any_error` semantics unchanged. Self-host is the first real bite of `00-intent.md`'s bootstrap finish line; the SessionStart gate that refuses to start on a failing harness is later work."
  }
]
