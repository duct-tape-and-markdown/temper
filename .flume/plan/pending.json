[
  {
    "tag": "HELP-TEXT-RECUT",
    "summary": "guard --help still speaks shared/surface (runtime is note/warn/block) and verbs' help cite the deleted specs/architecture/* paths — user-facing strings, outside CITE-RETAG's comment scope",
    "per": {
      "path": "specs/distribution.md",
      "section": "The placements and their enforcement modes"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "clap help/about strings: `guard --help` still speaks shared/surface, but the runtime migrated to the block/warn/note enforcement vocabulary — recut the help to it. Most verbs' help cite the removed specs/architecture/* layout; retag to the recut specs/** layout. These are user-facing strings, distinct from CITE-RETAG's comment scope."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.rs",
        "asserts": "no help/about string speaks shared/surface or cites a specs/architecture/* path; guard --help names block/warn/note"
      }
    ],
    "acceptance": "No user-facing help string speaks shared/surface or cites a specs/architecture/* path; guard --help names block/warn/note.",
    "notes": "Field report 1. Explicitly outside CITE-RETAG (comment/citation scope, already shipped): these are user-facing strings. Runtime already migrated shared/surface -> note/warn/block."
  },
  {
    "tag": "BUNDLE-SKILL-INSTALL-VERB",
    "summary": "the bundled operate-the-gate skill + plugin description teach the retired `import` verb; the CLI has no Import subcommand — channel 3's skill must name `install`, the single on-ramp",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/bundle.rs",
          "description": "Three shipped strings teach the retired `import` verb: PLUGIN_DESCRIPTION (:52 'import the harness into a typed surface'), the OPERATE_SKILL frontmatter `description` (:72 'importing a harness into the typed surface'), and the OPERATE_SKILL body 'Run the gate' bullet (:83 `temper import <harness-path> --into .temper` — 'scan the harness ... re-importing an unchanged harness is a no-op'). The CLI has no Import subcommand (Install/Check/Emit/Schema/Guard/Bundle/Explain); `install` is the single on-ramp. Recut all three to `temper install`, and rewrite the on-ramp bullet to describe install's behavior (discovery report, one question, lift + first emit + placement; re-running converges) rather than import's scan-into-a-surface framing. Channel 3: the skill teaches when to install / emit / check."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/bundle.rs",
        "asserts": "no shipped bundle string (plugin description, operate-the-gate skill body/frontmatter) speaks `temper import`; the skill's on-ramp bullet names `temper install`"
      }
    ],
    "acceptance": "No shipped bundle string (plugin description or operate-the-gate skill) names the retired `import` verb; the skill's on-ramp bullet names `temper install` and describes install's actual behavior.",
    "notes": "Residue sweep 8aeb64e..HEAD. `import` verb retired (CLI: Install/Check/Emit/Schema/Guard/Bundle/Explain, no Import); bundle.rs shipped skill+description still teach it. Distinct from EXPLAIN-RESOLVER (read.rs not-found suggestion) and HELP-TEXT-RECUT (main.rs clap help). Not PACKAGING-CHANNELS (parked release infra) — a live shipped-content bug, as SCAFFOLD was carved from PACKAGING. Disjoint over src/bundle.rs + tests/bundle.rs."
  },
  {
    "tag": "COVERAGE-KIND-AWARE",
    "summary": "coverage_note false-flags a surface a locked custom kind governs as unmodeled, and says 'built-in kinds' while counting custom members — it consults only built-ins, not the lock's kinds",
    "per": {
      "path": "specs/model/representation.md",
      "section": "kind"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/coverage_note.rs",
          "description": "check(root, kinds, member_counts) receives only builtin_kind::definitions() as `kinds`, so the unmodeled-surface suppression over KNOWN_SURFACES never sees a custom kind's `governs` — `.claude/commands`, governed by a locked `command` kind, is still flagged coverage.unmodeled-surface. Read the lock's kind rows (drift::read_declarations(root/.temper).kinds -> CustomKind) so a known surface a locked kind governs is suppressed; drop 'built-in' from the CHECKED_RULE count message since member_counts already includes custom-kind members. Read the lock internally so main.rs's call site is untouched (stays disjoint from the main.rs chain)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/coverage_note.rs",
        "asserts": "a locked custom kind governing .claude/commands suppresses the unmodeled-surface finding; the checked-count message carries no 'built-in' qualifier when a custom kind is counted"
      }
    ],
    "acceptance": "A surface a locked custom kind governs is not flagged coverage.unmodeled-surface; the checked-count message does not say 'built-in' when custom-kind members are counted.",
    "notes": "Field report 3 T9. The gate already runs custom-kind floors and counts them (member_counts), and install places their notes (emit_owned_targets is kind-generic) — those T9 sub-claims shipped with REQUIREMENT-GATE; the sole residue is coverage_note's built-in-only kind set. Scoped to coverage_note.rs (reads the lock internally) to stay disjoint."
  },
  {
    "tag": "DISCOVERY-SKIPS-SURFACE",
    "summary": "kind discovery ranges into the `.temper/` surface workspace — a CLAUDE.md there is counted as a second `memory` member ('memory (2)'); discovery must skip the surface unconditionally",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "Install"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "discoverable_paths walks the harness root with WalkBuilder excluding only `.git`; `.temper/` is committed (not gitignored), so a `**/CLAUDE.md` in the surface workspace enters the discoverable set and the `memory` kind counts it as a second member. Skip the surface workspace (TEMPER_DIR) unconditionally in the discovery walk — it holds temper's authored modules + lock, never harness members."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "a CLAUDE.md under .temper/ is not discovered as a memory member; harness-root and .claude/ members still discovered"
      }
    ],
    "acceptance": "A file under the `.temper/` surface workspace is never discovered as a harness member; discovery of harness-root and `.claude/` members is unchanged.",
    "notes": "Field report 3 T12. Renaming the sidecar was the field workaround; the fix is scoping discovery. .temper/ is the surface (authored modules + lock), categorically not a harness member — the surface/harness split runs throughout pipeline.md. Disjoint (import.rs untouched by other entries)."
  },
  {
    "tag": "SCAFFOLD-OUTPUT-VALID",
    "summary": "scaffolded skill modules omit required Skill.description (fails tsc) and install pins SDK ^0.0.2 — caret pins the patch to 0.0.2, which predates the `file` export the scaffold imports",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "Install"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "Scaffold template: carry the skill's `description` into scaffolded skill modules — Skill.description is required in the SDK types, so omitting it fails tsc (the advertised keystroke wall) even though the runtime erases types. Bump SDK_VERSION_RANGE off `^0.0.2` (:56) to the seam the binary speaks: caret on 0.0.x pins the PATCH (>=0.0.2 <0.0.3), so fresh installs get 0.0.2, which predates the `file` export the scaffold imports; 0.0.4 (seam v2, has `file`) is published. Scaffold output and version range are one contract."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "scaffolded skill modules include description; the scaffolded module + version range import a seam that exports `file`"
      }
    ],
    "acceptance": "Scaffolded modules typecheck (skill modules carry description) and import an SDK seam that exports `file`; install pins a range that resolves to it.",
    "notes": "Field reports T6 + finding 3. WIN-INSTALL-SPAWN (same install.rs) shipped da6b127 — now open, head of the install.rs chain (SCAFFOLD->PATH-SEP-NORMALIZE->GUARD-OWNPATH). Supersedes PACKAGING's 'release-owned bump' framing: the range is a live install-correctness bug fixed here; release owns only future re-pins."
  },
  {
    "tag": "EXPLAIN-RESOLVER",
    "summary": "explain prints not-found (retired `import` verb) then narrates the member anyway, and misreports a locked requirement's fill status — the resolver checks one namespace before it finds the member",
    "per": {
      "path": "specs/model/contract.md",
      "section": "Read verbs"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/read.rs",
          "description": "The member resolver prints the not-found error (suggesting the retired `import` verb) then narrates the member anyway — it checks one namespace before the one the resolver eventually finds it in; resolve before the not-found check and drop the retired-verb suggestion. explain <requirement> reports 'required, and unfilled — which check reports as an error' even with a satisfier locked and check reporting nothing; read the lock's requirement/satisfies declarations so explain narrates fill status over the same resolved edges the gate uses."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "explain <member> resolves without a spurious not-found and never suggests the retired `import` verb; explain <requirement> reports fill status agreeing with the gate"
      }
    ],
    "acceptance": "explain <member> resolves without a spurious not-found; explain <requirement> reports fill status agreeing with the gate; no output suggests the retired `import` verb.",
    "notes": "Field report 2 T2 + report 1 finding 7. REQUIREMENT-GATE shipped f0e1e74 (harness_diagnostics now reads the lock's requirement/satisfies rows) — now open; explain projects requirement fill over the same resolved edges the gate uses."
  },
  {
    "tag": "PATH-SEP-NORMALIZE",
    "summary": "on Windows scaffolded modules and lock rows embed backslashes (file(\"../.claude/rules\\\\x.md\"), mixed-sep source_path) — .temper/ is committed and cross-platform; normalize both writes to /",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "The lock"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "SCAFFOLD-OUTPUT-VALID"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "The scaffold writer emits host-separator paths in `file(...)` directives (`file(\"../.claude/rules\\\\cls.md\")` on Windows); emit `/`-separated paths regardless of host separator."
        },
        {
          "path": "src/document.rs",
          "description": "Lock provenance rows record member source paths with the host separator (mixed-sep source_path on Windows); normalize recorded source paths to `/` so the committed .temper/ artifacts are byte-stable across platforms. (If the source-path row is written elsewhere in the emit path, normalize at that writer.)"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "scaffolded file(...) paths and recorded source paths use `/` on every host"
      }
    ],
    "acceptance": "Scaffolded module paths and lock source_path rows use `/` on every host; committed .temper/ round-trips byte-identically across platforms.",
    "notes": "Field report 1. .temper/ is committed and must be cross-platform; a `\\`-bearing lock row or scaffold path forks by host. blockedBy SCAFFOLD-OUTPUT-VALID (same install.rs file)."
  },
  {
    "tag": "GUARD-OWNPATH",
    "summary": "the PreToolUse guard regex-matches any .claude/ file_path without reading the lock; for file()-carried members the .claude/ file IS the authored source, so block denies editing the source of truth",
    "per": {
      "path": "specs/distribution.md",
      "section": "The placements and their enforcement modes"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "PATH-SEP-NORMALIZE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "guard() matches GUARD_PATH_MATCH (:104) against any `.claude/` file_path without consulting the lock. For a file()-carried member the `.claude/` file IS its authored source (install already skips placements on such members; emit adopts their direct edits), so under `block` the guard denies editing the source of truth. Read the lock's owned-source paths so a write to an own_path target passes through, while writes to projected `.claude/` files stay bound."
        },
        {
          "path": "src/kind.rs",
          "description": "Read `owns_source` (already present) to identify a member's own authored path when the guard consults the lock — read-only from the guard's side."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "guard passes a write to a file()-carried member's own .claude/ source (own_path) and still binds writes to projected .claude/ files"
      }
    ],
    "acceptance": "The guard passes a write to a file()-carried member's own .claude/ source (own_path) and continues to bind writes to projected .claude/ files, reading the lock rather than a path regex alone.",
    "notes": "Field report 1. For file()-carried members the .claude/ file is the source of truth, not a projection; the guard must read the lock's owned-source paths. blockedBy PATH-SEP-NORMALIZE (same install.rs file)."
  },
  {
    "tag": "MENTION-EDGE-LANDS",
    "summary": "the mention (`n`) edge is resolution-checked at emit but never compiled into the payload/lock/graph — it reaches no shared edge enumeration, so no degree clause or read verb ranges over it",
    "per": {
      "path": "specs/model/contract.md",
      "section": "edge"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "HELP-TEXT-RECUT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/declarations.ts",
          "description": "Compile the mention (`n`) family into the emit payload's declaration rows, paralleling the `satisfies` family already carried on member/unit rows — an `n`'s (source member, target address) becomes a declared edge, not merely an emit-time resolution check."
        },
        {
          "path": "sdk/src/emit.ts",
          "description": "The `n` targets already resolution-checked here (the `nable` set) also surface into the payload's declaration rows so the edge survives type erasure, not just the loud dangling-mention refusal."
        },
        {
          "path": "src/drift.rs",
          "description": "Add the mention edge's declaration-row family to the lock (read + write), paralleling the `satisfies` rows — emit is the sole producer."
        },
        {
          "path": "src/main.rs",
          "description": "edges_from_declarations includes the mention rows so assembly_edges carries mentions into the one resolved edge enumeration the gate and read verbs share."
        },
        {
          "path": "src/graph.rs",
          "description": "Bind mention edges in the reference graph so `degree` (obligation-free by default — a contract may count them) and explain range over them; explain build's 'it points at no member' resolves once the edge is bound."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/lock_declaration_rows.rs",
        "asserts": "a `n` authored on a member's prose leaf compiles to a mention declaration row, rides the lock, and binds in the graph so a degree clause over it evaluates; explain narrates the resolved target"
      }
    ],
    "acceptance": "A mention (`n`) authored in prose lands as a declared edge in the payload, the lock, and the graph; a degree clause can count it and explain narrates its resolved target rather than 'points at no member'.",
    "notes": "Field report 3 T10. contract.md: a mention is one of four edge loci; every edge resolves into the one enumeration the gate and read verbs share. Obligation-free by default (no shipped clause counts it; a contract may). Model on the satisfies family, which already threads SDK->payload->lock->graph. blockedBy HELP-TEXT-RECUT (shared main.rs)."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper` 0.0.4, single package, `(sdk-package-layout)`); channel 1's SDK foundation shipped. No `.github/workflows/release.yml` exists (only the `temper.yml` check job); root package.json is still the private flume manifest."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra, mostly outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm (`@dtmd/temper`, published 0.0.4); ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs `^0.0.2` range-correctness now owned by SCAFFOLD-OUTPUT-VALID (a live install bug); release owns only future re-pins."
  }
]
