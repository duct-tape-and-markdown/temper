[
  {
    "tag": "LAYOUT-COLLECTION-MEMBER-OWN-SPAN-LEAF",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [
      "layout-own-span-leaf"
    ],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/layout.rs",
          "description": "read_collection_member (222) takes a LayoutMember's leaves from its immediate sub-headings only, keyed by slug; the paragraph directly under the member's own heading lands nowhere. Land it under whichever reserved leaf key `(layout-own-span-leaf)` rules — composed leaves are author-named (NestedMemberRow.leaves keyed by field name, no reserved own-span key exists there; the SDK's `FRAMEWORK_KEYS`/kind.ts:245 `prose` is a member-level value, not a leaf-name reservation). Hazards the fork must rule on: node.body includes sub-heading text (the own span must cut at the first child heading), and a sub-heading slugged to the reserved name would collide with it."
        },
        {
          "path": "tests/layout_kind.rs",
          "description": "Regression: a layout collection member whose body is `### Title` + a paragraph + `**Test.** ...` (no sub-headings) materializes a non-empty leaves map carrying the paragraph under the ruled key."
        },
        {
          "path": "sdk/src/kind.ts",
          "description": "FRAMEWORK_KEYS (~245) reserves `prose` as a member-level value only; once `(layout-own-span-leaf)` rules a reserved own-span leaf key, an author's sub-heading slugging to that same name needs a collision guard (or explicit precedence rule) — the exact key and whether it joins FRAMEWORK_KEYS stays whatever the fork rules, not invented here."
        }
      ],
      "retire": []
    },
    "summary": "A layout collection member's own paragraph span lands nowhere — not a leaf, not the lock, not explain — while its sub-headings' spans do.",
    "per": {
      "path": "specs/intent.md",
      "section": "Invariants"
    },
    "tests": [
      {
        "path": "tests/layout_kind.rs",
        "asserts": "a layout collection member's own span (the text directly under its heading) lands in its leaves, under the ruled reserved key"
      }
    ],
    "acceptance": "A layout collection member's own paragraph (not just its sub-headings) reaches a leaf, addressable and visible to leaf predicates, leaf addresses, and explain, matching the composed half's full-leaves behavior. Per 0048's conservation bar, the regression pins non-vacuity: it asserts the captured own-span text is non-empty before checking it resolves as a leaf.",
    "notes": "observed at ad10e8cb (F11, GH #43 leaves gap); corrected at bf4b4168: 'the composed half already names it prose' is false — no reserved own-span leaf name exists in the corpus today. Reserving one is a Decision, tracked by fork `layout-own-span-leaf`, which f07d4f84's `layout_prose` family has since given a third candidate; this entry waits on the ruling either way. Serialized behind KINDS-IN-PLAY and LAYOUT-UNDECLARED-FINDING for tests/layout_kind.rs. scoped at bf2e3eba"
  },
  {
    "tag": "EXPLAIN-KIND-REGISTRATION-LOCUS-AND-ADDRESS-FORM",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "MEMBER-ADDRESS-GRAMMAR-ONE-HOME",
        "EXPLAIN-CLI-RUNNER-ONE-HOME"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/read.rs",
          "description": "`narrate_kind` (:534, binding the `KindFactRow` at :544 and holding the whole `kind_facts` slice at :537) renders three strands — guidance and cite, layout regions plus skeleton, hosted kinds — and nothing else (:534-582), so a kind whose row declares only guidance prints one line and stops. Three more strands, all renderable off data already in hand, no signature change: (i) the **locus** — and it is NOT either/or: `hook` carries `governs_root = \".claude\"`, `governs_glob = \"settings.json\"` AND `collection_address = { manifest, key_path }`, so render the collection address (in src/drift.rs: the `KindFactRow` column at :3440, its `manifest`/`key_path` fields at :3479/:3482) as where the MEMBER keys and `governs_root`/`governs_glob` (:3390/:3394) as where the host document is discovered, plus the `registration` channel labels (:3417 — the wire spelling is the call form the SDK writes, `event(event)` for `hook`, per sdk/src/declarations.ts:219-234 and .temper/lock.toml:72) and the `commitment` class (:3405), which is part of where a member lands (a local kind's members are uncommitted); a file-locus kind shows the governs line alone. (ii) the **hosting kinds** inverse — which kinds host members of this one — restored, not dropped: it is the prerequisite for (iii), because the row cannot say whether a member is top-level or embedded (`orientation` is embedded yet its row carries `unit_shape = \"file\"` and no `governs`), and the oracle is a scan over the other `kind_facts` rows' path-less `templates` entries and `content.regions[].member_kind` for this kind's name. (iii) the **address form** — `<kind>:<name>` for a top-level kind, `<host-address>/<kind>/<key>` for one only ever hosted, EMBEDDED VALUE OR FILE CHILD ALIKE: a nested-file child is a `[[declaration.nested_member]]` row carrying `host = \"skill:test-skill\"` (tests/coverage_note.rs:391-396), so `supporting-doc` takes the host-qualified form exactly as an embedded value does. Which means strand (ii)'s oracle must scan path-BEARING `templates` entries too, not only path-less ones, or a file-child kind's hosts go unnamed and (iii) has nothing to read — read from the single address-grammar home MEMBER-ADDRESS-GRAMMAR-ONE-HOME establishes (`src/member_address.rs`), never a second hand-spelling. Two mechanics: the early return at :554-567 (\"nothing to teach\") fires when guidance, cite, layout, embedded and file-children are all absent, so it must gain the locus and hosting terms or the new strands never print; and the address form must ride that same guard rather than rendering unconditionally off the kind name, or `explain_narrates_a_kind_absent_guidance_cleanly` (tests/read_verbs.rs:1065, which passes `&[]` rows) goes red. Reword that line from \"nothing to teach before a member of it exists\" to naming the strands. All of this must render with ZERO members of the kind in the corpus — the adopter's actual moment, and the reason this is not `corpus_leaves` (:817), which can only report what existing members carry."
        },
        {
          "path": "tests/read_verbs.rs",
          "description": "Regressions on a MEMBER-LESS surface, per tests[] — the non-vacuity bar is that the fixture carries zero members of the explained kind, so the lines cannot be coming from a corpus read. `explain_kind` (:819) is the helper both new tests use; it synthesizes contracts from the rows, so a kind is only resolvable if it has a row. Keep the existing arm at :1065 where a kind with no row at all still prints its clean \"nothing declared\" line rather than falling silent. It is NOT byte-for-byte: it asserts `contains(\"No authoring guidance is declared\")` (:1101) and the absence of a `‣` marker (:1105), so the reword must keep that opening clause and may change only the tail after the em dash — and the address form must ride the same guard, or the arm goes red on the marker-free line growing an address it has no row to source. No snapshot moves: nothing under tests/snapshots/ asserts explain-kind output, and the CLI-driven `explain_in` calls in tests/layout_kind.rs and tests/lock_declaration_rows.rs all target members, not kinds."
        },
        {
          "path": "tests/common/mod.rs",
          "description": "VERIFY-ONLY — expect no diff. The `kind_facts(name, governs_root, governs_glob)` builder (:614-631) always sets BOTH governs fields, so every existing bare-kind test that builds a row gains a locus line: tests/read_verbs.rs:763, :915, :970, :1015. All four assert with `contains`, so they stay green while their output grows. In the fence so the builder can gain a no-locus variant if a new arm needs one, not because an edit is expected."
        }
      ],
      "retire": []
    },
    "summary": "`explain kind:<x>` never says where a member of the kind lands or how a reference to one is spelled — the two lines an adopter with no member yet most needs.",
    "per": {
      "path": "specs/model/contract.md",
      "section": "Read verbs"
    },
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "`explain kind:hook` on a surface with zero hook members names its `hooks.<Event>` collection address, the `.claude/settings.json` document its members are discovered in, its `event(event)` registration channel label, and the address form a reference to one of its members takes"
      },
      {
        "path": "tests/read_verbs.rs",
        "asserts": "`explain kind:rule` on a surface with zero rule members names its file locus and its `kind:name` address form; a kind hosted only by another names the `<host-address>/<kind>/<key>` form and the kinds that host it; and a kind with no row at all still prints the \"nothing declared\" line unchanged"
      }
    ],
    "acceptance": "On a surface carrying no member of the kind, `explain kind:<name>` prints where a member of it lands and how a reference to one is spelled, alongside the guidance, layout and hosted-kind strands 3dcc73f1 already renders — the read verb narrating a kind, not only a kind that already has members. `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Routed from inbox 09-07 (cascade on GH #47, observed at ab61fea4). Their fourth line — the leaves an embedded kind's members carry before any member exists — is NOT here: a kind declares no field set anywhere, so it is the fork `(kind-declared-leaf-schema)`. Amended twice 09-07: the locus is not either/or, commitment joins it, the hosting-kinds inverse is the address form's oracle. HOOK-COLLISION shipped (76e29dc1); the live chain is MEMBER-ADDRESS then EXPLAIN-CLI-RUNNER. scoped at ea3c0484"
  },
  {
    "tag": "KIND-DECLARED-CITABLE-HANDLE",
    "gate": {
      "kind": "deferred",
      "reason": "An idea, not a defect (the raw note's own framing) — a `handle` fact on a target kind has no named consumer today, and the containment-selection family (open question) is the higher-priority extension to this same render-branching surface. Revive when a render hook wants a spelling the closed target-fact set (name, address, kind, projection path, repo-rooted path) can't already express cleanly."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "Anticipated: a `handle` fact on KindFacts, read by a render hook branching on target.kind, alongside the existing closed engine-derived target-fact set."
        }
      ],
      "retire": []
    },
    "summary": "A kind-declared citable handle ('how a reference to a member of this kind reads') — a lower-priority idea, not a defect, with no consumer yet.",
    "per": {
      "path": "specs/model/representation.md",
      "section": "kind"
    },
    "tests": [],
    "acceptance": "A kind declaring `handle` lets a reference render hook spell a member of that kind without hand-rolling the branch on target.kind — provable once a real render hook wants one.",
    "notes": "observed at ad10e8cb (cascade, idea). Compatible with the closed-set rule as a fifth engine-derived fact read off the target kind's declaration; the raw note itself calls this lower priority and not a defect. scoped at e1b26728"
  },
  {
    "tag": "TAP-ISO8601-DATETIME-CRATE",
    "gate": {
      "kind": "parked",
      "reason": "Adopting a new dependency is the human sanctioned-set call CLAUDE.md's Tech stack section reserves to itself, not plan- or build-derivable; needs either a `time` crate addition to the sanctioned set or a ruled pinned-semantics exception at the site."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/tap.rs",
          "description": "iso8601_utc_timestamp (313-368) and is_leap_year (370-372) hand-roll epoch-day Gregorian calendar arithmetic off SystemTime/UNIX_EPOCH to format an RFC 3339 UTC timestamp; replace with a sanctioned date/time crate (`time`, a narrower fit than `chrono` for one stamp) once added to CLAUDE.md's Tech stack, or mark the site a deliberate pinned-semantics exception (rust.md's comment taxonomy) if the human declines the dependency."
        }
      ],
      "retire": []
    },
    "summary": "src/tap.rs hand-rolls ISO-8601 calendar math for a timestamp; no sanctioned crate does date/time, so adopting one needs a human sanctioned-set call.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Libraries before hand-rolls"
    },
    "tests": [],
    "acceptance": "src/tap.rs's timestamp formatting runs through a sanctioned date/time crate instead of hand-rolled calendar math, or the site carries a ruled comment stating why it stays a hand-roll.",
    "notes": "drained from .flume/refactor/plan-tap-iso8601-hand-roll.md (filed at cd35a551); re-verified live at HEAD — no crate in the sanctioned set (CLAUDE.md, Tech stack) does date/time and this remains the sole iso8601 site (rg -n \"iso8601|SystemTime::now\" src/ tests/). scoped at e1b26728"
  },
  {
    "tag": "HOOK-COLLECTION-ADDRESS-DUPLICATE-REFUSAL",
    "gate": {
      "kind": "parked",
      "reason": "Human re-ruling 09-03 (interactive): the first ruling — refuse a duplicate member address within a kind — was built (e4ff7bc6) and failed temper's own self-host test at afterMerge: Claude Code allows N matcher groups per event and this repo's own harness carries two PostToolUse groups, so a duplicate-event refusal rejects every real harness. The defect (silent last-writer-wins on edge resolution to `hook:<Event>`) stands; the remedy is a hook identity discriminator, which is a model fork, not build's to invent."
    },
    "dependsOnForks": [
      "hook-member-identity"
    ],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/admissibility.rs",
          "description": "New sibling check beside `collection_address_collision_diagnostics` (which 21f61463 added for two *kinds* sharing one collectionAddress): a *single* kind's own members registering on the same key within that address (e.g. three `hook` members on `SessionStart`, which collapse to one entry in the `kind:name` composition map — silent last-writer-wins) is a declaration-time refusal with a named error, not a silent collapse."
        },
        {
          "path": "src/gate.rs",
          "description": "Wire the new diagnostic beside the existing `collection_address_collision_diagnostics` call (~gate.rs:200s)."
        },
        {
          "path": "tests/hook_kind.rs",
          "description": "Regression: three hook members declared on the same event refuse loud, naming all colliding members — mirroring 21f61463's kind-collision test shape."
        }
      ],
      "retire": []
    },
    "summary": "Refuse a duplicate member registration key within one kind's collection address (three hooks on SessionStart collapse to one) with a named error, not last-writer-wins.",
    "per": {
      "path": "specs/model/representation.md",
      "section": "Reach"
    },
    "tests": [
      {
        "path": "tests/hook_kind.rs",
        "asserts": "duplicate hook members on the same event name are refused loud, naming both"
      }
    ],
    "acceptance": "Two `hook` members declared on the same `event` refuse at declaration time with a named error rather than silently composing to whichever was last.",
    "notes": "GH #32. First pick built the duplicate-key refusal; gate-reverted by tests/cli.rs self-host test — temper's own settings.json has two PostToolUse groups. The revert itself isn't a resolvable commit (worktree-local, reverted pre-merge); see 080311b9 for the park record (its predecessor citation, e4ff7bc6, doesn't resolve at HEAD). Parked on `(hook-member-identity)`; on resolution, rewrite around the ruled discriminator (files[] will change). scoped at bf4b4168."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) is now proven live, not merely wired: the v0.0.11 tag fired it and its publish smoke gate went green (b6dc0a3's commit body)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "summary": "Packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing) and the plugin/bundle + marketplace.json channel (channel 3).",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json) — distribution.md's v0.1 launch gate: prebuilt binaries install on all three OSes.",
    "notes": "Park re-tested on disk at 01ba369: release.yml still states the darwin + channel-3 deferral verbatim (lines 7-9). The lockstep clause carried here since ac40c72/b676656 is resolved, dropped: da77c60 walked Cargo.toml 0.1.0→0.0.11 to match npm's cadence directly (no v0.1-tag flip), b6dc0a3 fixed the lockfile mismatch, and tag v0.0.11→da77c60 fired release.yml with a green smoke gate per its own commit body — channel 2 is proven live, not just wired. scoped 01ba369."
  },
  {
    "tag": "GAUNTLET-EXTERNAL-HARNESS-FIXTURES",
    "gate": {
      "kind": "parked",
      "reason": "Fixture choice is human territory (licensing): docs/ledger.md's 07-22 external-yield probe ran check over 9 real external harnesses and found the temper bugs the self-mirror can't (roots of GH #26/#30/#33/#38). Pick 2-3 of those 9 to vendor, license-check, and trim to their .claude/ trees before this unparks."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/fixtures/gauntlet_external/",
          "description": "Anticipated: the human-chosen 2-3 external harnesses' trimmed .claude/ trees, each vendored with its license noted (path/name unset until the human picks)."
        }
      ],
      "edit": [
        {
          "path": "tests/gauntlet.rs",
          "description": "Anticipated: one check+emit snapshot test per vendored external fixture, alongside the existing in-repo kitchen-sink corpus — the standing gate the 07-22 probe wasn't."
        }
      ],
      "retire": []
    },
    "summary": "Extend the gauntlet with 2-3 real external harness fixtures so the July external-yield probe (9 foreign repos, 4 temper bugs found) becomes a standing gate, not a one-off.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "The gauntlet corpus"
    },
    "tests": [
      {
        "path": "tests/gauntlet.rs",
        "asserts": "check/emit run over each vendored external fixture and snapshot-pin its findings"
      }
    ],
    "acceptance": "temper check/emit run clean (or with real, human-verified findings) over 2-3 vendored external harness .claude/ trees, snapshot-tested in tests/gauntlet.rs, so the July external-yield probe is a standing gate rather than a one-time manual run.",
    "notes": "observed at e77766c5, cross-cutting note — every defect in the 09-03 batch came from a foreign harness, none from the self-mirror; docs/ledger.md's 07-22 probe (9 real harnesses) is the candidate pool. Human picks 2-3 (licensing); test wiring is build's once chosen. scoped at 89d96cca."
  },
  {
    "tag": "GUIDANCE-FIELD-DECLARATION-CHANNEL",
    "gate": {
      "kind": "deferred",
      "reason": "0045's Consequences name field-declaration guidance beside kind-level, but the SDK's only per-field metadata today is a clause's `field:` target (predicate-bound) or `EdgeField` (edge-typed only) — no 'field declaration' list a bare advisory could attach to. None of the six promoted notes (GUIDANCE-BUILTIN-NOTES-PROMOTE) target a single field, inventing the mechanism now is speculative scope with no kind to exercise it ('An export earns its consumer', engineering.md). Revive when a field wants counsel no clause carries."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "Anticipated: a field-name-keyed guidance fact on `KindFacts`, analogous to `EdgeField`'s field-name-keyed list (196) but for arbitrary fields — shape unset; no field in any built-in kind wants one yet."
        },
        {
          "path": "src/drift.rs",
          "description": "Anticipated: a `KindFactRow` column (or sibling row) carrying per-field guidance/cite, following the `to_table`/`from_table` pattern GUIDANCE-KIND-SDK-LOCK-FACT establishes for the kind-level pair."
        },
        {
          "path": "src/schema.rs",
          "description": "Anticipated: `emit()`'s per-property `description` (156-172) folds field-declaration guidance in alongside a clause's, the same 'join into one description' discipline `push_description` (316-329) already runs for several guided clauses on one field."
        },
        {
          "path": "src/read.rs",
          "description": "Anticipated: field-level narration extends `narrate_governing_contract` (374-402) or a sibling to print a field's declared guidance."
        }
      ],
      "retire": []
    },
    "summary": "0045 also frees guidance onto a field declaration, no predicate — deferred: none of the six promoted built-in notes targets a single field, so there is no consumer yet",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "tests": [],
    "acceptance": "A field declaring `guidance` (no predicate) surfaces it via `schema` hover and `explain`, never gating — provable once a real kind needs one.",
    "notes": "0045 Consequences name field-declaration guidance beside kind-level; this waits for a driver, the same 'no consumer' bar prior deferred entries in this queue's history have held to. scoped at 946e303."
  },
  {
    "tag": "SCHEMA-KEYSTROKE-JSON-TOML-WIRING",
    "gate": {
      "kind": "deferred",
      "reason": "project_modeline (src/install.rs:1922) reads YAML frontmatter only (frontmatter::frontmatter_matter); a JSON document's native $schema key or a TOML equivalent needs its own design before schema/install extend to those kinds. Ruled (interactive, 2026-07-23) explicitly NOT build's to invent — surface only, never half-invent it. Revive when a JSON/TOML-kind keystroke gap has a real driver."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "Anticipated: `project_modeline`/`schema_ref` (~1594-1623) grow a JSON-document and/or TOML-document variant once a design exists — a JSON `$schema` top-level key insertion (JSON has no comment channel, so no `MODELINE_MARKER` prefix), a TOML equivalent is unprecedented and needs its own ruling."
        }
      ],
      "retire": []
    },
    "summary": "Keystroke JSON Schema wiring for JSON/TOML-document kinds (settings-local, plugin-manifest, marketplace, dial) has no modeline mechanism — deferred out of the YAML-frontmatter widening.",
    "per": {
      "path": "specs/distribution.md",
      "section": "The placements and their enforcement modes"
    },
    "tests": [],
    "acceptance": "A JSON- or TOML-document kind (settings-local, plugin-manifest, marketplace, dial, or a declared kind sharing one of those formats) gets keystroke schema validation wired by `install`, once a modeline-equivalent mechanism is designed for its format.",
    "notes": "Surfaced by inbox #13 (observed at fc77716) alongside SCHEMA-KIND-DOMAIN-WIDEN; deliberately split so the YAML-frontmatter widening isn't blocked on this open design question. scoped at 162e1cce."
  },
  {
    "tag": "MEMBER-ADDRESS-GRAMMAR-ONE-HOME",
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/member_address.rs",
          "description": "The one home for the member-address grammar `specs/model/representation.md` (\"member\") spells — `<kind>:<name>`, `<host-address>/<kind>/<key>`, and `/<leaf>` beneath it — with a writer and a parser per spelling and the leaf address parsed as a TAIL of the nested one rather than a second, differently-shaped parse. Distinct from `src/address.rs`, which is field addressing (a clause's `field` path), not member addressing; a new flat top-level module is what `specs/process/architecture.md` (\"The tree stays flat\") calls for, and engineering.md's \"One job, one home\" obliges the commit body to name `src/address.rs` as the near-duplicate considered and why it did not fit. Eight symbols move in: the two writers `extract::host_address` and `graph::nested_address`; the nested parser `graph::NestedAddress` + `parse_nested_address`; its two thin readers `graph::embedded_source_host` (:1802) and `graph::nested_key` (:1815) — both pure grammar, built on nothing but the parser, so they follow it or `parse_nested_address` goes `pub` for their sake alone; and the leaf pair `read::ParsedLeaf` + `parse_leaf_address`. `graph::embedded_hosts_by_key` (:1835) does NOT move — it indexes `Features`, which is corpus work, not grammar; it imports the reader instead. One design call the builder makes explicitly: `embedded_source_host` returns `graph::Node` (`pub type Node = (String, String)`, graph.rs:82), so either member_address.rs takes a `use crate::graph::Node` (a leaf module depending on graph for a tuple alias) or the reader returns `(&str, &str)` pairs and its two callers (graph.rs's `edge_host` :332, `embedded_hosts_by_key` :1841) do the `to_string`. Prefer the second — the grammar module owes graph nothing. It also ships the READER half the tree still lacks: a `<kind>:<name>` host-address parser. There is none today, so the grammar is hand-split at FIVE surviving duplicate sites, re-verified on disk at 67abe15f — graph.rs:1357, graph.rs:1651, read.rs:254, read.rs:2034, drift.rs:892 — plus graph.rs:1780, the host split inside `parse_nested_address` itself, which is the one home once the parser moves rather than a site to re-point. Each of the five is a one-to-three-line substitution in a file already in this fence. (The eight-site inventory this entry carried was cut to six by 9c5c7a71: gate.rs:627 went with the deleted `embedded_hosts_by_source`, and graph.rs:333 folded into `embedded_source_host`.)"
        }
      ],
      "edit": [
        {
          "path": "src/lib.rs",
          "description": "Declare `pub mod member_address;` in the alphabetical run (~63-64, between `layout` and `path`)."
        },
        {
          "path": "src/extract.rs",
          "description": "Move `host_address` (:742) and its unit test `host_address_is_kind_colon_id` (:1166) into the new module. Its doc comment already names the two other writers of the same `${kind}:${name}` form (drift::NestedMemberRow::host, sdk/src/declarations.ts's nestedMemberRows) — that comment moves with it."
        },
        {
          "path": "src/graph.rs",
          "description": "Move `NestedAddress` (:1738), `nested_address` (:1755), `parse_nested_address` (:1772), `embedded_source_host` (:1802) and `nested_key` (:1815) into the new module. `parse_nested_address` becomes `pub`, and so do `NestedAddress` and THREE of its five fields — `host` (:1739), `kind` (:1744), `key` (:1746) — which graph.rs still reads after the move at :1645/:1647 (`target_identity`) and :1705 (`member_lookup`); `host_kind`/`host_name` (:1741, :1743) are read only by `embedded_source_host`, which moves with them, so they stay private to the new module. Re-point the surviving callers — `target_identity` (:1644), `member_lookup` (:1704), `edge_host` (:332, which now calls `embedded_source_host`, not the parser), `embedded_hosts_by_key` (:1841) — and route this file's TWO surviving host-address hand-splits through the new reader: :1357 (`node_from_address`, a `kind:name` address with a REQUIREMENT_KIND default when no colon) and :1651 (`target_identity`'s bare `kind:name` fallback). :1780 is the split inside `parse_nested_address` and moves with it. Intra-doc links: :329 ([`embedded_source_host`]) and :1633 ([`parse_nested_address`]) stay in graph.rs and must gain the new module path; and the moving comments carry five links to items that STAY in graph.rs — [`target_identity`] and [`member_named`] (:1735), [`edge_host`] (:1736 and :1799), [`embedded_hosts_by_key`] (:1800) — which break under the crate-wide `-D broken_intra_doc_links` once the comments land elsewhere; the first three are private in graph.rs, so absolute paths will not save them — rewrite or drop. :1767's [`crate::read::parse_leaf_address`] re-points at the new module (both ends move). `src/graph.rs:25`'s `use crate::read::{parse_leaf_address, resolve_leaf};` splits: `parse_leaf_address` follows into the new module, `resolve_leaf` stays in read.rs (graph.rs:1405 calls both inside `edge_resolves`)."
        },
        {
          "path": "src/read.rs",
          "description": "Move `ParsedLeaf` (:1169) and `parse_leaf_address` (:1179) into the new module and re-cut the parse as the nested address's `/<leaf>` tail: the first segment is a `<kind>:<name>` host address, not the bare member id it reads today. `resolve_leaf` (:1201) stays in read.rs but MUST be edited, not merely re-imported — it compares `parsed.member` to `features.id` at :1207, and a top-level member's `features.id` is the bare id, so re-cutting the parser alone leaves `spec:20-surface/decision/surface-authority/chosen` resolving nowhere and this entry's acceptance unmet; the outer kind is already in hand as the loop key at :1205, so match `parsed.member` against BOTH `features.id` and the host-qualified `host_address(outer_kind, &features.id)`. Keep the bare short form resolving alongside the canonical one: the committed mention targets at tests/graph.rs:1939/:1973 and the SDK's own leaf writer spell it bare, so retiring it would re-spell committed locks against 0024. Bare-key ambiguity is already ruled and SHIPPED (9c5c7a71): `graph::member_lookup` answers `Membership::Ambiguous` when several hosts carry one key — do not restore first-match-wins, and do not re-decide it here. Three callers take the moved import: `impact_leaf` (:1238), `context_leaf` (:1418, resolving at :1426) and graph.rs:1405. Two host-address hand-splits in this file read the new module's parser as well: :254 (the `kind:name` species resolution) and :2034 (`prose_strand`, taking the name segment off a `LayoutProseRow.member`). This file also gained three `graph::nested_key` call sites at 9c5c7a71 — :245, :287, and the plain-`//` comment naming it at :243 — all of which follow the reader into the new module (import-path plus one comment word; no rustdoc gate reads a `//` line)."
        },
        {
          "path": "src/compose.rs",
          "description": "Import-path only: `extract::host_address` (:743, :1052), `graph::nested_address` (:1214), and the two intra-doc links naming it (:1199, :1212)."
        },
        {
          "path": "src/drift.rs",
          "description": "`use crate::extract::host_address` (:29) and its five call sites (:1292, :1909, :1976, :2170, :2413) take the new import path — and one real edit beside them: `nested_file_path` (:879) hand-splits its host address at :891-893, refusing with \"its host `{address}` is no `kind:name` address\". That is the writer's own inverse; it reads the new module's host parser, and the refusal wording stays."
        },
        {
          "path": "src/admissibility.rs",
          "description": "Import-path only: `graph::nested_address(host, kind, key)` at :113 — a writer call site 9c5c7a71 added after this entry was last scoped, caught by the ripple check at 67abe15f. Unfenced, it is one `cargo build` failure the moment the writer moves."
        },
        {
          "path": "src/builtin_kind.rs",
          "description": "Import-path only: the `extract::host_address` call site (:678)."
        },
        {
          "path": "src/gate.rs",
          "description": "Import-path only, and only that now: `extract::host_address(&kind.name, &unit.id)` at :73 follows the writer into the new module. The second reader this entry once predicted here is GONE — 9c5c7a71 deleted `embedded_hosts_by_source` and its `row.host.split_once(':')`, and the consumer at :524 now calls `graph::embedded_hosts_by_key`, which stays in graph.rs; re-verified at 67abe15f, no `split_once(':')` survives in this file. (`target_identity` at :619 is a plain-backtick prose mention, not an intra-doc link — no gate reads it, so it rides per the standing prose-cite rule.)"
        },
        {
          "path": "tests/graph.rs",
          "description": "The two embedded-leaf mention fixtures spell the leaf address bare — `note/requirement/my-req/chosen` (:1939) and `note/requirement/my-req/rejected` (:1973). Add the canonical host-qualified spelling beside them so both forms are pinned: the canonical one resolves, and a bogus key under it still fires `graph.route`."
        },
        {
          "path": "tests/read_verbs.rs",
          "description": "a_leaf_address_walks_impact_and_context_at_leaf_grain_and_discloses_coverage (:239) drives `explain` at the bare spelling `20-surface/decision/surface-authority/chosen` (:258). Extend it to the canonical `spec:20-surface/...` spelling, asserting the same leaf-grain narration."
        },
        {
          "path": "tests/nested_member.rs",
          "description": "an_address_with_a_leaf_tail_names_no_member (:903) asserts `service:beta/domain/common/purpose` names no MEMBER — that must stay true under the unified parser (the leaf tail is a different grain, `parse_nested_address`'s own doc comment rules it out at :1688-1694). Re-verify it, and where the file's `resolve` helper reaches it, pin the twin: the same address DOES name a leaf."
        }
      ],
      "retire": []
    },
    "summary": "One member-address grammar is written and parsed in three unrelated modules, and its nested and leaf parsers disagree on whether the first segment is `<kind>:<name>` or a bare id.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "explain at the canonical host-qualified leaf address `<kind>:<name>/<kind>/<key>/<leaf>` narrates at leaf grain, and the bare-member short form still does"
      },
      {
        "path": "tests/graph.rs",
        "asserts": "a mention targeting a host-qualified embedded-leaf address route-resolves at check, and a bogus key under the same spelling still fires graph.route"
      }
    ],
    "acceptance": "`rg 'fn host_address|fn nested_address|fn parse_nested_address|fn parse_leaf_address|fn embedded_source_host|fn nested_key' src/` returns hits in src/member_address.rs and nowhere else, and so does `rg \"split_once\\\\(':'\\\\)\" src/` — the five surviving hand-splits of the host grammar route through the reader this entry ships, and the sixth rides inside the parser that moved. Every caller reads the one module. A host-qualified leaf address resolves through the same parser that reads the nested address it is a tail of, and the bare short forms both parsers accept today keep resolving. `cargo test --no-fail-fast` and `cargo clippy --all-targets -- -D warnings` are green, and so is `cargo doc --no-deps --document-private-items` — the moved symbols carry intra-doc links from two files (graph.rs at :329, :1633, :1735, :1736, :1767, :1799, :1800; compose.rs at :1199 — compose.rs:1212 is a plain `//` comment no rustdoc gate reads, so it rides) and that gate is denied crate-wide. Known prose cite left stale on purpose: sdk/src/declarations.ts:529 points at `src/read.rs`'s parse_leaf_address; a TS comment no gate reads, so it rides the next entry that opens declarations.ts (SECTION-CONTAINS-LABEL-MARKER-COLLISION or KINDS-IN-PLAY-NAME-COLLISION-REFUSAL) per the standing prose-cite rule, rather than pulling a twice-contended file into this fence.",
    "notes": "Refactor capture build-member-address-inventory-delta drained here (observed 1d480a99): 9c5c7a71 closed 2 of the 8 hand-splits — gate.rs:627 went with the deleted `embedded_hosts_by_source`, graph.rs:333 folded into the new reader `embedded_source_host`. Five duplicates survive, all re-verified on disk. Every graph.rs and read.rs cite re-cut (+~40/+70); two moving readers and three new `nested_key` callers added; src/admissibility.rs:113 folded in from the ripple. scoped at 67abe15f"
  },
  {
    "tag": "EXPLAIN-CLI-RUNNER-ONE-HOME",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "MEMBER-ADDRESS-GRAMMAR-ONE-HOME"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/common/mod.rs",
          "description": "The one home: add `explain_in(root, target) -> String` beside `check_in`/`check_harness_in`/`run_guard`, capturing combined stdout+stderr — the module header already names absorbing per-suite copies as its purpose, and `#![allow(dead_code)]` at :10 covers the binaries that never call it."
        },
        {
          "path": "tests/layout_kind.rs",
          "description": "Retire the private `explain_in` at :631 (stdout+stderr variant); its one call site at :619 reads `common::explain_in`."
        },
        {
          "path": "tests/layout_edge_slot.rs",
          "description": "Retire the private `explain_in` at :76 (stdout+stderr variant); its one call site at :273 reads the shared one."
        },
        {
          "path": "tests/requirement_roster.rs",
          "description": "Retire the private `explain_in` at :704 — the stdout-ONLY variant. Its call sites (:757, :861) now see stderr too, so the `!out.contains(\"It fills no requirements\")` assertion at :763 can no longer read an empty narration as agreement."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "Retire the private `explain_in` at :2450 — the other stdout-only variant. Four call sites (:453, :1377, :1909, :2523); the negative assertions at :1920 and :2529 gain the same stderr coverage."
        },
        {
          "path": "tests/read_verbs.rs",
          "description": "Retire `explain_via_cli` at :1113 — the fifth spelling of the same runner under a different name (stdout+stderr); its callers read `common::explain_in`."
        }
      ],
      "retire": []
    },
    "summary": "One `temper explain` CLI runner is written five times across five test files, in two variants that disagree on whether a refusal's stderr is captured at all.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "tests": [
      {
        "path": "tests/requirement_roster.rs",
        "asserts": "the lock-declared-satisfies narration case still passes reading combined stdout+stderr — the positive `Requirements it satisfies` assertion unchanged, its paired negative now ranging over a stream a refusal would actually appear in"
      },
      {
        "path": "tests/lock_declaration_rows.rs",
        "asserts": "the nested-member and mention narration cases still pass on the shared runner, with `rendered-key` and `it points at no member` still absent from a now-combined stream"
      }
    ],
    "acceptance": "`rg -n 'arg\\(\"explain\"\\)' tests/` returns hits in tests/common/mod.rs and nowhere else — the four `explain_in` copies and `explain_via_cli` are gone and every caller reads the one runner. `cargo test --no-fail-fast` and `cargo clippy --all-targets -- -D warnings` green with no assertion rewritten: this is an extraction, and any call site needing its expectation changed to pass is the divergence being reported, not absorbed. Stated honestly, because it bounds the claim: no test is vacuous today — each of the three `!out.contains(...)` assertions over a stdout-only runner (requirement_roster.rs:763, lock_declaration_rows.rs:1920, :2529) is paired with a positive `contains` on the same string, which an empty stdout fails first. What this closes is the latent hazard those two variants carry, that a refusal renders to stderr and a stdout-only reader sees silence. If MEMBER-ADDRESS-GRAMMAR-ONE-HOME or EXPLAIN-KIND-REGISTRATION-LOCUS-AND-ADDRESS-FORM unifies the runner first as its own consolidation, this entry drains to whatever sites survive rather than being re-shipped.",
    "notes": "Swept 09-07 from the files-ripple on EXPLAIN-KIND-REGISTRATION-LOCUS-AND-ADDRESS-FORM: `explain_in` is not a shared helper but four private copies (layout_kind.rs:631, layout_edge_slot.rs:76, requirement_roster.rs:704, lock_declaration_rows.rs:2450), plus read_verbs.rs:1113's `explain_via_cli` — one job, five sites, two variants. Chained on the three entries sharing tests/lock_declaration_rows.rs, tests/layout_kind.rs and tests/read_verbs.rs. scoped at ea3c0484"
  },
  {
    "tag": "SDK-MEMBER-ADDRESS-GRAMMAR-ONE-HOME",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "MEMBER-ADDRESS-GRAMMAR-ONE-HOME"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "sdk/src/member-address.ts",
          "description": "The SDK's one home for the member-address grammar `specs/model/representation.md` (\"member\") spells — `<kind>:<name>`, `<host-address>/<kind>/<key>`, and `/<leaf>` beneath it — a writer per spelling plus the parser the writers round-trip against, mirroring the Rust module MEMBER-ADDRESS-GRAMMAR-ONE-HOME lands (`graph::nested_address` and `parse_nested_address` are the shape to match, so the two ends of the seam cannot disagree about what an address is). Internal: not re-exported from sdk/src/index.ts, which is the curated authoring API and gains no consumer here (engineering.md, \"An export earns its consumer\")."
        },
        {
          "path": "sdk/test/member-address.test.ts",
          "description": "The test that would have caught this: every writer's output parses back through the one parser, and the qualified spelling built for the member table equals the one built for `declaredAddresses` and for `EdgeTargetFacts.address` given the same host and value — the agreement twenty-one inline literals can only assert by inspection."
        }
      ],
      "edit": [
        {
          "path": "sdk/src/emit.ts",
          "description": "Thirteen live address-producing lines here, not the nine this entry first counted: the member table's top-level keys (:771, :775), the nested table's qualified (:798) and bare (:799-800) spellings, `resolvedTargetFacts`'s returned address (:353), the placement keys (:515, :555), the host on payload members (:815), and the three diagnostic-prose spellings (:181, :208, :341) — which render an address into a message and take the same writer, so a refusal quotes the spelling the resolver actually looked up. Plus the bare-form LIFT at :295 (`edge.to.length === 1 && !address.includes(\":\") ? `${edge.to[0]}:${address}` : address`), the direct counterpart of Rust `target_identity`'s single-element prefix strip (src/graph.rs:1609-1614), and its verbatim restatement in the comment at :294 — both matched by this entry's own acceptance regex, so both move or the acceptance stays red. Design bar on that one: the bare `kind:key` spelling is what 0049 lists under Rejected (\"Corpus-unique keys as the address ... Uniqueness is the resolver's bar, not the grammar's\"), so expose it as a LOOKUP KEY, never as an address writer — the two ends already agree (Rust strips the prefix back off before matching) and a builder must not \"fix\" the apparent mismatch."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "Seven sites, every cite 26 higher than this entry first spelled them: the host address at :480, :502, :570, :725 and :869, the qualified nested address at :873, and the leaf address at :534 — whose own doc comment (the block at :523-531, the cross-seam cite at :529) points at `src/read.rs`'s `parse_leaf_address` as the grammar it must match, the cite that goes stale the moment either side moves. That cite is re-pointed at the new module here, discharging the stale-prose exception MEMBER-ADDRESS-GRAMMAR-ONE-HOME's acceptance parks on this file."
        },
        {
          "path": "sdk/src/prose.ts",
          "description": "`mentionOf` (:29) spells `${member.kind}:${member.name}` — its doc comment (:24-26) calls it \"the convention every corpus repeats to cite a member from prose, captured once here\", which is true of this function and false of the module: it is one of twenty-one producers across three files. Read the writer."
        },
        {
          "path": "sdk/test/refusals.test.ts",
          "description": "VERIFY-ONLY — expect no diff, and the file this entry's \"byte-identical\" claim actually rides on: :320's case pins the ambiguous-bare-key refusal and :361 asserts its wording byte-for-byte (`/a bare key 2 hosts carry \\(`rule:rust`, `rule:sdk`\\)/`). Routing emit.ts:341's prose spelling through a writer must leave that string untouched. In the fence so a forced wording change can land visibly rather than as a surprise red."
        }
      ],
      "retire": []
    },
    "summary": "The member-address grammar is spelled by twenty-one inline template literals across three SDK modules, with no writer and no parser — the TS half of a consolidation the Rust half already has.",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "tests": [
      {
        "path": "sdk/test/member-address.test.ts",
        "asserts": "each address writer round-trips through the one parser, and the qualified nested spelling the member table, declaredAddresses and EdgeTargetFacts each produce for one host+value are byte-identical"
      },
      {
        "path": "sdk/test/emit.test.ts",
        "asserts": "unchanged and green — the extraction is behaviour-preserving, so the existing edge-resolution and member-table suites are the non-vacuity pin"
      }
    ],
    "acceptance": "`rg '\\}:\\$\\{|\\}/\\$\\{' sdk/src/*.ts` returns hits in sdk/src/member-address.ts and nowhere else; every producer reads the one module. `pnpm --dir sdk test` (strict tsc + node --test) is green with no behavioural diff, and `cargo test` stays green — the payload and lock rows the SDK emits are byte-identical, which is what makes this an extraction rather than a change.",
    "notes": "Filed 09-07: ab500f80 gave the Rust side one writer/parser and c60ddd3f then shipped exactly this bug class — the SDK member table indexed `kind:name` while the grammar spelled three segments. tests/seam_bindings_current.rs compares row TYPES, not the grammar, so nothing catches the ends drifting. Amended twice from cascade's audits: declarations.ts cites were 26 low, emit.ts:295 and :815 omitted, refusals.test.ts is the real pin, the new test file's description said twenty. scoped at 6b9eacae"
  },
  {
    "tag": "LOCAL-LAYOUT-PROSE-REACHES-NO-READ-TIME-ROW",
    "gate": {
      "kind": "blockedBy",
      "tags": [
        "EXPLAIN-KIND-REGISTRATION-LOCUS-AND-ADDRESS-FORM"
      ]
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "`local_document_rows` (:851) calls `read_layout_document`, which already returns the captured spans on `LayoutDocumentRows.prose` (src/drift.rs:1955), and then extends only `rows.nested` and `rows.satisfies` (:883-884) — the prose is read and dropped. Carry it: extend `rows.prose` too, and in `assemble_lock_family` (:1054-1055) collect the local kinds' prose rows into a new `local_layout_prose: Vec<drift::LayoutProseRow>` field on `LockFamily` (:405), beside `local_members` (:424) and for the same stated reason — the documents are uncommitted, so no consumer below can find them again short of a second walk that could disagree with this read. It does NOT go on `drift::Declarations` (:3331): that is the SDK→engine seam payload with a generated TS twin, and `layout_prose` is emit-derived output the seam has never carried."
        },
        {
          "path": "src/read.rs",
          "description": "`explain_target` destructures `LockFamily` at :2038 (taking `declarations` and `overlaid_builtin_kinds`) and its prose strand reads `drift::layout_prose_from_doc(&lock_doc)` at :2189 — the committed rows only. Take `local_layout_prose` off the same destructure and union it with the lock-document rows before `prose_strand` (:1989), so one narration covers both faces. Update the strand's sited comment above :2185, which today says the rows are 'a lock row keyed by the host's address' — true of the committed half only once local rows join it."
        },
        {
          "path": "tests/local_locus.rs",
          "description": "Regression: a local-locus layout kind whose document carries a preamble under a verbatim prose region — `explain <kind>:<id>` narrates the captured span, the same strand a committed layout member gets. Pins the read-time half of f07d4f84, which shipped the committed half and named this one as unwired."
        }
      ],
      "retire": []
    },
    "summary": "A local-locus layout member's captured prose is read and dropped at read time, so `explain` narrates nothing where the committed half now narrates the span.",
    "per": {
      "path": "specs/intent.md",
      "section": "Invariants"
    },
    "tests": [
      {
        "path": "tests/local_locus.rs",
        "asserts": "`explain` on a local-locus layout member narrates its captured prose region, matching the committed half's strand"
      }
    ],
    "acceptance": "`cargo test` green with the local_locus regression. A local-locus layout member's verbatim prose region reaches a read-time row and `explain`'s prose strand, so no layout document's authored span is read and dropped on either face (invariant 6). Non-vacuity per 0048's bar: the regression asserts the narrated span carries the document's actual preamble text, never merely that a strand appeared.",
    "notes": "Swept 09-07 from f07d4f84's own stated remainder ('the local read path is compose's to wire'). Verified on disk: `LayoutDocumentRows.prose` is already populated by `read_layout_document` and discarded by `local_document_rows` at src/compose.rs:883-884, so the fix is wiring, not derivation. Chained on EXPLAIN for src/read.rs, which transitively covers src/compose.rs (BUILTIN-KIND-RELOCATION). scoped at bf2e3eba"
  }
]
