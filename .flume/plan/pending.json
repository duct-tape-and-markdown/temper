[
  {
    "tag": "LOCK-ROW-REJECT-LOUD",
    "summary": "A present-but-malformed lock declaration row is a load error in every row family — the tool-written lock admits no degrade-to-absent read; absence stays legitimate",
    "per": {
      "path": "specs/intent.md",
      "section": "Invariants"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "BUILTIN-CONTRACT-ARRAY-SURGERY"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "`family()` (drift.rs:2179, filter_map at 2183) and the per-family `from_table` lifts silently drop a present row that fails its lift, narrated as 'the hand-editable-lock tolerance the rest of the readers share' (drift.rs:1401, 2144, 2274, 2593, 2711) — a premise pipeline.md rejects (tool-written whole, never patched) and invariant 6 names a gap, not a license: a dropped satisfies row forges an unfilled-requirement finding, a dropped kind row unmodels members. A present-but-malformed row (wrong-typed column, missing required column, malformed array element) becomes a load error surfaced through `read_declarations`' existing miette::Result (drift.rs:2110); a missing lock, an absent family, or an absent optional column stays legitimate absence. `source_deps` (drift.rs:1402) takes the same discipline; the retired narration goes with the behavior. `layout_edge_fields` (drift.rs:690; filter_map at 694, new at fcaf08e) skips a present `fact = \"edge\"` row missing its `field` column the same way — one more site of the class, covered by the load-tier reject or made loud in place."
        },
        {
          "path": "src/kind.rs",
          "description": "The lock-row column reads that degrade an out-of-vocabulary value to absent — the locus/format label reads (kind.rs:695, 723) and the KIND.md-era narration beside them (kind.rs:531) — reject loud the same way; the unit test asserting the degrade (kind.rs:1323) flips to asserting the error."
        },
        {
          "path": "src/main.rs",
          "description": "The `fact = \"edge\"` assembly-row read `edges_from_declarations` (main.rs:1269: 'a row missing a column is skipped, not errored') is the same class — a present edge row missing its column is a load error, never a silently absent edge."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "The cases asserting a malformed row reads as silent absence flip to asserting the load error; add per-family malformed-present cases beside the existing legitimate-absence ones."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/lock_declaration_rows.rs",
        "asserts": "each declaration row family rejects a present-but-malformed row (wrong-typed column, out-of-vocabulary label, missing required column) as a read_declarations error naming the family; a missing lock, absent family, or absent optional column still reads as legitimate absence"
      }
    ],
    "acceptance": "A committed lock carrying one malformed declaration row fails check/emit/read verbs loud at load, naming the row family — never a silently narrowed verdict; a lockless harness and every legitimately absent family/column behave exactly as today; all gates green.",
    "notes": "Found+scoped at residue sweep e5a7077; layout_edge_fields site added at sweep 503de24; re-anchored at ship audit 008a995 (main.rs edge read 1184→1269). c8b78c5 set the pattern (ClauseRowError) at the predicate tier; this is the from_table tier beneath it. read_declarations already returns miette::Result, so the ripple stays inside drift/kind/main plus the row suite. blockedBy is file-serialization only (shares src/main.rs with BUILTIN-CONTRACT-ARRAY-SURGERY), no semantic dependency."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper`, single package, `(sdk-package-layout)`, still at 0.0.5 pending publish — SDK-VERSION-LOCKSTEP); channel 1's SDK foundation shipped. Re-verified at 008a995 (ship audit): still no `.github/workflows/release.yml` (only `temper.yml`, a check job); root package.json is still the private flume manifest (`temper-flume-harness`)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm `@dtmd/temper`; ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs range-correctness shipped in SCAFFOLD-OUTPUT-VALID (^0.0.4); release owns only future re-pins."
  }
]
