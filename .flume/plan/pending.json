[
  {
    "tag": "MARKETPLACE-KIND",
    "summary": "`marketplace` ships: `.claude-plugin/marketplace.json` as a typed file member — identity from `name` against the reserved-names deny list, `owner.name` required, each entry's `source` union",
    "per": {
      "path": "specs/builtins.md",
      "section": "The shipped kinds"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/marketplace_kind.rs",
          "description": "The kind's acceptance: a `.claude-plugin/marketplace.json` fixture surfaces one member with identity from `name`; a reserved name is a finding; a missing `owner.name` is a finding; a `plugins[]` entry missing `name` or `source` is a finding; each documented `source` form resolves and an undocumented one is a finding. `tests/plugin_manifest_kind.rs` (shipped c68f625) is the sibling pattern to follow, not to copy — shared scaffolding lives in `tests/common`."
        }
      ],
      "edit": [
        {
          "path": "src/builtin_kind.rs",
          "description": "`claude_code_marketplace()` joins `all_kinds()` — **re-derived on disk this tick: the vec is at 344, not the 315 the entry carried before c68f625 inserted `claude_code_plugin_manifest()` above it (313-341). Read it at pick-up; the number moves whenever a sibling lands.** Facts: `format: Some(Format::JsonDocument)`, `unit_shape: NamedField { field: \"name\" }`, `governs { root: \".claude-plugin\", glob: \"marketplace.json\" }` — a distinct glob from `plugin-manifest`'s (shipped c68f625), so the two file kinds never contend. `claude_code_plugin_manifest()` is the sibling to read first. Cite `code.claude.com/docs/en/plugin-marketplaces` with the retrieval date at encode time."
        },
        {
          "path": "src/builtin_lock.toml",
          "description": "Generated data, never hand-edited — the kind row plus its clause rows. Regenerate per the file's header."
        },
        {
          "path": "src/builtin_lock.rs",
          "description": "Module header and tests."
        },
        {
          "path": "sdk/src/builtins.ts",
          "description": "The `Marketplace` type, the kind value, and `marketplaceDefaultContract`. **Re-fetch and cite per clause at encode time.** Verified 2026-07-16 against `code.claude.com/docs/en/plugin-marketplaces`, every fact below decidable: `name` is kebab-case and checked against a documented **reserved-names deny list** — `claude-code-marketplace`, `claude-code-plugins`, `claude-plugins-official`, `claude-plugins-community`, `claude-community`, `anthropic-marketplace`, `anthropic-plugins`, `agent-skills`, `anthropic-agent-skills`, `knowledge-work-plugins`, `life-sciences`, `claude-for-legal`, `claude-for-financial-services`, `financial-services-plugins`, `first-party-plugins`, `healthcare` — re-read the list at encode time rather than trusting this transcription, since the page itself records the list growing (before v2.1.205, `first-party-plugins` and `healthcare` were not reserved). The honest bound: the docs ALSO block names that 'impersonate official marketplaces' (`official-claude-plugins`, `anthropic-plugins-v2`), and impersonation is semantic judgment — so the enumerated deny list is the decidable subset and the impersonation rule is guidance, never a clause (intent.md, invariant 2). `owner.name` required, `owner.email` optional. Each `plugins[]` entry requires `name` plus `source`, the documented union: a relative-path string (must start with `./`, resolved relative to the marketplace root, NOT `.claude-plugin/`), or an object — `github` (`repo`, `ref?`, `sha?`), `url` (`url`, `ref?`, `sha?`), `git-subdir` (`url`, `path`, `ref?`, `sha?`), `npm` (`package`, `version?`, `registry?`). Where both `ref` and `sha` are set on a git-based source, `sha` is the effective pin; a marketplace entry's `defaultEnabled` beats `plugin.json`'s. Per-field precedence is clause-and-cite territory here (decision 0031). `pluginManifestDefaultContract` (shipped c68f625) is the sibling to match — on shape, and on how it names the holds its clauses cannot decide; **its header re-verified on disk this tick at 599-627, the export at 628** (the entry previously said 605-620)."
        },
        {
          "path": "sdk/src/claude-code.ts",
          "description": "The provider face exports the kind value, its type, and its default contract. With this entry `specs/builtins.md`'s roster is filled: 'Ten kinds ship. Seven are file members' — seven file kinds (skill, command, agent, rule, memory, plugin-manifest, marketplace) plus three registration kinds (hook, mcp-server, installed-plugin). The engine's `all_kinds()` holds **eleven** at that point, because `supporting-doc` ships outside the enumeration; the two numbers are both right and neither checks the other. Verify on disk."
        },
        {
          "path": "sdk/test/builtins.test.ts",
          "description": "The kind's facts and its default contract's clause set, the deny-list clause included."
        },
        {
          "path": "tests/builtin_lock_frozen.rs",
          "description": "`MEMBERLESS_BUILTIN_PROGRAM` imports and `expect`-binds the kind."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "The row-count-exact kind-fact walk: the `kinds.len()` assert moves to **eleven**, not ten — `supporting-doc` ships without joining `specs/builtins.md`'s enumeration (as `requirement` does), so the engine roster runs one above the corpus's ten. Re-verified on disk this tick: the assert reads **10 at 2113** (a second `kinds.len()` assert at 1572 is a single-kind fixture — not this one). Read it on disk at pick-up and do not trust this number sight-unseen."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/marketplace_kind.rs",
        "asserts": "a `.claude-plugin/marketplace.json` reads as one member with identity from `name`; a reserved name, a missing `owner.name`, and a `plugins[]` entry missing `name` or `source` are each findings; every documented `source` form resolves and an undocumented one is a finding"
      }
    ],
    "acceptance": "`marketplace` ships as `specs/builtins.md`'s roster declares it, completing the ten-kind roster decision 0031 ratified — the seven file kinds and three registration kinds that page enumerates, `supporting-doc` shipping beside them outside the count. `temper check .` exits 0. Its default contract holds the strictest documented profile with every clause decidable, severity-declared, guided, cited and dated; the impersonation rule ships as guidance, never a clause. `cargo test`, `cargo clippy --all-targets -- -D warnings`, `pnpm --dir sdk test` green.",
    "notes": "Derived from decision 0031 (16ce347), the producer half's second kind. Same warrant as its sibling: `temper bundle` writes this file ungated today (`src/bundle.rs:191`). Gate opened at 4084dcd: PLUGIN-MANIFEST-KIND shipped (c68f625), verified on disk. Disjoint from every other open entry. scoped at e81c758; cites re-derived on disk 21e003a — `all_kinds()` had MOVED 315→344 under c68f625, so the prior 'unmoved' claim was wrong."
  },
  {
    "tag": "TYPE-PREDICATE-ROUND-TRIPS",
    "summary": "The `type` predicate is exported, typed, and decided but carries no declared kind and no lock column — it round-trips nowhere, so an author who reaches for it gets a hard load error",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/type_predicate.rs",
          "description": "The seam's acceptance, end to end: an SDK-authored `type` clause emits a lock row carrying its declared kind, reads back through `read_declarations`, and the engine decides it — a wrong-typed field is a finding, a right-typed one holds, an absent field stays silent (the shipped `Outcome::Holds` on absence, `src/engine.rs:626`). Its own file rather than `tests/lock_declaration_rows.rs` (the declaration-row family's home) for one reason only: that file is MARKETPLACE-KIND's, and two open entries never share a path. If MARKETPLACE-KIND has already shipped at pick-up, folding these asserts into that existing home instead is the better answer — `specs/process/engineering.md`, 'One job, one home' — and is in scope; say which you chose in the commit body."
        }
      ],
      "edit": [
        {
          "path": "sdk/src/contract.ts",
          "description": "`type = (field: string)` (68, re-verified on disk 21e003a) takes no declared kind, so it cannot express the thing the predicate exists for (`keywords` must be a `list`) even before it is emitted — give it its second argument. The `Predicate` interface (23) carries a slot per predicate argument (`target`, `gate`, `charset`, `keys`, `values`, `range`, `section`) and none for a value type; add one, spelled to match the lock column. **The wire-spelling collision to settle first:** `ValueType::name()`/`from_name()` (`src/extract.rs:59`/`76`) is the single home of the lattice's name table and spells it lowercase (`string`/`integer`/`number`/`boolean`/`list`/`map`/`null`), while the ts-rs binding `sdk/src/generated/ValueType.ts` spells the variants capitalized (`\"List\"`). The author-facing argument and the column must agree with `from_name`, or the round-trip fails at the decoder — pick one spelling and say which in the commit body."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "`clauseRow` (64) maps each predicate's own argument onto its own column; add the value-type column, the way `gate` (85) is mapped for `mention-reachable`."
        },
        {
          "path": "sdk/src/generated/ClauseRow.ts",
          "description": "Generated by ts-rs, never hand-edited: it carries a column per predicate argument (`bound`, `charset`, `keys`, `values`, `count`, `target`, `degree`, `range`, `section`, `gate`) and none for a value type. Regenerate with `BLESS_SEAM_BINDINGS=1` per `tests/seam_bindings_current.rs`'s header and commit the diff; that test byte-compares and fails loud on stale bindings."
        },
        {
          "path": "src/drift.rs",
          "description": "`ClauseRow` (2459, re-verified on disk 21e003a) is the lock's clause projection and carries no value-type column — add it beside `gate` (2497), the precedent added for the other predicate whose arguments outrun the shared `field` column, and carry it through both faces: `ClauseRow::to_table` (3304 — `to_table` is a trait method with several impls, at 3091/3304/3394; 3304 is `ClauseRow`'s) writes the row, the `from_table` read (3370, where `gate: opt_str(table, \"gate\")` sits) lifts it. Note `ValueType` derives neither `Serialize` nor `Deserialize` (`src/extract.rs:36` — `schemars::JsonSchema` and `ts_rs::TS` only), so the column's wire form is the lowercase `from_name` spelling decoded at the boundary, not a `ValueType` field. The struct's doc comment enumerates which predicates carry their own column and moves with the change."
        },
        {
          "path": "src/contract.rs",
          "description": "`predicate_from_row` (321, re-verified on disk 21e003a) has an arm for every predicate in the closed vocabulary **except `type`**, so a `type` row lifts to `None` — which `src/compose.rs:181` turns into `ClauseRowError::Predicate`, rejected loud. Correct refusal, fired on a predicate the SDK told the author to use. Add the arm, decoding the new column through `ValueType::from_name`. `Predicate::Type { field, kind }` (97) is already typed and needs no change, and neither does `src/engine.rs:626` (it already decides it) nor `src/schema.rs:64` (it already projects it to JSON Schema's `type` keyword). This is the one missing home: nothing here grows the closed algebra."
        },
        {
          "path": "src/extract.rs",
          "description": "Narration riders this entry's own work falsifies — they ride here rather than standing alone. `ValueType`'s doc (30-35) parenthesizes 'the slice-1 shortcut this entry corrects': a build tick's own voice leaked into shipped prose, naming an entry no reader can resolve. `FeatureValue`'s doc calls the `type` primitive '(forthcoming)' (95), false the moment it round-trips. Re-word both to the shipped state — a surface states the rule, never the incident that taught it."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/type_predicate.rs",
        "asserts": "an SDK-authored `type` clause survives emit → lock → read → decide: the row carries its declared kind, a wrong-typed field is a finding, a right-typed field holds, and an absent field is silent"
      }
    ],
    "acceptance": "The `type` predicate round-trips the whole seam — SDK constructor to lock column to engine verdict — so the closed vocabulary the SDK exports is the vocabulary the engine can receive (`specs/model/contract.md`, 'clause': the enum in code is the authority, and an unknown predicate is rejected at load, never skipped). An author who reaches for `type` gets the check they asked for, not a load error. `cargo test`, `cargo clippy --all-targets -- -D warnings`, `pnpm --dir sdk test` green, `tests/seam_bindings_current.rs` green against regenerated bindings.",
    "notes": "Drained from the refactor capture `build-type-predicate-cannot-cross-the-lock` (observed 024ba9b); every home re-verified on disk at 21e003a. Wiring only — nothing grows the closed algebra. The consumer clause (`plugin-manifest`'s `keywords`) and `builtins.ts`'s hold paragraph are out of scope: that file is MARKETPLACE-KIND's, and two open entries never share one. Sibling gap: `(closed-surface-predicate)`. JSON-DOCUMENT-BODY-REFUSAL serializes behind this on `src/drift.rs`. scoped at 4084dcd."
  },
  {
    "tag": "JSON-DOCUMENT-BODY-REFUSAL",
    "summary": "`emit` silently drops an authored body on a `json-document` member — `project_bytes` renders fields alone, and `plugin-manifest` is body-bearing, so authored prose vanishes at a `.json` path",
    "per": {
      "path": "specs/intent.md",
      "section": "Invariants"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "TYPE-PREDICATE-ROUND-TRIPS"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "`project_bytes` (1734) takes the projection's `body` and, on the `Some(Format::JsonDocument)` arm (1740-1742), returns `write_document(fields)` — `body` is never read there. Every other arm renders it. So a `json-document` member's authored prose is dropped from its projection with no error: invariant 3 ('temper never rewords, synthesizes, or drops authored words') and invariant 6 ('no path silently degrades ... or emits over an unresolved input') both break on the same arm. This is the sibling of the bug f8d2431 fixed — that tick made `format` load-bearing on the write face and reasoned that install's `placements` cannot exist over JSON bytes *by construction* (`placement_lines` matches only a `---\\n` prefix or a banner marker), which is sound; it did not reason about `body`, which has no such construction argument. **The fix is a refusal, not a render** — a JSON document has no prose slot to render into, so the honest act is to refuse loudly rather than invent a home for the words. Site it where the refusal can carry a `Result` and fire before any byte is written (`specs/model/pipeline.md`, 'Emit': refusal precedes output): the `Projection`-building loop (985-991, where `format: format_from_row(facts)?` is already lifted at 988 and where the include resolution already refuses) is the natural home; `project_bytes` itself returns `String` and cannot refuse without a signature change. Add the `DriftError` variant beside `IncludeNotUtf8` (238) / `IncludeArity` (252) — same shape: a `#[diagnostic(code(...))]`, the member's `kind:name` address, and a message naming the member and its format, carrying the reason: the words have no home in this format, so route them to a body-bearing member or drop them at the source. `project_bytes`'s doc comment asserts the JSON face 'takes no `placements` and drops nothing' — true of placements, false of `body` today; it moves with the change."
        },
        {
          "path": "tests/json_document_format.rs",
          "description": "The format's own home (its header: 'the format's own faces, held apart from any roster'), and no other entry opens it — so the acceptance belongs here rather than in a new file. Add the falsifier: a `json-document` payload member carrying a non-empty body refuses, naming the member, and writes no bytes; the empty-body case (every fixture today) still emits byte-identically, so the refusal cannot be satisfied by breaking the shipped path. Drive the include case too if it costs little — it is the vivid one: `splice_includes` (983) resolves the include, pushes its `include_row` into the lock, and then the spliced bytes are dropped, so temper fingerprints a dependency whose content reaches no artifact."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/json_document_format.rs",
        "asserts": "a `json-document` member with a non-empty body refuses before any byte is written, naming the member; an empty-body member emits its canonical document unchanged"
      }
    ],
    "acceptance": "No authored word reaches a `json-document` projection's write face and vanishes: a body that cannot be rendered is an error at author-time, never a silent drop (`specs/intent.md`, invariant 3 'Verbatim prose' and invariant 6 'Loud or nothing' — the per-surface refusal clauses are instances of that rule, and a surface without one is a gap, not a license). `cargo test`, `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Sweep finding, window 14c5de4..4084dcd. Reachable today, not theoretical: `pluginManifest` (`sdk/src/builtins.ts:589`) declares no `shape`, so it is body-bearing (`content` absent = `file`, one verbatim prose body), and `MemberInit` carries `prose?` (`sdk/src/kind.ts:197`) unconditionally — the SDK type refuses nothing. `blockedBy` TYPE-PREDICATE-ROUND-TRIPS on `src/drift.rs` alone; nothing else is shared. scoped at 21e003a."
  },
  {
    "tag": "BUNDLE-EMIT-THROUGH-KINDS",
    "summary": "`temper bundle`'s two hard-coded `serde_json` manifest writers reconcile onto the shipped kinds — emit through the kind system, one home",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "MARKETPLACE-KIND"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/bundle.rs",
          "description": "`run()` (178) writes `.claude-plugin/plugin.json` (185) and `.claude-plugin/marketplace.json` (191) through `write_json` (278) over the hand-built `plugin_manifest()` (223) and `marketplace_manifest()` (238) values — every cite re-verified unmoved on disk at 21e003a. **Note what is NOT duplicated:** `write_json` already delegates to `crate::json_manifest::write_manifest`, so the JSON *encoder* has one home already and is not this entry's target. What is duplicated is the manifest *values*: hand-built `BTreeMap`s spelling a shape the kind system types once `plugin-manifest` (shipped c68f625) and `marketplace` ship. The manifests become members of their kinds, rendered by the kind's format, so `bundle` stops hand-spelling a shape the engine can now type and gate. This is the consolidation decision 0031 names ('bundle's hard-coded writers reconcile onto the kinds — emit through the kind system, one home; the entry names the unification'), and `specs/process/engineering.md` makes it the entry rather than scope creep — name the unification in the commit body. NOT in scope: the `SessionStart` hook's `hooks/hooks.json` (`hooks_manifest()`, 259) and the skill's `SKILL.md` — `hooks.json` is a plugin-cache artifact of neither kind, and widening to it is the creep the same page forbids. If any part of either manifest's shape cannot route through a kind, say so plainly rather than leaving a silent hand-rolled remainder."
        },
        {
          "path": "tests/bundle.rs",
          "description": "The bundle's existing acceptance. Its assertions over both manifests are the regression net that makes this a true refactor: the emitted bytes should not move, and if they do, that is a behavior change to state out loud in the commit body, never a snapshot to bless quietly."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/bundle.rs",
        "asserts": "`bundle` emits both `.claude-plugin/` manifests through their kinds' formats, byte-identically to the pre-refactor output, and the emitted manifests pass their own kinds' default contracts — the product checks what it writes"
      }
    ],
    "acceptance": "The two manifest writers have one home: `bundle`'s output is its members rendered by their kinds' formats, not hand-built `serde_json` beside a kind that types the same shape (`specs/process/engineering.md`, 'One job, one home': a job the tree already performs is extended where it lives, never reimplemented beside itself). The bundle temper publishes passes the contract temper ships for it, closing the gap decision 0031 named — the product wrote files it refused to check. `cargo test`, `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Derived from decision 0031 (16ce347), the third derivation item. Blocked on `marketplace` alone now — `plugin-manifest` shipped at c68f625. It consumes EMIT-JSON-DOCUMENT-WRITE-FACE's dispatch rather than building it: that shipped at f8d2431, verified on disk. Distinct from the `(plugin-author-dogfood)` fork: `bundle` still assembles its tree into an output dir, and no `.claude-plugin/` is committed here. scoped at e81c758; cites re-verified 21e003a."
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics — the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (⇒ 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (⇒ 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence — never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. All cites re-verified unmoved at 21e003a. `MAX_IMPORT_HOPS = 5` (65) sits under a doc (61-64) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' — a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (627) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds (654), one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts — hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it — 525 ('hop-capped at [`MAX_IMPORT_HOPS`]') and 624 ('capped at [`MAX_IMPORT_HOPS`] hops (the `at-import` recursion depth the format documents)') — carry the same claim and move with it, as does the propagation comment at 649-652 ('capped at the format's import recursion depth')."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1356, re-verified unmoved at 21e003a) already pins the boundary mechanically rather than in a comment — `r1..r5` rescued from a live seed, `r6` dead as the sole finding — so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1357-1361) spells 'capped at the format's five-hop import recursion' (1359) and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper — the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Park re-tested at 21e003a and holds — nothing ruled the hop semantics; the inbox is empty and the window (f8d2431, c68f625) touched no import mechanics. `src/graph.rs` untouched by that window: `MAX_IMPORT_HOPS` still reads 5 at 65 under a cite claiming five, and 525/624/654 plus `tests/graph.rs:1356` are all unmoved. Stays parked: the constant may still be wrong. rescoped 4a33911; re-cited 53519b9; re-verified 21e003a."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested on disk at 21e003a, holding on every clause: `git tag -l` carries only the four era tags and no version tag, crate 0.1.0 vs npm 0.0.7, and release.yml:7-9 still states the darwin + channel-3 deferral verbatim. Channel 3's publish is the release act; BUNDLE-EMIT-THROUGH-KINDS is the orthogonal question of how those bytes are produced. scoped at b676656."
  }
]
