[
  {
    "tag": "PROSE-INCLUDE",
    "summary": "Composed prose declares an include — the target's contents pulled into the host's emitted bytes, fingerprinted in the lock, a declared edge, refusing loudly when dangling",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "The SDK"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/prose_include.rs",
          "description": "An include declared in composed prose lands the target's contents byte-identical inside the host's emitted artifact; the dependency is fingerprinted in the lock so a moved target is drift; the edge joins the one resolved enumeration explain/impact narrate; a dangling include refuses before a byte is written (pipeline.md, Emit: 'Refusing' names the unresolvable include explicitly)."
        }
      ],
      "edit": [
        {
          "path": "sdk/src/prose.ts",
          "description": "The include declaration on composed prose — an intent apart from the mention: it moves content (pipeline.md, The SDK). Spelling is code's call (depth rule); the mention interpolation (the `MENTION_SLOT` sentinel, prose.ts:31) is the precedent. NB the sentinel is a literal NUL, so grep treats this file as binary — use `grep -a`. The path resolves relative to the stating module, never the workspace."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "A new include row family beside `mentionRows` (declarations.ts:299), carried across the seam in `Declarations` (mirror: `mentions`, src/drift.rs:1426) — a new `IncludeRow` ts-rs binding lands under sdk/src/generated/."
        },
        {
          "path": "src/drift.rs",
          "description": "Engine-side resolve + fingerprint + pull-into-emitted-bytes. The resolution unifies with `resolve_layout_import` (drift.rs:688) — the consolidation LAYOUT-PROSE-IMPORT's notes sanctioned and d27a624 deferred for want of this second consumer; the fingerprint takes the same never-reaped source-dependency posture as `LayoutImportRow`."
        },
        {
          "path": "src/graph.rs",
          "description": "The include edge reuses `ImportDeclaration`/`resolved_import_edges` (graph.rs:896/910) under the existing `IMPORT_FIELD` locus — path-resolved once at emit, no second lift."
        },
        {
          "path": "src/main.rs",
          "description": "The include edges fold into the same resolved reference set at the existing mention/import merge sites (main.rs:451/566, helpers at 1174/1191)."
        },
        {
          "path": "tests/seam_bindings_current.rs",
          "description": "Guards the generated seam bindings — the new `IncludeRow` binding ripples here; regenerate and re-assert."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/prose_include.rs",
        "asserts": "include content lands byte-identical in the emitted artifact; the dependency fingerprint reaches the lock; the edge is in the one enumeration; a dangling include refuses before any write"
      }
    ],
    "acceptance": "A composed-prose include pulls its target's contents into the host's emitted bytes, fingerprinted in the committed lock and visible as a declared edge to the gate and read verbs; a dangling include refuses loudly before any byte is written; the resolver is one surface shared with the layout import; all gates green.",
    "notes": "Scoped at 961c9c2 (residue sweep): pipeline.md 'The SDK' has carried the include since e842a32 (0001/0002) with no entry ever operationalizing it; d27a624's body confirms no engine surface exists. The resolve_layout_import unification is the entry's named consolidation, not creep (engineering.md). Unblocked at e30e5a6 ship audit — LAYOUT-EDGE-SLOT shipped (670d54e); all cited anchors re-verified on disk and line-pinned to that HEAD."
  },
  {
    "tag": "LAYOUT-RELATIONSHIP-EDGES",
    "summary": "A layout host's declared-relationship edge slot's entries reach the one resolved edge enumeration — resolved like a file member's frontmatter reference, never silently dropped",
    "per": {
      "path": "specs/model/representation.md",
      "section": "kind"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "PROSE-INCLUDE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "`layout_unit` (main.rs:816) drops `LayoutReading::edges`: the Unit carries only `reading.fields`, and `edge_field_slots()` (kind.rs:609) excluded the slot from frontmatter, so the declared-edge check (`graph::check`, wired at main.rs:692 over unit features) never sees a layout host's relationship entries — they silently vanish. Fold the parsed entries onto the unit in the shape the check already consumes — a file member's frontmatter list reference is the precedent; the exact shape is code's call (depth rule)."
        },
        {
          "path": "src/drift.rs",
          "description": "Emit's layout read passes only the framework `satisfies` set (drift.rs:625) where check passes the full `edge_field_slots()` union — one document read under two edge-slot sets. Unify on the union so a relationship slot never parses as a verbatim field on one path and as addresses on the other."
        },
        {
          "path": "tests/layout_edge_slot.rs",
          "description": "Extend the shipped edge-slot suite (one home, engineering.md): a declared-relationship edge slot's entries resolve as the host's edges, visible to the gate and read verbs; a dangling entry is a finding, never a silent drop."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/layout_edge_slot.rs",
        "asserts": "a layout kind declaring an edge field gets that slot's entries into the resolved edge enumeration (gate + read verbs); a dangling entry is a finding; the span never lands as a verbatim frontmatter field"
      }
    ],
    "acceptance": "A layout kind that declares an edge field (SDK-expressible today: `edgeFields` kind.ts:102 beside `content: Layout` kind.ts:105) resolves that slot's entries into the same enumeration the gate and read verbs share; a dangling entry is a finding, never a silent drop; all gates green.",
    "notes": "Scoped at ced4f94 (residue sweep of 670d54e): the ship wired only `satisfies` and named the cut ('no wired consumer'), but the check-time silent drop violates intent invariant 6. No new lock family needed: file members' relationship edges resolve live off frontmatter; a layout document is a source read live at check, so it folds the same way. Reader side already general (LayoutReading::edges kind.rs:162, parse_edge_entries kind.rs:386)."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper`, single package, `(sdk-package-layout)`, still at 0.0.5 pending publish — SDK-VERSION-LOCKSTEP); channel 1's SDK foundation shipped. Re-verified at e30e5a6 (ship audit): still no `.github/workflows/release.yml` (only `temper.yml`, a check job); root package.json is still the private flume manifest (`temper-flume-harness`)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm `@dtmd/temper`; ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs range-correctness shipped in SCAFFOLD-OUTPUT-VALID (^0.0.4); release owns only future re-pins."
  }
]
