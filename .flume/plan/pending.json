[
  {
    "tag": "REFERENCE-NORMALIZATION",
    "summary": "Engine gains kind-declared reference normalization: an optional `strip_suffix` on the `references` primitive maps a ref value to a member id before the exact-match resolve",
    "per": {
      "path": "specs/15-kinds.md",
      "section": "Decision: reference resolution is declared by the kind, never guessed by the engine"
    },
    "gate": { "kind": "open" },
    "files": {
      "new": [
        {
          "path": "tests/reference_resolution.rs",
          "description": "End-to-end proof: a custom kind whose `references` primitive declares `strip_suffix = \".md\"` and a `[[relationships]]` edge, over members whose backtick-filename refs (`` `NN-name.md` ``) resolve to sibling member ids with zero dangling findings under `graph::check`; a control (no `strip_suffix`) proves the same ref dangles — the exact-match discipline the Decision demands (never a loose fallback)."
        }
      ],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Add an optional declared `strip_suffix` to `Primitive::References` (the reference syntax's own normalization, `specs/15-kinds.md`), parse it from the `[[extraction]]` references table (a new *known* key — unknown keys still rejected), and apply it in `Primitive::apply` after `backtick_filename_refs`: strip the declared suffix from each ref before it becomes the feature value, so a later exact id-match resolves without a loose fallback. Additive/optional — absent ⇒ current behavior, so the by-kind floor and the current dogfood `spec` KIND.md (no suffix declared) are byte-unchanged. Update the in-module `Primitive::References {…}` test constructions for the new field."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/reference_resolution.rs",
        "asserts": "a kind declaring `strip_suffix = \".md\"` resolves `` `NN-name.md` `` refs to sibling member ids (no dangle) under graph resolution; the same ref without the declared normalization dangles."
      }
    ],
    "acceptance": "the `references` primitive accepts an optional declared `strip_suffix`, applied at extraction so a custom kind's backtick-filename refs resolve to member ids under `graph::check`/`resolved_edges`; the floor-only path is byte-unchanged; cargo fmt/clippy -D warnings/test all green.",
    "notes": "Reconciles code to `(reference-id-normalization)` RESOLVED. Engine capability ONLY — wiring the dogfood spec kind's `[[relationships]]` + `strip_suffix` into `.temper/kinds/spec/KIND.md` is a HUMAN `.temper/` follow-up (surfaced, never a build entry — `.temper/**` is human territory). by_kind already carries custom-kind features + edges, so resolution needs only the normalization step."
  },
  {
    "tag": "CONTRACTS-RETIRE",
    "summary": "Delete the dead `contracts/*.toml` std-lib mirror — the engine embeds built-in packages from `packages/<name>/PACKAGE.md` (build.rs); nothing loads `contracts/`",
    "per": {
      "path": "specs/10-contracts.md",
      "section": "Decision: a package is project-authorable, not vendor-privileged — and is itself a kind"
    },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [],
      "retire": [
        "contracts/rule.toml",
        "contracts/skill.anthropic.toml"
      ]
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`contracts/rule.toml` and `contracts/skill.anthropic.toml` are gone; cargo build/test/clippy -D warnings stay green — no code loads them (build.rs embeds `packages/`, and the two tests naming the string `contracts/skill.anthropic.toml` assert the retired `contract` key is rejected, not a file load).",
    "notes": "The spec's `contracts/` retirement (`10-contracts.md`): the built-in std-lib's authoritative home is the `packages/` product tree, embedded by build.rs — 'one artifact, no contracts/ mirror.' Verified on disk: no `include_str!`/loader reads contracts/; only comments (still accurate — they cite the retirement) and test-fixture string literals name it. Pure deletion, behavior-neutral."
  },
  {
    "tag": "COMMENT-DIET(compose)",
    "summary": "Comment-etiquette sweep of `src/compose.rs` — cut module-head spec narrative and what-comments; keep why/invariant/checklist/pointer tags. Behavior-neutral.",
    "per": { "path": ".claude/rules/rust.md", "section": "Style & structure" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "Apply the sourced comment etiquette (RFC 1574; Ousterhout APoSD ch.13; antirez). Keep: one-line `///` summaries (+ Errors/Panics where reachable), why/invariant/checklist comments, terse spec pointer tags, ~10-line `//!` overview. Cut: module-head narrative restating specs/, what-comments narrating ordinary code, compliance narration. Comments-only — zero code changes. compose.rs is the heaviest offender (~900 comment lines)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`src/compose.rs` carries only the sanctioned comment classes; the diff changes zero code; cargo fmt/clippy -D warnings/test all green (behavior-neutral, proven by the unchanged gate suite)."
  },
  {
    "tag": "COMMENT-DIET(main)",
    "summary": "Comment-etiquette sweep of `src/main.rs` — cut module-head spec narrative and what-comments; keep why/invariant/checklist/pointer tags. Behavior-neutral.",
    "per": { "path": ".claude/rules/rust.md", "section": "Style & structure" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "Apply the sourced comment etiquette (rust.md). Keep the sanctioned classes (one-line `///`, why/invariant, terse pointer tags, ~10-line `//!`); cut restated-spec narrative, what-comments, and compliance narration. Comments-only — zero code changes; `main` stays a thin clap dispatch. main.rs is ~47% comment lines — a heavy offender."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`src/main.rs` carries only the sanctioned comment classes; the diff changes zero code; cargo fmt/clippy -D warnings/test all green."
  },
  {
    "tag": "COMMENT-DIET(roster)",
    "summary": "Comment-etiquette sweep of `src/roster.rs` — cut module-head spec narrative and what-comments; keep why/invariant/checklist/pointer tags. Behavior-neutral.",
    "per": { "path": ".claude/rules/rust.md", "section": "Style & structure" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/roster.rs",
          "description": "Apply the sourced comment etiquette (rust.md): keep one-line `///`, why/invariant, terse pointer tags, ~10-line `//!`; cut restated-spec narrative, what-comments, compliance narration. Comments-only — zero code changes."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`src/roster.rs` carries only the sanctioned comment classes; the diff changes zero code; cargo fmt/clippy -D warnings/test all green."
  },
  {
    "tag": "COMMENT-DIET(drift)",
    "summary": "Comment-etiquette sweep of `src/drift.rs` — cut module-head spec narrative and what-comments; keep why/invariant/checklist/pointer tags. Behavior-neutral.",
    "per": { "path": ".claude/rules/rust.md", "section": "Style & structure" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "Apply the sourced comment etiquette (rust.md): keep one-line `///`, why/invariant/checklist, terse pointer tags, ~10-line `//!`; cut restated-spec narrative, what-comments, compliance narration. Comments-only — zero code changes; the three-state drift invariants stay documented."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`src/drift.rs` carries only the sanctioned comment classes; the diff changes zero code; cargo fmt/clippy -D warnings/test all green."
  },
  {
    "tag": "COMMENT-DIET(graph)",
    "summary": "Comment-etiquette sweep of `src/graph.rs` — cut module-head spec narrative and what-comments; keep why/invariant/checklist/pointer tags. Behavior-neutral.",
    "per": { "path": ".claude/rules/rust.md", "section": "Style & structure" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "Apply the sourced comment etiquette (rust.md): keep one-line `///`, why/invariant, terse pointer tags, ~10-line `//!`; cut restated-spec narrative, what-comments, compliance narration. Comments-only — zero code changes; disjoint from REFERENCE-NORMALIZATION (that entry touches only src/kind.rs, not graph.rs)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`src/graph.rs` carries only the sanctioned comment classes; the diff changes zero code; cargo fmt/clippy -D warnings/test all green."
  },
  {
    "tag": "COMMENT-DIET(import)",
    "summary": "Comment-etiquette sweep of `src/import.rs` — cut module-head spec narrative and what-comments; keep why/invariant/checklist/pointer tags. Behavior-neutral.",
    "per": { "path": ".claude/rules/rust.md", "section": "Style & structure" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "Apply the sourced comment etiquette (rust.md): keep one-line `///`, why/invariant, terse pointer tags, ~10-line `//!`; cut restated-spec narrative, what-comments, compliance narration. Comments-only — zero code changes; the provenance/idempotence invariants stay documented."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`src/import.rs` carries only the sanctioned comment classes; the diff changes zero code; cargo fmt/clippy -D warnings/test all green."
  },
  {
    "tag": "COMMENT-DIET(contract)",
    "summary": "Comment-etiquette sweep of `src/contract.rs` — cut module-head spec narrative and what-comments; keep why/invariant/checklist/pointer tags. Behavior-neutral.",
    "per": { "path": ".claude/rules/rust.md", "section": "Style & structure" },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/contract.rs",
          "description": "Apply the sourced comment etiquette (rust.md): keep one-line `///`, why/invariant, terse pointer tags, ~10-line `//!`; cut restated-spec narrative, what-comments, compliance narration. Comments-only — zero code changes; the closed-algebra/admissibility rationale stays where it earns its keep."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`src/contract.rs` carries only the sanctioned comment classes; the diff changes zero code; cargo fmt/clippy -D warnings/test all green."
  },
  {
    "tag": "COMMENT-DIET(kind)",
    "summary": "Comment-etiquette sweep of `src/kind.rs` — cut module-head spec narrative and what-comments; keep why/invariant/checklist/pointer tags. Behavior-neutral.",
    "per": { "path": ".claude/rules/rust.md", "section": "Style & structure" },
    "gate": { "kind": "blockedBy", "tag": "REFERENCE-NORMALIZATION" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Apply the sourced comment etiquette (rust.md): keep one-line `///`, why/invariant, terse pointer tags, ~10-line `//!`; cut restated-spec narrative, what-comments, compliance narration. Comments-only — zero code changes."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`src/kind.rs` carries only the sanctioned comment classes; the diff changes zero code; cargo fmt/clippy -D warnings/test all green.",
    "notes": "Serialized after REFERENCE-NORMALIZATION — both edit `src/kind.rs`, so parallel `open` would merge-conflict and spin the wave (the GOV-RANGE/GOV-COUNT failure). Sweep kind.rs once the `strip_suffix` field addition has landed."
  },
  {
    "tag": "AGENT-KIND",
    "summary": "Add the `agent` built-in harness kind — `.claude/agents/*.md` imported as one AGENT.md member document, checked by an authored agent package",
    "per": {
      "path": "specs/20-surface.md",
      "section": "Artifact kinds & package binding"
    },
    "gate": {
      "kind": "deferred",
      "reason": "the requirements/satisfies reframe deprioritizes more built-in kinds; the surface-language chain it waited on has shipped, so the blocker is priority alone — revive only if a story demands the agent kind"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/agent.rs",
          "description": "The Agent IR — the typed model for a Claude Code subagent at `.claude/agents/<name>.md`, mirroring `src/rule.rs`: typed YAML frontmatter on the projection side (`name`, `description`, optional `tools`/`model`), unknown keys preserved, content-faithful body, provenance. Identity is the path (file stem). The surface member is one AGENT.md document (clause-module header) like every kind."
        },
        {
          "path": "packages/agent/PACKAGE.md",
          "description": "The agent kind's authored built-in package (clauses + guidance) in the one-document medium, embedded with the std-lib by build.rs (the packages/ product tree — NOT a contracts/agent.toml). Clauses: `name` required + `allowed_chars` `[a-z0-9-]`; `description` required; `forbidden_keys` for the Cursor `.mdc` keys Claude Code ignores; an advisory `max_lines`."
        },
        {
          "path": "tests/agent_contract.rs",
          "description": "Fixture-driven proof mirroring `tests/rule_contract.rs`: one broken agent per clause proving it fires, a clean agent that trips nothing, and an insta import round-trip proving idempotence."
        }
      ],
      "edit": [
        {
          "path": "src/lib.rs",
          "description": "Add `pub mod agent;` beside the other artifact-kind modules (this entry owns the line)."
        },
        {
          "path": "src/check.rs",
          "description": "Extend `Workspace` with `agents` loaded under `<dir>/agents/*` and a `WorkspaceError` arm; update the load tests."
        },
        {
          "path": "src/extract.rs",
          "description": "Add `agent_features` mirroring `rule_features`."
        },
        {
          "path": "src/import.rs",
          "description": "Discover `.claude/agents/*.md`, project each to one `<into>/agents/<name>/AGENT.md` member document, add an `[[agent]]` lock row per agent."
        },
        {
          "path": "src/main.rs",
          "description": "Bind the agent package, wire admissibility + conformance streams and `by_kind` for the kind."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/agent_contract.rs",
        "asserts": "each decidable clause of the agent package fires on its broken fixture and stays silent on a clean agent; import of an unchanged agent is idempotent (insta snapshot)."
      }
    ],
    "acceptance": "`temper check` dispatches `.claude/agents/*.md` to the agent package (conformance) and that package passes admissibility; `temper import` projects agents as one-document members idempotently with `[[agent]]` lock rows; cargo fmt/clippy -D warnings/test all green.",
    "notes": "Deferred by the requirements/satisfies reframe — more built-in kinds is the wrong direction; revive only if a story demands it. Shaped to the surface-language forms: one AGENT.md member document, an authored packages/agent/PACKAGE.md embedded like the rule/skill built-ins, no meta.toml, no contracts/agent.toml. On revival its shared-surface blast radius (lib/check/extract/import/main) serializes via blockedBy against any then-open entry touching those files."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship acquisition channels — npm optionalDependencies binary wrapper, Homebrew, cargo install — auto-detected, fail-loud on a missing platform binary",
    "per": {
      "path": "specs/50-distribution.md",
      "section": "Decision: acquisition rides the ecosystem's package managers"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs human release setup (npm org, Homebrew tap, signing/publish creds) before the mechanical wrappers can be validated end-to-end"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages, GitHub release assets, and the Homebrew tap; channel auto-detected."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`, confirmed on disk). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages (Biome's pattern) plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, `specs/50-distribution.md`). Reconcile with the flume manifest (a non-private publish entry, or a split package dir) rather than clobbering the harness manifest. Scoped/prefixed names per the resolved (project-name) fork."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger can acquire the prebuilt binary via npm (platform-specific optionalDependencies), Homebrew, standalone release assets, or `cargo install`; a missing platform binary is an install error, never a silent skip.",
    "notes": "Release infra, mostly outside `src/`; the cargo gates can't validate npm/Homebrew publish, and channels need human creds (npm org, tap, signing). NB: root `package.json` already exists as the *private* flume manifest — reconcile or split, never blindly create a second. Parked until a human sets creds up."
  }
]
