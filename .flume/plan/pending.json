[
  {
    "tag": "RETIRE-STALE-HEADING-QUOTES",
    "summary": "Strip the three stale spec-heading quotes the last sweep missed (drift.rs module doc + two SDK-pin cites; document.rs module doc) to the terse pointer-tag form",
    "per": {
      "path": ".claude/rules/rust.md",
      "section": "Style & structure"
    },
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "The module `//!` header (line 4) still quotes a non-existent heading `\"Decision: \\`re-add\\` is retired — hand-edits route to the source\"`; COMMENT-VERB-CITATION-SWEEP fixed the body cite at :589 but missed the header. Lines 152 and 244 quote `\"The SDK pins its engine version\"` as a `20-surface.md` heading — it is spec *prose* (`the SDK pins its engine`, 50-distribution.md:262 / 10-contracts.md:430), not a heading. Reduce all three to the terse file-path pointer-tag form (rust.md exit clause: cite the file path only; a quoted section title is a dangling reference waiting for a rename). Also cut the `re-add retired` era-narration parenthetical at :200 (era narration is commit-body material). Leave the live `place()` three-state descriptions (:16, :648, :676) untouched — those describe current code and ride the (place-three-state-retire) fork."
        },
        {
          "path": "src/document.rs",
          "description": "The module `//!` header (line 2) quotes `\"The member document — the surface language\"` as a `20-surface.md` heading; no such heading exists in the corpus. Reduce to the file-path pointer-tag form (rust.md exit clause). Comment-only; do not touch the still-live `+++`-header doc parser the corpus depends on (90-spec-system.md: migration-era headers)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "No comment in src/drift.rs or src/document.rs quotes a spec section title absent from specs/; the stale heading quotes are file-path pointer tags; cargo build / clippy -D warnings / fmt --check stay green (comment-only, no gate re-tests comment prose — the reason this residue persisted).",
    "notes": "Fifth and final tail of the comment-citation sweeps: the manifest/verb noun sweeps have bottomed out (remaining `manifest` = real npm/plugin JSON manifests + test-fixture labels; temper.toml/activation/altitude/rung/carriage all clean). Disjoint from PACKAGING-CHANNELS (package.json + release.yml). Live place() three-state descriptions deliberately left — see (place-three-state-retire)."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/architecture/50-distribution.md",
      "section": "Three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper` 0.0.3, single package, `(sdk-package-layout)`); channel 1's SDK foundation shipped. No `.github/workflows/release.yml` exists (only the `temper.yml` check job); root package.json is still the private flume manifest — nothing moved this window."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (50-distribution)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, 50-distribution). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (50-distribution 'Fail-loud delivery').",
    "notes": "Release infra, mostly outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm (`@dtmd/temper`, published 0.0.3); ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs still pins SDK `^0.0.2` (0.0.3 published) — a release-owned bump, not this sweep."
  }
]
