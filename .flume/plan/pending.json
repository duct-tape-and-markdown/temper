[
  {
    "tag": "VACATE-KIND-NOUN",
    "summary": "Rename the scalar value-type lattice `extract::Kind` off the kernel noun — `representation.md` makes `kind` the type of a member; the enum only enumerates field value types (string/number/list/map)",
    "per": {
      "path": "specs/model/representation.md",
      "section": "kind"
    },
    "gate": { "kind": "open" },
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/extract.rs",
          "description": "The definition site: `pub enum Kind` (line 38) with its `name`/`from_name` impl is the closed scalar/container value-type lattice (String/Integer/Number/Boolean/Null/List/Map). Rename the type off `Kind` (build owns the spelling — equal representation; it must NOT be `kind` and should read as a value type, e.g. `ValueType`). Update the `FeatureValue::kind()` accessor and `number_kind` helper only insofar as they return the renamed type — do not touch `FeatureValue`'s own list/map variant surface beyond the type reference. Refresh ONLY this renamed enum's own doc-comment `cite`s (they point at retired `specs/architecture/10-contracts.md`) onto `specs/model/representation.md` \"kind\" as they ride."
        },
        { "path": "src/schema.rs", "description": "~10 call sites of `extract::Kind` (the JSON-Schema `type`-name mapping for the lattice). Retarget the import and every `Kind::` reference to the renamed type." },
        { "path": "src/engine.rs", "description": "~11 `Kind::` call sites (the `type`-predicate evaluation reads a field's parsed value type). Retarget import + references." },
        { "path": "src/kind.rs", "description": "~20 `Kind::` references (value-type comparisons in the `Primitive`/`type` machinery — distinct from this file's own `Format`/`UnitShape`/`Registration` enums, which are unaffected). Retarget the value-type references only." },
        { "path": "src/builtin_kind.rs", "description": "~6 `extract::Kind` references. Retarget." },
        { "path": "src/contract.rs", "description": "Import + the `type` clause's declared-`Kind` reference (line ~95). Retarget." },
        { "path": "src/graph.rs", "description": "`extract::Kind` reference. Retarget." },
        { "path": "src/roster.rs", "description": "`extract::Kind` reference. Retarget." },
        { "path": "src/read.rs", "description": "`extract::Kind` reference (imported alongside `GenreValue`; leave `GenreValue` untouched — that is separate retired-vocabulary residue). Retarget the value-type reference only." },
        { "path": "tests/extract_equivalence.rs", "description": "References the renamed type in feature-extraction assertions; retarget. Any `insta` snapshot that renders the type name (extract_equivalence__*.snap) is re-accepted." },
        { "path": "tests/read_verbs.rs", "description": "Retarget value-type references." },
        { "path": "tests/graph.rs", "description": "Retarget value-type references." }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      { "path": "tests/extract_equivalence.rs", "asserts": "field value-type extraction still classifies scalars/containers identically under the renamed type; gates green." }
    ],
    "acceptance": "The scalar/container value-type lattice no longer bears the identifier `Kind`; `kind` is free for its kernel meaning (the type of a member, `representation.md`). `cargo build`, `cargo clippy -D warnings`, `cargo test` green.",
    "notes": "Foundational vacate — pure rename, no behavior. Frees the noun before the requirement/member-kind arc (embedded locus, requirement-as-kind, satisfier-set bug) lands, all un-derived from 0001. The enum derives `JsonSchema`+`TS`, so generated bindings and name-rendering snapshots move too. Broader retired vocab in these files (`genre`/`GenreValue`, `means`, `architecture/*` cites) is NOT this entry — it rides its own future opener."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper` 0.0.3, single package, `(sdk-package-layout)`); channel 1's SDK foundation shipped. No `.github/workflows/release.yml` exists (only the `temper.yml` check job); root package.json is still the private flume manifest — nothing moved this window."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra, mostly outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm (`@dtmd/temper`, published 0.0.3); ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs still pins SDK `^0.0.2` (0.0.3 published) — a release-owned bump, not this sweep."
  }
]
