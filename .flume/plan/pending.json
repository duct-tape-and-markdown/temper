[
  {
    "tag": "RESOLVE-KIND-UNITS-DEAD-OVERLAY",
    "summary": "resolve_kind_units re-derives compose::overlay_builtin_kind on a kind its own two callers already overlaid — dead per-kind work run twice per resolution",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "resolve_kind_units (591) opens with `let overlaid = overlay_builtin_kind(kind, declarations)?;` (598) — but its only two callers (`rg` confirmed) already pass a pre-overlaid kind: kind_units_and_features (673-680) overlays explicitly right before calling it, and assemble_lock_family's local-kind loop (909+) iterates `declared_kinds(committed)?.values()`, which overlays every builtin kind inside declared_kinds (524-536) before resolve_kind_units ever sees it. `CustomKind::overlay_templates`/`overlay_content` (kind.rs) replace wholesale rather than accumulate, so re-deriving reproduces the identical CustomKind — replace the re-derivation with `let overlaid = kind.clone();`. The `declarations` param stays: resolve_kind_units still needs it below (layout_edge_fields, declared_kinds, manifest_units)."
        },
        {
          "path": "src/compose.rs",
          "description": "Add an `OVERLAY_BUILTIN_KIND_COUNT` thread-local + `overlay_builtin_kind_count()` accessor beside `RESOLVE_KIND_UNITS_COUNT`/`resolve_kind_units_count()` (30-46), incremented inside `overlay_builtin_kind` (398) itself — the count-pin this fix ships, mirroring the existing precedent for the identical 'called twice per kind' bug shape one layer up."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/main.rs",
        "asserts": "kind_units_and_features resolved over one builtin kind calls overlay_builtin_kind exactly once (pre-fix: twice — once explicit in kind_units_and_features, once inside resolve_kind_units), the same doubled-call shape RESOLVE_KIND_UNITS_COUNT already pins one layer up (main.rs:743-765), applied to this layer"
      }
    ],
    "acceptance": "overlay_builtin_kind_count() delta for a single kind_units_and_features call over one builtin kind is 1, not 2. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Split off .flume/refactor/plan-overlay-builtin-kind-dedup.md (filed 17a1eb5, observed 29c5baf; re-verified at HEAD 17a1eb5 — no src/sdk/tests commits landed since). This is the capture's #4, the mechanical low-risk sub-fix the capture itself flagged as shippable alone regardless of the wider hoist. scoped at 17a1eb5."
  },
  {
    "tag": "OVERLAY-BUILTIN-KIND-SHARED-HOIST",
    "summary": "compose::overlay_builtin_kind is independently re-derived per builtin kind at five remaining call sites in one gate()/explain() run though declarations.kinds never changes across them",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "RESOLVE-KIND-UNITS-DEAD-OVERLAY"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "Compute the overlaid builtin-kind map (`BTreeMap<String, CustomKind>`) once per invocation, mirroring the `ManifestCache`/`build_manifest_cache` precedent (GATE-MANIFEST-SHARED-READ-HOIST). The natural hoist point sits earlier than any current consumer: `assemble_lock_family` (909) is called first (gate.rs:63), before builtin_defs/manifest_cache/either admissibility call exist — so `LockFamily` (struct at 356) likely grows a field carrying the map out, and assemble_lock_family's own local-kind loop, `declared_kinds` (524-536, called again independently by build_manifest_cache at 1159), and kind_units_and_features's explicit overlay call (673-679) all thread from the hoisted map instead of recomputing."
        },
        {
          "path": "src/gate.rs",
          "description": "Thread the hoisted map through the dispatch loop's kind_units_and_features call (137) and both admissibility calls (168 governs_collision_diagnostics, 176 local_locus_admissibility — called back-to-back with identical &builtin_defs/&declarations args) instead of each recomputing the overlay from builtin_defs+declarations."
        },
        {
          "path": "src/admissibility.rs",
          "description": "governs_collision_diagnostics (262) and local_locus_admissibility (209) each call compose::overlay_builtin_kind per builtin kind internally today — change their signature to accept the pre-overlaid map (from gate.rs) instead of re-deriving from builtin_defs/declarations."
        },
        {
          "path": "src/read.rs",
          "description": "The explain path's assemble_lock_family call (1554) is LockFamily's second consumer — thread the hoisted map out here too so the subsequent build_manifest_cache/builtin_units_and_features_by_kind calls reuse it rather than re-deriving, keeping explain and gate's cost shape identical."
        }
      ],
      "retire": []
    },
    "schemaDelta": "human-readable: LockFamily (src/compose.rs) gains a field carrying the shared overlaid builtin-kind map",
    "tests": [
      {
        "path": "src/main.rs",
        "asserts": "one full gate() run over a harness with N builtin kinds calls overlay_builtin_kind at most N times total (once per builtin kind), not once per the five call sites named in files[] — extends the overlay_builtin_kind_count() pin RESOLVE-KIND-UNITS-DEAD-OVERLAY ships, mirroring resolve_kind_units_count's own shape (main.rs:743-765)"
      }
    ],
    "acceptance": "overlay_builtin_kind_count() delta for one full gate()/explain() invocation equals the number of builtin kinds needing overlay, not that count times five; LockFamily carries the shared map to every consumer named in files[]. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Split off .flume/refactor/plan-overlay-builtin-kind-dedup.md (17a1eb5; observed 29c5baf; re-verified at HEAD — no src/sdk/tests commits since). Capture flagged the hoist point as a design call, not mechanical; files[] carries the sketch (mirrors the ManifestCache precedent, GATE-MANIFEST-SHARED-READ-HOIST) so build implements rather than invents it. blockedBy the narrow sub-fix, avoiding two open entries touching the same overlay_builtin_kind neighborhood at once. scoped at 17a1eb5."
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics — the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (⇒ 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (⇒ 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence — never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. `MAX_IMPORT_HOPS = 5` (75) sits under a doc (71-74) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' — a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (650) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds (677), one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts — hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it — 548 ('hop-capped at [`MAX_IMPORT_HOPS`]') and 647-648 ('capped at [`MAX_IMPORT_HOPS`] hops (the `at-import` recursion depth the format documents)') — carry the same claim and move with it, as does the propagation comment at 672-675 ('capped at the format's import recursion depth'). f6731fb (DIRECTIVE_FIELD consolidated into graph.rs ahead of this region, in the d1af9a5..HEAD window) shifted every address below its insertion point; re-read and confirmed at HEAD (0522eff), so every address above now carries at its new line."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1547) already pins the boundary mechanically rather than in a comment — `r1..r5` rescued from a live seed, `r6` dead as the sole finding — so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1548-1552) spells 'capped at the format's five-hop import recursion' (1550) and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number. Addresses re-read and confirmed at HEAD (0522eff) after upstream graph.rs/test-file shifts; they carry at these lines."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper — the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Park re-tested at 0522eff and holds — nothing ruled the hop semantics. f6731fb shifted graph.rs's const region from 59/55-58 to 75/71-74 (DIRECTIVE_FIELD moved in ahead of it); value and cite date unchanged (5, 2026-07-02). `files[]` line cites rewritten to match. The park's subject is untouched, and may still be wrong. A build-side friction capture records the same disagreement; the human's to read, not plan's to drain. rescoped 0522eff."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested on disk at 0522eff, holding on every clause: no v0.1 tag (only era tags), crate 0.1.0, release.yml still states the darwin + channel-3 deferral verbatim. `git diff 68fae5c..HEAD -- .github/` is empty. scoped b676656."
  }
]
