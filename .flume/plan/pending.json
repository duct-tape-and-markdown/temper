[
  {
    "tag": "LOCK-ROW-PATHS-HARNESS-RELATIVE",
    "summary": "`emit --into` bakes the cwd prefix into every lock row and reads rows back raw, so a committed lock resolves only from the harness root — a wrong-cwd emit corrupts 20 rows at exit 0",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "The lock"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "Both faces of the row-path contract, which today silently agree on the wrong base. WRITE: `harness_root` is `workspace_dir.parent()` (839) — a cwd-relative path — and `source_path = harness_root.join(member_projection_path(...))` (955) carries that prefix into every provenance/rollup row (`to_lock_path(&projection.source_path)` at 1144, `to_lock_path(path)` at 1159), into the reap's `owned_paths` (1063) and `layout_paths` (976), and into the manifest keys (1069). READ: rows come back raw — `fs::read(&row.source_path)` in `source_dep_stale` (2174), `fs::remove_file(&row.source_path)` in the reap (1173) — so nothing joins them under the targeted root. The two families already disagree inside one emit pass: include rows go through `harness_relative` (1003), member rows do not. Spell every row harness-root-relative through the existing `harness_relative` (1508) — the one home for the transform, never a second — and keep the joined path as the I/O target, the split `resolve_source_dependency(&host, &relative, harness_root, ...)` (1005) already models. Readers join the row under `harness_root` rather than reading it raw. `normalize_lock_path` (608) stays the canonicalizer on both sides of the reap join (decision 0024's robust read)."
        },
        {
          "path": "tests/emit.rs",
          "description": "The test home for the compiler tier (module doc, 1-12). Most cases pass an absolute tempdir `into` (`harness.join(\".temper\")`, 69) — the very shape that emits cwd-prefixed rows today — while the `./`-lock reap case (~1118) is the one that serializes on the module's cwd mutex, chdirs to the harness, and passes a relative `into` (1129) to get harness-relative rows. That mutex is the established pattern for any new case that must fix the process cwd; a case driving `drift::emit` at a tempdir workspace needs no chdir at all, since a tempdir is already outside cwd. The assertion at 1139 (`source_path = \"./.claude/rules/rust.md\"`) pins today's relative spelling and stays green."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/emit.rs",
        "asserts": "emitting a workspace whose harness root is not the process cwd writes byte-identical lock rows to the same payload emitted with cwd = the harness root — no row carries a cwd prefix — and reaps nothing"
      },
      {
        "path": "tests/emit.rs",
        "asserts": "a re-emit of a workspace from a foreign cwd leaves a live projection the prior lock owns unreaped: the reap join resolves rows under the targeted root, never cwd"
      }
    ],
    "acceptance": "`emit --into <dir>` writes the same lock bytes from any cwd as the same emit run with cwd = the harness root; every provenance and rollup row is harness-root-relative, and the reap join plus source-dependency staleness resolve rows under the targeted root. The lock stays resolvable by a verb aimed at a harness at an explicit path (pipeline.md, 'The lock': the gate and every read verb consume committed artifacts plus the lock, offline). Both committed locks (`.temper/lock.toml`, `examples/base-harness/.temper/lock.toml`) are byte-unchanged — each was emitted with cwd = its own root, so the correct spelling is already on disk. `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Confirmed on disk at c370924, both faces read. Field report: .flume/friction/build-emit-into-resolves-source-paths-from-cwd.md (`--into examples/base-harness/.temper` from the repo root exits 0, re-bases 20 rows). The inbox line's refusal alternative is rejected — `--into`'s only non-default use IS naming a workspace off cwd, so refusing it guts the flag. `common::write_lock` passes no members, so no fixture fan-out. scoped at c370924"
  },
  {
    "tag": "SDK-BLOCKS-FILE-REFUSAL",
    "summary": "`file()` composed inside `blocks()` crashes with a raw TypeError from `resolveMemberLeaves` — `blocks()`'s own type already excludes `File`, so the constructor refuses loudly instead of crashing",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "Emit"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/prose.ts",
          "description": "`blocks(...values: (Text | EmbeddedMemberValue)[])` (242) is the refusal's home: its declared parameter type already excludes `File` (130), so the guard states at runtime what the type states at compile time rather than inventing a new rule. `text` (205) is the pattern to mirror — it throws on a reserved marker because 'a stray one is a loud authoring error, not a silent mis-split' — with the same posture here: name the offending block's index and what `blocks()` admits (a `text` span or an embedded member value), and point a file-bodied author at the two homes that already exist — `file()` as a member's whole `Prose` body (the `Prose = File | Text | Blocks` union, 170), or `include()` (232) interpolated into a `text` span when a file's bytes belong inside a composed body, the path the lock fingerprints as a dependency. RIDER — the open-questions record 'Pre-recut vocabulary survives in sdk/src/prose.ts's doc comments' names this file and this entry is its carrier: reconcile the doc-comment narration while here — `law 5` (6, 141, 258), `law 8` (11), `posture N` (126, 156, 161, 188, 238), and the two cites to files that no longer exist, `15-kinds.md` (126) and `20-surface.md` (200). All ten re-verified on disk at c370924, and the `blocks()` doc at 238 is one of them. Doc comments only: no symbol or behavior moves with them."
        },
        {
          "path": "sdk/test/refusals.test.ts",
          "description": "The declare-side refusal home (module doc, 1-14: 'a broken source yields no output, never silent bytes'), already importing `blocks`, `text`, and `embeddedMemberValue` (19). The new case lands beside the existing fifteen rather than opening a file of its own."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/refusals.test.ts",
        "asserts": "a `file()` value composed inside `blocks()` throws a typed authoring error naming the offending block's index and what `blocks()` admits — never a raw TypeError, and never a silently rendered member fence"
      }
    ],
    "acceptance": "`blocks(file(...))` refuses at the constructor with a typed error an author can act on, so the runtime holds the line its parameter type already draws and no authored input reaches `resolveMemberLeaves` with absent `leaves` (intent.md invariant 6, 'Loud or nothing': a failure temper can detect is an error message at author-time — no path silently degrades). `file()` as a whole body and `include()` inside a `text` span both keep working unchanged. `pnpm --dir sdk test` (strict tsc + node --test) green.",
    "notes": "Confirmed at c370924. Whether the spelling is legal — the inbox line's first question — the SDK's own type answers: `blocks` (242) admits only `Text | EmbeddedMemberValue`, so strict tsc rejects it and only a JS or cast caller reaches the crash, via `isTextSpan` (251) → `renderMemberBlock` (emit.ts:353) → `resolveMemberLeaves` (288). Legalizing it would mint a second, unfingerprinted home for `include()`'s job. scoped at c370924"
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics — the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (⇒ 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (⇒ 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence — never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. All cites re-verified unmoved at 8913b59. `MAX_IMPORT_HOPS = 5` (65) sits under a doc (61-64) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' — a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (627) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds (654), one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts — hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it — 525 ('hop-capped at [`MAX_IMPORT_HOPS`]') and 624 ('capped at [`MAX_IMPORT_HOPS`] hops (the `at-import` recursion depth the format documents)') — carry the same claim and move with it, as does the propagation comment at 649-652 ('capped at the format's import recursion depth')."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1356, re-verified unmoved at 8913b59) already pins the boundary mechanically rather than in a comment — `r1..r5` rescued from a live seed, `r6` dead as the sole finding — so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1357-1361) spells 'capped at the format's five-hop import recursion' (1359) and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper — the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Park re-tested at 8913b59 and holds — nothing ruled the hop semantics; the 8913b59..c370924 window touches no import mechanics (`git diff 8913b59..c370924 -- src/` is empty). `MAX_IMPORT_HOPS` still reads 5 at 65 under a cite claiming five. Stays parked: the constant may still be wrong. rescoped 4a33911; re-cited 53519b9; re-verified 8913b59."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested on disk at 8913b59, holding on every clause: `git tag -l` carries only the four era tags and no version tag, crate 0.1.0 vs npm 0.0.7, and release.yml:7-9 still states the darwin + channel-3 deferral verbatim. The 8913b59..c370924 window touched no release surface. scoped at b676656."
  },
  {
    "tag": "TEST-MCP-FIXTURE-WRITER-ONE-HOME",
    "summary": "The `.mcp.json` fixture writer is the manifest locus the last fold missed — `write_mcp` in one file, a `{}`-hardwired `write_mcp_json` in another, while `tests/common` now homes every sibling locus",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/common/mod.rs",
          "description": "The home, and it now holds this exact job for every manifest locus but one: `write_plugin_json` (157) at `.claude-plugin/plugin.json`, `write_marketplace_json` (164) at `.claude-plugin/marketplace.json`, `write_settings` (170) at `.claude/settings.json` — each a one-line compose of `write_sibling` (141), the create-parents-then-write primitive eb9674c generalized here out of `memory_gate`. `.mcp.json` is the locus left out, though `mcp-server` is a shipped built-in whose governed locus is exactly the harness root's `.mcp.json` (`src/builtin_kind.rs`; `tests/mcp_server_kind.rs` module doc, 1-12). Add one `write_mcp_json(root, body)` beside `write_marketplace_json`, composing `write_sibling` like every sibling — the path is root-flat, so `write_sibling`'s `create_dir_all(parent)` is a no-op over an existing root and no second primitive is warranted — and carry the locus citation both copies already state (`.claude/rules/rust.md`, 'Harness-input fixtures mirror the real Claude Code layout'). Body stays a parameter, never hardwired: the `{}` caller passes `{}`."
        },
        {
          "path": "tests/mcp_server_kind.rs",
          "description": "`write_mcp` (49-51) with its doc comment (46-48) — `fs::write(root.join(\".mcp.json\"), body)`, the body-passthrough copy. Folds onto the home; its four callers (76, 106, 132, 150) keep their `BROKEN_MCP`/`CLEAN_MCP` bodies verbatim, so a behavior change at the folded surface surfaces as a failure rather than a re-blessed expectation."
        },
        {
          "path": "tests/coverage_note.rs",
          "description": "`write_mcp_json` (52-54) with its doc comment (48-51) — the second copy, the same job with the body hardwired to `{}`: the exact shape `write_settings_json` had here before eb9674c folded it, and the sibling that pass left standing. Its three callers (248, 268, 285) reach the home. The doc comment's own context — that `.mcp.json` is the wholly-ungoverned probe only when the kinds handed in carry no `mcp-server` row — is this caller's context, so it stays at the call site rather than being ported into `tests/common`. NOT folded: `write_skill` (36) already composes `common::write_skill` and only builds a body, which is the composition this entry asks for everywhere else."
        },
        {
          "path": "tests/manifest_adapter.rs",
          "description": "The one inline harness-root site: `fs::write(harness.join(\".mcp.json\"), MANIFEST)` (130), inside `read_kind_routes_a_manifest_kind_through_its_governs_locus` (125) — a real governs-locus fixture at a harness root, so it reaches the home. The file's four other `.mcp.json` writes (45, 84, 102, 116) are OUT and stay inline: each binds `let file = dir.join(\".mcp.json\")` to hand to `Manifest::read(&file, ...)`, a path-taking API under test — the path is the argument, not a harness locus, and a writer returning nothing does not serve them."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/common/mod.rs",
        "asserts": "no new assertion — this is test scaffolding, and the proof is the existing suite unchanged and green: every folded site keeps its current assertions and fixture bodies verbatim, so a behavior change at any folded surface surfaces as a failure rather than a re-blessed expectation"
      }
    ],
    "acceptance": "The `.mcp.json` fixture writer lives in `tests/common` beside its sibling locus writers and no private per-file copy survives (engineering.md, 'One job, one home': test scaffolding lives in one home, never copy-pasted per file; generalize the near-duplicate before adding new). Mechanically: `rg -n '^fn write_mcp' tests/*.rs` returns nothing, `tests/common/mod.rs` holds one `.mcp.json` writer composing `write_sibling`, and the manifest-locus family there covers all four shipped manifest loci. `cargo test`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all --check` green.",
    "notes": "Same rule as eb9674c, the axis it left: `settings.json`'s pair-shape exactly — a body-passthrough copy plus a `{}`-hardwired copy — at the last manifest locus with no home. NOT folded, and 8913b59's ruling holds on re-read: `memory_gate::write_claude_md` (45, pads to N lines) vs `requirement_roster::write_claude_md` (749, verbatim body) stay different jobs behind a colliding name, sharing one `fs::write` line. scoped at c370924"
  }
]
