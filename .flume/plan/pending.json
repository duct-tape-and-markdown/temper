[
  {
    "tag": "APPLY-REEMIT-PROJECTION",
    "summary": "`apply` re-emits the projection deterministically from the member document — retire the surgical frontmatter-patch path the re-emit Decision supersedes",
    "per": {
      "path": "specs/20-surface.md",
      "section": "Decision: the projection is re-emitted; the surface is patched"
    },
    "gate": { "kind": "open" },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "Replace `project_bytes`'s patch branch with deterministic full-file re-emission: render every desired field (typed fields then `extra`, in the order the existing `*_fields` builders already produce) into a fresh `---`-delimited frontmatter block via `render_field`, then append the surface body byte-for-byte — for every artifact, whether or not disk had frontmatter. Retire the surgical-patch machinery now unused: `SourceSplit`/`split_source`, `patch_frontmatter`, `Segment`/`parse_segments`, and any `parse_frontmatter_json` helper. This is the superseded '(yaml-writeback)' path (specs/20-surface.md Decision, Rejected: 'surgically patching the projection to preserve hand edits'). Leave the three-state classification, conflict detection, and lock-fingerprint advance unchanged — only the desired-bytes computation changes. Custom-kind (spec) projection rides the same `project_bytes`, so it is covered without touching kind.rs."
        },
        {
          "path": "src/main.rs",
          "description": "Rewrite the `Command::Apply` doc comments (today 'patching only changed fields' / 'patch only changed fields back onto ... provenance path') to the re-emit model: apply re-emits the projection deterministically, byte-stable and idempotent; a direct edit to the projection is drift for `re-add` to lift, never something apply patches around."
        },
        {
          "path": "tests/apply.rs",
          "description": "Rewrite `a_surface_edit_patches_only_the_changed_field` and its 'patch-not-re-emit fidelity' framing to assert the re-emit contract: a surface edit regenerates the whole projection deterministically, and on-disk-only frontmatter comments/extra bytes are NOT preserved (that is drift, `re-add`'s to reconcile). Keep and strengthen the idempotence (`apply_is_idempotent_over_a_clean_surface`, `successive_surface_edits_each_apply_cleanly`) and the world-drift-conflict test — apply twice yields byte-identical output, and a world drift still surfaces a conflict rather than clobbering."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/apply.rs",
        "asserts": "apply re-emits the whole projection deterministically from the member document (byte-stable, idempotent); a surface field edit regenerates the frontmatter rather than surgically patching it; on-disk-only frontmatter bytes are dropped; a world drift still surfaces a conflict instead of clobbering."
      }
    ],
    "acceptance": "`temper apply` regenerates each projection file full-file from its member document (deterministic frontmatter emission + byte-for-byte body), idempotent on re-run; the `split_source`/`patch_frontmatter`/`parse_segments` surgical path is gone; world-drift still conflicts rather than clobbers; cargo fmt/clippy -D warnings/test all green.",
    "notes": "Code still runs the superseded '(yaml-writeback)' patch model; the live Decision re-emits the projection (it is generated — nothing human to preserve; direct edits are `re-add`'s drift). Net simplification: `render_field` already emits fields and the drift field-builders already order them, so emit all and delete the segment/patch machinery. Sole open entry — drift.rs/main.rs/tests only, disjoint from the parked/deferred ones."
  },
  {
    "tag": "AGENT-KIND",
    "summary": "Add the `agent` built-in harness kind — `.claude/agents/*.md` imported as one AGENT.md member document, checked by an authored agent package",
    "per": {
      "path": "specs/20-surface.md",
      "section": "Artifact kinds & package binding"
    },
    "gate": {
      "kind": "deferred",
      "reason": "more built-in kinds is the wrong direction under the requirements/satisfies reframe; the surface-language chain it waited on has shipped, so the blocker is priority alone — revive only if a story demands the agent kind"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/agent.rs",
          "description": "The Agent IR — the typed model for a Claude Code subagent at `.claude/agents/<name>.md`, mirroring `src/rule.rs`: typed YAML frontmatter on the projection side (`name`, `description`, optional `tools`/`model`), unknown keys preserved, content-faithful body, provenance. Identity is the path (file stem). The surface member is one AGENT.md document (clause-module header) like every kind."
        },
        {
          "path": "packages/agent/PACKAGE.md",
          "description": "The agent kind's authored built-in package (clauses + guidance) in the one-document medium, embedded with the std-lib by build.rs (the packages/ product tree — NOT a contracts/agent.toml). Clauses: `name` required + `allowed_chars` `[a-z0-9-]`; `description` required; `forbidden_keys` for the Cursor `.mdc` keys Claude Code ignores; an advisory `max_lines`."
        },
        {
          "path": "tests/agent_contract.rs",
          "description": "Fixture-driven proof mirroring `tests/rule_contract.rs`: one broken agent per clause proving it fires, a clean agent that trips nothing, and an insta import round-trip proving idempotence."
        }
      ],
      "edit": [
        {
          "path": "src/lib.rs",
          "description": "Add `pub mod agent;` beside the other artifact-kind modules (this entry owns the line)."
        },
        {
          "path": "src/check.rs",
          "description": "Extend `Workspace` with `agents` loaded under `<dir>/agents/*` and a `WorkspaceError` arm; update the load tests."
        },
        {
          "path": "src/import.rs",
          "description": "Discover `.claude/agents/*.md`, project each to one `<into>/agents/<name>/AGENT.md` member document, add an `[[agent]]` lock row per agent."
        },
        {
          "path": "src/main.rs",
          "description": "Bind the agent package, wire admissibility + conformance streams and `by_kind` for the kind; feed agent features via the embedded-KIND extraction driver (post-BUILTIN-EXTRACT-GENERIC path, not a hand-coded feature fn)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/agent_contract.rs",
        "asserts": "each decidable clause of the agent package fires on its broken fixture and stays silent on a clean agent; import of an unchanged agent is idempotent (insta snapshot)."
      }
    ],
    "acceptance": "`temper check` dispatches `.claude/agents/*.md` to the agent package (conformance) and that package passes admissibility; `temper import` projects agents as one-document members idempotently with `[[agent]]` lock rows; cargo fmt/clippy -D warnings/test all green.",
    "notes": "Deferred — more built-in kinds is the wrong direction under the requirements/satisfies reframe; revive only if a story demands it. On revival, add the agent KIND definition under kinds/ (embedded like skill/rule) and serialize its shared-file blast radius (lib/check/import/main) via blockedBy against any then-open entry touching those files."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship acquisition channels — npm optionalDependencies binary wrapper, Homebrew, cargo install — auto-detected, fail-loud on a missing platform binary",
    "per": {
      "path": "specs/50-distribution.md",
      "section": "Decision: acquisition rides the ecosystem's package managers"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs human release setup (npm org, Homebrew tap, signing/publish creds) before the mechanical wrappers can be validated end-to-end"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages, GitHub release assets, and the Homebrew tap; channel auto-detected. Distinct from the existing `.github/workflows/temper.yml` CI job."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`, confirmed on disk). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages (Biome's pattern) plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, `specs/50-distribution.md`). Reconcile with the flume manifest (a non-private publish entry, or a split package dir) rather than clobbering the harness manifest. Scoped/prefixed names per the resolved (project-name) fork."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger can acquire the prebuilt binary via npm (platform-specific optionalDependencies), Homebrew, standalone release assets, or `cargo install`; a missing platform binary is an install error, never a silent skip.",
    "notes": "Release infra, mostly outside `src/`; the cargo gates can't validate npm/Homebrew publish, and channels need human creds (npm org, tap, signing). NB: root `package.json` already exists as the *private* flume manifest — reconcile or split, never blindly create a second. Parked until a human sets creds up."
  },
  {
    "tag": "COMMUNITY-DOCS",
    "summary": "Add CONTRIBUTING.md and SECURITY.md — the two missing launch community docs (two-sided AI-authorship disclosure; vuln-reporting evidence bar)",
    "per": {
      "path": "specs/55-offering.md",
      "section": "The community surface — small, honest, gated"
    },
    "gate": {
      "kind": "parked",
      "reason": "build's writable fence (.flume/chain.ts BUILD_WRITABLE_PATHS) lists only README/CHANGELOG/AGENTS/LICENSE among root docs — no root CONTRIBUTING.md/SECURITY.md glob — so build would bail; a human must widen the fence (chain.ts is human territory) and enable private vulnerability reporting (a repo setting) before build can author these"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "CONTRIBUTING.md",
          "description": "Small and honest per the Decision: how to file a good issue, 'search first', PRs by prior discussion, and the two-sided AI-authorship policy (inbound AI-assisted contributions welcome WITH disclosure; the contributor must understand and defend the change without the assistant). Expectation-setting, not an invitation to co-maintain."
        },
        {
          "path": "SECURITY.md",
          "description": "Vulnerability-reporting policy with an explicit evidence bar — a report must demonstrate, not speculate (the 'unverified AI-generated vuln reports are the new spam' lesson). Points at private vulnerability reporting (the GitHub repo setting a human enables)."
        }
      ],
      "edit": [],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "CONTRIBUTING.md and SECURITY.md exist at root, matching specs/55's launch-set Decisions (two-sided disclosure; demonstrate-not-speculate); the fence has been widened so build's entry-fence preflight admits them.",
    "notes": "Launch set (specs/55). Already on disk: README, AGENTS.md, CHANGELOG.md, LICENSE-MIT/APACHE, issue forms (.github/ISSUE_TEMPLATE/{bug,feature,config}.yml). Missing only CONTRIBUTING.md + SECURITY.md. Both are root-level per the spec but outside build's root-docs fence — parked on a human fence-widen (chain.ts) + enabling private reporting. Parked entries are skipped by the entry-fence gate, so this files cleanly."
  }
]
