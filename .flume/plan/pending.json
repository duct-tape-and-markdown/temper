[
  {
    "tag": "DECLARATIONS-ZERO-CONSUMER-EXPORTS-PRUNE",
    "summary": "`mentionScope` and `SEAM_VERSION` (sdk/src/declarations.ts) are exported with zero callers anywhere in sdk/src or sdk/test \u2014 each used only inside its own defining file",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "An export earns its consumer"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/declarations.ts",
          "description": "Drop the `export` keyword from `mentionScope` (849, called only internally at 884's `nestedMemberRows` call) and from `SEAM_VERSION` (891, read only internally at 900's `encodeSeam`); `rg 'mentionScope|SEAM_VERSION' sdk/src sdk/test` (excluding declarations.ts's own definitions and doc-comment @link mentions) finds no import or call site anywhere else. Both stay module-private; no behavior change. Addresses re-read and shifted -24 by DECLARATIONS-TELEMETRY-HOOK-PROVIDER-FACE-MOVE (a6db2b5), which removed the TELEMETRY_EVENT_HOOKS block ahead of this region; confirmed at HEAD (4ab6fe2)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "`export` is gone from `mentionScope` and `SEAM_VERSION` in sdk/src/declarations.ts; `rg 'export function mentionScope|export const SEAM_VERSION' sdk/src/declarations.ts` finds nothing. `pnpm --dir sdk test` green, unchanged behavior.",
    "notes": "Zero-consumer, grep-verified over sdk/src + sdk/test excluding declarations.ts's own definitions. SDK root also excludes both from its public surface (sdk/test/emit.test.ts:683 asserts SEAM_VERSION/compileDeclarations absent). Blocker DECLARATIONS-TELEMETRY-HOOK-PROVIDER-FACE-MOVE shipped (19258b7/a6db2b5), verified clean \u2014 entry unblocked; cited lines rescoped -24 for that move's shift. scoped at 4ab6fe2."
  },
  {
    "tag": "GUIDANCE-KIND-SDK-LOCK-FACT",
    "summary": "0045 frees guidance from the clause for a kind: give `KindFacts` an advisory `guidance`/`cite` pair and serialize it as a new `KindFactRow` lock column",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "DECLARATIONS-ZERO-CONSUMER-EXPORTS-PRUNE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "Add `readonly guidance?: string` and `readonly cite?: string` to `KindFacts` (168-209) \u2014 advisory authoring counsel for the kind as a whole (decision 0045), no predicate, no severity, mirroring the doc comment `Contract.guidance` already carries at src/contract.rs:32-36 (unwired \u2014 see GUIDANCE-KIND-CONTRACT-SCHEMA-EXPLAIN)."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "`kindFactRow()` (294-315) copies `facts.guidance`/`facts.cite` into the returned row's new `guidance`/`cite` fields, alongside its existing flat fact copies."
        },
        {
          "path": "src/drift.rs",
          "description": "Add `guidance: Option<String>` and `cite: Option<String>` to `KindFactRow` (3045-3106), the tolerant-round-trip optional-fact pattern every other column takes; extend `KindFactRow::to_table`/`from_table` (3953-4015) with the same `if let Some(...) table.insert(...)` / `opt_str(table, ...)` pair `ClauseRow`'s own guidance/cite columns already use (~3229)."
        },
        {
          "path": "tests/seam_bindings_current.rs",
          "description": "Rerun with `BLESS_SEAM_BINDINGS=1` to regenerate `sdk/src/generated/KindFactRow.ts` with the two new fields (doc comment 1-8 names the bless procedure); commit the diff."
        }
      ],
      "retire": []
    },
    "schemaDelta": "KindFactRow (lock row) gains two optional string columns: `guidance`, `cite`.",
    "tests": [
      {
        "path": "src/drift.rs",
        "asserts": "a KindFactRow with guidance+cite round-trips through to_table/from_table unchanged; a row declaring neither omits both columns, the tolerant round-trip every other optional fact takes"
      }
    ],
    "acceptance": "`KindFacts.guidance`/`cite` exist in the SDK and serialize into a committed lock's kind-fact row; `sdk/src/generated/KindFactRow.ts` and `src/drift.rs`'s `KindFactRow` agree (bindings test green). No reader consumes the new columns yet. `cargo test` and `pnpm --dir sdk test` green.",
    "notes": "(a) of the inbox's guidance-channel split (0045; contract.md 'clause'). `Contract.guidance` (src/contract.rs:32-36) already exists but is dead: every construction site sets it `None`, nothing reads it. blockedBy DECLARATIONS-ZERO-CONSUMER-EXPORTS-PRUNE \u2014 both edit sdk/src/declarations.ts (pending-entry rule, 'Disjoint, or serialized'), that entry smaller and already pickable. scoped at 946e303."
  },
  {
    "tag": "GUIDANCE-KIND-CONTRACT-SCHEMA-EXPLAIN",
    "summary": "Thread a kind's lock-carried guidance into the long-dead `Contract.guidance` and deliver it: `schema`'s top-level `description`, `explain`'s governing-contract narration",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GUIDANCE-KIND-SDK-LOCK-FACT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "`default_contract_from_rows` (190-203) and `builtin_contract` (1238-1245) each gain a `kinds: &[KindFactRow]` parameter, look up the row named `kind`, and set `Contract.guidance` from its `.guidance` \u2014 currently hardcoded `None` at line 202 (and inherited via 1240), the same per-kind lift `clause_from_row` already runs per-clause."
        },
        {
          "path": "src/builtin.rs",
          "description": "`contract_for_kind` (19-23) passes `&builtin_lock::declarations().kinds` through, so an embedded kind's own SDK-authored guidance (GUIDANCE-BUILTIN-NOTES-PROMOTE) reaches `Contract.guidance`; its doc comment (17-18), which claims the constructed contract's guidance 'stays `None`', is corrected."
        },
        {
          "path": "src/gate.rs",
          "description": "Three `default_contract_from_rows`/`builtin_contract` call sites (149, 225, 308) pass `&declarations.kinds` alongside the `&declarations.clauses` each already threads."
        },
        {
          "path": "src/main.rs",
          "description": "Four call sites (283, 289, 485, 501) pass `&declarations.kinds` alongside `&declarations.clauses`."
        },
        {
          "path": "src/read.rs",
          "description": "Two call sites (1591, 1607) pass `&declarations.kinds`; `narrate_governing_contract` (374-402) prints the kind's `contract.guidance`, when present, as an authoring-counsel line alongside the existing clause-label bullets."
        },
        {
          "path": "src/admissibility.rs",
          "description": "`joined_kind_admissibility` (362-375) judges kinds this corpus declares none of, so it has no corpus-owned kinds list to source guidance from \u2014 passes `&[]`, leaving `Contract.guidance` `None` there rather than forcing an unresolvable lookup."
        },
        {
          "path": "src/schema.rs",
          "description": "`emit()` (51-208) inserts a top-level `description` key from `contract.guidance` (never read today), assembled alongside `$schema`/`type` (186-207) under the same 'docs channel, never mixed into validation' discipline the per-field loop (156-172) already holds."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/schema.rs",
        "asserts": "a contract with kind-level guidance emits a top-level `description`; absent guidance emits none, and it never leaks into `properties`/`required`/`allOf`"
      },
      {
        "path": "tests/read_verbs.rs",
        "asserts": "`explain` narrates a kind's authored guidance when its governing contract carries one, and omits the line when absent"
      }
    ],
    "acceptance": "`temper schema --kind X` for a kind with authored guidance carries a top-level `description`; `temper explain` narrates it. Every `default_contract_from_rows`/`builtin_contract` caller compiles against the widened signature. `cargo test` green.",
    "notes": "(b) of the inbox's split (0045; contract.md 'clause'). Every calling site but one already holds `declarations.kinds` beside `declarations.clauses` (gate.rs/main.rs/read.rs/builtin.rs); admissibility.rs's foreign-kind path is the exception, scoped to `&[]`. blockedBy GUIDANCE-KIND-SDK-LOCK-FACT for the row column this reads. scoped at 946e303."
  },
  {
    "tag": "GUIDANCE-BUILTIN-NOTES-PROMOTE",
    "summary": "Promote the six built-in kinds' stranded 'authoring notes the clauses cannot carry' JSDoc paragraphs in sdk/src/builtins.ts into real kind-attached `guidance`",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GUIDANCE-KIND-CONTRACT-SCHEMA-EXPLAIN"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/builtins.ts",
          "description": "Move each of the six kind-wide stranded paragraphs into its kind()'s new `guidance` fact, trimming the doc comment to its non-stranded remainder: `plugin-manifest` (paragraph 705-706, kind() 669-676), `marketplace` (1049-1050, kind() 990-997), `skill` (1279-1280, kind() 182-190), `rule` (1467-1468, kind() 335-341), `memory` (1515-1528, kind() 357-361), `hook` (1595-1596, kind() 397-404)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/builtins.test.ts",
        "asserts": "each of the six kinds' `KindFacts.guidance` matches its promoted authoring note, retrievable off the kind's `facts`"
      }
    ],
    "acceptance": "`rg 'Authoring notes the clauses cannot carry|What the clauses cannot carry, as guidance' sdk/src/builtins.ts` finds nothing \u2014 each note lives as its kind's `guidance` fact, visible through `schema`/`explain`. `pnpm --dir sdk test` green.",
    "notes": "(c) of the inbox's split (0045 Consequences: 'the built-in kinds' JSDoc authoring notes move into the channel'). All six confirmed kind-wide, none field-scoped \u2014 the field-declaration half has no consumer among them (GUIDANCE-FIELD-DECLARATION-CHANNEL, deferred). blockedBy GUIDANCE-KIND-CONTRACT-SCHEMA-EXPLAIN so the migration ships already-visible, not silently inert. scoped at 946e303."
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics \u2014 the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (\u21d2 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (\u21d2 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence \u2014 never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. `MAX_IMPORT_HOPS = 5` (75) sits under a doc (71-74) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' \u2014 a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (650) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds (677), one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts \u2014 hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it \u2014 548 ('hop-capped at [`MAX_IMPORT_HOPS`]') and 647-648 ('capped at [`MAX_IMPORT_HOPS`] hops (the `at-import` recursion depth the format documents)') \u2014 carry the same claim and move with it, as does the propagation comment at 672-675 ('capped at the format's import recursion depth'). f6731fb (DIRECTIVE_FIELD consolidated into graph.rs ahead of this region, in the d1af9a5..HEAD window) shifted every address below its insertion point; re-read and confirmed at HEAD (0522eff), so every address above now carries at its new line."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1547) already pins the boundary mechanically rather than in a comment \u2014 `r1..r5` rescued from a live seed, `r6` dead as the sole finding \u2014 so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1548-1552) spells 'capped at the format's five-hop import recursion' (1550) and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number. Addresses re-read and confirmed at HEAD (0522eff) after upstream graph.rs/test-file shifts; they carry at these lines."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper \u2014 the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Park re-tested at ac40c72 and holds \u2014 nothing ruled the hop semantics. f6731fb shifted graph.rs's const region from 59/55-58 to 75/71-74 (DIRECTIVE_FIELD moved in ahead of it); value and cite date unchanged (5, 2026-07-02). `files[]` line cites rewritten to match. The park's subject is untouched, and may still be wrong. A build-side friction capture records the same disagreement; the human's to read, not plan's to drain. rescoped 0522eff."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary\u2194npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships \u2014 three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) \u2014 no signing creds \u2014 but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested on disk at ac40c72, holding on every clause: no v0.1 tag (only era tags), crate 0.1.0, release.yml still states the darwin + channel-3 deferral verbatim. `git diff a23269d..HEAD -- .github/` is empty. scoped b676656."
  },
  {
    "tag": "GUIDANCE-FIELD-DECLARATION-CHANNEL",
    "summary": "0045 also frees guidance onto a field declaration, no predicate \u2014 deferred: none of the six promoted built-in notes targets a single field, so there is no consumer yet",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "deferred",
      "reason": "0045's Consequences name field-declaration guidance beside kind-level, but the SDK's only per-field metadata today is a clause's `field:` target (predicate-bound) or `EdgeField` (edge-typed only) \u2014 no 'field declaration' list a bare advisory could attach to. None of the six promoted notes (GUIDANCE-BUILTIN-NOTES-PROMOTE) target a single field; inventing the mechanism now is speculative scope with no kind to exercise it ('An export earns its consumer', engineering.md). Revive when a field wants counsel no clause carries."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "Anticipated: a field-name-keyed guidance fact on `KindFacts`, analogous to `EdgeField`'s field-name-keyed list (196) but for arbitrary fields \u2014 shape unset; no field in any built-in kind wants one yet."
        },
        {
          "path": "src/drift.rs",
          "description": "Anticipated: a `KindFactRow` column (or sibling row) carrying per-field guidance/cite, following the `to_table`/`from_table` pattern GUIDANCE-KIND-SDK-LOCK-FACT establishes for the kind-level pair."
        },
        {
          "path": "src/schema.rs",
          "description": "Anticipated: `emit()`'s per-property `description` (156-172) folds field-declaration guidance in alongside a clause's, the same 'join into one description' discipline `push_description` (316-329) already runs for several guided clauses on one field."
        },
        {
          "path": "src/read.rs",
          "description": "Anticipated: field-level narration extends `narrate_governing_contract` (374-402) or a sibling to print a field's declared guidance."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A field declaring `guidance` (no predicate) surfaces it via `schema` hover and `explain`, never gating \u2014 provable once a real kind needs one.",
    "notes": "0045 Consequences name field-declaration guidance beside kind-level; this waits for a driver, the same 'no consumer' bar prior deferred entries in this queue's history have held to. scoped at 946e303."
  }
]
