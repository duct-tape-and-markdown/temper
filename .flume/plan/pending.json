[
  {
    "tag": "ACCEPTANCE",
    "summary": "Slice-1 acceptance: insta snapshots of import + check over a trimmed coordinate fixture, plus the import-idempotence (no-diff) check.",
    "per": {
      "path": "spec/RELEASE-v0.1.md",
      "section": "Tests / acceptance"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/acceptance.rs",
          "description": "insta snapshot of the import surface over a trimmed copy of the coordinate skill (stable across re-runs); insta snapshot of check diagnostics over the rules fixtures (the expected diagnostic set); assert re-running import yields no diff."
        },
        {
          "path": "tests/fixtures/coordinate/",
          "description": "Trimmed real-shaped coordinate skill (SKILL.md + a companion) used as the import golden fixture."
        }
      ],
      "edit": [],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/acceptance.rs",
        "asserts": "import surface snapshot is byte-stable across two runs; check over the fixtures reproduces the expected diagnostic set; import-then-import produces no diff."
      }
    ],
    "acceptance": "The slice acceptance from the spec passes: import <fixture> --into <tmp>, check <tmp> reproduces the expected diagnostics, and re-import produces no diff; `cargo test` green.",
    "notes": "Drives the lib import/check functions (logic lives in lib per rust.md) so no binary harness/dep is needed. Trimmed fixture only — do not copy the full ~/.claude/skills/coordinate. The check-diagnostics snapshot can reuse the existing tests/fixtures/rules/* tree (already on disk); only the coordinate import fixture is new."
  }
]
