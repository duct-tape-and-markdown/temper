[
  {
    "tag": "DRIFT-SOURCE-DEP-PARSE-HOIST",
    "summary": "gate() and explain() each re-read-and-reparse lock.toml several times per invocation via source_deps' own independent parse — hoist to the already-parsed declarations document",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "`source_deps` (2574) does its own `fs::read_to_string` + `.parse::<DocumentMut>()` of `lock.toml`, independent of `read_declarations`'s (3485) own full parse via `declarations_from_doc` (3526, which already takes a parsed `&DocumentMut` — the doc-scoped-extractor shape DRIFT-EMIT-LOCK-PARSE-HOIST establishes for the provenance side, upstream in this chain). `source_deps` is called by `layout_imports` (2599), `includes` (2610), and — via `source_dep_stale` (2624) — by `layout_import_stale` (2665) and `include_stale` (2682): four independent re-reads/re-parses on top of `read_declarations`'s own. Within one `gate()` invocation (src/main.rs) this lands as 5 full parses of the same file in one run: `read_declarations` (main.rs:858), `layout_imports`+`includes` via `import_edges_from_lock` (main.rs:891), then `layout_import_stale`+`include_stale` (main.rs:1208-1209) — each a fresh read+parse; `explain()` (main.rs:513, 525, 587) repeats the same pattern minus the two staleness calls. `Declarations` (struct drifted since scoping, unchanged shape) does not currently carry the `layout_import`/`include` lock families read back from disk — `includes: Vec<IncludeRow>` is documented seam-inbound-only, 'never written back into this declaration table, so a lock round-trip reads it empty' — which is why `source_deps` re-parses independently rather than filtering an already-parsed set. Fix: extend `declarations_from_doc` (or a parallel doc-scoped extractor, the same pattern DRIFT-EMIT-LOCK-PARSE-HOIST establishes) to lift the `[declaration.layout_import]`/`[declaration.include]` families from the one already-parsed document, and have `layout_imports`/`includes`/`source_dep_stale` filter that in-memory set instead of each independently opening and parsing the file. Land after EXTRACT-FOUNDATION-BOUNDARY-RESTORE, the last entry in the existing chain to touch this file, so this entry's extractor sits beside the settled post-move shape rather than racing it."
        },
        {
          "path": "src/main.rs",
          "description": "`gate()` (847) and `explain()` (513) each call `drift::read_declarations` once and then separately call `import_edges_from_lock` (2563, calling `drift::layout_imports` then `drift::includes`) — two more independent parses on top of the first — and `gate()` additionally calls `drift::layout_import_stale`/`drift::include_stale` (1208-1209), two more still. Once `source_deps`'s callees read off the shared parsed document instead, thread the already-parsed document/`Declarations` value through these call sites rather than re-deriving it — the same one-parse-per-run shape `read_declarations`'s own call already establishes for the rest of the declaration family."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "a check run over a fixture whose lock.toml carries layout-import and include source-dependency rows reads/parses lock.toml exactly once per gate() invocation — a count pin, the same doctrine this file's discovery-walk/glob-compile/lock-row-walk pins already establish"
      },
      {
        "path": "tests/emit.rs",
        "asserts": "existing layout-import-stale / include-stale drift findings stay byte-identical — the hoist is signature-only, no behavior change to the staleness comparison"
      }
    ],
    "acceptance": "gate() and explain() each parse the workspace's lock.toml exactly once per invocation; layout_imports/includes/layout_import_stale/include_stale all derive from that one parse rather than each independently re-reading the file. No behavior change (engineering.md, 'Cost scale is hoisted'). `cargo test` and `cargo clippy -D warnings` green.",
    "notes": "Unblocked this tick: EXTRACT-FOUNDATION-BOUNDARY-RESTORE shipped (404b73a) and was removed from the queue; the blockedBy target was dangling. Re-verified drift.rs claim true on disk, refreshed line cites (~100 lines drifted from the extract.rs move). scoped at a8dd112."
  },
  {
    "tag": "GRAPH-RESOLVED-EDGE-WALK-CONSOLIDATE",
    "summary": "gate() recomputes the edge-resolution walk 2-4x per invocation — check's own copy plus resolved_edges/resolved_arcs re-derived by acyclic/degree/mention_reachable — consolidate to one shared walk",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "DRIFT-SOURCE-DEP-PARSE-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "`check` (pub fn, 111-129) is a from-scratch copy of the admissible-edge x source x target walk, producing dangling-route `Diagnostic`s. `resolved_edges` (pub(crate), 967-990) is the identical iteration shape producing `Vec<ResolvedEdge>`; `resolved_arcs` (private, 1001-1010) folds a fresh `resolved_edges` call (1006) into adjacency. `acyclic` (195-211) calls `resolved_arcs` at 196 (always-on); `degree` (236-...) calls it again at 252 (opt-in); `mention_reachable` (355-...) calls `resolved_edges` directly at 371 (opt-in) — so one `gate()` invocation recomputes the same walk 2 (check + acyclic, always-on) to 4 times (adding degree/mention_reachable when their selections opt in). Change `resolved_arcs` to fold an already-computed `&[ResolvedEdge]` slice into adjacency instead of calling `resolved_edges` itself (drop the call at 1006); change `acyclic`, `degree`, and `mention_reachable`'s signatures to accept the shared `&[ResolvedEdge]` (computed once by `gate()`) rather than `edges`/`by_kind`, folding/filtering over it. `check` needs the *unresolved* half `resolved_edges` discards (a dangling route is exactly what it skips) — the real design decision, left as a judgment call for build (name the choice in the commit body): either (a) widen `resolved_edges` to return both the resolved set and the dangling occurrences in one pass (a `(Vec<ResolvedEdge>, Vec<Diagnostic>)` or small struct), retiring `check`'s separate loop in favor of reading its half off the shared pass, or (b) give `check` its own filter over a richer per-target enumeration `resolved_edges` derives from, keeping it a thin projection rather than a parallel walk. Whichever shape lands, reword `resolved_edges`'s doc comment (964-966) and the module header's READ-EDGE-UNIFY claim (11-13, 'one resolved-edge enumeration... shared') to state the runtime-sharing this consolidation establishes, not just the type-sharing it already had."
        },
        {
          "path": "src/main.rs",
          "description": "`gate()` (847) calls `graph::check` (1131, always-on), `graph::acyclic` (1147, always-on), `graph::degree` (1154, opt-in), and `graph::mention_reachable` (1162, opt-in) — four independent triggers of graph.rs's walk today. Once graph.rs's functions take the shared `&[ResolvedEdge]` slice (or the widened resolved+dangling shape), compute it once here, after `graph::admissibility` (1130) and before the four consumers, and thread it into each call, replacing the `edges`/`by_kind` pair those calls pass today wherever the walk itself moved to the caller."
        },
        {
          "path": "src/read.rs",
          "description": "`why`'s narration (`resolved` at 335) calls `graph::resolved_edges(edges, by_kind)` directly — a separate verb invocation from `gate()`'s own run, so it is not part of the within-gate duplication this entry fixes and needs no consolidation of its own. It is `resolved_edges`'s only consumer outside graph.rs (doc-comment cites at 21, 62, 309, 470) — if the design decision above widens `resolved_edges`'s return shape, this call site's usage narrows to the resolved half; verify at implementation time whether the signature change ripples here, and update the call plus the four doc-comment cites to match whatever shape lands."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "a fixture whose selections carry both a degree and a mention-reachable clause (exercising all four graph.rs call sites) asserts the resolved-edge walk runs exactly once per gate() invocation — a new count-pin instrumented the way import::walk_count()/kind::glob_compile_count() already are in this file"
      },
      {
        "path": "src/graph.rs",
        "asserts": "existing acyclic/degree/mention_reachable/check inline tests pass calling the consolidated functions over the shared slice — signature-only change, no diagnostic output changes"
      }
    ],
    "acceptance": "gate() computes the admissible-edge x source x target walk exactly once per invocation; acyclic, degree, and mention_reachable derive their adjacency/edge set from that one shared computation instead of each independently recomputing via resolved_edges/resolved_arcs, and check's dangling-route diagnostics derive from the same shared pass. A count-pin test proves it. No behavior change (engineering.md, 'Cost scale is hoisted and pinned by count'). `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Drained from .flume/refactor/plan-graph-resolved-edge-walk-duplication.md (observed e2325df, filed sweeping judges' graph.rs). Re-verified unmoved at fa9c87f (git log e2325df..HEAD -- src/ empty). Output shape is a judgment call for build, named in the commit body. Serialized behind DRIFT-SOURCE-DEP-PARSE-HOIST, the last main.rs-touching link in the existing DISCOVERY chain, for shared-file safety (pending-entry.md) — no functional dependency. scoped at fa9c87f."
  },
  {
    "tag": "GRAPH-WORLD-ZERO-CONSUMER-PRUNE",
    "summary": "graph.rs's world() is pub(crate) but called only from reachable() in the same file — zero outside consumer; narrow to private per the export-earns-its-consumer bar",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "An export earns its consumer"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GRAPH-RESOLVED-EDGE-WALK-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "`world` (79-81, `pub(crate) fn world() -> Node { (\"world\".to_string(), \"world\".to_string()) }`, doc 72-78) has zero caller anywhere in src/, tests/, sdk/ outside this file's own `reachable` (554, `let world = world();`) plus its own inline test (1970, `assert_eq!(world(), (\"world\".to_string(), \"world\".to_string()))`) — grep-verified zero hits for `world()` calls anywhere else. Same shape as the shipped ENGINE-SELECTOR-LABEL-ZERO-CONSUMER-PRUNE precedent (a pub item called only from one function in its own file). Drop `pub(crate)` from `world`, leaving it a module-private fn callable from `reachable` and its own inline test unchanged."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/graph.rs",
        "asserts": "reachable's existing coverage and the inline world() assertion at 1970 stay green — the narrowing is visibility-only"
      }
    ],
    "acceptance": "world is a private fn in src/graph.rs, not pub(crate). `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Found sweeping the judges subsystem (posture rotation, re-verifying subsystems 1f6afe5's ship touched). Serialized behind GRAPH-RESOLVED-EDGE-WALK-CONSOLIDATE for shared graph.rs safety (pending-entry.md, 'Disjoint, or serialized') — no functional dependency. scoped at 4baa5c4."
  },
  {
    "tag": "GATE-KIND-UNITS-DOUBLE-RESOLVE-HOIST",
    "summary": "gate()/explain() resolve every kind's members via resolve_kind_units twice per run — the two-greens dispatch, then collect_directive_members again",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GRAPH-RESOLVED-EDGE-WALK-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "`resolve_kind_units` (fn 1319, doc comment 1302-1318: 'the one corpus both gate and explain range over') walks a kind's governs locus and reads+parses every member file off disk — whole-input work scaling with the harness's member count. `gate()` (847) and `explain()` (513) each invoke it TWICE per kind, per run: once through the two-greens dispatch — `gate()`'s built-in loop (917-945) calls `kind_features` (1518-1529, which calls `resolve_kind_units` at 1524) per built-in kind at 938, and its custom-kind loop (974-989) calls it per custom kind at 982; `explain()` reaches the same built-in path via `builtin_features_by_kind` (1843-1853, calling `kind_features` per kind) at line 532, and inlines a direct `resolve_kind_units` call for custom kinds at 556 — and a second time through `collect_directive_members` (2166-2197, called by `gate()` at 1004 and `explain()` at 599), which independently re-derives `builtin_kind::definitions()` (2171, already held as `builtin_defs` in both callers), re-calls `resolve_kind_units` per built-in kind (2173) and per custom kind (2186), and re-runs `partition_kind_rows` (2183, already computed at gate():955/explain():549) to get the same custom-kind rows a second time. Every member document in the corpus is read from disk and parsed twice per `gate()`/`explain()` invocation. Secondary, same functions: `gate()` also calls `builtin_kind::definitions()` a second time inline at ~1193 as an argument to `coverage_note::check`, discarding the already-computed `builtin_defs` (cheap, in-memory, but fixable alongside). Fix: thread the units/features `kind_features`'s callers already resolve into `collect_directive_members` instead of a second `resolve_kind_units` pass — the exact carrying shape (widening `kind_features`'s return to keep each unit's `source_path` beside its `Features`, or having callers hold the `Vec<Unit>` alongside) is a judgment call for build; name the choice in the commit body."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "a fixture with at least one built-in-kind member and one custom-kind member proves resolve_kind_units's per-kind file-read/parse work runs exactly once per gate()/explain() invocation — a count-pin instrumented the way import::walk_count()/kind::glob_compile_count() already are in this file, not a wall-clock threshold"
      }
    ],
    "acceptance": "gate() and explain() each resolve every kind's members off disk exactly once per invocation; collect_directive_members consumes that shared resolution instead of re-deriving it via a second resolve_kind_units/partition_kind_rows/builtin_kind::definitions() pass. No behavior change (engineering.md, 'Cost scale is hoisted'). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping the `verbs` subsystem (posture rotation: provider done, verbs this tick). resolve_kind_units's own doc comment already claims single-corpus sourcing the code doesn't structurally guarantee. Serialized behind GRAPH-RESOLVED-EDGE-WALK-CONSOLIDATE, the last entry in the existing chain touching main.rs (pending-entry.md, 'Disjoint, or serialized') — no functional dependency. scoped at ad66e46."
  },
  {
    "tag": "COVERAGE-NOTE-LOCK-PARSE-HOIST",
    "summary": "coverage_note::check's with_locked_kinds re-reads and re-parses lock.toml inside gate(), which already parsed the identical file into `committed` moments earlier",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GATE-KIND-UNITS-DOUBLE-RESOLVE-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/coverage_note.rs",
          "description": "`with_locked_kinds` (290-302, doc 280-289) calls `drift::read_declarations(&root.join(crate::WORKSPACE_DIR))` (295) — a full fresh open+parse of the committed lock — every time `check` (142) runs, which `gate()` (src/main.rs) does on every invocation via the coverage-note tier (main.rs:1191). Change `with_locked_kinds` to take the already-parsed kind rows (or the merged map) the caller already holds instead of a `root: &Path` it re-derives from disk; `check`'s own `root: &Path` param stays (independently used for `present`/`manifest_top_level_keys`/the stray-file walk at 265-278, unrelated to the lock read). Exact threading shape (a new param on `check`, or a pre-merged map argument) is a judgment call for build — name the choice in the commit body."
        },
        {
          "path": "src/main.rs",
          "description": "`gate()` (847) already computes `committed = drift::read_declarations(workspace)?` (858) before calling `coverage_note::check(harness_root, &builtin_kind::definitions()?, &member_counts)` (1191-1195, passing builtins only, letting `with_locked_kinds` silently pull the custom-kind half from a second disk read). Thread `committed.kinds` (or an already-merged kinds map derived from it) into the call instead, dropping the redundant re-parse `with_locked_kinds` performs today."
        },
        {
          "path": "tests/coverage_note.rs",
          "description": "Four call sites (250, 258, 279, 296) construct fixture directories carrying real (or corrupt/missing) `lock.toml` files specifically to exercise `with_locked_kinds`'s own disk-reading behavior (corrupt lock errors, missing lock degrades to `kinds` alone, a present lock's kinds merge in). Once `check` no longer reads the lock itself, these tests need restructuring to pre-parse (or intentionally omit) the lock before calling `check`, preserving the same three behaviors at whichever layer now owns them — naming which layer surfaces a corrupt-lock error is part of the same build-time judgment call as the signature shape above."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "a fixture whose gate() run exercises both the top-level declarations read and the coverage-note tier proves lock.toml is parsed exactly once per invocation — a count-pin, the same doctrine this file's discovery-walk/glob-compile/lock-row-walk pins already establish"
      },
      {
        "path": "tests/coverage_note.rs",
        "asserts": "the corrupt-lock/missing-lock/governed-lock behaviors stay observable, each restructured to the new signature, with the same outcomes"
      }
    ],
    "acceptance": "gate() parses the workspace's lock.toml exactly once per invocation; coverage_note::check consumes the already-parsed kind rows instead of independently re-reading and re-parsing the file. No behavior change to which surfaces are reported governed (engineering.md, 'Cost scale is hoisted'). cargo test and cargo clippy -D warnings green.",
    "notes": "Found sweeping judges (posture rotation, re-verifying subsystems 1f6afe5's ship touched). Distinct from DRIFT-EMIT-LOCK-PARSE-HOIST/DRIFT-SOURCE-DEP-PARSE-HOIST (drift.rs's own double-parses) and GATE-KIND-UNITS-DOUBLE-RESOLVE-HOIST (resolve_kind_units) — this is coverage_note.rs's own re-read on the same gate() path. Serialized behind GATE-KIND-UNITS-DOUBLE-RESOLVE-HOIST, last chain entry touching main.rs — no functional dependency. scoped at 4baa5c4."
  },
  {
    "tag": "JSON-MANIFEST-DISCOVERY-BOUNDARY-RESTORE",
    "summary": "json_manifest.rs's Manifest::read_kind imports crate::import and self-discovers — formats reaching into pipeline, unlike frontmatter's pure-parse split",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Invariants — stated as absences"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "COVERAGE-NOTE-LOCK-PARSE-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/json_manifest.rs",
          "description": "`Manifest::read_kind` (419-431, doc 406-417) takes `disc: &crate::import::Discovery` and calls `crate::import::discover_kind_files` (426-431) itself — the only formats-subsystem function that imports `crate::import`, unlike the sibling frontmatter path where `main.rs` does the discovery walk and calls the pure-parse `frontmatter::Member::from_source_rooted` (frontmatter.rs never imports `crate::import`). Same class of edge architecture.md's Invariants section has already ruled on four times (0040 x3, normalize_path this cycle) — resolved by moving code to its layer-correct home, never by amending the map. Narrow `read_kind` to take an already-discovered file list (e.g. `&[PathBuf]`) instead of `&crate::import::Discovery`, dropping the `crate::import::discover_kind_files` call and the `crate::import` dependency from this file; the discovery call moves to whichever caller now performs it (see main.rs edit). Exact signature shape (a slice, an iterator, a `Vec<PathBuf>`) is a judgment call for build — name the choice in the commit body."
        },
        {
          "path": "src/main.rs",
          "description": "`manifest_units` (1495-1508) calls `json_manifest::Manifest::read_kind(disc, kind)` (1501) today, letting `read_kind` do its own discovery. Once `read_kind` narrows to take a file list, `manifest_units` performs the discovery call itself before delegating — mirroring `resolve_kind_units`'s frontmatter `governs` branch (1369-1376, `import::discover_kind_files(disc, kind, governs, import::LocalOverride::Honored)`) exactly, so both file-governed loci discover the same way and only `main.rs` (a verb, not a format) imports `crate::import` for this purpose."
        },
        {
          "path": "tests/hook_kind.rs",
          "description": "Two call sites (78, 120) construct `crate::import::Discovery` and pass it to `Manifest::read_kind` — update to the new signature (discover via `import::discover_kind_files` inline, then pass the resulting file list), matching whatever shape build lands in json_manifest.rs."
        },
        {
          "path": "tests/manifest_adapter.rs",
          "description": "One call site (142) updates to the new `read_kind` signature."
        },
        {
          "path": "tests/mcp_server_kind.rs",
          "description": "One call site (68) updates to the new `read_kind` signature."
        },
        {
          "path": "tests/installed_plugin_kind.rs",
          "description": "Four call sites (68, 82, 112, 198) update to the new `read_kind` signature."
        },
        {
          "path": "tests/known_marketplace_kind.rs",
          "description": "Three call sites (58, 89, 176) update to the new `read_kind` signature."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/manifest_adapter.rs",
        "asserts": "existing manifest discovery/read coverage stays green — the move is signature-only, no change to which files are discovered or how they parse"
      },
      {
        "path": "tests/installed_plugin_kind.rs",
        "asserts": "existing installed-plugin manifest read coverage stays green across all four call sites"
      }
    ],
    "acceptance": "src/json_manifest.rs imports no crate::import; Manifest::read_kind takes an already-discovered file list, and the discovery walk for manifest-shaped kinds lives in main.rs's manifest_units, mirroring resolve_kind_units's frontmatter branch. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Drained from .flume/refactor/plan-json-manifest-import-layering.md (observed 9e197d6, unmoved at HEAD). Serialized behind COVERAGE-NOTE-LOCK-PARSE-HOIST, last chain entry touching main.rs (pending-entry.md, 'Disjoint, or serialized'). Also shares json_manifest.rs with the already-open JSON-MANIFEST-READ-DECODE-CONSOLIDATE — queue-front, ships first regardless. scoped at 7a5f86c."
  },
  {
    "tag": "INSTALL-GUARD-MANIFEST-MESSAGE-PRUNE",
    "summary": "install.rs's GUARD_MANIFEST_MESSAGE const is pub with zero consumer outside its own module — narrow to private per the export-earns-its-consumer bar",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "An export earns its consumer"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "`GUARD_MANIFEST_MESSAGE` (132, `pub const`) has exactly one use anywhere in the tree — inside `render_manifest_findings` (821-826, same module, `String::from(GUARD_MANIFEST_MESSAGE)` at 822) — grep-verified zero hits across src/, tests/, sdk/ outside this file. Its sibling `GUARD_MESSAGE` (124, `pub const`) carries a doc comment stating why it's public ('Public so the `guard` subcommand ([`main`]) prints it...') and a real external caller (`main.rs:416,420`, `eprintln!(\"{}\", install::GUARD_MESSAGE)`) — the earned-pub shape `GUARD_MANIFEST_MESSAGE` lacks; `render_manifest_findings` itself is the pub fn main.rs calls (main.rs:398, unchanged), and it's the one that needs the message text, not main.rs directly. Drop `pub` from `GUARD_MANIFEST_MESSAGE`, leaving it a module-private const read only by `render_manifest_findings`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.rs",
        "asserts": "guard_reads_a_pretooluse_payload_and_acts_on_the_posture stays green — the narrowing is visibility-only, render_manifest_findings's output is unchanged"
      }
    ],
    "acceptance": "GUARD_MANIFEST_MESSAGE is a private const in src/install.rs, not pub; GUARD_MESSAGE keeps its earned pub. cargo test and cargo clippy -D warnings green (no unused-pub warning survives).",
    "notes": "Unblocked this tick: INSTALL-PROJECTION-MATCH-CONSOLIDATE shipped (278ae4c) and was removed from the queue; the blockedBy target was dangling. Re-verified zero outside consumer still true on disk (grep across src/tests/sdk), refreshed line cites. scoped at a8dd112."
  },
  {
    "tag": "INSTALL-PLACEMENT-KIND-ENUM",
    "summary": "InstallEntry.placement is four bare &str consts matched with a wildcard fallback in gate_installed — the MODELINE case is never named, only reached via `_`",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "A shared concept is one type"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "INSTALL-GUARD-MANIFEST-MESSAGE-PRUNE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "`InstallEntry.placement: &'static str` (370) is one of four consts — `SESSION_START` (91), `MODELINE` (93), `GUARD_HOOK` (95), `NOTE` (97) — set at its five construction sites (582, 606, 612, 642, 656). `gate_installed`'s tally (522-533) matches them as `SESSION_START => hook = true, GUARD_HOOK => guard = true, NOTE => notes += 1, _ => modelines += 1` — `MODELINE` is never named in the match, reached only through the `_` fallback, which would also silently absorb any future fifth placement as a modeline, uncaught by the compiler. This is the shared-concept posture's own described failure shape ('a `_` arm over a shared concept is a seam defect waiting for its field report'). Replace the four string consts and the `&'static str` field with a real `#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum Placement { SessionStart, Modeline, GuardHook, Note }`, update the five construction sites and `gate_installed`'s match to name every variant exhaustively (no `_` arm), and give `Placement` a `Display`/label method carrying today's exact strings ('session-start hook', 'schema modeline', 'guard hook', 'managed-by note') for the two `.to_string()` uses at 540/543 and the report line built from it."
        },
        {
          "path": "tests/install.rs",
          "description": "`outcome_of` (137-147) and `has_entry` (150-152) take `placement: &str` and compare with `==` against `entry.placement`; ~15 call sites pass string literals (`\"session-start hook\"` 233/430/544, `\"guard hook\"` 236/428/717/751/897/918, `\"managed-by note\"` 237, `\"schema modeline\"` 238/440/790). Once `InstallEntry.placement` becomes `Placement`, both helpers' signatures change to `placement: Placement` and every call site's string literal becomes the matching enum variant (`Placement::SessionStart`, `Placement::GuardHook`, `Placement::Note`, `Placement::Modeline`)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "every existing outcome_of/has_entry assertion (hook/guard/note/modeline placements across the install/re-install/dry-run coverage) passes unchanged calling the enum-typed helpers — the change is representation-only, no behavior change to which placements land or their reported outcome"
      }
    ],
    "acceptance": "InstallEntry.placement is a real Placement enum with an exhaustive match in gate_installed (no `_` arm); the four bare &str consts are gone. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping the `verbs` subsystem (posture rotation: provider done, verbs this tick). Serialized behind INSTALL-GUARD-MANIFEST-MESSAGE-PRUNE for shared install.rs safety (pending-entry.md) — no functional dependency. scoped at ad66e46."
  },
  {
    "tag": "BUNDLE-INSTALL-SESSION-START-SHAPE-CONSOLIDATE",
    "summary": "bundle.rs re-declares install.rs's SESSION_START_COMMAND const and re-inlines its session_start_group() JSON shape — one job, authored twice",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "INSTALL-PLACEMENT-KIND-ENUM"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "`session_start_group` (1114-1116, private, `json!({ \"hooks\": [ { \"type\": \"command\", \"command\": SESSION_START_COMMAND } ] })`) is the canonical builder of the exec-form `SessionStart` hook group, called at this file's own 904, 979, 1023, 1031. `SESSION_START_COMMAND` itself (88, `pub const`) is already shared correctly — `tests/session_start.rs:360` reads it as `temper::install::SESSION_START_COMMAND`. Widen `session_start_group` from private to `pub(crate)` so `bundle.rs` can call it instead of re-deriving its return shape inline (no other change: same body, same call sites)."
        },
        {
          "path": "src/bundle.rs",
          "description": "`const SESSION_START_COMMAND` (63, doc comment 58-62) is a second, byte-identical copy of `install::SESSION_START_COMMAND` (grep-verified same literal string `\"temper check . --reporter session-start\"`), and `hooks_manifest` (284-296) independently inlines `{ \"SessionStart\": [ { \"hooks\": [ { \"type\": \"command\", \"command\": SESSION_START_COMMAND } ] } ] }` — the identical object `install::session_start_group()` already builds, one array-wrap deeper. Delete the local `const` and its doc comment; change `hooks_manifest` to `json!({ \"SessionStart\": [crate::install::session_start_group()] })`, dropping the hand-inlined inner shape. Update this file's own inline test `manifests_are_well_formed_json` (~430, `SESSION_START_COMMAND` reference) to read `crate::install::SESSION_START_COMMAND` instead of the deleted local const. No behavior change: identical command string, identical emitted JSON shape."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/bundle.rs",
        "asserts": "the bundle's `hooks/hooks.json` still carries `[\"hooks\"][\"SessionStart\"][0][\"hooks\"][0][\"command\"]` equal to the exec-form check command (asserted ~63), and the byte-identical-to-canonical-write assertion (~217-222) stays green — the consolidation is call-site-only, no change to the emitted bytes"
      },
      {
        "path": "src/bundle.rs",
        "asserts": "manifests_are_well_formed_json stays green reading install's const instead of the deleted local copy"
      }
    ],
    "acceptance": "bundle.rs carries no private SESSION_START_COMMAND const and no inline re-derivation of the SessionStart hook-group JSON shape; both read/call install.rs's existing SESSION_START_COMMAND and session_start_group (widened to pub(crate)). No behavior change (engineering.md, 'One job, one home'). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping the `verbs` subsystem (posture rotation: provider done, verbs this tick — rotation's last subsystem this cycle). Serialized behind INSTALL-PLACEMENT-KIND-ENUM, the last chain entry touching install.rs, for shared-file safety (pending-entry.md). Also shares bundle.rs with the already-open, queue-front BUILTIN-KIND-DEFINITION-RESULT-COLLAPSE (lines 317/402 there, unrelated symbols) — ships first regardless, no blockedBy needed. scoped at b710f2d."
  },
  {
    "tag": "BUNDLE-ERROR-ZERO-CONSUMER-PRUNE",
    "summary": "bundle.rs's BundleError enum is pub with zero consumer outside its own module — narrow to private per the export-earns-its-consumer bar",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "An export earns its consumer"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/bundle.rs",
          "description": "`BundleError` (140, `pub enum BundleError`, doc 138) is grep-verified zero-consumer outside this file across src/, tests/, sdk/ — `run` (201) and `write_member`/its sibling write helpers (its only internal callers, returning `Result<(), BundleError>`) all stay within bundle.rs; `run` itself returns `miette::Result<BundleReport>`, converting via `?` at the pub boundary, so the concrete error type never crosses the module boundary. Same shape as the shipped GRAPH-WORLD-ZERO-CONSUMER-PRUNE/INSTALL-GUARD-MANIFEST-MESSAGE-PRUNE precedents. Drop `pub` from `BundleError`, leaving it module-private; its intra-doc links (307-308, `write_member`'s own Errors section) stay valid within the same crate build."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/bundle.rs",
        "asserts": "existing bundle coverage (the_cli_bundle_verb_composes_the_plugin, bundle_report_names_shipped_artifacts_over_an_empty_surface, and the module's other tests) stays green — the narrowing is visibility-only"
      }
    ],
    "acceptance": "BundleError is a private enum in src/bundle.rs, not pub. cargo test and cargo clippy --all-targets -- -D warnings green (no unused-pub warning survives).",
    "notes": "Found sweeping the verbs subsystem (posture rotation). Serialized against BUNDLE-MANIFEST-PATH-GOVERNS-DERIVE for shared bundle.rs safety (pending-entry.md, 'Disjoint, or serialized') — no functional dependency; that entry chains blockedBy this one. scoped at 2d1c5a6."
  },
  {
    "tag": "BUNDLE-MANIFEST-PATH-GOVERNS-DERIVE",
    "summary": "bundle.rs's write_member takes a hardcoded relative manifest path instead of deriving it from the kind's own declared Governs locus, duplicating builtin_kind.rs's cited fact",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Derived state is computed, never stored beside its source"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "BUNDLE-ERROR-ZERO-CONSUMER-PRUNE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/bundle.rs",
          "description": "`run`'s two `write_member` calls (206-212, 213-219) pass `Path::new(\".claude-plugin/plugin.json\")` and `Path::new(\".claude-plugin/marketplace.json\")` as literals. Both are byte-identical to the `Governs { root: \".claude-plugin\", glob: \"plugin.json\"/\"marketplace.json\" }` already declared on `claude_code_plugin_manifest`/`claude_code_marketplace` (builtin_kind.rs:376-379 citing code.claude.com/docs/en/plugins-reference retrieved 2026-07-16, :406-408 citing code.claude.com/docs/en/plugin-marketplaces retrieved 2026-07-16). `write_member` (309-324) already fetches the kind's `definition` (316) to read `.format` but ignores its `.governs`, taking the path as a separate `relative: &Path` parameter (311) instead — the path is a value computable from state `write_member` already holds, hand-authored a second time at each call site instead. Fix: drop the `relative` parameter, derive it inside `write_member` from `definition.governs` (`Path::new(&governs.root).join(&governs.glob)`), refusing loud (a `BundleError` variant) if a kind passed here declares no `governs` — the exact refusal shape is build's call, named in the commit body. Update both call sites to drop their now-redundant `Path::new(...)` argument."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/bundle.rs",
        "asserts": "the_kinded_manifests_are_rendered_by_their_kinds_write_face and the_published_manifests_bytes_are_pinned stay green at the same .claude-plugin/plugin.json and .claude-plugin/marketplace.json paths — the derivation is signature-only, no output path changes"
      }
    ],
    "acceptance": "write_member derives its write path from the kind's declared Governs locus instead of taking it as a caller-supplied literal; a relocation of either kind's governs (e.g. via overlay_builtin_kind) changes bundle's output path without a second edit here. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping the verbs subsystem (posture rotation) — two sources of truth for one manifest's location. Serialized behind BUNDLE-ERROR-ZERO-CONSUMER-PRUNE for shared bundle.rs safety (pending-entry.md, 'Disjoint, or serialized') — no functional dependency. scoped at 2d1c5a6."
  },
  {
    "tag": "KIND-ZERO-CONSUMER-EXPORTS-PRUNE",
    "summary": "kind.rs ships three pub fns with zero callers anywhere outside their own file — Commitment::label, Content::label, CustomKind::qualified_name — prune per the export-earns-its-consumer bar",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "An export earns its consumer"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Three `pub` items grep-verified zero-consumer (no `.label()`/`.qualified_name()` call anywhere in src/, tests/, or sdk/ outside kind.rs's own inline tests): `impl Commitment { pub fn label(self) -> &'static str { .. } }` (59-69) — the whole `impl Commitment` block, since `label` is its only method; `impl Content { pub fn label(&self) -> &'static str { .. } }` (164-176) — likewise the whole block, `label` is its only method; `CustomKind::qualified_name` (932-937, the doc comment plus `pub fn qualified_name(&self) -> String { self.name.clone() } `) inside the larger `impl CustomKind` block — remove only this method, the surrounding impl block has other live methods. Each type (`Commitment`, `Content`, `CustomKind`) stays; only the dead method/impl goes."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/kind.rs",
        "asserts": "the now-orphaned inline test `qualified_name_is_the_bare_name` (1814-1818) is removed alongside the fn it exercises; every other kind.rs test (Commitment/Content coverage via their variants, not their removed label methods) stays green"
      }
    ],
    "acceptance": "`Commitment::label`, `Content::label`, and `CustomKind::qualified_name` are gone from src/kind.rs, along with their now-vacuous inline test. `cargo test` and `cargo clippy -D warnings` green (no unused-code warning survives).",
    "notes": "Unblocked this tick: EXTRACT-FOUNDATION-BOUNDARY-RESTORE shipped (404b73a) and was removed from the queue; the blockedBy target was dangling. Re-verified all three still zero-consumer on disk (grep across src/tests/sdk), refreshed line cites. scoped at a8dd112."
  },
  {
    "tag": "CONTRACT-DECLARED-KEYS-EXHAUSTIVE-MATCH",
    "summary": "declared_keys falls through `_ => None` instead of naming every Predicate variant like neighbors target/documented_field do — a new key-declaring variant would silently vanish from the allow-list",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "A shared concept is one type"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "KIND-ZERO-CONSUMER-EXPORTS-PRUNE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/contract.rs",
          "description": "`declared_keys` (736-746) matches `Predicate::Required { field } | Predicate::Optional { field } => Some(field)` then falls through `_ => None` for every other variant. Two functions immediately above it over the same enum — `target` (642-673) and `documented_field` (683-720ish) — instead list every non-mapped `Predicate` variant explicitly in the `None` arm's pipe-group (engineering.md's own precedent: 'a `_` arm over a shared concept is a seam defect waiting for its field report'); a future key-declaring variant added to `Predicate` would compile silently against `declared_keys`'s wildcard while `target`/`documented_field` would force a decision. Rewrite `declared_keys`'s match to mirror `target`/`documented_field`'s style: keep the `Required`/`Optional` arm producing `Some(field)`, replace `_ => None` with every remaining variant named in one pipe-grouped arm mapping to `None` (the same variant list `target` already spells, since no other predicate declares a schema key)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/closed_keys.rs",
        "asserts": "a_member_carrying_only_declared_keys_holds and the file's other closed-keys coverage stay green — the rewrite is exhaustiveness-only, declared_keys returns the identical set for every existing predicate"
      }
    ],
    "acceptance": "declared_keys's match names every Predicate variant explicitly, no `_` wildcard remains, matching the exhaustive style of target/documented_field two functions above it. `cargo test` and `cargo clippy -D warnings` green.",
    "notes": "Found sweeping 'model' subsystem (posture rotation). Serialized behind KIND-ZERO-CONSUMER-EXPORTS-PRUNE purely for chain linearity with CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP next (both touch contract.rs, and the schema's single-tag blockedBy can't express 'wait for two independent priors' — same tradeoff DRIFT-LOCK-ROW-WALK-CONSOLIDATION's chain already accepted). No functional dependency on the kind.rs entry. scoped at 4adb1fb."
  },
  {
    "tag": "CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP",
    "summary": "Predicate::RequireSections can't be authored or decoded — the SDK constructor takes no sections arg, ClauseRow has no column for it, predicate_from_row has no arm — though engine.rs fully judges it",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "The lock"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "CONTRACT-DECLARED-KEYS-EXHAUSTIVE-MATCH"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/contract.ts",
          "description": "`requireSections = (): Predicate => ({ key: \"require_sections\" })` (168) takes no argument even though its own doc comment reads 'The named headings are present' — unlike `dependencyExists` (whose missing constructor is explicitly documented: 'the engine holds it back absent a decidable reference syntax'), nothing here names this as a deliberate hold. Change the signature to `requireSections = (sections: readonly string[]): Predicate => ({ key: \"require_sections\", sections })`. Add a `sections?: readonly string[]` field to the `Predicate` interface (76, beside `section`), documented the same way `keys`/`values` are ('`require_sections`'s required heading list')."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "`clauseRow` (65-115) maps each `Predicate` field to its `ClauseRow` column by explicit `predicate.key === \"...\"` branches (`keys`, `values`, `section`, etc. — no generic spread). Add: `sections: predicate.key === \"require_sections\" && predicate.sections ? [...predicate.sections] : undefined,` alongside the existing `keys`/`values`/`section` branches (110-114), copying into a fresh array per the file's own documented reason ('the generated rows carry mutable columns; the predicate's ... is read-only, so copy each into a fresh array')."
        },
        {
          "path": "src/drift.rs",
          "description": "`ClauseRow` (struct at 2869-2971) carries no column a `require_sections` row could use — `keys`/`values`/`section` are documented as each other predicate's own. Add a new field `pub sections: Option<Vec<String>>,` after `section` (2970), with a doc comment naming it `require_sections`'s required-heading-list column, matching the `#[serde(default)]` pattern every other optional column carries. This regenerates `sdk/src/generated/ClauseRow.ts` via the existing ts-rs export test — do not hand-edit the generated file."
        },
        {
          "path": "src/contract.rs",
          "description": "`predicate_from_row` (424-...) has arms for every predicate key except `require_sections` and the constructor-less `dependency-exists` (falls through `_ => return None`). Add, near the `must_define` (458) / `section_contains` (461) arms: `\"require_sections\" => Predicate::RequireSections { sections: row.sections.clone()? },`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/contract.test.ts",
        "asserts": "a new test asserts requireSections([\"Usage\", \"Decision\"]) composes { key: \"require_sections\", sections: [\"Usage\", \"Decision\"] } as an ordinary Predicate value, the pattern the file's other node-set constructor tests (count, unique, membership) already follow"
      },
      {
        "path": "tests/lock_declaration_rows.rs",
        "asserts": "a new (or extended) round-trip test pushes a require_sections ClauseRow with a sections column through emit + read_declarations and asserts predicate_from_row lifts it back to Predicate::RequireSections { sections } with the same list, the same shape a_floor_clause_row_round_trips_its_node_scope_predicate_argument already proves for forbidden_keys/allowed_chars"
      }
    ],
    "acceptance": "An SDK author can call requireSections([...]) with real heading names, the emitted lock round-trips them through a new sections ClauseRow column, and predicate_from_row decodes a require_sections row back into Predicate::RequireSections { sections } — closing the one predicate in the closed vocabulary whose lock declaration rows (pipeline.md, 'The lock') could not carry its own argument. `cargo test` (Rust) and `pnpm --dir sdk test` (SDK gate) green.",
    "notes": "Found sweeping 'model' via predicate_from_row. DependencyExists's parallel missing arm is NOT part of this entry — its SDK constructor is deliberately absent (contract.ts says why), genuinely unreachable, unlike RequireSections's unusable half-built constructor. Serialized behind CONTRACT-DECLARED-KEYS-EXHAUSTIVE-MATCH for shared contract.rs/drift.rs safety. scoped at 4adb1fb."
  },
  {
    "tag": "DRIFT-EMIT-OUTCOME-LABEL-ZERO-CONSUMER-PRUNE",
    "summary": "drift.rs's EmitOutcome::label is pub but called only from render_emit in the same file — zero outside consumer; narrow to private per the export-earns-its-consumer bar",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "An export earns its consumer"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "`EmitOutcome::label` (535-546, `pub fn label(self) -> &'static str`) has exactly one call site in the whole tree — `render_emit` (2166-2188), same file, at line 2180 (`entry.outcome.label()`) — grep-verified zero hits for `EmitOutcome::label` or a `.label()` call on an `EmitOutcome` value anywhere else in src/, tests/, sdk/ (the sibling `.label()` hit at src/install.rs:1719 is `ApplyOutcome::label`, an unrelated enum with its own real cross-module caller). No test calls `.label()` directly; `tests/emit.rs` (1121-1122) asserts against `render_emit`'s rendered string output (`rendered.contains(\"reaped\")` etc.), never the method itself. This is the same defect shape ENGINE-SELECTOR-LABEL-ZERO-CONSUMER-PRUNE already pruned in `src/engine.rs` (`Selector::label`, called only from `finding()` in the same file) — per that precedent, drop `pub` from `label`, leaving it a module-private method callable from `render_emit` unchanged."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/emit.rs",
        "asserts": "existing render_emit coverage (the outcome-tally assertions at 1121-1122 and every other emit-report rendering test) stays green — the narrowing is visibility-only, render_emit's rendered text is unchanged"
      }
    ],
    "acceptance": "EmitOutcome::label is a private method in src/drift.rs, not pub. `cargo test` and `cargo clippy --all-targets -- -D warnings` green (no unused-pub warning survives).",
    "notes": "Found sweeping the pipeline subsystem (posture rotation: formats done, pipeline this tick). Serialized behind CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP, the last entry in the existing chain touching drift.rs (pending-entry.md, Disjoint, or serialized) — no functional dependency. scoped at 4bc8508."
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics — the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (⇒ 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (⇒ 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence — never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. `MAX_IMPORT_HOPS = 5` (59) sits under a doc (55-58) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' — a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (630) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds (657), one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts — hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it — 528 ('hop-capped at [`MAX_IMPORT_HOPS`]') and 626-629 ('capped at [`MAX_IMPORT_HOPS`] hops (the `at-import` recursion depth the format documents)') — carry the same claim and move with it, as does the propagation comment at 652-655 ('capped at the format's import recursion depth'). 6667265 (mention-reachable, in the b85df4a..HEAD window) shifted every address after ~519 — the const (59) and its doc (55-58) sit above the edit and are unmoved; the later addresses re-read at HEAD, so every address carries."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1407) already pins the boundary mechanically rather than in a comment — `r1..r5` rescued from a live seed, `r6` dead as the sole finding — so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1408-1412) spells 'capped at the format's five-hop import recursion' (1410) and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number. 6667265 (mention-reachable, in the b85df4a..HEAD window) added the mention-reachable fixture and shifted this test down; addresses re-read at HEAD, so they carry."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper — the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Park re-tested at d1af9a5 and holds — nothing ruled the hop semantics; VERIFIER-TYPED's window touched graph.rs but not the const region (still 5 at graph.rs:59, cite still 2026-07-02 at 55-58). The park's subject is untouched, and may still be wrong. A build-side friction capture records the same disagreement; the human's to read, not plan's to drain. rescoped 4a33911."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested on disk at d1af9a5, holding on every clause: no v0.1 tag (only era tags), crate 0.1.0, release.yml still states the darwin + channel-3 deferral verbatim. `git diff 68fae5c..HEAD -- .github/` is empty. scoped b676656."
  },
  {
    "tag": "NORMALIZE-PATH-SUBSYSTEM-PLACEMENT",
    "summary": "graph.rs's normalize_path is foundation-shaped leaf vocabulary consumed upward by pipeline — ruled a debt edge (53df138); moves to address.rs per architecture.md's amended Invariants section",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Invariants — stated as absences"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GRAPH-WORLD-ZERO-CONSUMER-PRUNE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "`normalize_path` (929-940, `pub(crate)`, doc 924-928: 'Lexically normalize a path ... without touching disk') has three internal callers in this file (875, 883, 921 — reverify at pickup) plus the external callers below. architecture.md's Invariants section now names this the fourth ratified debt edge (53df138): pure std::path::Component-only leaf vocabulary homed in graph.rs (judges) with every external caller in pipeline modules, moving to address.rs (foundation) where its shape already lives. Move the fn verbatim to src/address.rs and update every internal call site to `crate::address::normalize_path`."
        },
        {
          "path": "src/address.rs",
          "description": "Add `normalize_path` here verbatim (moved from graph.rs), foundation subsystem, alongside this file's own path/field mechanics — architecture.md's codemap now names address.rs as its home (53df138)."
        },
        {
          "path": "src/import.rs",
          "description": "Two call sites (396, 517) requalify from `crate::graph::normalize_path` to `crate::address::normalize_path`."
        },
        {
          "path": "src/drift.rs",
          "description": "Three call sites (672, 1799, 1885) requalify from `crate::graph::normalize_path` to `crate::address::normalize_path`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "existing dead-edge/reachability coverage exercising normalize_path's internal callers (875, 883, 921) stays green — the move is signature/location-only, no path-resolution behavior change"
      },
      {
        "path": "tests/install.rs",
        "asserts": "existing discovery/drift coverage exercising import.rs's and drift.rs's normalize_path call sites stays green — same paths resolve identically post-move"
      }
    ],
    "acceptance": "normalize_path lives in src/address.rs (foundation) with every caller requalified, matching architecture.md's Invariants section (amended 53df138 to name this the fourth ratified debt edge). `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Unparked (inbox, observed 07a9c04): architecture.md now names the fourth edge and its direction (53df138), closing the park. Serialized behind GRAPH-WORLD-ZERO-CONSUMER-PRUNE, last entry sharing graph.rs and (transitively) drift.rs. Also shares import.rs with the separately-queued, already-open BUILTIN-KIND-DEFINITION-RESULT-COLLAPSE — single-tag blockedBy can't express both waits; that entry is queue-front and ships first regardless. scoped at 07a9c04."
  },
  {
    "tag": "GRAPH-ENGINE-GLOB-EXTRACTOR-CONSOLIDATE",
    "summary": "graph.rs's declared_globs and engine.rs's field_globs duplicate one glob-extraction job — ruled (53df138): the shared extractor holds declared_globs' trim/filter semantics, both judges consume it",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "NORMALIZE-PATH-SUBSYSTEM-PLACEMENT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "`declared_globs` (780-796, doc 774-779) and engine.rs's `field_globs` (1137-1143, doc 1134-1136) both extract the glob strings a `FeatureValue` field carries — `declared_globs` trims each glob and drops blank/whitespace-only entries; `field_globs` does neither. Ruled (inbox, observed 07a9c04): the shared extractor holds `declared_globs`' trim/filter semantics — glob-valid's own documented mission ('a pattern that silently matches nothing becomes a finding') is better served judging the trimmed set, since a whitespace-padded entry judged raw compiles as a literal-space glob that silently matches nothing yet passes. Blank/whitespace-only entries stay dropped-not-flagged (the reachability judge's documented fallback, pinned by tests/graph.rs:1466 `an_absent_or_blank_paths_field_is_reachable`) — no new blank-entry finding class is added. Land one shared extractor (this file, or a leaf both files call) implementing `declared_globs`'s existing trim/filter body, and route both graph.rs's reachability judge and engine.rs's glob-valid predicate through it; `field_globs` is retired in favor of calling it."
        },
        {
          "path": "src/engine.rs",
          "description": "`field_globs` (1137-1143) is the glob-string extractor the `glob-valid` predicate arm (915-923, feeding `crate::kind::compile_glob`) calls — pinned by `glob_valid_fires_once_per_unparseable_glob_and_passes_valid_brace_globs` (2168). Ruled (07a9c04): the shared extractor holds trim/filter semantics, so a whitespace-padded glob is now judged trimmed by glob-valid too — an observable delta from today's untrimmed `field_globs`, to be named in the commit body. Remove this private duplicate and route the glob-valid arm (922) through the shared extractor graph.rs now hosts, updating the one call site."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "an_absent_or_blank_paths_field_is_reachable (1466) stays green — blank/whitespace-only entries stay dropped-not-flagged under the shared extractor, unchanged from today"
      },
      {
        "path": "src/engine.rs",
        "asserts": "glob_valid_fires_once_per_unparseable_glob_and_passes_valid_brace_globs (2168) stays green; a new case pins the ruled delta — a whitespace-padded glob is now judged trimmed, where trimming changes its validity"
      }
    ],
    "acceptance": "One shared glob-string extractor serves both graph.rs's reachability judge and engine.rs's glob-valid predicate, holding declared_globs' ruled trim/filter semantics (07a9c04); declared_globs and field_globs collapse to the one implementation, no new blank-entry finding class. `cargo test` and `cargo clippy --all-targets -- -D warnings` green. The commit body states the observable delta (whitespace-padded globs now judged trimmed).",
    "notes": "Unparked (inbox, observed 07a9c04): the ruling names the shared semantics — declared_globs' trim/filter, both judges consuming one filtered set — closing the design-call park. Serialized behind NORMALIZE-PATH-SUBSYSTEM-PLACEMENT, sharing graph.rs. Also shares engine.rs with the separately-queued, already-open ENGINE-JUDGE-SELECTION-EXHAUSTIVE-MATCH — schema's single-tag blockedBy can't express both waits; that entry should ship first regardless. scoped at 07a9c04."
  },
  {
    "tag": "DRIFT-WRITE-PLACEMENT-CONSOLIDATE",
    "summary": "drift.rs's write_placement (mkdir+write+DriftError::Write) is reimplemented inline in emit_manifest and emit_one instead of calling the existing helper",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "NORMALIZE-PATH-SUBSYSTEM-PLACEMENT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "`write_placement` (2298-2309, doc 2295-2297) is the named helper for 'create the parent dirs, write the bytes, map both failures to `DriftError::Write`' — called at its own call sites (2265, 2290) inside the placement-apply path. `emit_manifest` (1475-1486) and `emit_one` (2071-2082) each reimplement the identical body inline instead of calling it: the same `fs::create_dir_all(parent).map_err(|source| DriftError::Write { path: parent.to_path_buf(), source })?` followed by `fs::write(path, desired.as_bytes()).map_err(|source| DriftError::Write { path: path.to_path_buf(), source })?`, each already wrapped in the same `if !dry_run { .. }` guard `write_placement`'s own callers use — three copies of one job in one file. Replace both inline blocks with `write_placement(path, &desired)?` (emit_manifest) and `write_placement(&disk_path, &desired)?` (emit_one), inside their existing `if !dry_run` guards. No behavior change: identical mkdir/write/error-map sequence, same error variant, same paths."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/emit.rs",
        "asserts": "dry_run_reports_the_outcome_but_writes_nothing (250) and a_represented_manifest_emits_whole_through_the_write_face_not_json_splice (524) stay green — the consolidation is call-site-only, identical mkdir/write/error behavior for both the member-projection and manifest emit paths"
      }
    ],
    "acceptance": "emit_manifest and emit_one both call the existing write_placement helper instead of each carrying its own inline mkdir+write+DriftError::Write block; the job has exactly one implementation in src/drift.rs. `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Found sweeping pipeline (posture rotation: formats done, pipeline this tick). Serialized behind NORMALIZE-PATH-SUBSYSTEM-PLACEMENT, last chain entry touching drift.rs by array position (pending-entry.md, 'Disjoint, or serialized'). DRIFT-EMIT-OUTCOME-LABEL-ZERO-CONSUMER-PRUNE also touches drift.rs on a parallel branch off EXTRACT-FOUNDATION-BOUNDARY-RESTORE — single-tag blockedBy can't express both waits; expected to ship independently regardless. scoped at 69e7571."
  },
  {
    "tag": "READ-GRAPH-DIRECTIVE-FIELD-CONSOLIDATE",
    "summary": "read.rs's DIRECTIVE_FIELD_LABEL duplicates graph.rs's private DIRECTIVE_FIELD const byte-for-byte, self-documented as a mirror instead of one shared value",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GRAPH-ENGINE-GLOB-EXTRACTOR-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "`DIRECTIVE_FIELD` (43, doc 40-42, 'Lets a reader tell a directive edge from a declared reference edge in the one resolved-edge set') is a private const, consumed only within this file (894). Widen its visibility to `pub(crate)` so `read.rs` can import it directly instead of carrying its own duplicate string. No other change."
        },
        {
          "path": "src/read.rs",
          "description": "`DIRECTIVE_FIELD_LABEL` (1226, doc 1223-1225, 'the mirror of `graph`'s private `DIRECTIVE_FIELD`') duplicates `graph::DIRECTIVE_FIELD`'s value (`\"at-import\"`) byte-for-byte, with no compiler-checked link between the two — a future rename of one would silently desync the other. Delete the const and its doc comment; requalify its two use sites — the production narration inside `impact` (1191) and the inline test helper `directive` (1601-1607, used at 1652) — to `crate::graph::DIRECTIVE_FIELD` (a qualified reference, or a `use crate::graph::DIRECTIVE_FIELD;` import). No behavior change: identical string value."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/read.rs",
        "asserts": "removing_an_imported_member_unbacks_the_import (1642) stays green — its assertion on the rendered `imports it via `@at-import`` text is unaffected by which module owns the constant"
      }
    ],
    "acceptance": "read.rs carries no private DIRECTIVE_FIELD_LABEL; its two use sites read graph::DIRECTIVE_FIELD (widened to pub(crate)) instead of a hand-maintained duplicate. `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Found sweeping pipeline (posture rotation: formats done, pipeline this tick); DIRECTIVE_FIELD_LABEL's own doc comment already concedes it mirrors graph's const, but nothing ever consolidated them. Serialized behind GRAPH-ENGINE-GLOB-EXTRACTOR-CONSOLIDATE, last chain entry touching graph.rs — its blockedBy chain already passes through GRAPH-RESOLVED-EDGE-WALK-CONSOLIDATE, read.rs's only other pending toucher, so one blocker covers both files. scoped at 69e7571."
  },
  {
    "tag": "COVERAGE-NOTE-GOVERNS-FILE-LEAF-CONSOLIDATE",
    "summary": "coverage_note.rs's governs and governs_segment each re-derive the identical root/glob-leaf file match three times instead of sharing one helper",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "COVERAGE-NOTE-LOCK-PARSE-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/coverage_note.rs",
          "description": "`governs` (345-365) tests whether a kind's `governs` locus selects a file leaf twice in its own body — once inside the `collection_address` branch (354-355, `normalize_root(&governs.root) == parent && compile_glob(governs.glob_leaf()).is_some_and(|matcher| matcher.is_match(leaf))`) and once in the non-address branch (357, 361-363, same expression with `root` pre-bound) — and `governs_segment` (372-382) repeats the identical expression a third time (379-380), all three preceded by the same `split_file` call. Factor a private `fn governs_file_leaf(governs: &crate::kind::Governs, path: &str) -> bool` doing `let (parent, leaf) = split_file(path); normalize_root(&governs.root) == parent && compile_glob(governs.glob_leaf()).is_some_and(|matcher| matcher.is_match(leaf))`, and call it from all three sites. No behavior change: identical inputs produce identical outputs."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/coverage_note.rs",
        "asserts": "a_partially_governed_settings_json_names_only_the_present_ungoverned_residue, a_fully_represented_settings_json_retires_its_unmodeled_surface_finding, and a_wholly_ungoverned_mcp_json_keeps_the_full_finding_a_governed_one_retires_it stay green — the consolidation is call-site-only, both file-whole and segment governance verdicts are unchanged"
      }
    ],
    "acceptance": "governs and governs_segment both call one shared governs_file_leaf helper for the root/glob-leaf file match instead of each re-deriving it; the expression appears once in src/coverage_note.rs, not three times. `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Found sweeping the judges subsystem (posture rotation: pipeline done, judges this tick). Serialized behind COVERAGE-NOTE-LOCK-PARSE-HOIST, the current last chain entry touching coverage_note.rs, for shared-file safety (pending-entry.md, 'Disjoint, or serialized') — no functional dependency. scoped at 662cf07."
  },
  {
    "tag": "GATE-MANIFEST-SHARED-READ-HOIST",
    "summary": "gate()/explain() re-read+re-parse a manifest file (settings.json) once per governing kind, and coverage_note reads it again for coverage — hoist to one shared parse per file per run",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "JSON-MANIFEST-DISCOVERY-BOUNDARY-RESTORE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "`manifest_units` (1495-1508) calls `json_manifest::Manifest::read_kind(disc, kind)` (1501) once per governing kind — called from `kind_features`'s per-kind loop (gate()'s built-in/custom-kind dispatch) and again from `collect_directive_members`'s own per-kind loop until GATE-KIND-UNITS-DOUBLE-RESOLVE-HOIST collapses that second pass. Three built-in kinds — `hook`, `installed-plugin`, `known-marketplace` (builtin_kind.rs:239,303,344) — each govern `.claude/settings.json`, so one gate()/explain() run independently opens and parses that one file up to three times, each call passing only its own kind's single address (`json_manifest::Manifest::read` already accepts `addresses: &[&CollectionAddress]`, a set — the primitive supports a combined read, no caller uses it). Once JSON-MANIFEST-DISCOVERY-BOUNDARY-RESTORE lands `manifest_units`'s own discovery call, group the governing kinds sharing one discovered manifest path and call `Manifest::read` once per path with every governing kind's address, then route each kind's own collection's members back to its caller. Exact caching/threading shape (a per-run path-keyed cache gate()/explain() builds once and passes down, or a module-level cache) is a judgment call for build, named in the commit body."
        },
        {
          "path": "src/json_manifest.rs",
          "description": "`read_kind` (419-434) discovers and reads one kind's manifest file(s) with only that kind's own `address` (433, `Manifest::read(file, &[address])`) — the caller-shape half of the duplication; `Manifest::read`'s own signature (`addresses: &[&CollectionAddress]`, 331-334) already supports a combined multi-kind read. No change needed to `Manifest::read`/`parse` themselves — the fix is in how `read_kind`'s caller (main.rs) groups kinds by manifest path before calling it, per the main.rs edit."
        },
        {
          "path": "src/coverage_note.rs",
          "description": "`manifest_top_level_keys` (410-417) independently calls `crate::json_manifest::Manifest::read(&root.join(surface.path), &[])` (414) for a surface (e.g. `.claude/settings.json`) that gate()'s own per-kind resolution has already read and parsed for every governing kind — a fourth read of the same file. Its own doc comment (405-409) already concedes 'by this point the gate's own manifest kinds have already read the same file' without reusing that read. Once gate()/explain() hold the shared per-path Manifest cache the main.rs edit establishes, thread it (or its `opaque_fields`) into `coverage_note::check` (142) and have `manifest_top_level_keys` read from it instead of calling `Manifest::read` itself."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "a fixture whose .claude/settings.json is governed by hook, installed-plugin, and known-marketplace together (the real built-in overlap) proves the file is read+parsed exactly once per gate()/explain() invocation — a new count-pin, the same doctrine this file's discovery-walk/glob-compile/lock-row-walk pins already establish"
      },
      {
        "path": "tests/coverage_note.rs",
        "asserts": "a_partially_governed_settings_json_names_only_the_present_ungoverned_residue stays green — the hoist is signature-only, no change to which keys are classified checked vs residue"
      }
    ],
    "acceptance": "Within one gate()/explain() invocation, a manifest file governed by more than one kind (settings.json's hook/installed-plugin/known-marketplace overlap) is opened and parsed exactly once, and coverage_note::check's top-level-key read draws from that same parse rather than re-reading the file. A count-pin test proves it. No behavior change to which members or coverage residue is reported. cargo test and cargo clippy -D warnings green.",
    "notes": "Found sweeping judges (pipeline done, judges this tick) via coverage_note.rs's manifest_top_level_keys, whose doc concedes the redundancy unfixed — tracing it surfaced deeper per-kind duplication in main.rs/json_manifest.rs. Serialized behind JSON-MANIFEST-DISCOVERY-BOUNDARY-RESTORE, last chain entry touching both, whose read_kind narrowing this builds on. Shares coverage_note.rs with COVERAGE-NOTE-GOVERNS-FILE-LEAF-CONSOLIDATE too; ships independently. scoped at 662cf07."
  },
  {
    "tag": "JSON-MANIFEST-MEMBERS-ZERO-CONSUMER-PRUNE",
    "summary": "json_manifest.rs's manifest_members is pub(crate) but called only from Manifest::parse in the same file — zero outside consumer; narrow to private per the export-earns-its-consumer bar",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "An export earns its consumer"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GATE-MANIFEST-SHARED-READ-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/json_manifest.rs",
          "description": "`manifest_members` (538-564, `pub(crate)`, doc 519-537) has exactly one call site anywhere in the tree — `Manifest::parse` (378, same file) — plus its own inline tests (942-1051, e.g. `manifest_members_walk_each_entry_raw_and_skip_a_non_object` at 942); grep-verified zero hits in src/, tests/, sdk/ outside this file (kind.rs:213's mention is an intra-doc link, not a call — see that file's edit below). Its sibling grammar fns moved from extract.rs in the same commit (404b73a, EXTRACT-FOUNDATION-BOUNDARY-RESTORE) were narrowed correctly there — `enablement_member_fields`/`hook_member_fields` are already private, called only in-module — but `manifest_members` kept the `pub(crate)` visibility without ever gaining a cross-module caller at its new address, the same shape the shipped GRAPH-WORLD-ZERO-CONSUMER-PRUNE and KIND-ZERO-CONSUMER-EXPORTS-PRUNE precedents already pruned (a `pub`/`pub(crate)` item called only from one function in its own file). Drop `pub(crate)`, leaving `manifest_members` module-private — `Manifest::parse` and the inline tests are unaffected (private items stay visible to a module's own child `tests` mod). The doc comment's justifying line (536-537, 'pub(crate) so the JSON manifest adapter reads a collection address's members off the one grammar the frontmatter path also parses to') names no real external consumer — the 'JSON manifest adapter' it gestures at *is* this module — so drop that clause along with the visibility rather than leaving a stale rationale for a narrowed fn."
        },
        {
          "path": "src/kind.rs",
          "description": "`ENABLEMENT_FIELD`'s doc comment (211-213, '...the read face that names it ([`crate::json_manifest::manifest_members`])...') carries an intra-doc link to the item this entry makes private — unresolvable once `manifest_members` loses `pub(crate)`, since rustdoc cannot link to a private item from another module. Reword to a plain code-span reference (`` `json_manifest::manifest_members` ``, no brackets) rather than ship a broken intra-doc link."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/json_manifest.rs",
        "asserts": "manifest_members_walk_each_entry_raw_and_skip_a_non_object, enablement_members_carry_the_scalar_value_and_re_render_identically, and hook_members_decompose_the_matcher_group_array_and_re_nest_identically all stay green — the narrowing is visibility-only, no behavior change to the function"
      }
    ],
    "acceptance": "manifest_members is a private fn in src/json_manifest.rs, not pub(crate); kind.rs's ENABLEMENT_FIELD doc comment no longer carries an intra-doc link to it. cargo test and cargo clippy --all-targets -- -D warnings green (no unused-pub warning survives).",
    "notes": "Found sweeping the formats subsystem (posture rotation: model done, formats this tick). Serialized behind GATE-MANIFEST-SHARED-READ-HOIST, the last entry in the existing chain touching json_manifest.rs (pending-entry.md, 'Disjoint, or serialized') — no functional dependency. scoped at e733883."
  },
  {
    "tag": "ENGINE-PREDICATE-FENCE-EXHAUSTIVE-MATCH",
    "summary": "engine.rs's bodyless/judgeless/vacuities fall through `_ =>` over Predicate instead of naming every variant like sibling addressed_field does in the same file",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "A shared concept is one type"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GRAPH-ENGINE-GLOB-EXTRACTOR-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/engine.rs",
          "description": "Three admissibility-fence functions over the closed `Predicate` enum (27 variants) close their match with a wildcard instead of naming every variant, unlike this file's `addressed_field` (201-235, doc 198-200: 'The match names every arm rather than defaulting: a bound that is not enforced is not a bound, and a wildcard would admit the next field-carrying predicate ... instead of asking its author this question at compile time') and the file's own `decide`/`judge` dispatch, both already exhaustive. `bodyless` (256-272) matches only `RequireSections`/`SectionContains`/`NameMatchesDir` then `_ => return None` (264) — a future body-derived predicate added without updating this match silently skips the embedded-locus fence instead of 'degrading to a check that never ran', the very failure its own doc warns against. `judgeless` (278-287) matches only `DependencyExists` then `_ => None` (285). `vacuities` (297-408) guards 13 variants (`ClosedKeys`, `Enum`, `Deny`, `ForbiddenKeys`, `Type`, `RequireSections`, `SectionContains`, `Range`, `Count`, `Membership`, `Degree`, `Kind`, `MentionReachable`) then falls through `_ => Vec::new()` (407) for the remaining 14 (`Required`, `Optional`, `MinLen`, `MaxLen`, `AllowedChars`, `Shape`, `Extent`, `MustDefine`, `NameMatchesDir`, `UniqueName`, `DependencyExists`, `Unique`, `GlobValid`, `FormatPlacesEdges`). Replace each wildcard with one pipe-grouped arm naming every unmatched variant explicitly, mapped to the same fallback value (`None` for bodyless/judgeless, `Vec::new()` for vacuities) — mirroring addressed_field's style. No behavior change: every currently-unmatched variant already produces the wildcard's value."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/engine.rs",
        "asserts": "existing inline tests over bodyless/judgeless/vacuities (embedded-locus fence, dependency-exists hold, closed-keys/enum/range/degree/mention-reachable vacuity cases) pass unchanged at the new exhaustive matches — the rewrite is exhaustiveness-only"
      },
      {
        "path": "tests/gate_fail_loud.rs",
        "asserts": "a_body_shaped_clause_bound_to_an_embedded_kind_fails_admissibility stays green — exercises bodyless's fence end to end"
      }
    ],
    "acceptance": "bodyless, judgeless, and vacuities each name every Predicate variant explicitly in their match, no `_` wildcard arm remains in any of the three, matching the exhaustive style addressed_field already holds over the same enum in the same file. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping the judges subsystem (posture rotation: pipeline done, judges this tick). Same shape as the already-queued KIND-DECLARED-FIELDS-EXHAUSTIVE-MATCH/CONTRACT-DECLARED-KEYS-EXHAUSTIVE-MATCH precedent (a `_ =>` fallback over a shared enum beside an already-exhaustive sibling match in the same file). Serialized behind GRAPH-ENGINE-GLOB-EXTRACTOR-CONSOLIDATE, last chain entry touching engine.rs, for shared-file safety — no functional dependency. scoped at 662cf07."
  },
  {
    "tag": "CONTRACT-WHEN-GUARD-CLAUSE-FORM",
    "summary": "The clause algebra gains `when`: a guard predicate (enum/type) over an addressable field conditioning a body of ordinary clauses, one shared address binding, no nesting (decision 0041)",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "ENGINE-PREDICATE-FENCE-EXHAUSTIVE-MATCH"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/contract.rs",
          "description": "Decision 0041 ratifies a conditional clause form: a clause may carry a **guard** — one predicate from the scalar-decidable set (initially `Predicate::Enum`/`Predicate::Type` only, both of which already carry their own `field`; the 26-variant `Predicate` enum spans 132-399) — conditioning a **body** of ordinary clauses, evaluated only at the elements the guard's field locates (shared address binding: each element `FieldPath::locate`, address.rs:135-141, yields is judged independently, body clauses evaluating at that same element), silent — no finding, not even a vacuity — wherever the guard doesn't hold, guarded-field absence included. No nesting: a body clause carrying its own guard is inadmissible. The cleanest fit given the existing architecture: add `Predicate::When { guard: Box<Predicate>, body: Vec<Clause> }` as the 27th variant — `Clause` (44-68: label/severity/predicate/guidance/source) stays structurally unchanged, matching the precedent `RequirementRow` already sets nesting `Vec<ClauseRow>` (drift.rs ~3036-3054) — but the concrete representation is build's call; name the choice in the commit body. Whichever shape, EVERY already-exhaustive match over `Predicate` in this file needs a `When` arm added at implementation time, since two of them ship exhaustive ahead of this entry in the chain (CONTRACT-DECLARED-KEYS-EXHAUSTIVE-MATCH, CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP, both earlier in the blockedBy chain this entry sits behind): `target` (642-675), `documented_field` (686-722), and `declared_keys` (736-745, post-exhaustiveness-fix) all report `None` for `When` — guard/body carry no field or schema key of their own to expose the way a single-field predicate does. `predicate_from_row` (424-548, 24/26 keys handled today, falls through `_ => return None` at 546) gains a `\"when\"` arm decoding the guard sub-predicate and recursing over the body's nested `ClauseRow`s via `compose::clause_from_row`. Also add `Shape::LeadingDotSlash` (783-803, joining `HyphenPlacement`/`NoXmlTags`) — decision 0041's second, smaller addition, the relative-path spelling rule marketplace's `source` string form documents — updating every exhaustive match over `Shape` (`name`/`from_name`/`pattern`/`match_holds`/`admits`/`demand`, ~813-897) with the third arm."
        },
        {
          "path": "src/drift.rs",
          "description": "`ClauseRow` (2878-2979) needs a shape carrying a `when` row's guard and nested body through the lock. `RequirementRow` (~3036-3054) already nests `pub clauses: Vec<ClauseRow>` for an unrelated reason (a requirement's own attached clauses) — the same nesting shape is available here: a `when` row's guard (reusing the existing `value_type`/`values` columns the way a bare `Enum`/`Type` row already would) plus a `body: Option<Vec<ClauseRow>>` column nesting the body's own rows. `stamp_clause_label` (1363, writes every clause row's compiled address over the whole lock) needs to recurse into a `when` row's nested body rows so each body clause still gets its own address. Exact column shape is build's call, matching whichever `Predicate::When` representation contract.rs lands; document it the way every other `ClauseRow` column already is. This regenerates `sdk/src/generated/ClauseRow.ts` via the existing ts-rs export test — do not hand-edit the generated file."
        },
        {
          "path": "src/compose.rs",
          "description": "`clause_from_row` (238, `Result<contract::Clause, ClauseRowError>`) decodes a `ClauseRow` via `severity_from_label` (239), `contract::predicate_from_row` (243), and the row's `label` (248). Once `predicate_from_row` gains a `\"when\"` arm, decoding the guard predicate itself needs no structural change here — but decoding the body's nested `ClauseRow`s requires recursing into this same function per nested row, surfacing a malformed body clause as a loud `ClauseRowError` rather than dropping it silently (extend the existing `Severity`/`Predicate`/`Label` variants or add one for a malformed body row; name the choice in the commit body)."
        },
        {
          "path": "src/builtin.rs",
          "description": "A second `clause_from_row` (25, doc 9, decoding `ClauseRow`s for the embedded built-in lock read path) needs the same `when`-decoding update as `compose::clause_from_row` — verify at implementation time whether the two functions' `when`-handling can share one body-decoding helper rather than each carrying its own copy, since this file's own header already frames itself as the mirror path `compose.rs`'s function serves."
        },
        {
          "path": "src/engine.rs",
          "description": "`admissibility` (110-118) and `inadmissibilities` (153-163, per clause: `judgeless`, `bodyless`, `unaddressable`, `vacuities`) gain the two `when`-specific admissibility checks the decision names: the guard predicate is restricted to `Enum`/`Type` (any other guard predicate is inadmissible, never silently accepted), and a body clause whose own predicate is itself `When` is inadmissible (no nesting) — sited wherever build judges fits the existing per-predicate-rule shape. Since ENGINE-PREDICATE-FENCE-EXHAUSTIVE-MATCH (this entry's own blocker) makes `bodyless`, `judgeless`, and `vacuities` exhaustive matches ahead of this entry, all three also need an explicit `When` arm at implementation time (mapping to their existing fallback value — `None`/`None`/`Vec::new()` — unless the design decision above chooses to fold the no-nesting/guard-restriction check into one of them instead of a new helper). `addressed_field` (201-235, doc 198-200: 'The match names every arm rather than defaulting') gains a `When` arm returning `None`, matching the key/marker/set predicates' existing `None` arms. `decide` (774-1101, per-member exhaustive match) and `judge` (487-540, whole-selection match) both gain `When` arms implementing the shared-binding evaluation: `decide` locates every element the guard's field addresses, judges the guard at each, and for each element where it holds, evaluates every body clause's own `decide` at that same element, folding results — silent at elements where the guard doesn't hold or the field is absent."
        },
        {
          "path": "tests/gauntlet.rs",
          "description": "Per engineering.md's gauntlet-corpus and vacuity bars: add a `when`-guarded clause over a mixed collection (one string-form element, one object-form element — mirroring the marketplace `plugins[].source` shape decision 0041 is driven by) proving the shared-binding invariant: the object-form element's guarded body clause fires only at that element and never leaks a finding onto the string-form element; plus a case where the guarded field is absent everywhere, asserted as the explicit vacuous-but-legitimate empty case, never silently inherited."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/gauntlet.rs",
        "asserts": "a when-guarded clause over a mixed collection fires its body only at elements where the guard holds, never leaking onto elements where it doesn't (or where the guarded field is absent) — the shared-binding invariant decision 0041 names by name"
      },
      {
        "path": "src/engine.rs",
        "asserts": "inline admissibility tests: a when guard predicate outside Enum/Type is inadmissible; a body clause carrying its own when guard (nesting) is inadmissible"
      },
      {
        "path": "src/contract.rs",
        "asserts": "predicate_from_row round-trips a when row (guard plus a body of ordinary clauses) back into the chosen Predicate::When shape, matching the pattern target/documented_field's exhaustive matches already hold over every other variant"
      }
    ],
    "acceptance": "A `when` guard-and-body clause form exists end-to-end in the Rust engine: admissible only over an `Enum`/`Type` guard on an addressable field, never nested; body clauses evaluate exactly at the elements the guard's field locates, silent (not a finding, not a vacuity) wherever the guard doesn't hold including field-absence. Every exhaustive match over `Predicate` (`target`, `documented_field`, `declared_keys`, `addressed_field`, `bodyless`, `judgeless`, `vacuities`, `decide`, `judge`) answers the new variant by construction. `cargo test` and `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Decision 0041. Concrete Rust representation (Predicate::When{guard,body} vs a Clause-level guard/body pair) is build's call — name it in the commit body; RequirementRow's nested Vec<ClauseRow> is the closest precedent. Serialized behind ENGINE-PREDICATE-FENCE-EXHAUSTIVE-MATCH (its own chain covers CONTRACT-DECLARED-KEYS-EXHAUSTIVE-MATCH/CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP too) so the new variant lands after those exhaustive matches ship."
  },
  {
    "tag": "BUILTIN-CONTRACT-FOR-KIND-CONSOLIDATE",
    "summary": "builtin.rs's contract_for_kind hand-rolls compose.rs's default_contract_from_rows' filter-by-kind/build-Contract shape over the embedded lock instead of calling it",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "CONTRACT-WHEN-GUARD-CLAUSE-FORM"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/builtin.rs",
          "description": "contract_for_kind (36-48) filters builtin_lock::declarations().clauses by row.kind.as_deref() == Some(kind), maps each row through this file's own private clause_from_row wrapper (25-30, itself only compose::clause_from_row(row).expect(...)), collects, and builds Contract{name: kind.to_string(), clauses, guidance: None} — the identical filter/map/collect/construct shape compose.rs's default_contract_from_rows (141-155) already implements over the committed lock's rows, down to the same Contract field construction; the two differ only in trusting the embedded rows to never error versus propagating ClauseRowError. compose.rs's own doc comment (132-133) already names the two as 'the same lift ... run here over the committed lock's [rows]', but the code duplicates the shape rather than sharing it. Replace contract_for_kind's body with a call to compose::default_contract_from_rows(&builtin_lock::declarations().clauses, kind), unwrapped via .expect() carrying the existing panic message unchanged ('the embedded built-in lock declares only required/advisory severities and this projection's supported predicates, each carrying its required argument'), and delete the now-unused private clause_from_row wrapper (18-30, doc included) along with any import it alone required."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/contract_template.rs",
        "asserts": "the_shipped_built_in_packages_are_admissible stays green and non-vacuous over the whole embedded roster — every kind's floor contract is byte-identical after routing through the shared lift"
      },
      {
        "path": "tests/rule_contract.rs",
        "asserts": "the exact pinned decidable clause vector for rule's embedded floor is unchanged by the refactor"
      }
    ],
    "acceptance": "builtin.rs's contract_for_kind calls compose::default_contract_from_rows instead of re-implementing its filter/collect/construct-Contract shape; the local clause_from_row wrapper is gone. No behavior change (engineering.md, 'One job, one home'). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping the provider subsystem (posture rotation: pipeline done, provider this tick; provider's only touch since its last sweep at fe3ff3f is 516f8f6, already-shipped, no residue). Serialized behind CONTRACT-WHEN-GUARD-CLAUSE-FORM, the sole queue entry touching builtin.rs, for shared-file safety — no functional dependency; that entry's files[] already flags this exact duplication as a to-verify item, so this resolves it first. scoped at 7622f13."
  },
  {
    "tag": "CONTRACT-WHEN-SDK-AUTHORING-ROUNDTRIP",
    "summary": "SDK gains a `when(guard, body)` clause constructor and lock round-trip for decision 0041's guard form; declarations.ts/generated types/root export updated",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "The lock"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "CONTRACT-WHEN-GUARD-CLAUSE-FORM"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/contract.ts",
          "description": "The `Predicate` interface (24-79) and its constructor functions (node-scope predicates at 96-227, e.g. `type` 110-114, `enumOf` 187-191 — both restricted-to candidates for the guard) gain a new `when(guard: Predicate, body: readonly Clause[]): Clause`-shaped export, following the file's own JSDoc-plus-`export const camelName = (...): T => ({...})` pattern. Restrict `guard` to `type`/`enumOf`-shaped predicates the way build's chosen Rust representation admits — either at the TypeScript type level or via the same emit-time admissibility check the engine performs (never silently accepting a third predicate as a guard). Signature and return type mirror whichever Rust shape CONTRACT-WHEN-GUARD-CLAUSE-FORM lands; verify before implementing. Also add the `leading-dot-slash` variant to `sdk/src/generated/Shape.ts` (machine-generated via the ts-rs export test — regenerate, never hand-edit) feeding `shape()` (125-136)."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "`clauseRow` (65-116, doc 51-64) maps each `Predicate` field to its `ClauseRow` column via `predicate.key === \"...\"` branches. Add the `when` branch: emit the guard's own predicate columns (reusing the existing per-predicate columns the guard's own key already populates, e.g. `value_type`/`values`) plus the body's nested rows, recursing `clauseRow` over each body clause — matching whichever nested-row shape `ClauseRow` lands in drift.rs."
        },
        {
          "path": "sdk/src/index.ts",
          "description": "Export `when` from the root alongside the other predicate/clause constructors (25-56, alphabetical list — inserts between `uniqueName` and `type`, or wherever alphabetization lands it)."
        },
        {
          "path": "sdk/test/contract.test.ts",
          "description": "New test following the `skillClauseRow` helper pattern (77-87, e.g. the `enumOf` test at 105-113): `when(enumOf(...), [required(...)])` composes the documented `Predicate`/`ClauseRow` shape, and the mixed-array binding case (one string-form, one object-form element) round-trips through `compileDeclarations` with the body firing only at the guarded element."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "A round-trip test analogous to the `require_sections` precedent CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP establishes: push a `when`-carrying `ClauseRow` (guard plus a nested body) through `emit` and `read_declarations`, asserting `compose::clause_from_row` decodes it back into the same guard-and-body shape with the same elements."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/contract.test.ts",
        "asserts": "when(guard, body) composes the documented Predicate/ClauseRow shape and the mixed-array binding case round-trips through compileDeclarations, matching the engine's own gauntlet cell"
      },
      {
        "path": "tests/lock_declaration_rows.rs",
        "asserts": "a when-carrying ClauseRow round-trips through emit + read_declarations back into the same guard-and-body shape, the same bar CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP's round-trip test sets for require_sections"
      }
    ],
    "acceptance": "An SDK author calls when(guard, body) composing a guarded clause; it emits through declarations.ts into the lock's ClauseRow shape, and the Rust read side decodes it back losslessly into the same guard-and-body value — closing decision 0041's SDK half. `pnpm --dir sdk test` and `cargo test` green.",
    "notes": "Decision 0041, SDK half. Blocked by CONTRACT-WHEN-GUARD-CLAUSE-FORM since ts-rs generates ClauseRow.ts from the Rust struct and this entry's round-trip test needs the Rust decode side settled first. Exact TS signature/type restriction is build's call, matching whichever Rust representation lands upstream."
  },
  {
    "tag": "BUILTINS-DEFAULT-CONTRACT-HOLDS-CLOSE",
    "summary": "Re-examine the three shipped default-contract holds against decision 0041's widened vocabulary — marketplace's source union, mcp-server's transport fields, hook's per-event schema",
    "per": {
      "path": "specs/builtins.md",
      "section": "Default contracts"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "CONTRACT-WHEN-SDK-AUTHORING-ROUNDTRIP"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/builtins.ts",
          "description": "Three default contracts in this file carry a documented `hold` (specs/builtins.md, 'Default contracts': a documented, decidable rule the vocabulary cannot yet spell, named at the point of enforcement with the widening that closes it, decision 0033) that decision 0041's `when`/`enum`/`type` guard widening was ratified to close, per its own Consequences. (1) `marketplaceDefaultContract` (1018-1109, hold doc 1018-1053, hold quote 1027-1037: the `plugins[].source` `string|object` union — a relative-path string form documenting a leading `./` spelling, or an object form whose `source` discriminator, `\"github\"|\"url\"|\"git-subdir\"|\"npm\"` per code.claude.com/docs/en/plugin-marketplaces, decides its own required companion fields) — complete it with `when(enumOf(\"plugins[*].source.source\", [...documented discriminator values]), [required(...per-kind companion fields...)])` clauses plus a `shape(..., \"leading-dot-slash\")` clause validating the string form, re-verifying the exact per-discriminator required-field list against current docs (retrieved date updated) before citing it, and retire the header hold paragraph (1027-1037). (2) `mcpServerDefaultContract` (1547-1580, hold doc 1557-1572, hold quote 1566-1571: `type` discriminates a `stdio` server's need for `command` vs. a remote transport's need for `url`) — same shape, `when(enumOf(\"type\", [...]), [required(\"command\")])` / `when(enumOf(\"type\", [...]), [required(\"url\")])` per the documented transport set (code.claude.com/docs/en/mcp, re-verify exact values), retiring its header hold. (3) `hookDefaultContract` (1520-1545, hold-like doc 1520-1537, quote 1529-1536: the per-event handler's own `type`/`command`/`url`/`timeout` schema lives one array level deeper than the collection address `hooks.<Event>` walks into) — re-examine against the widened vocabulary and confirm on disk whether any addressing extension since this hold was written now reaches the nested matcher-group array; per this tick's investigation, none does, so this hold is an addressing-reach gap `when`/`enum`/`type` cannot close (they guard a field that must first be addressable) — reword its header to state it was re-examined against decision 0041 and confirmed to still hold, rather than leaving it looking unexamined. If the re-examination instead finds the field newly addressable, complete the clause instead and say so in the commit body."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/marketplace_kind.rs",
        "asserts": "the completed source-union contract catches every documented violation shape (bad discriminator value, missing companion field per kind, a string form missing the leading ./) and passes every valid one"
      },
      {
        "path": "sdk/test/builtins.test.ts",
        "asserts": "the completed mcp-server transport-conditional contract catches a stdio entry missing command and a remote entry missing url"
      },
      {
        "path": "sdk/test/builtins.test.ts",
        "asserts": "hookDefaultContract's re-examined hold stays documented and unresolved if the addressing gap is confirmed unchanged — a vacuity-honest non-resolution, named as such"
      }
    ],
    "acceptance": "marketplaceDefaultContract's source union and mcpServerDefaultContract's transport-conditional requirements are both fully expressed via when/enumOf/shape clauses with current per-clause cites, their header holds retired; hookDefaultContract's hold is either completed or its header explicitly confirms re-examination against decision 0041's widened vocabulary with the addressing-reach reason it still stands. `cargo test` and `pnpm --dir sdk test` green.",
    "notes": "Decision 0041 Consequences: marketplace + 'other two' vocabulary holds, combined since all three sit in sdk/src/builtins.ts (one Consequences bullet). Investigation this tick found exactly three vocabulary-shaped holds — no fourth candidate; every other shipped contract's absence is explicitly semantic, not vocabulary-blocked. Blocked by CONTRACT-WHEN-SDK-AUTHORING-ROUNDTRIP for the when()/leading-dot-slash vocabulary consumed here."
  },
  {
    "tag": "DRIFT-CONFIG-STALE-LOCK-PARSE-HOIST",
    "summary": "gate()'s config_stale re-reads lock.toml via walk_lock_rows instead of the shared doc DRIFT-SOURCE-DEP-PARSE-HOIST threads through; walk_lock_rows also duplicates read_lock_document's own read+parse",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "DRIFT-SOURCE-DEP-PARSE-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "walk_lock_rows (1996-2007) duplicates read_lock_document (2029-2044)'s fs::read_to_string + .parse::<DocumentMut>() + increment_lock_reads/increment_lock_parses sequence instead of calling read_lock_document then walk_lock_rows_from_doc(&doc) — a second implementation of the same read-and-parse job. config_stale (2462, loop at 2465) calls walk_lock_rows(workspace_dir) independently inside gate(), a further full read+parse of lock.toml on top of the ones DRIFT-SOURCE-DEP-PARSE-HOIST already targets (read_declarations, layout_imports+includes, layout_import_stale, include_stale) — five-plus parses of one file in one gate() invocation today. Once DRIFT-SOURCE-DEP-PARSE-HOIST lands gate() holding one shared parsed lock document, thread it into config_stale too so it calls walk_lock_rows_from_doc(&doc) instead of walk_lock_rows(workspace_dir). Separately (independent of gate()'s own hoist), rewrite walk_lock_rows itself to call read_lock_document(workspace_dir) then walk_lock_rows_from_doc(&doc) rather than duplicating the read+parse+counter body — its one remaining standalone caller, emit_owned_targets (2717), is a one-shot CLI entry point outside gate()/emit() with no shared doc to reuse, so this half is pure one-job-one-home dedup, not a cost fix."
        },
        {
          "path": "src/main.rs",
          "description": "gate() (847) calls drift::config_stale(workspace) at 1202. Once config_stale takes the shared doc/Declarations value DRIFT-SOURCE-DEP-PARSE-HOIST's threading establishes (exact shape settled by that entry), update this call site to pass it instead of the bare workspace path."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "a fixture whose gate() run exercises config.stale findings reads/parses lock.toml exactly once per invocation — a count-pin, the same doctrine this file's discovery-walk/glob-compile/lock-row-walk pins already establish"
      },
      {
        "path": "tests/install.rs",
        "asserts": "config_stale's existing coverage (536, 607) stays green — the hoist and the walk_lock_rows rewrite are signature/location-only, no behavior change to which findings fire"
      }
    ],
    "acceptance": "config_stale consumes the same shared parsed lock document gate() already holds instead of independently re-reading/re-parsing lock.toml; walk_lock_rows calls read_lock_document + walk_lock_rows_from_doc instead of duplicating the read+parse body. No behavior change (engineering.md, 'Cost scale is hoisted'). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping pipeline, second rotation pass (posture rotation: formats done, pipeline this tick — a fresh full read since 0282dc7/72daab3/112b188 touched drift.rs). Distinct from DRIFT-SOURCE-DEP-PARSE-HOIST (source_deps' own layout_import/include family) — this is config_stale's separate walk_lock_rows path plus walk_lock_rows's own duplicate read+parse body. Serialized behind DRIFT-SOURCE-DEP-PARSE-HOIST, whose gate()-side doc-threading this builds directly on. scoped at 7ac498a."
  },
  {
    "tag": "MAIN-GUARD-DECLARATIONS-DOUBLE-READ-HOIST",
    "summary": "Command::Guard reads and parses lock.toml's declarations twice per invocation — mode_from_lock and guarded_manifests each independently call drift::read_declarations",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "DRIFT-SOURCE-DEP-PARSE-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "`Command::Guard`'s handler (368-425) calls `mode_from_lock(&workspace_dir)` (382), which calls `drift::read_declarations` (642-644) — a full `fs::read_to_string` + TOML parse — then separately calls `guarded_manifests(&workspace_dir)` (385), which calls `drift::read_declarations` again (688) on the identical path: an independent second read+parse of the same file within one CLI invocation. `mode_from_lock` already has a sibling `mode_from_declarations` (652-661) built for exactly a caller holding declarations already (its own doc: 'a caller that has already read them — gate, which needs the mode... and must never reach a second verdict on the posture from a second read'), but Command::Guard doesn't use it. `guarded_manifests` (687) does nothing else with `workspace_dir` beyond that one read (grep-verified — its body only uses the returned `declarations`), so its signature can take `&drift::Declarations` directly. Fix: Command::Guard reads declarations once, calls `mode_from_declarations(&declarations)` for the mode and passes the same `&declarations` into a re-signatured `guarded_manifests`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.rs",
        "asserts": "guard_reads_a_pretooluse_payload_and_acts_on_the_posture and guard_rejects_a_corrupt_lock_loud_and_defaults_only_on_a_missing_one stay green — the hoist is signature-only, no change to guard's mode/manifest resolution"
      }
    ],
    "acceptance": "Command::Guard's handler parses the workspace's lock.toml declarations exactly once per invocation; guarded_manifests takes &drift::Declarations instead of workspace_dir. No behavior change (engineering.md, 'Cost scale is hoisted'). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping verbs (posture rotation) — untouched by the five queued *-PARSE-HOIST entries, all scoped to gate()/explain(). No runtime count-pin: read_declarations doesn't feed drift's lock_read_count()/lock_parse_count() counters — proven structurally instead, the named exception per 'A fix ships the test that would have caught it.' Serialized behind DRIFT-SOURCE-DEP-PARSE-HOIST since it and MAIN-LOCK-ROW-CONSTRUCTORS-TO-DRIFT are already both open over main.rs. scoped at 2d1c5a6."
  },
  {
    "tag": "DRIFT-MANIFEST-SEGMENT-REAP-READ-HOIST",
    "summary": "emit()'s manifest_segment_reaps and emit_manifest each independently read the same on-disk manifest file per represented manifest, within one emit() pass",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "DRIFT-CONFIG-STALE-LOCK-PARSE-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "manifest_segment_reaps (1546, doc 1528-1545) reads harness_root.join(path) via fs::read_to_string (1573) and parses it via json_manifest::Manifest::parse (1576) to diff discovered members/opaque keys against the build's segments/residue, for drift detection before any write. Called once per represented manifest at 1300-1301, ahead of any emit. Later in the same emit() call, emit_manifest (1465) reads the identical file again at 1499 via fs::read(path) — this time purely to compare raw bytes for the Unchanged/Emitted decision — called per manifest at the loop 1322-1333. manifest_segment_reaps already holds `raw` (the exact file text) when its read succeeds; thread it (or the read outcome) into emit_manifest instead of re-reading the same path. Exact threading shape (pass an `Option<String>` alongside `(path, build)` through the `manifests` iteration, or fold the two loops into one pass per manifest) is a judgment call for build, named in the commit body."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/emit.rs",
        "asserts": "existing dry-run/unchanged/emitted coverage for represented manifests stays green — the hoist is read-count-only, no output or write-decision change"
      },
      {
        "path": "tests/check_cost.rs",
        "asserts": "a fixture with at least one represented manifest proves the manifest file is read exactly once per emit() pass — a new count-pin, the same doctrine this file's discovery-walk/glob-compile/lock-row-walk pins already establish"
      }
    ],
    "acceptance": "within one emit() run, each represented manifest's on-disk file is read exactly once, shared between the segment-reap diff and the Unchanged/Emitted decision. No behavior change (engineering.md, 'Cost scale is hoisted'). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping pipeline, second rotation pass. Distinct from JSON-MANIFEST-DISCOVERY-BOUNDARY-RESTORE/GATE-MANIFEST-SHARED-READ-HOIST (those are gate()/explain()'s per-governing-kind reads of settings.json outside emit) — this is emit()'s own double-read of one manifest file within its own pass. Serialized behind DRIFT-CONFIG-STALE-LOCK-PARSE-HOIST, both touching drift.rs (pending-entry.md, 'Disjoint, or serialized'). scoped at 7ac498a."
  },
  {
    "tag": "IMPORT-DECLARED-GOVERNED-PATHS-HOIST",
    "summary": "import.rs's declared_governed_paths recomputes the same claimed-path set from scratch on every nested-file-kind resolution instead of once per run",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "NORMALIZE-PATH-SUBSYSTEM-PLACEMENT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "declared_governed_paths (285-297, doc 280-284) iterates every declared kind carrying a `governs` locus and walks its files (discover_kind_files) to build the claimed-path BTreeSet — purely a function of `kinds`/`over`/`disc`, none of which vary across calls within one run. Its sole caller, discover_nested_file (237, calling it at 249), runs once per nested-file kind's resolution — reached via discover_builtin (195, call at 203) and directly from main.rs's resolve_kind_units (src/main.rs:1353), itself called once per declared kind (twice until GATE-KIND-UNITS-DOUBLE-RESOLVE-HOIST lands). A harness with N nested-file kinds recomputes the identical claimed set N times (2N today). Hoist: compute the claimed set once per run — cached on Discovery the way its own local/standard flavor walks already are, or computed once by the caller and threaded down — and reuse across every discover_nested_file call. Exact caching shape is a judgment call for build, named in the commit body."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/nested_member.rs",
        "asserts": "existing coverage for a governs-carve-out interacting with a nested-file kind's discovery stays green — the hoist is call-count-only, no change to which files are claimed or found (re-verify the exact fixture at pickup)"
      }
    ],
    "acceptance": "declared_governed_paths's claimed-path set is computed once per gate()/emit()/explain() run and shared across every nested-file kind's discover_nested_file call, not recomputed per kind. No behavior change (engineering.md, 'Cost scale is hoisted'). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping pipeline, second rotation pass. No existing pending entry or refactor capture names this fn. Serialized behind NORMALIZE-PATH-SUBSYSTEM-PLACEMENT, the only other pending entry touching import.rs (pending-entry.md, 'Disjoint, or serialized'). scoped at 7ac498a."
  },
  {
    "tag": "READ-BY-KIND-INDEX-HOIST",
    "summary": "explain()'s Member-species path independently scans the whole by_kind corpus at five-plus separate sites instead of sharing one id-keyed lookup built once",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "READ-GRAPH-DIRECTIVE-FIELD-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/read.rs",
          "description": "For a Species::Member target (the common case), explain (218, dispatch 233-264) calls resolve (158, its own by_kind scan), why (320, its own by_kind linear match-build at 348-360), impact (593 -> impact_one, 1115, whose sole-satisfier check calls count_satisfiers (1231) once per requirement in features.satisfies, each call its own by_kind scan), context (856 -> context_member_one, 983, via narrate_satisfied (1089), its own by_kind scan), and field (1464, its own by_kind.values().flat_map(...) scan at 1476-1480) — five-plus independent linear passes over the full by_kind corpus for one explain() call. by_kind is exactly the whole-input structure the corpus resolves once per run in main.rs and threads down; explain never indexes it once and shares that index across its own strands. Build (once, in explain or a helper it calls first) an id-keyed index — e.g. a BTreeMap<&str, Vec<(&str, &Features)>> — and thread it alongside (or instead of) by_kind into why/count_satisfiers/narrate_satisfied/field, replacing each function's own by_kind scan with an index lookup. Exact index shape and which call sites switch to it is a judgment call for build, named in the commit body — distinct from GRAPH-RESOLVED-EDGE-WALK-CONSOLIDATE (the edge-resolution walk resolved_edges/resolved_arcs, a different derivation why/impact also call but not this by_kind member-lookup scan)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "existing explain() narration coverage for a member target (multi-strand output) stays byte-identical — the hoist is internal-lookup-only, no narration text change"
      }
    ],
    "acceptance": "a single explain() call for a Species::Member target builds its by_kind lookup index once and every strand (why/count_satisfiers/narrate_satisfied/field) reads from it instead of independently rescanning by_kind. No behavior change (engineering.md, 'Cost scale is hoisted'). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping pipeline, second rotation pass (read.rs is the pipeline subsystem's 'read verbs' resolution' module). Distinct from GRAPH-RESOLVED-EDGE-WALK-CONSOLIDATE (edge walk, not member lookup) and READ-GRAPH-DIRECTIVE-FIELD-CONSOLIDATE (a const dedup, not a cost hoist). Serialized behind READ-GRAPH-DIRECTIVE-FIELD-CONSOLIDATE, the last pending entry touching read.rs (pending-entry.md, 'Disjoint, or serialized'). scoped at 7ac498a."
  },
  {
    "tag": "IMPORT-READ-DIRS-VACUOUS-PIN-RETIRE",
    "summary": "READ_DIRS's guarded fs::read_dir call site was cut at 0bc0ee9 — the counter can never move, so check_cost.rs's read_dirs==0 assertion is vacuous; retire counter, getter, assertion",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "A green verdict is proven non-vacuous"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "IMPORT-DECLARED-GOVERNED-PATHS-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "Retire the `READ_DIRS` thread_local (118-126, doc: 'a scan reintroducing a fs::read_dir bumps it off zero') and its getter `read_dir_count()` (128-134). `rg 'fs::read_dir\\b' src/` returns zero call sites anywhere in the crate — the guarded site 0bc0ee9 removed was the only one that ever existed, and `collect_glob` (419) derives every kind's matches from the in-memory `Discoverable` index, never the filesystem. The counter is structurally unconstructable to nonzero; nothing wires it, nothing can."
        },
        {
          "path": "tests/check_cost.rs",
          "description": "Drop `read_dirs_before`/`read_dirs` (148,154), the `{read_dirs}` term in the phase-1 `eprintln!` (170), and the `read_dirs == 0` `assert_eq!` block (194-203) it backs. The module doc (1-10) frames the invariant as one the test 'decides by counts' alongside the two live pins (`compiles`, `walks`) it keeps — reword the sentence naming `read_dirs` to state only what those two still assert."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "check_cost_is_diagnosed_and_glob_compilation_is_pinned_per_distinct_glob still pins the two live counts (glob compiles <= 32, walks <= 2) and prints its diagnosis without a read_dirs term — no assertion claims a count nothing in the crate can move"
      }
    ],
    "acceptance": "src/import.rs carries no READ_DIRS counter or read_dir_count() getter; tests/check_cost.rs's diagnosis and assertions name only the two live pins. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Drained from .flume/refactor/plan-import-read-dirs-vacuous-pin.md (observed 7ac498a; git log 7ac498a..HEAD -- src/import.rs tests/check_cost.rs is empty at 0388334, claim unchanged). Serialized behind IMPORT-DECLARED-GOVERNED-PATHS-HOIST, the chain's last entry touching src/import.rs (pending-entry.md, 'Disjoint, or serialized'). scoped at 0388334."
  },
  {
    "tag": "IMPORT-ROLLUP-WRITER-PLACEMENT",
    "summary": "import.rs's RollupEntry/write_rollup/rollup_tables move to drift.rs — drift already owns 'the lock' per the codemap and is the writer's sole caller",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Codemap — the engine (`src/`)"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "`RollupEntry` (160-174), `write_rollup` (585-608), `rollup_tables` (613-624), and its `create_dir_all`/`write_bytes` helpers (627-640) move to drift.rs; the module header loses the roll-up-writer clause."
        },
        {
          "path": "src/drift.rs",
          "description": "Already the sole caller — `use crate::import::{RollupEntry, write_rollup}` (29), `RollupEntry` construction (1310, 1325), and the `write_rollup` call (1367) become intra-module."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/emit.rs",
        "asserts": "the existing lock.toml round-trip coverage (emit_compiles_every_projection_and_the_whole_lock_from_the_payload and its neighbors) stays green — the move is location-only"
      }
    ],
    "acceptance": "import.rs's roll-up writer has moved to drift.rs; architecture.md's codemap holds without contradiction. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Ruled via inbox (observed a8dd112): move to drift.rs, no codemap edit needed — drift already owns 'the lock' and is the sole caller. Supersedes the prior parked either/or."
  },
  {
    "tag": "READ-CONTEXT-MEMBER-CITER-GRAIN",
    "summary": "read.rs's narrate_citers and context_member_one's inline filter share one predicate-taking selection helper; each site's own print form stays separate",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/read.rs",
          "description": "narrate_citers (778-810) and context_member_one's inline citer block (1017-1041) both filter `citations` then narrate a bullet list; extract the filter into one predicate-taking selection helper both call, leaving each site's own print form — leaf grain's plain 'cites it' vs member grain's full leaf address — untouched."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "a_member_target_walks_why_impact_and_context (107) and a_leaf_address_walks_impact_and_context_at_leaf_grain_and_discloses_coverage (237) keep asserting both grains' citer narration unchanged"
      }
    ],
    "acceptance": "One shared selection helper serves both call sites; each grain's print form is unchanged. cargo test green.",
    "notes": "Ruled via inbox (observed a8dd112): consolidate the selection only — a predicate+formatter closure was judged more indirection than the ~30 shared lines earn, so print stays per-site."
  },
  {
    "tag": "READ-VERB-STRAND-COHESION",
    "summary": "read.rs's telemetry field strand (field/event_label, zero shared machinery) extracts to a new pipeline module; the four explain traversals stay under read's one-verb framing",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Growth rules"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/telemetry.rs",
          "description": "`field` (1464-1537) and `event_label` (1538-1545) move here verbatim — a new pipeline-subsystem module beside tap.rs's record type; declares 'pipeline' as its subsystem in the commit body per Growth rules."
        }
      ],
      "edit": [
        {
          "path": "src/read.rs",
          "description": "explain()'s dispatch (259) calls the new module instead of the local `field`; the module header (1-3) drops the 'plus a telemetry field strand' clause, framing itself as the four traversals alone."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/read_verbs.rs",
        "asserts": "the field-strand scenarios (tap_readout, tap_record, and the no-field-strand baseline) keep passing unchanged — the move is location-only"
      }
    ],
    "acceptance": "The field strand lives in its own pipeline module; read.rs's module doc names only the four traversals. cargo test green.",
    "notes": "Ruled via inbox (observed a8dd112): only the field strand extracts (shares zero machinery — accidental cohabitation); the Species-dispatched four traversals are real cohesion, ruled to stay. Codemap listing follows on the next human specs/ pass."
  },
  {
    "tag": "MAIN-LOCK-ROW-CONSTRUCTORS-TO-DRIFT",
    "summary": "main.rs's row→model constructors over drift's own Row types move beside the lock-row reading they interpret, into drift.rs",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Invariants — stated as absences"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "`requirement_from_row` (2457), `clause_from_row` (2485), `edges_from_declarations` (2502) + `edge_column` (2531), `mention_edges_from_declarations` (2542), and `import_edges_from_lock` (2566) move out; their call sites in gate() (883-891) and explain() (579-587) become `drift::` calls."
        },
        {
          "path": "src/drift.rs",
          "description": "Gains the six functions beside the `ClauseRow`/`RequirementRow`/`Declarations` definitions they interpret — already the home of `compose::clause_from_row`'s sibling caller, `default_contract_from_rows`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/gauntlet.rs",
        "asserts": "the gauntlet corpus's gate/explain output is byte-identical across the move — location-only, no behavior change"
      }
    ],
    "acceptance": "The six row→model constructors live in drift.rs; main.rs calls them via drift::. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "First of the ruled MAIN-THIN-DISPATCH-COHESION chain (inbox, observed a8dd112): row→model constructors move beside the lock-row reading they interpret. Chains ahead of MAIN-CORPUS-ASSEMBLY-TO-COMPOSE for main.rs shared-file safety."
  },
  {
    "tag": "MAIN-CORPUS-ASSEMBLY-TO-COMPOSE",
    "summary": "main.rs's corpus-assembly functions (resolve_kind_units, manifest_units, kind_features, assemble_lock_family, assemble_by_kind) move to compose.rs",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Codemap — the engine (`src/`)"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "MAIN-LOCK-ROW-CONSTRUCTORS-TO-DRIFT"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "`resolve_kind_units` (1319), `manifest_units` (1498), `kind_features` (1521), `assemble_lock_family` (1582), and `assemble_by_kind` (1862) move out; call sites become `compose::` calls. Flag at build time whether their main.rs-local helpers (`overlay_builtin_kind`, `declared_kinds`, `local_document_rows`) travel too or stay as a seam — not pre-decided here."
        },
        {
          "path": "src/compose.rs",
          "description": "Gains the five functions beside `default_contract_from_rows` — the module's stated 'member composition' job, which this work already is."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/gauntlet.rs",
        "asserts": "the gauntlet corpus's gate/explain output is byte-identical across the move — location-only, no behavior change"
      }
    ],
    "acceptance": "The five corpus-assembly functions live in compose.rs; main.rs calls them via compose::. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Second of the ruled MAIN-THIN-DISPATCH-COHESION chain (inbox, observed a8dd112): corpus-assembly functions move to compose.rs's 'member composition' home. blockedBy the row-constructor move for main.rs shared-file safety."
  },
  {
    "tag": "MAIN-JUDGE-VERB-HOME-RULING",
    "summary": "main.rs's admissibility/collision judges don't cluster by any visible seam, and gate/explain may want a new library entry point — human home-pick needed",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Codemap — the engine (`src/`)"
    },
    "gate": {
      "kind": "parked",
      "reason": "The ruled chain (inbox, observed a8dd112) moves row-constructors and corpus-assembly cleanly, but names two pieces as genuinely ambiguous rather than guessing ('a function whose home is genuinely ambiguous parks that one question, never the campaign'). (1) The seven admissibility/collision-diagnostic functions — joined_kind_admissibility (1738), satisfies_label_admissibility (2034), nested_member_admissibility (2074), local_locus_admissibility (2341), governs_collision_diagnostics (2394), clause_collision_diagnostics (2299), kind_collision_diagnostic (2263) — don't cluster by 'engine.rs/graph.rs territory' as the original finding guessed: only joined_kind_admissibility calls engine::admissibility; the other six just scan drift::Declarations rows into check::Diagnostic with no engine or graph call at all, so no visible seam splits them cleanly between the two judge modules, or names them a third home together. (2) gate (847-1223) and explain (513-641) may want a new pipeline-facing library entry point that main.rs merely calls, rather than having their bodies distributed piecemeal to existing modules — a shape decision, not a mechanical move. Both need a human ruling on the codemap before either becomes an entry."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "The seven admissibility/collision judges and the gate (847-1223)/explain (513-641) bodies — whichever the ruling favors, once MAIN-LOCK-ROW-CONSTRUCTORS-TO-DRIFT and MAIN-CORPUS-ASSEMBLY-TO-COMPOSE have already thinned the file."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/gauntlet.rs",
        "asserts": "the gauntlet corpus's gate/explain output is byte-identical across whichever resolution lands — location-only, no behavior change"
      }
    ],
    "acceptance": "architecture.md's codemap either names a home (or homes) for the seven judges and gate/explain's own shape, or affirms main.rs keeping them as an accepted, named cost — a human ratification either way.",
    "notes": "Residual of the ruled MAIN-THIN-DISPATCH-COHESION chain (inbox, observed a8dd112): the two pieces the ruling itself flagged as ambiguous, parked rather than guessed. Does not block the chain's other two entries."
  },
  {
    "tag": "TAP-PAYLOAD-SCHEMA-SPLIT",
    "summary": "tap.rs (foundation) hardcodes Claude Code's hook-payload schema inline — the payload-to-TapEvent classification is provider knowledge and moves to the provider face",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Invariants — stated as absences"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/tap.rs",
          "description": "`record_from_payload` (126-171, doc 117-124) matches Claude Code's raw hook payload shape inline: `hook_event_name` values `\"InstructionsLoaded\"`/`\"UserPromptExpansion\"`/`\"PostToolUse\"` (138,144,149), the `tool_name == \"Skill\"` special case and its `tool_input.skill` field path (150-159), and the field names `file_path`/`load_reason`/`command_name`/`tool_name` — all cited as an external fact at the point of claim (131-137, `code.claude.com/docs/en/hooks`, retrieved 2026-07-17) but living in `tap`, a foundation module (`architecture.md`'s codemap: 'leaf vocabulary with no internal dependencies ... nothing here knows what a harness is'). Per the 0042 frame applied one level up (ruled, inbox observed ab01fb4): this is provider knowledge embedded as literals outside the provider face, invisible to the import graph. Extract the classification match (138-162) into a function `builtin_kind.rs` owns (see that file's own edit below; NOT `builtin.rs`, which is the lock/contract-projection module, an unrelated job), returning `(TapEvent, String, Option<String>)` or equivalent; `record_from_payload` calls it and keeps the version/session assembly, `TapRecord`/`TapEvent`/`TapError`/`TAP_RECORD_VERSION`, and the log-path/append/read IO — the module's own doc comment (1-10) already frames IO as tap's one home, unaffected by this split. Exact function signature is a judgment call for build, named in the commit body; `TapEvent`'s own doc comments (34-44) already read as generic lifecycle vocabulary, not Claude-Code-specific spelling, so the default expectation is that only the raw-field matching moves, not the enum itself."
        },
        {
          "path": "src/builtin_kind.rs",
          "description": "The natural home for the relocated classification: this module already carries every other cited Claude Code format fact (`claude_code_hook`'s own doc comment cites `code.claude.com/docs/en/hooks`, retrieved 2026-07-16, at the definition immediately above its `CollectionAddress` construction) and is named by architecture.md's codemap as exactly this job — 'the shipped kinds and their cited format facts.' Gains the payload-classification function `src/tap.rs` calls, carrying tap.rs's existing citation (re-verify current at pickup per collaboration.md's citing discipline, since the date moves to a new site) — a new `pub(crate) fn` returning `tap::TapEvent` plus identity/reason, or whatever shape the build-time signature judgment call lands."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/tap.rs",
        "asserts": "tap_extracts_each_lifecycle_event (72) and an_unrecognized_payload_records_nothing_and_exits_zero (107) stay green calling record_from_payload unchanged — the split is call-site-only, identical classification behavior"
      }
    ],
    "acceptance": "src/tap.rs's record_from_payload no longer inlines Claude Code's hook-payload field names/event values; the classification lives in the provider face and tap.rs calls it, keeping only the record vocabulary and IO. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Routed from inbox (RULED, session standing delegation, observed ab01fb4): the 0042 frame ('did we widen at the wrong level') applied at subsystem level to embedded provider knowledge, now a standing posture-sweep lens. Design is pre-decided; only the exact seam shape is build's call."
  },
  {
    "tag": "COVERAGE-KNOWN-SURFACES-RELOCATE",
    "summary": "coverage_note.rs (judges) compiles a cited Claude Code surface registry plus a hardcoded .claude scan root — both are provider knowledge and relocate to the provider face",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Codemap — the engine (`src/`)"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/coverage_note.rs",
          "description": "`KNOWN_SURFACES` (87-..., doc 74-86, `const KNOWN_SURFACES: &[KnownSurface]`) is a curated registry of documented Claude Code surfaces (`.claude/settings.json`, `.claude/agents`, etc.), each row citing `SETTINGS_DOC` (72, `code.claude.com/docs/en/settings`, retrieved 2026-07-16) — this file's own module doc (15-17) already names it 'an external fact.' `claude_entries` (256-...) additionally hardcodes the scan root `root.join(\".claude\")` (256) independently of the provider face's own repeated declaration of the same root (`builtin_kind.rs`'s `Governs { root: \".claude\".to_string(), .. }` at lines 245, 317, 350, 435, 826). architecture.md's codemap names the provider face's job precisely: 'the shipped kinds and their cited format facts' (`builtin`, `builtin_kind`) — `KNOWN_SURFACES`, the `KnownSurface`/`Segment` types, and the `.claude` scan root all fit that description exactly but live in `judges` instead. Move `KnownSurface`, `Segment`, `KNOWN_SURFACES`, and `SETTINGS_DOC` to the provider face (verify at pickup: `builtin_kind.rs`, beside the shipped kind definitions and their own format cites, is the natural fit); `coverage_note.rs` consumes them as loaded data (an import, not a local const) and derives its `.claude` scan root from the same provider-declared root the built-in kinds already use instead of a second hardcoded literal at line 256. Exact re-export/threading shape is a judgment call for build, named in the commit body."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/coverage.rs",
        "asserts": "existing known-surface coverage findings (an unmodeled `.mcp.json`, a partially-governed `settings.json`) stay byte-identical — the move is location-only, no change to which surfaces are named or cited"
      },
      {
        "path": "tests/coverage_note.rs",
        "asserts": "the file's own inline coverage-note tests (grep at pickup, e.g. the KNOWN_SURFACES-driven fixtures around lines 590-930) stay green reading the relocated const through its new import"
      }
    ],
    "acceptance": "KNOWN_SURFACES, its supporting types, and the .claude scan root live in the provider face; coverage_note.rs consumes them as loaded data rather than declaring its own copy of Claude Code surface knowledge. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Routed from inbox (RULED, session standing delegation, observed ab01fb4): the 0042 frame applied at subsystem level to embedded provider knowledge, now a standing posture-sweep lens. Design is pre-decided; only the exact module home/threading shape is build's call."
  },
  {
    "tag": "GUARD-DECLARED-LOCUS-FILTER",
    "summary": "install.rs's guard filter admits only .claude/ paths by regex, so a declared-locus kind outside .claude escapes silently — derive the filter from lock-declared targets",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Invariants — stated as absences"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "BUNDLE-INSTALL-SESSION-START-SHAPE-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "`GUARD_PATH_MATCH` (140, doc 134-139: 'the extended-regex `temper guard` greps the `PreToolUse` payload for: a `file_path` value under a `.claude/` locus') is consumed by `claude_file_path` (714-722), called from `guard` (692-710) as the first gate before consulting `targets: Option<&[drift::EmitOwnedEntry]>` (695, the lock's emit-owned projection set, doc 678-684). Since the regex hard-admits only `.claude/`-rooted paths before `targets` is ever consulted, a declared-locus kind whose `governs` root sits outside `.claude` (0038 layout kinds) can have its emit-owned projection written to unguarded — the `file_path` never matches the regex, so `claude_file_path` returns `None` and `guard` (697-699) allows it silently, regardless of what `targets` declares. Per the 0042 frame (ruled, inbox observed ab01fb4): the filter should derive from the lock's own declared targets (already threaded into `guard` as `targets`) rather than a provider-shaped `.claude/` regex decided ahead of them; the `.claude` regex survives only as the documented no-lock fallback `guard`'s own doc (680-684) already names ('`targets` is `None` for a harness with no `lock.toml` at all ... the guard falls back to binding any `.claude/` `file_path`'). Rework `guard`/`claude_file_path` so that when `targets: Some(_)`, the `file_path` extraction (or its match test) is driven by the declared target paths themselves rather than requiring a `.claude/` match first; `GUARD_PATH_MATCH`'s regex stays, scoped explicitly to the `targets: None` fallback branch. Exact extraction shape (a looser `file_path`-capturing regex plus a target-path suffix test, replacing today's `.claude/`-gated one) is a judgment call for build, named in the commit body."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "a new case proves a PreToolUse payload naming a lock-declared emit-owned target OUTSIDE .claude/ is bound by the guard (not silently allowed), alongside existing coverage that a .claude/ path outside any declared target stays allowed and the no-lock fallback still binds any .claude/ file_path"
      }
    ],
    "acceptance": "temper guard binds a PreToolUse write against any lock-declared emit-owned target, not only .claude/-rooted ones; the .claude/ regex survives only as the documented no-lock fallback. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Routed from inbox (RULED, standing delegation, observed ab01fb4). Serialized behind BUNDLE-INSTALL-SESSION-START-SHAPE-CONSOLIDATE, the last chain entry touching install.rs, for shared-file safety — no functional dependency. A declared-locus kind outside .claude currently escapes the guard silently, a real gap, not just a shape defect."
  },
  {
    "tag": "INSTALL-ERROR-ZERO-CONSUMER-PRUNE",
    "summary": "install.rs's InstallError enum is pub with zero consumer outside its own module — narrow to private per the export-earns-its-consumer bar",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "An export earns its consumer"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GUARD-DECLARED-LOCUS-FILTER"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "`InstallError` (168, `pub enum InstallError`, doc 163-167) is grep-verified zero-consumer outside this file across src/, tests/, sdk/ — every fallible pub fn in this module (`run`, `guard`, `discover`, `gate_installed`, …) returns `miette::Result<T>`, never `Result<T, InstallError>`, so the concrete type never crosses the module boundary; internal callers (`read_optional` 844, `fresh_settings`/`hoisted_settings` 882/891/922, `write_scaffold_file` 1407, `ensure_node_available` 1426, `ensure_dependency` 1470, `ensure_package_json` 1493) all stay within install.rs and convert to `miette::Report` via `?` at the pub boundary. Same shape as the shipped GRAPH-WORLD-ZERO-CONSUMER-PRUNE/INSTALL-GUARD-MANIFEST-MESSAGE-PRUNE precedents. Drop `pub` from `InstallError`, leaving it module-private; its two intra-doc links (line 30's module header, line 385's `run` doc) stay valid since rustdoc resolves private intra-links within the same crate build."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "the module's existing error-path coverage (read/write/settings-parse failures surfacing as miette diagnostics) stays green — the narrowing is visibility-only, no error variant or message changes"
      }
    ],
    "acceptance": "InstallError is a private enum in src/install.rs, not pub. cargo test and cargo clippy --all-targets -- -D warnings green (no unused-pub warning survives).",
    "notes": "Found sweeping the verbs subsystem (posture rotation). Serialized behind GUARD-DECLARED-LOCUS-FILTER, the last entry in the existing install.rs chain, for shared-file safety (pending-entry.md, 'Disjoint, or serialized') — no functional dependency. scoped at 2d1c5a6."
  },
  {
    "tag": "KIND-ENTRY-SHAPE-DATA-DECLARE",
    "summary": "CollectionAddress gains a closed entry-shape fact (object/scalar/group-array) as kind data; ENABLEMENT_FIELD's field-name half becomes the enabledPlugins kind's own declared shape (decision 0042)",
    "per": {
      "path": "specs/model/representation.md",
      "section": "kind"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "KIND-ZERO-CONSUMER-EXPORTS-PRUNE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "`CollectionAddress` (184-190, two fields: `manifest`, `key_path: CollectionKeyPath`) gains a third field carrying decision 0042's closed entry-shape fact: `object` (the value's keys are the member's fields, today's unmarked default), `scalar` (the value is the member's one declared field, carrying that field's name — this is where `ENABLEMENT_FIELD`'s (219, `pub(crate) const ENABLEMENT_FIELD: &str = \"enabled\"`) knowledge relocates), and `group-array` (the value is an array of groups, each carrying lifted shared fields plus a member array — the member-array key and lifted field names are its own parameters, settled from the format docs `claude_code_hook`'s doc comment already cites). Exact Rust shape (an `EntryShape` enum with data-carrying variants, `Scalar { field: String }`/`GroupArray { member_key: String, lifted_fields: Vec<String> }`) is a judgment call for build, named in the commit body. `CollectionKeyPath` (196-302, its four variants and `collection_key`/`key_field`/`identity_edge`/`spans_whole_manifest` methods) is UNCHANGED — it still names the manifest's top-level key; only the identity-branching *consumers* of it (a later chain entry) retire, not the type. `collection_address_from_row` (1155-1169) decodes the new row column into the new field; `ENABLEMENT_FIELD` itself is NOT deleted here — it retires only once its last two consumers (json_manifest.rs, graph.rs — later chain entries) are gone."
        },
        {
          "path": "src/builtin_kind.rs",
          "description": "The four shipped `CollectionAddress { .. }` literals declare their real shape, each already carrying the citation this fact draws from in its surrounding doc comment: `claude_code_hook` (238-241, doc 225-230, `code.claude.com/docs/en/hooks`, retrieved 2026-07-16) declares `group-array`; `claude_code_mcp_server` (265-268, doc 254-259, 'a server entry is an object') declares `object`; `claude_code_installed_plugin` (302-305) declares `scalar` with field `\"enabled\"` (retiring what `ENABLEMENT_FIELD` encoded); `claude_code_known_marketplace` (343-346, doc: 'Its value is an object, unlike an installed plugin's bare boolean') declares `object`. The two in-module test literals (835, 872) gain the matching field."
        },
        {
          "path": "src/drift.rs",
          "description": "`CollectionAddressRow` (2893-2900, `manifest`/`key_path` only) gains the wire column carrying the new fact; `collection_address_table`/`collection_address_from_table` (3806-3813/3815-3823) read/write it. `src/builtin_lock.toml`'s four registration rows (approx lines 106/115/124/140 — hook/mcp-server/installed-plugin/known-marketplace) gain matching real values, since `KindFactRow` decodes only `Deserialize` (no default) and the embedded lock is every bare harness's default-contract source (`.flume/plan/open-questions.md`, 'Default-contract auto-adoption')."
        },
        {
          "path": "sdk/src/kind.ts",
          "description": "`CollectionAddress` interface (137-144, `manifest`/`keyPath` only) gains the mirrored `entryShape`-shaped fact, matching whichever Rust shape build lands."
        },
        {
          "path": "sdk/src/declarations.ts",
          "description": "`collectionAddressRow` (235-238) lowers the new fact into the row's new wire column, alongside the existing `manifest`/`key_path` mapping."
        },
        {
          "path": "tests/hook_kind.rs",
          "description": "Line 19/57-60's `CollectionAddress { .. }` assertion against `definition(\"hook\")` gains the group-array shape's expected value."
        },
        {
          "path": "tests/mcp_server_kind.rs",
          "description": "Line 22/53-56's `CollectionAddress { .. }` assertion gains the object shape's expected value."
        },
        {
          "path": "tests/installed_plugin_kind.rs",
          "description": "Line 24/96-99's `CollectionAddress { .. }` assertion gains the scalar shape's expected value (field `\"enabled\"`); the lock-row read at 217-234 gains the new column's raw-wire assertion."
        },
        {
          "path": "tests/known_marketplace_kind.rs",
          "description": "Line 26/73-76's `CollectionAddress { .. }` assertion gains the object shape's expected value."
        },
        {
          "path": "tests/manifest_adapter.rs",
          "description": "The `mcp_address()` helper (17, used at 23-27 and across the file) constructs a bare `CollectionAddress` directly — gains the new field (object shape, matching `mcp-server`'s real declaration)."
        },
        {
          "path": "tests/emit.rs",
          "description": "`a_hook_round_trips_settings_json_read_to_members_to_write_byte_identical` (924, `CollectionAddress { .. }` at 960-963) gains the group-array shape's expected value — the round trip itself is unaffected, this entry adds no dispatch change."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "`a_kinds_fields_only_shape_and_collection_address_round_trip_the_lock_and_reach_the_engine` (1165-1207) constructs `CollectionAddress { .. }` directly to assert `CustomKind::from_kind_fact_row`'s decode output — gains the new field on both sides of the round trip."
        }
      ],
      "retire": []
    },
    "schemaDelta": "CollectionAddressRow (src/drift.rs, ts-rs-mirrored to sdk/src/generated/CollectionAddressRow.ts) gains a new column carrying the closed entry-shape fact; exact wire shape is a judgment call for build, named in the commit body. Regenerates via the existing ts-rs export test — never hand-edit the generated file.",
    "tests": [
      {
        "path": "tests/lock_declaration_rows.rs",
        "asserts": "the new entry-shape column round-trips losslessly through emit + read_declarations for all three shape kinds (object/scalar/group-array), the same bar CONTRACT-REQUIRE-SECTIONS-ROUNDTRIP's precedent sets"
      },
      {
        "path": "tests/builtin_lock_frozen.rs",
        "asserts": "the embedded lock's frozen-snapshot test is re-recorded (not silently stale) to include the four registration rows' new column — grep this file's own re-record discipline at pickup"
      }
    ],
    "acceptance": "CollectionAddress carries a closed entry-shape fact as kind data (object/scalar/group-array); the four shipped registration kinds declare their real shape with the citations they already carry; the lock row and SDK mirror round-trip it losslessly. Nothing dispatches on it yet — this entry is purely additive, no behavior change. cargo test, cargo clippy --all-targets -- -D warnings, and pnpm --dir sdk test all green.",
    "notes": "Derived from decision 0042 (specs/decisions/0042-the-entry-declares-its-shape.md), Consequences bullets 1 (kind.rs half), 3, and 4. Shares kind.rs with the open KIND-ZERO-CONSUMER-EXPORTS-PRUNE (blocked on it), builtin_kind.rs with the open TAP-PAYLOAD-SCHEMA-SPLIT, and drift.rs with the open MAIN-LOCK-ROW-CONSTRUCTORS-TO-DRIFT/IMPORT-ROLLUP-WRITER-PLACEMENT — single-tag blockedBy can't express all four; non-overlapping regions in each, ships independently regardless. scoped at efe64aa."
  },
  {
    "tag": "JSON-MANIFEST-ENTRY-SHAPE-DISPATCH",
    "summary": "manifest_members and emit's write faces dispatch on the collection's declared entry-shape instead of CollectionKeyPath identity; the HooksEvent/EnabledPlugins branches delete (decision 0042)",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "KIND-ENTRY-SHAPE-DATA-DECLARE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/json_manifest.rs",
          "description": "`manifest_members` (538-565) branches on collection *identity* — `if collection_key == CollectionKeyPath::HooksEvent.collection_key()` (545) and `::EnabledPlugins.collection_key()` (555) — with the object-shape read (`entry_fields`, 571-579) as the unconditional fallthrough tail (561-564). Re-key the dispatch to match on the resolved collection's declared entry-shape (from KIND-ENTRY-SHAPE-DATA-DECLARE) instead: `Scalar`/`GroupArray`/`Object`, deleting both identity comparisons. `enablement_member_fields` (598-603) and `enablement_entry_value` (615-620, `pub(crate)`) stop reading the bare `ENABLEMENT_FIELD` constant and take the shape's own field name as a parameter instead. `hook_member_fields` (630-658) and `hook_matcher_group` (667-686, `pub(crate)`) stop hardcoding the literal strings `\"matcher\"`/`\"hooks\"` and take the shape's own member-array key / lifted field names as parameters instead. `entry_fields` (object-shape read) has no named write-side inverse today — its write is inlined in `drift.rs`'s `emit` (see below); promote it into a first-class function beside its siblings for symmetry, or leave the inline and only change its dispatch condition — a judgment call for build, named in the commit body."
        },
        {
          "path": "src/drift.rs",
          "description": "`emit`'s registration-write loop (1006-1058) branches on the identical identity pair: `if collection == CollectionKeyPath::HooksEvent.collection_key()` (1028) and `::EnabledPlugins.collection_key()` (1040), with the object-shape write inlined unconditionally in the `else` (1049-1057). Re-key to dispatch on the registration's collection's declared entry-shape, calling `json_manifest`'s now-shape-parameterized write functions (and the object-shape write, wherever it now lives per the json_manifest.rs judgment call) instead of the identity `if`/`else if`/`else` chain."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/json_manifest.rs",
        "asserts": "enablement_members_carry_the_scalar_value_and_re_render_identically (~970-1007), an_enablement_member_with_no_enabled_field_renders_the_documented_true (~1009-1014), and hook_members_decompose_the_matcher_group_array_and_re_nest_identically (~1016-1051) all pass calling the now-parameterized functions — identical behavior, dispatch-only rewrite"
      },
      {
        "path": "tests/emit.rs",
        "asserts": "a_hook_round_trips_settings_json_read_to_members_to_write_byte_identical (924) stays byte-identical through the rekeyed dispatch"
      },
      {
        "path": "tests/gauntlet.rs",
        "asserts": "composition 3 (116-126: sessionHook/formatterPlugin/acmeMarketplace — group-array/scalar/object composed in one settings.json) stays snapshot-identical, proving the rekeyed dispatch handles all three shapes together in one manifest — this already-existing composition discharges decision 0042's 'gauntlet gains a fixture composing the three shapes' consequence (verified on disk this tick, no new fixture needed)"
      }
    ],
    "acceptance": "manifest_members and emit's registration-write loop both dispatch on the collection's declared entry-shape; no CollectionKeyPath identity comparison remains in either function. Byte-identical output to today (engineering.md, 'The fix lands at the mechanism': the special case generalizes, the case stops being special). cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Derived from decision 0042, Consequences bullet 2 (json_manifest.rs half) plus the drift.rs write-face half the decision's own text folds into 'the write faces re-key.' Bullet 5 (gauntlet fixture) verified-already-moot on disk this tick via tests/gauntlet.rs:116-126 — see tests[] above."
  },
  {
    "tag": "GRAPH-ENABLEMENT-FIELD-RETIRE",
    "summary": "graph.rs's dead-plugin judge stops reading the bare ENABLEMENT_FIELD constant; kind.rs deletes it once both consumers are gone (decision 0042's retirement, completed)",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "The fix lands at the mechanism"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "JSON-MANIFEST-ENTRY-SHAPE-DISPATCH"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "`dead_registration`'s `Registration::Enablement` arm (727-739, reading `crate::kind::ENABLEMENT_FIELD` at 728 and 737) is the constant's last consumer once json_manifest.rs's own two uses (JSON-MANIFEST-ENTRY-SHAPE-DISPATCH) are gone. `dead_registration` receives only `&Registration`/`&Features` — no `CollectionAddress`/kind data reaches this call site today (traced: `dead_channel_set` → `reachable`/`live_members`, both keyed by bare kind name off `main.rs:591-596`'s `registrations: BTreeMap<&str, Vec<Registration>>`, which drops `CustomKind::collection_address` when building the map). Two designs, a judgment call for build, named in the commit body: (a) widen `Registration::Enablement` to carry its own `field: String`, mirroring the sibling variants `DescriptionTrigger`/`PathsMatch`/`Event`'s existing per-variant field, populated from the enabled-plugins kind's declared `Scalar { field }` shape when `main.rs` builds the registration map; or (b) thread `CollectionAddress`/entry-shape data down through `reachable`/`live_members`/`dead_channel_set`/`dead_registration`'s signatures. Once resolved, delete `kind.rs`'s now-fully-unused `ENABLEMENT_FIELD` const (219, doc 211-218)."
        },
        {
          "path": "src/main.rs",
          "description": "If design (a) is chosen: the registration-map construction (591-596) populates `Registration::Enablement`'s new `field` from the enabled-plugins kind's declared shape when building `registrations`. If design (b): the map (or a parallel one) carries `CollectionAddress` data through to `graph::reachable`'s call site — exact threading is the same build-time judgment call as the graph.rs edit."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/installed_plugin_kind.rs",
        "asserts": "a_false_valued_entry_gates_its_member_off_every_channel (154) stays green — the disabled plugin is still found dead, the two enabled ones still live, unchanged behavior through whichever design lands"
      },
      {
        "path": "src/graph.rs",
        "asserts": "existing dead_registration inline tests (e.g. a_paths_match_glob_dies_only_when_no_repo_file_matches_it and its neighbors) stay green — the retirement is signature-only for the Enablement arm, no change to other channels"
      }
    ],
    "acceptance": "graph.rs's dead-plugin judge reads the field name from kind data, never the bare ENABLEMENT_FIELD constant; kind.rs no longer declares it. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Derived from decision 0042, completing Consequences bullet 1's ENABLEMENT_FIELD retirement (graph.rs was not named in the decision's own text, which lists only kind.rs/json_manifest.rs — but ENABLEMENT_FIELD cannot retire while this file still reads it; the retirement's completion, not new scope). Last entry in the 0042 chain. scoped at efe64aa."
  },
  {
    "tag": "BUILTIN-KIND-SURFACE-UNIT-CONSOLIDATE",
    "summary": "builtin_kind.rs's test module carries a private surface_unit byte-identical to tests/common's, instead of the crate's own designated shared-fixture home, test_support.rs",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "KIND-ENTRY-SHAPE-DATA-DECLARE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/builtin_kind.rs",
          "description": "The inline `#[cfg(test)] mod tests` (570) declares a private `fn surface_unit(member: &crate::frontmatter::Member) -> Unit` (888-900, doc 885-887 'Lift an imported ... straight into the raw Unit the composed extractor reads', closing brace 901) that is a byte-for-byte body duplicate — and near-identical doc comment — of `tests/common::surface_unit` (`tests/common/mod.rs`, pub fn at 463-476, doc 460-462, same wording). This same test module already imports `crate::test_support::tmpdir` (574) — the crate's own designated home for in-src `#[cfg(test)]` fixture scaffolding (`src/test_support.rs`'s own header: 'Shared scaffolding for in-src `#[cfg(test)] mod tests` blocks'), so the pattern for sharing this exact class of helper is already established and simply wasn't used for this one. Delete the private `surface_unit` (885-901); add `crate::test_support::surface_unit` to the existing `use crate::test_support::tmpdir;` import line (or a neighboring one), and leave its five call sites (933, 984, 1013, 1027, 1046) unchanged — same name, same signature, now resolved from the shared home."
        },
        {
          "path": "src/test_support.rs",
          "description": "Add `pub(crate) fn surface_unit(member: &crate::frontmatter::Member) -> Unit { .. }` here, lifted verbatim from builtin_kind.rs's deleted copy (same field-by-field construction: id, frontmatter, body, source_path, satisfies, satisfies_clauses). Add `crate::kind::Unit` to this file's existing `use crate::kind::{CustomKind, Extraction, Format, Governs, Primitive, UnitShape};` import line; `crate::frontmatter::Member` stays referenced by its qualified path in the signature, as the deleted original did, needing no new import."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/builtin_kind.rs",
        "asserts": "skill_features_fold_unknown_keys_and_surface_satisfies_off_the_surface and its four sibling tests calling surface_unit (984, 1013, 1027, 1046) stay green — the move is location-only, identical Unit construction"
      }
    ],
    "acceptance": "builtin_kind.rs's test module has no private surface_unit; it calls crate::test_support::surface_unit, the same shared home the module already draws tmpdir from. src/test_support.rs is the fn's one implementation for in-src unit tests. cargo test and cargo clippy --all-targets -- -D warnings green.",
    "notes": "Found sweeping provider (posture rotation: judges skip-forwarded since fe3ff3f, provider touched by 404b73a/516f8f6). tests/common::surface_unit stays untouched — test_support.rs is pub(crate) (lib.rs:58), a different compilation unit, not a three-way merge. Serialized behind KIND-ENTRY-SHAPE-DATA-DECLARE, sharing builtin_kind.rs; TAP-PAYLOAD-SCHEMA-SPLIT also touches it, open/queue-front, ships first regardless. scoped at e5d01fe."
  }
]
