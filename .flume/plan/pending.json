[
  {
    "tag": "SCRATCH-RETIRE",
    "summary": "Delete the copy-tree scratch import pipeline (import::run + surface writers + emit_manifest + carry_representation + scratch_surface); keep the discovery walk and write_rollup as emit's lock-writer",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "Decision: one authored surface, one implementation"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "Delete the copy-tree scratch pipeline: `run`/`run_with_builtins` (143/154) + their in-module unit tests (~1666/1703/1728), `import_frontmatter_kind` (535), the copy-tree writers `write_member_surface`/`existing_surface_member`/`copy_companion` (643/692/862), and `collect_declarations` (917) + `carrier_count` (520) — all orphaned once `run_with_builtins` is gone (private fns ⇒ clippy `-D warnings` on any survivor). Delete `emit_manifest` (426) — its only caller is the retired tests/temper_toml.rs. KEEP `write_rollup` (888) + `RollupEntry`: they are `drift::emit`'s lock-writer (the sole producer, S1 — drift.rs:31,439), NOT dead. KEEP `discover_kind_units`/`discover_builtin` (713/569) — the discovery walk is the sole extractor — and `init`/`lift`/`load_manifest`/`MANIFEST_FILENAME` (survive to S5)."
        },
        {
          "path": "src/frontmatter.rs",
          "description": "Retire `carry_representation` (frontmatter.rs:318) — its only caller is the deleted `write_member_surface` (import.rs:656), so it goes dead with the copy-tree writers. No other caller in src/ or tests/."
        },
        {
          "path": "src/main.rs",
          "description": "Rewire the two transitional scratch producers to gate the harness root directly. The `Check --harness` branch (main.rs:286) and `session_start_diagnostics` (main.rs:589) both do `scratch_surface()` → `import::run(harness, scratch)` → `gate(scratch, harness/temper.toml)` → teardown. Replace each with `gate(harness_root, harness_root.join(TEMPER_TOML))`: `resolve_kind_units` already discovers members straight off `harness_root` via `effective_governs` (main.rs:977) + `discover_kind_files` when no committed lock exists (each kind's embedded governs = the built-in lock), so no scratch import is needed. Then delete `scratch_surface` (main.rs:1183) and both former call sites' teardown."
        }
      ],
      "retire": [
        "tests/temper_toml.rs"
      ]
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/emit.rs",
        "asserts": "emit + check operate with no copy-tree scratch import; the discovery walk is the only member extractor; `--harness`/session-start gate the harness root directly"
      }
    ],
    "acceptance": "`import::run`/`run_with_builtins`, `import_frontmatter_kind`, the copy-tree surface writers, `collect_declarations`, `carrier_count`, `emit_manifest`, `carry_representation`, and `scratch_surface` are gone; `write_rollup`/`RollupEntry` survive as emit's lock-writer; the discovery walk (`discover_kind_units`/`discover_builtin`) survives; `--harness`/session-start gate the harness root directly; `cargo test` green (tests/temper_toml.rs retired).",
    "notes": "Clean slate (John 07-06): no compat shim. temper_toml.rs retires HERE not S5 — its live `import::run` (96/1014) + `emit_manifest` (97/1015) calls die this slice. write_rollup is NOT dead: drift::emit is the sole producer (S1) and calls it. `init`/`lift` deletion waits for S5 (the codec they drive). Shares import.rs/frontmatter.rs/main.rs — serialized ahead of S5."
  },
  {
    "tag": "CODEC-RETIRE",
    "summary": "Retire the [[member]] manifest codec both directions plus AuthorLayer's member readers, and remove the init/lift verbs (their only remaining callers) under clean slate",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "Decision: install is the front door; the lift is its yes-path; the postures stay equal"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "SCRATCH-RETIRE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "Retire the `[[member]]` codec both directions: writers `write_members`/`member_to_table` and the genre/section sub-writers (compose.rs:1512-1656), and parsers `parse_member`/`parse_member_fields`/`parse_member_sections`/`parse_member_fenced`/`parse_member_genres`/`parse_member_published` (1738-1980). Retire `AuthorLayer::members()` (its consumers die with init/lift). VERIFY on disk when this becomes head: `inplace_members()` is still read by the surviving gate (`resolve_kind_units`, main.rs) — retire it only if that read is gone (install-front-door territory, S6), else keep it. Keep the `[role.*]`/severity layer if still read elsewhere; S7 finishes it."
        },
        {
          "path": "src/import.rs",
          "description": "Remove the `init`/`lift` verbs (import.rs:254/337) — the codec's only remaining callers. Clean slate: a temporary on-ramp gap on trunk is acceptable until S6 ships the front door."
        },
        {
          "path": "src/main.rs",
          "description": "Drop the `Command::Init` variant + dispatch (main.rs:256) and its clap definition; `install` is the sole on-ramp (S6). No `init` alias survives (clean slate)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/genre_leaf.rs",
        "asserts": "genre leaf addressing survives without the [[member]] manifest round-trip block"
      }
    ],
    "acceptance": "The `[[member]]` codec (both directions), `AuthorLayer::members()`, and the `init`/`lift` verbs are gone; `temper.toml` as a manifest dialect is unreadable; `cargo test` green (tests/temper_toml.rs already retired in SCRATCH-RETIRE; genre_leaf's manifest block edited out).",
    "notes": "Clean slate (John 07-06): `init` may be removed before the front door ships — a trunk on-ramp gap is acceptable, green gates the only bar. Shares compose.rs/import.rs/main.rs — serialized behind S4. tests/temper_toml.rs retired one slice earlier in SCRATCH-RETIRE (its import::run+emit_manifest calls died there). genre_leaf.rs manifest round-trip block trimmed here."
  },
  {
    "tag": "INSTALL-FRONT-DOOR",
    "summary": "Rebuild install as the one on-ramp: discovery report → one question → scaffold the SDK program + first emit on yes, reporter alone on no; guard/note grounded in the lock",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "install — the front door; the lift, once"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "CODEC-RETIRE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "(a) Scaffolder: discovery walk → generated TS member modules (`file()` pointers at the original paths, bytes untouched) + `harness.ts` skeleton — the lift's output is the SDK program and nothing else, no lock. (b) Verb: discovery report first, one question 'represent this project?' (`--yes` for agents/CI, `--no-represent`); no-path = consented SessionStart reporter alone (Node-free); yes-path = require Node+`.temper` (hard refusal with instructions when absent), ensure `@dtmd/temper` dep, scaffold, run first emit (the adoption moment), place per lock. (c) Guard/note lock-grounded: rewrite `GUARD_MESSAGE` (install.rs:101) and the managed-by note off the lock's emit-owned rows; place the modeline only when its schema artifact exists, the guard only when emit-owned paths exist ('the guard arrives with its constituency')."
        },
        {
          "path": "src/main.rs",
          "description": "`Command::Install` dispatch carries the one-question flow (`--yes`/`--no-represent`); `Command::Guard`'s posture reads the lock, not a `temper.toml` AuthorLayer (main.rs:390)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "install reports findings, asks once; no-path wires only the session-start reporter; yes-path scaffolds the SDK program, runs first emit, places note/guard only for lock-declared emit-owned paths; an all-in-place member gets no guard claim"
      }
    ],
    "acceptance": "`temper install` is the sole on-ramp: report → one question → reporter-alone on no, or scaffold+first-emit+lock-grounded placements on yes; guard/note bind only lock-declared emit-owned paths; a `file()`-own-path member is authored territory (no guard). Cascade's report (1) dissolves.",
    "notes": "Resolves `(carriage-aware-placements)` in code (the lock names a path a projection — 20-surface 'surface authority is a declared posture'). Depends on the built-in lock (S1) + lock-read gate (S2) + codec/init removal (S5). Shares install.rs/main.rs — serialized behind S5."
  },
  {
    "tag": "TEMPER-TOML-ZERO",
    "summary": "Eradicate every remaining temper.toml reader — AuthorLayer load/authority/fold_local, the TEMPER_TOML const, explain's load_layer residue — the manifest filename is gone",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "The lock and drift — one vocabulary"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "INSTALL-FRONT-DOOR"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "Retire `AuthorLayer::load`/`authority`/`fold_local` and the `TEMPER_TOML` const — no verb reads a `temper.toml`. Any `[role.*]`/severity layer S5 left standing retires here if nothing reads it."
        },
        {
          "path": "src/main.rs",
          "description": "Remove the `TEMPER_TOML` usages in `explain` (main.rs:439, `load_layer`) and any guard/check residue — posture and layers come from the lock."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.rs",
        "asserts": "no verb reads or writes a temper.toml; a file by that name is inert (old temper, never current temper's input)"
      }
    ],
    "acceptance": "`rg -c 'temper\\.toml' src/ tests/` = 0; the manifest filename is retired entirely (20-surface: 'the name temper.toml retires with the manifest era entirely'). Cascade re-onboards via the new install; its manifest-era artifacts are old temper by definition.",
    "notes": "Chain terminal. Cascade's reports (2)-(4) — the accepted-debt DATUM under `(inplace-lock-producer)` — close with this slice: no manifest-mode coverage remains. Shares compose.rs/main.rs — serialized behind S6."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, the engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/architecture/50-distribution.md",
      "section": "Three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper` 0.0.2, PR #4); channel 1's SDK foundation shipped"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (50-distribution)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, 50-distribution). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (50-distribution 'Fail-loud delivery').",
    "notes": "Release infra, mostly outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm (`@dtmd/temper`, published 0.0.2 07-06); ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. USPTO name screen due before launch."
  }
]
