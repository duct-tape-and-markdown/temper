[
  {
    "tag": "MARKETPLACE-KIND",
    "summary": "`marketplace` ships: `.claude-plugin/marketplace.json` as a typed file member — identity from `name` against the reserved-names deny list, `owner.name` required, each entry's `source` union",
    "per": {
      "path": "specs/builtins.md",
      "section": "The shipped kinds"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "PLUGIN-MANIFEST-KIND"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "tests/marketplace_kind.rs",
          "description": "The kind's acceptance: a `.claude-plugin/marketplace.json` fixture surfaces one member with identity from `name`; a reserved name is a finding; a missing `owner.name` is a finding; a `plugins[]` entry missing `name` or `source` is a finding; each documented `source` form resolves and an undocumented one is a finding."
        }
      ],
      "edit": [
        {
          "path": "src/builtin_kind.rs",
          "description": "`claude_code_marketplace()` joins `all_kinds()` (315, re-verified unmoved at 14c5de4): `format: Some(Format::JsonDocument)`, `unit_shape: NamedField { field: \"name\" }`, `governs { root: \".claude-plugin\", glob: \"marketplace.json\" }` — a distinct glob from `plugin-manifest`'s, so the two file kinds never contend. Cite `code.claude.com/docs/en/plugin-marketplaces` with the retrieval date at encode time."
        },
        {
          "path": "src/builtin_lock.toml",
          "description": "Generated data, never hand-edited — the kind row plus its clause rows. Regenerate per the file's header."
        },
        {
          "path": "src/builtin_lock.rs",
          "description": "Module header and tests."
        },
        {
          "path": "sdk/src/builtins.ts",
          "description": "The `Marketplace` type, the kind value, and `marketplaceDefaultContract`. **Re-fetch and cite per clause at encode time.** Verified 2026-07-16 against `code.claude.com/docs/en/plugin-marketplaces`, every fact below decidable: `name` is kebab-case and checked against a documented **reserved-names deny list** — `claude-code-marketplace`, `claude-code-plugins`, `claude-plugins-official`, `claude-plugins-community`, `claude-community`, `anthropic-marketplace`, `anthropic-plugins`, `agent-skills`, `anthropic-agent-skills`, `knowledge-work-plugins`, `life-sciences`, `claude-for-legal`, `claude-for-financial-services`, `financial-services-plugins`, `first-party-plugins`, `healthcare` — re-read the list at encode time rather than trusting this transcription, since the page itself records the list growing (before v2.1.205, `first-party-plugins` and `healthcare` were not reserved). The honest bound: the docs ALSO block names that 'impersonate official marketplaces' (`official-claude-plugins`, `anthropic-plugins-v2`), and impersonation is semantic judgment — so the enumerated deny list is the decidable subset and the impersonation rule is guidance, never a clause (intent.md, invariant 2). `owner.name` required, `owner.email` optional. Each `plugins[]` entry requires `name` plus `source`, the documented union: a relative-path string (must start with `./`, resolved relative to the marketplace root, NOT `.claude-plugin/`), or an object — `github` (`repo`, `ref?`, `sha?`), `url` (`url`, `ref?`, `sha?`), `git-subdir` (`url`, `path`, `ref?`, `sha?`), `npm` (`package`, `version?`, `registry?`). Where both `ref` and `sha` are set on a git-based source, `sha` is the effective pin; a marketplace entry's `defaultEnabled` beats `plugin.json`'s. Per-field precedence is clause-and-cite territory here (decision 0031)."
        },
        {
          "path": "sdk/src/claude-code.ts",
          "description": "The provider face exports the kind value, its type, and its default contract. With this entry `specs/builtins.md`'s roster is filled: 'Ten kinds ship. Seven are file members' — seven file kinds (skill, command, agent, rule, memory, plugin-manifest, marketplace) plus three registration kinds (hook, mcp-server, installed-plugin). The engine's `all_kinds()` holds **eleven** at that point, because `supporting-doc` ships outside the enumeration; the two numbers are both right and neither checks the other. Verify on disk."
        },
        {
          "path": "sdk/test/builtins.test.ts",
          "description": "The kind's facts and its default contract's clause set, the deny-list clause included."
        },
        {
          "path": "tests/builtin_lock_frozen.rs",
          "description": "`MEMBERLESS_BUILTIN_PROGRAM` imports and `expect`-binds the kind."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "The row-count-exact kind-fact walk: the `kinds.len()` assert (9 at 2093, re-verified unmoved at 14c5de4) moves to **eleven**, not ten — `supporting-doc` ships without joining `specs/builtins.md`'s enumeration (as `requirement` does), so the engine roster runs one above the corpus's ten. Read the assert on disk at pick-up; do not trust this number sight-unseen."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/marketplace_kind.rs",
        "asserts": "a `.claude-plugin/marketplace.json` reads as one member with identity from `name`; a reserved name, a missing `owner.name`, and a `plugins[]` entry missing `name` or `source` are each findings; every documented `source` form resolves and an undocumented one is a finding"
      }
    ],
    "acceptance": "`marketplace` ships as `specs/builtins.md`'s roster declares it, completing the ten-kind roster decision 0031 ratified — the seven file kinds and three registration kinds that page enumerates, `supporting-doc` shipping beside them outside the count. `temper check .` exits 0. Its default contract holds the strictest documented profile with every clause decidable, severity-declared, guided, cited and dated; the impersonation rule ships as guidance, never a clause. `cargo test`, `cargo clippy --all-targets -- -D warnings`, `pnpm --dir sdk test` green.",
    "notes": "Derived from decision 0031 (16ce347), the producer half's second kind. Same warrant as its sibling: `temper bundle` writes this file ungated today (`src/bundle.rs:191`). Serialized behind PLUGIN-MANIFEST-KIND — both edit `builtin_kind.rs`, `builtins.ts`, `claude-code.ts`, `builtin_lock.toml`, `builtin_lock.rs`, and the two lock tests. scoped at e81c758; cites re-verified 14c5de4 (`all_kinds()` 315 and the `kinds.len()` assert 2093 both unmoved)."
  },
  {
    "tag": "BUNDLE-EMIT-THROUGH-KINDS",
    "summary": "`temper bundle`'s two hard-coded `serde_json` manifest writers reconcile onto the shipped kinds — emit through the kind system, one home",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "MARKETPLACE-KIND"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/bundle.rs",
          "description": "`run()` (178) writes `.claude-plugin/plugin.json` (185) and `.claude-plugin/marketplace.json` (191) through `write_json` (278) over the hand-built `plugin_manifest()` (223) and `marketplace_manifest()` (238) values — every cite re-verified unmoved on disk at 14c5de4. Both are a second implementation of a job the kind system owns once `plugin-manifest` and `marketplace` ship: the manifests become members of their kinds, rendered by the kind's format, so `bundle` stops hand-spelling a shape the engine can now type and gate. This is the consolidation decision 0031 names ('bundle's hard-coded writers reconcile onto the kinds — emit through the kind system, one home; the entry names the unification'), and `specs/process/engineering.md` makes it the entry rather than scope creep — name the unification in the commit body. NOT in scope: the `SessionStart` hook's `hooks/hooks.json` (`hooks_manifest()`, 259) and the skill's `SKILL.md` — `hooks.json` is a plugin-cache artifact of neither kind, and widening to it is the creep the same page forbids. If any part of either manifest's shape cannot route through a kind, say so plainly rather than leaving a silent hand-rolled remainder."
        },
        {
          "path": "tests/bundle.rs",
          "description": "The bundle's existing acceptance. Its assertions over both manifests are the regression net that makes this a true refactor: the emitted bytes should not move, and if they do, that is a behavior change to state out loud in the commit body, never a snapshot to bless quietly."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/bundle.rs",
        "asserts": "`bundle` emits both `.claude-plugin/` manifests through their kinds' formats, byte-identically to the pre-refactor output, and the emitted manifests pass their own kinds' default contracts — the product checks what it writes"
      }
    ],
    "acceptance": "The two manifest writers have one home: `bundle`'s output is its members rendered by their kinds' formats, not hand-built `serde_json` beside a kind that types the same shape (`specs/process/engineering.md`, 'One job, one home': a job the tree already performs is extended where it lives, never reimplemented beside itself). The bundle temper publishes passes the contract temper ships for it, closing the gap decision 0031 named — the product wrote files it refused to check. `cargo test`, `cargo clippy --all-targets -- -D warnings` green.",
    "notes": "Derived from decision 0031 (16ce347), the third derivation item. Blocked until both kinds exist — nothing to reconcile onto before then. It consumes EMIT-JSON-DOCUMENT-WRITE-FACE's dispatch rather than building it: verify that entry shipped at pick-up. Distinct from the `(plugin-author-dogfood)` fork: `bundle` still assembles its tree into an output dir, and no `.claude-plugin/` is committed here. scoped at e81c758; cites re-verified 14c5de4."
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics — the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (⇒ 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (⇒ 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence — never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. All cites re-verified unmoved at 14c5de4. `MAX_IMPORT_HOPS = 5` (65) sits under a doc (61-64) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' — a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (627) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds (654), one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts — hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it — 525-526 ('hop-capped at [`MAX_IMPORT_HOPS`] as the format documents') and 624-625 ('capped at [`MAX_IMPORT_HOPS`] hops (the `at-import` recursion depth the format documents)') — carry the same claim and move with it, as does the propagation comment at 649-652 ('capped at the format's import recursion depth')."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1356, re-verified unmoved at 14c5de4) already pins the boundary mechanically rather than in a comment — `r1..r5` rescued from a live seed, `r6` dead as the sole finding (1386-1388) — so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1357-1361) spells 'capped at the format's five-hop import recursion' (1359) and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper — the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Park re-tested at 14c5de4 and holds — nothing ruled the hop semantics; the inbox is empty and the window (3ed8d2b, `json-document`) touched no import mechanics. `src/graph.rs` untouched this window: `MAX_IMPORT_HOPS` still reads 5 at 65 under a cite claiming five, and 525/624/654 plus `tests/graph.rs:1356` are all unmoved. Stays parked: the constant may still be wrong. rescoped 4a33911; re-cited 53519b9; re-verified 14c5de4."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested on disk at 14c5de4, holding on every clause: `git tag -l` carries only the four era tags and no version tag, crate 0.1.0 vs npm 0.0.7, and release.yml:7-9 still states the darwin + channel-3 deferral verbatim. Channel 3's publish is the release act; BUNDLE-EMIT-THROUGH-KINDS is the orthogonal question of how those bytes are produced. scoped at b676656."
  }
]
