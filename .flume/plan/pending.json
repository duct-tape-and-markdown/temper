[
  {
    "tag": "REACHABILITY-WIRE",
    "summary": "Wire the shipped `reachable` graph predicate into the gate — a dead world→member activation edge should become a check-time finding, but main.rs has no caller yet",
    "per": {
      "path": "specs/45-governance.md",
      "section": "The world is a node — reachability is a predicate"
    },
    "gate": {
      "kind": "parked",
      "reason": "how a graph-scope reachability fact carries author-declared severity is unsettled (fork reachability-gate-mechanism — acyclic is always-on, degree assembly-declared, the spec says 'the package's clause choice'). The curated activation lines this waited on have BOTH shipped: skill/KIND.md declares description-trigger and rule/KIND.md now declares paths-match (chore 9f7d176, verified on disk). So the fork's severity home is the sole remaining blocker; the human-authored data is done"
    },
    "dependsOnForks": [
      "reachability-gate-mechanism"
    ],
    "files": {
      "new": [
        {
          "path": "tests/reachable_gate.rs",
          "description": "Gate-side proof mirroring the library fixture at tests/graph.rs:460: a fixture custom kind declaring an `activation` plus a package/assembly opting its members into reachability — a dead edge (blank description-trigger field / zero-matching paths glob) surfaces a finding through the wired gate, a live one (including an unscoped rule with no paths field, post-PATHS-MATCH-ABSENCE) stays silent, and the severity follows whatever reachability-gate-mechanism resolves."
        }
      ],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "Build the `activations` map from the in-scope kind definitions' declared `Activation` (`CustomKind.activation`, kind.rs:89) and collect the repo file set (the `paths-match` liveness input), then dispatch `graph::reachable(&activations, &by_kind, &repo_files)` beside the always-on acyclic/degree graph pass (main.rs:654-666) and extend diagnostics. The opt-in/severity home (an always-on pass like acyclic, an assembly bound like degree, or a new graph-tier package clause) is exactly what reachability-gate-mechanism decides — so whether this also touches src/contract.rs/src/engine.rs is fork-dependent and not committed here."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/reachable_gate.rs",
        "asserts": "with reachability wired, a member whose declared activation edge is provably dead is a finding at the gate and a reachable member is silent; the finding names the world node, the kind, the member, and the dead-edge reason."
      }
    ],
    "acceptance": "`temper check` runs the `reachable` predicate over members of kinds that declare an activation and flags a dead world→member edge; cargo fmt/clippy -D warnings/test all green. (Parked: not built until reachability-gate-mechanism resolves the severity-declaration home. The curated skill/rule activation lines have shipped — no longer a blocker.)",
    "notes": "reachable/world/dead_activation live in src/graph.rs:342, library-proven (tests/graph.rs:460); no main.rs caller (654-666 = admissibility/check/acyclic/degree only). Both activation lines now on disk: skill/KIND.md description-trigger, rule/KIND.md paths-match (9f7d176). PATHS-MATCH-ABSENCE shipped: dead_activation reads absent/blank paths as live. Sole blocker: fork reachability-gate-mechanism (severity home unsettled)."
  },
  {
    "tag": "MEMORY-KIND",
    "summary": "Add the `memory` built-in kind — CLAUDE.md family (no frontmatter, dual root locus, `@path` imports); the first pure-data managed kind, unlocking the CLAUDE.md-onto-surface flip",
    "per": {
      "path": "specs/15-kinds.md",
      "section": "Two categories of kind — ownership, not mechanism"
    },
    "gate": {
      "kind": "parked",
      "reason": "the curated kinds/memory/KIND.md + packages/memory.anthropic/PACKAGE.md are outside build's fence (curated std-lib — a human authors them, cited to code.claude.com/docs/en/memory); and the no-frontmatter projection format + the `@path` edge extraction primitive are deliberate closed-vocab additions a human must sanction before the engine wiring can land"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "kinds/memory/KIND.md",
          "description": "The embedded built-in `memory` KIND.md (curated — humans author it, outside build's fence), beside kinds/skill and kinds/rule. Format facts per specs/15-kinds.md: no frontmatter, `@`-imports, dual root locus (project `./CLAUDE.md` + user `~/.claude/CLAUDE.md`). Declares a no-frontmatter projection format, markdown-structure extraction over the body, and `@path` imports as declared edges. build.rs auto-embeds it."
        },
        {
          "path": "packages/memory.anthropic/PACKAGE.md",
          "description": "The memory kind's built-in package `memory.anthropic` (curated product territory, outside build's fence), embedded with the std-lib by build.rs. Decidable clauses only over the memory format, per-clause `source` citations to the Claude Code memory docs (code.claude.com/docs/en/memory); the `@path` imports resolve as a referential clause over the declared edge (never a body grep — law 8)."
        },
        {
          "path": "tests/memory_contract.rs",
          "description": "Fixture-driven proof mirroring tests/rule_contract.rs: broken/clean memory fixtures through the generic adapter + loader, plus an import round-trip insta snapshot proving idempotence over a no-frontmatter unit."
        }
      ],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Engine wiring (inside the fence): add \"memory\" to BUILTIN_KINDS (kind.rs:30); add the no-frontmatter projection-format vocab entry beside `yaml-frontmatter` (the declared-adapter face); add an `@path`-import edge extraction primitive to the closed `Primitive` vocab (enum kind.rs:456 + parse_primitive load-guard kind.rs:1048). All are deliberate closed-vocab additions harvested because the memory kind demands them (specs/15-kinds.md Decision: extraction is a closed algebra)."
        },
        {
          "path": "src/builtin.rs",
          "description": "Add a `MEMORY_PACKAGE = \"memory.anthropic\"` const and register the memory kind→package binding beside SKILL_PACKAGE/RULE_PACKAGE (builtin.rs:32-37). build.rs auto-embeds the PACKAGE.md; no manual embed edit."
        },
        {
          "path": "src/extract.rs",
          "description": "The extractor helpers behind the memory format: an `@path`-import reader over the markdown body yielding declared edges (mirroring body_headings/body_sections), and the no-frontmatter unit read face (empty frontmatter map, body is the whole file)."
        },
        {
          "path": "src/document.rs",
          "description": "The generic adapter's no-frontmatter format path: split/emit a memory member whose on-disk artifact carries no YAML frontmatter (the `yaml-frontmatter` adapter assumes a fence), byte-deterministic re-emission of the body."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/memory_contract.rs",
        "asserts": "the memory package's decidable clauses fire on broken fixtures and stay silent on clean; a no-frontmatter memory member imports and re-emits idempotently (insta snapshot); an `@path` import resolves as a declared edge and a dangling one is a coverage finding."
      }
    ],
    "acceptance": "`temper check` dispatches the CLAUDE.md family to `memory.anthropic`; `temper import` projects memory files as no-frontmatter members idempotently via the generic adapter; the `@path` edge primitive builds the memory graph; cargo fmt/clippy -D warnings/test all green. (Parked: not built until the curated KIND.md/PACKAGE.md are human-authored and the format/primitive additions sanctioned.)",
    "notes": "Drained from the open-questions CLAUDE.md-bootstrap recipe (DFA shipped, so it's fileable). Memory is the flip-ceremony proof — shipping it moves CLAUDE.md onto the surface. Curated KIND.md/PACKAGE.md are outside build's fence: a human authors them (cited code.claude.com/docs/en/memory), then engine wiring lands. NB `@path` is its own edge primitive, NOT EXTRACTION-VOCAB-GAPS's fenced/key-path — that entry stays deferred, not revived here."
  },
  {
    "tag": "EXTRACTION-VOCAB-GAPS",
    "summary": "Extend the extraction algebra with the two primitives 15-kinds.md lists but code lacks — a `fenced` block extractor and key-path (nested) `field` lookup — held until a consumer kind needs them",
    "per": {
      "path": "specs/15-kinds.md",
      "section": "The extraction algebra — the soundness boundary, as data"
    },
    "gate": {
      "kind": "deferred",
      "reason": "no consumer — the extraction vocabulary is harvested from the built-in kinds and extended deliberately (15-kinds.md Decision: extraction is a closed algebra); no current kind (skill/rule) reads a fenced block or a nested key-path, so adding either now is speculative scope with no kind to exercise it. Revive when a kind demands it — a JSON-manifest kind's nested key-path field, or a spec/class kind's fenced-block extraction. (NB memory's `@path` is a separate edge primitive, not one of these — MEMORY-KIND does not revive this.)"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Two closed-vocabulary additions to `Primitive` (`specs/15-kinds.md`): (1) a `Fenced` primitive extracting fenced code-block content — the markdown-structure extractor the spec lists (`a fenced block`) but the enum omits (today: field/headings/sections/line_count/placement, kind.rs:456-477); (2) make `Primitive::Field` honor the key-path its own doc-comment promises — the impl currently does a flat `unit.frontmatter.get(key)` (kind.rs:499) so a nested `a.b.c` path can't be read. Both extend the closed-vocab load-guard's accepted primitive set (parse_primitive, kind.rs:1048). This entry owns both edits so they don't collide in parallel worktrees."
        },
        {
          "path": "src/extract.rs",
          "description": "The extractor helpers behind the two primitives: a `body_fenced_blocks`-style reader over the markdown body (mirroring `body_headings`/`body_sections`), and key-path traversal for the field extractor (walk nested TOML/JSON tables to the leaf before `json_to_feature`, kind-preserving per the `type` lattice, `specs/10-contracts.md`)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/extract_equivalence.rs",
        "asserts": "a `fenced` primitive yields the fenced block's content and skips prose; a key-path `field` reads a nested value and is absent (not errored) when the path doesn't resolve; both preserve the source scalar kind."
      }
    ],
    "acceptance": "`Primitive::Fenced` and a nested-key-path `Primitive::Field` are in the closed vocabulary, load-guarded, and extract over a `Unit`; a KIND.md declaring either reads the intended feature; cargo fmt/clippy -D warnings/test green. (Deferred: not built until a kind consumes one.)",
    "notes": "Disk re-confirmed both gaps: `Primitive` = field/headings/sections/line_count/placement (kind.rs:456-477); `Field` reads flat `frontmatter.get(key)` (kind.rs:499), no key-path; load-guard parse_primitive at kind.rs:1048. Neither breaks anything today (no kind uses them). ONE entry — both touch src/kind.rs + src/extract.rs. MEMORY-KIND does NOT revive this: `@path` is a distinct edge primitive, not fenced/key-path."
  },
  {
    "tag": "AGENT-KIND",
    "summary": "Add the `agent` built-in harness kind — `.claude/agents/*.md` as a member document, read through the generic loader and checked by the embedded `agent.anthropic` package",
    "per": {
      "path": "specs/20-surface.md",
      "section": "Artifact kinds & package binding"
    },
    "gate": {
      "kind": "deferred",
      "reason": "more built-in kinds is the wrong direction under the requirements/satisfies reframe; and post-adapter-wave the agent kind is mostly curated data (a KIND.md + PACKAGE.md, both outside build's fence) with near-zero engine code — revive only if a story demands the agent kind"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "kinds/agent/KIND.md",
          "description": "The embedded built-in `agent` KIND.md (curated — humans author it; outside build's fence), beside kinds/skill and kinds/rule: `governs = { root = '.claude/agents', glob = '*.md' }`, the declared `yaml-frontmatter` projection format + unit shape, composed extraction over the `name`/`description`/optional `tools`/`model` fields plus markdown structure. build.rs auto-embeds it — no engine extractor."
        },
        {
          "path": "packages/agent.anthropic/PACKAGE.md",
          "description": "The agent kind's built-in package `agent.anthropic` (curated product territory, outside build's fence), embedded with the std-lib by build.rs. Clauses: `name` required + `allowed_chars` `[a-z0-9-]`; `description` required; `forbidden_keys` for the Cursor `.mdc` keys Claude Code ignores; an advisory `max_lines`. Per-clause `source` citations to the Claude Code subagent docs."
        },
        {
          "path": "tests/agent_contract.rs",
          "description": "Fixture-driven proof mirroring tests/rule_contract.rs: one broken agent per clause proving it fires, a clean agent that trips nothing, and an import round-trip insta snapshot proving idempotence — all through the generic adapter + loader, no per-kind engine code."
        }
      ],
      "edit": [
        {
          "path": "src/kind.rs",
          "description": "Add `\"agent\"` to the `BUILTIN_KINDS` name-list (kind.rs:30) so a `[kind.agent]` binding reads as a built-in layer, not a custom kind."
        },
        {
          "path": "src/builtin.rs",
          "description": "Add the `AGENT_PACKAGE = \"agent.anthropic\"` const beside SKILL_PACKAGE/RULE_PACKAGE (builtin.rs:32-37) and register its kind→package binding. build.rs auto-embeds the PACKAGE.md; no manual embed edit."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/agent_contract.rs",
        "asserts": "each decidable clause of the agent package fires on its broken fixture and stays silent on a clean agent; import of an unchanged agent is idempotent (insta snapshot)."
      }
    ],
    "acceptance": "`temper check` dispatches `.claude/agents/*.md` to the `agent.anthropic` package (conformance) and that package passes admissibility; `temper import` projects agents as one-document members idempotently via the generic frontmatter adapter; cargo fmt/clippy -D warnings/test all green.",
    "notes": "Declared-adapter architecture: no src/agent.rs IR — the generic adapter projects it. Curated kinds/agent/KIND.md + packages/agent.anthropic/PACKAGE.md sit outside build's fence; engine wiring is near-zero (add \"agent\" to BUILTIN_KINDS kind.rs:30 + an AGENT_PACKAGE const/binding in builtin.rs; build.rs auto-embeds). Adapter wave drained (DFA shipped). Stays deferred: more built-in kinds is the wrong direction under requirements/satisfies."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship acquisition channels — npm optionalDependencies binary wrapper, Homebrew, cargo install — auto-detected, fail-loud on a missing platform binary",
    "per": {
      "path": "specs/50-distribution.md",
      "section": "Decision: acquisition rides the ecosystem's package managers"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs human release setup (npm org, Homebrew tap, signing/publish creds) before the mechanical wrappers can be validated end-to-end"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages, GitHub release assets, and the Homebrew tap; channel auto-detected. Distinct from the existing `.github/workflows/temper.yml` CI job."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`, confirmed on disk). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages (Biome's pattern) plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, `specs/50-distribution.md`). Reconcile with the flume manifest (a non-private publish entry, or a split package dir) rather than clobbering the harness manifest. Scoped/prefixed names per the resolved (project-name) fork."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger can acquire the prebuilt binary via npm (platform-specific optionalDependencies), Homebrew, standalone release assets, or `cargo install`; a missing platform binary is an install error, never a silent skip.",
    "notes": "Release infra, mostly outside `src/`; the cargo gates can't validate npm/Homebrew publish, and channels need human creds (npm org, tap, signing). NB: root `package.json` already exists as the *private* flume manifest — reconcile or split, never blindly create a second. Parked until a human sets creds up."
  },
  {
    "tag": "COMMUNITY-DOCS",
    "summary": "Add CONTRIBUTING.md and SECURITY.md — the two missing launch community docs (two-sided AI-authorship disclosure; vuln-reporting evidence bar)",
    "per": {
      "path": "specs/55-offering.md",
      "section": "The community surface — small, honest, gated"
    },
    "gate": {
      "kind": "parked",
      "reason": "build's writable fence (.flume/chain.ts BUILD_WRITABLE_PATHS) lists only README/CHANGELOG/AGENTS/LICENSE among root docs — no root CONTRIBUTING.md/SECURITY.md glob — so build would bail; a human must widen the fence (chain.ts is human territory) and enable private vulnerability reporting (a repo setting) before build can author these"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "CONTRIBUTING.md",
          "description": "Small and honest per the Decision: how to file a good issue, 'search first', PRs by prior discussion, and the two-sided AI-authorship policy (inbound AI-assisted contributions welcome WITH disclosure; the contributor must understand and defend the change without the assistant). Expectation-setting, not an invitation to co-maintain."
        },
        {
          "path": "SECURITY.md",
          "description": "Vulnerability-reporting policy with an explicit evidence bar — a report must demonstrate, not speculate (the 'unverified AI-generated vuln reports are the new spam' lesson). Points at private vulnerability reporting (the GitHub repo setting a human enables)."
        }
      ],
      "edit": [],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "CONTRIBUTING.md and SECURITY.md exist at root, matching specs/55's launch-set Decisions (two-sided disclosure; demonstrate-not-speculate); the fence has been widened so build's entry-fence preflight admits them.",
    "notes": "Launch set (specs/55). Already on disk: README, AGENTS.md, CHANGELOG.md, LICENSE-MIT/APACHE, issue forms (.github/ISSUE_TEMPLATE/{bug,feature,config}.yml). Missing only CONTRIBUTING.md + SECURITY.md. Both are root-level per the spec but outside build's root-docs fence — parked on a human fence-widen (chain.ts) + enabling private reporting. Parked entries are skipped by the entry-fence gate, so this files cleanly."
  }
]
