[
  {
    "tag": "EMIT-LF-NORMALIZE",
    "summary": "emit writes projection bytes verbatim including a source's CRLF; normalize every emitted projection to LF uniformly so double-emit byte-reproduces across platforms (a CRLF source emits clean)",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "Emit"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "`emit_one`:654 assembles `desired`:690 (and the `second_pass`:700 determinism check) from either `project_bytes`:745 or, for an own_path member, `projection.body` verbatim (:691/:701) — no line-ending normalization, so a member body carrying CRLF (a body authored on Windows) lands CRLF in the projection and the same program emits different bytes on different machines. Normalize `desired`/`second_pass` to LF (CRLF and lone CR → LF) before the double-emit compare (:705), before `sha256_hex`:711, and before `fs::write`:723, so the lock's emit_hash and the written bytes are the LF-normalized form. Both branches normalize — the own_path body (a file()-carried member's own source) too, per pipeline.md's 'projections are written LF uniformly, whatever the source's convention.'"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/emit.rs",
        "asserts": "a member whose body carries CRLF (and one carrying a lone CR) emits an LF-only projection on disk; the emit_hash and the idempotent Unchanged re-emit both compute over the LF-normalized bytes, so double-emit byte-reproduces regardless of the source's line-ending convention"
      }
    ],
    "acceptance": "A member body carrying CRLF (or lone CR) emits an LF-only projection; the emit hash and written bytes are the normalized form, so double-emit byte-reproduces on any platform — line endings never reach a projection as content.",
    "notes": "Field ruling 0010 (spec delta f4189c3): EOL is layout, not content — the verbatim invariant guards words, one byte wider than leaf whitespace. Emit-only; the import/read side is unchanged. own_path body also normalizes per 'uniformly, whatever the source's convention.' EMIT-REAP-ORPHANS shipped (f8e039b) → unblocked; disjoint from SETTINGS-FORMAT-PRESERVING (install.rs). scoped at 44b4f27."
  },
  {
    "tag": "SETTINGS-FORMAT-PRESERVING",
    "summary": "install's settings write re-serializes the whole settings.json (keys reordered, EOL churn) — make the hook merge format-preserving so existing keys/order/formatting survive the insertion",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "Install"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "`project_settings`:697 parses the existing `.claude/settings.json`, grafts the `SessionStart`/`PreToolUse` temper hooks, then re-emits the whole document via `serde_json::to_string_pretty`:765 — so a human's key order, indentation, and EOL are all rewritten, exactly the churn the round-trip discipline forbids for TOML/markdown. Make the write format-preserving: only the temper hook groups it inserts/updates change bytes; every existing key, its order, and the file's formatting survive (a hand-authored settings.json diffs by one hunk after install). Absent/empty file still writes a fresh canonical document. Mechanism is build's decidable call — a JSON CST-editor crate or a surgical splice of just the two hook arrays; lean surgical (dependency-free, subtraction-before-addition). Update the stale comment at :693-696 which currently asserts the gap ('there is no format-preserving JSON editor to round-trip through') — decision 0008 closes it (exit clause: cut on contact). If a crate is adopted, that is an ordinary Cargo.toml dependency add."
        },
        {
          "path": "tests/install.rs",
          "description": "`EXISTING_SETTINGS`:108 is already canonical 2-space pretty JSON, so a whole-file re-serialize is byte-identical to it — the fixture cannot falsify reordering/reformatting. Add a deliberately non-canonical settings fixture (a distinct indentation width and a key order serde would not reproduce) and assert that after install every pre-existing byte outside the grafted temper hook groups is unchanged (one-hunk diff), while the SessionStart/guard placements still land and re-running converges."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "installing into a settings.json with non-canonical formatting (custom indentation, non-serde key order) leaves every existing key's order and formatting byte-intact and diffs by a single hunk — the grafted temper hooks — never re-serializing the whole file; the SessionStart hook and PreToolUse guard still place and install stays idempotent"
      }
    ],
    "acceptance": "Install merges the SessionStart hook (and the guard, with its constituency) into an existing .claude/settings.json without re-serializing it — existing keys, order, and formatting survive, a hand-authored file diffs by one hunk — while an absent file still writes a fresh canonical document and re-running converges.",
    "notes": "Field ruling 0008 (f4189c3): the JSON peer of the toml_edit round-trip keystone. install.rs:693-696 asserts the gap ('no format-preserving JSON editor') — 0008 closes it; update the comment (exit clause). Mechanism (JSON CST crate vs. surgical splice of the two hook groups) is build's call; lean surgical, dependency-free. GUARD-OWNPATH shipped (6fd37f5) → unblocked. Shares install.rs+tests/install.rs with MODULE-RELATIVE-PATHS → it blockedBy this. scoped at 44b4f27."
  },
  {
    "tag": "MODULE-RELATIVE-PATHS",
    "summary": "a file() path resolves against one cwd/baseDir, not the module that states it — scaffold works only by directory-depth accident; recut resolution to the stating module, move scaffold + docs with it",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "The SDK"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "SETTINGS-FORMAT-PRESERVING"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/emit.ts",
          "description": "`fileSourcePath`:147 resolves a `file()` asset against `options.baseDir ?? process.cwd()` — one workspace/cwd base for every member, never the module that stated the path. Recut resolution so a `file()` path resolves relative to the module that declared it (the rule every import system teaches). The now-vestigial cwd/`baseDir` default and its carriers (`ResolveOptions.baseDir`:23-25, `EmitOptions.baseDir`:185-188, the `baseDir` thread through `emit`:222/`resolveBody`:40) retire or repurpose as the new mechanism needs — build's call. The member's `source_path`:164 must still end absolute (the engine's true source path). Update the module-relative doc comments (:22, :185) which currently describe a cwd base."
        },
        {
          "path": "sdk/src/prose.ts",
          "description": "`file(path)`:87 / the `File` interface:38 capture only a path string, so resolution cannot be module-relative. Give the constructor the stating module's location — ESM's only facility is `import.meta.url`, so the call carries it (`file(import.meta.url, relPath)` or `file(new URL(relPath, import.meta.url))` — exact spelling is build's decidable call among equivalents). `File.path`:41 (surfaced in emit's error text) and the `Module-relative path`:40 doc move with it."
        },
        {
          "path": "src/install.rs",
          "description": "`relative_to_workspace`:957 writes the scaffolded `file()` path with one `../` (relative to `.temper/`), but member modules live at `.temper/<kind>/<name>.ts` (:885-886) — two levels under root — so under module-relative resolution the path must recut to the member module's own directory (`../../.claude/...`), and `member_module_source`:980 must emit the new `file(...)` call shape (the `file({rel_path:?})` template at :991). Update the stale doc comment :950-953 ('SDK resolves against cwd, which emit_program sets to harness.ts's directory') — the exit clause covers cutting it. The unit tests at :1349/:1367 assert the old `file(\"../.claude/...\")` shape and move with the change. (install.rs line-cites here will drift when SETTINGS-FORMAT-PRESERVING ships; re-verify at unblock.)"
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "sdk/test/emit.test.ts",
        "asserts": "a file() asset resolves relative to the module that stated it (not cwd/baseDir): a member whose file() names a path relative to its own module resolves and reads byte-faithfully regardless of the process cwd, its payload source_path is the absolute stating-module-relative path, and a missing asset is still a loud emit error"
      },
      {
        "path": "tests/install.rs",
        "asserts": "a scaffolded member module at .temper/<kind>/<name>.ts writes a file() path that resolves module-relative to the source artifact (climbing from the module's own directory, not by cwd accident), and the emitted program's first emit reads that source back byte-identical"
      }
    ],
    "acceptance": "A file() asset path resolves relative to the module that stated it, never the workspace/cwd; scaffold writes module-relative paths that resolve from the member module's own directory (no longer saved by directory-depth accident); the SDK doc becomes true and emit stays byte-reproducible.",
    "notes": "Field ruling 0009 (f4189c3): resolution, scaffold, docs 'move together' — split leaves scaffold emitting one shape while resolution expects another, so one commit. ESM forces import.meta.url as the stating-module facility; call spelling is build's. Scope is file() assets (emit's one path-resolved surface). Shares install.rs+tests/install.rs with SETTINGS-FORMAT-PRESERVING → blockedBy it (install.rs chain tail; +SDK). scoped at 4bfa3cd."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper` 0.0.4, single package, `(sdk-package-layout)`); channel 1's SDK foundation shipped. No `.github/workflows/release.yml` exists (only the `temper.yml` check job); root package.json is still the private flume manifest."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm `@dtmd/temper`; ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs range-correctness shipped in SCAFFOLD-OUTPUT-VALID (^0.0.4); release owns only future re-pins."
  }
]
