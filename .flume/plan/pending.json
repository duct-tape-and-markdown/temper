[
  {
    "tag": "TMPDIR-HELPER-CONSOLIDATE(mainrs)",
    "summary": "Switch src/main.rs's lone hand-rolled test tempdir to the tempfile-backed tmpdir(label) pattern every other in-src test module already carries",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "`mod tests` (line 1128) has one test, `repo_file_set_stays_raw_disk_including_gitignored_targets` (line 1140), that hand-rolls its temp dir: `let root = std::env::temp_dir().join(format!(\"temper-backing-{}\", std::process::id()));` (line 1141) plus a pre-emptive `fs::remove_dir_all(&root)` (line 1142) and a trailing `fs::remove_dir_all(&root)` (line 1157). Every other in-src `#[cfg(test)]` module already carries an identical private helper for this (e.g. `src/frontmatter.rs:565`, `src/check.rs:344`, `src/bundle.rs:329`): `fn tmpdir(label: &str) -> PathBuf { tempfile::Builder::new().prefix(label).tempdir().expect(\"failed to create temp dir\").keep() }`. Add that same helper to `main.rs`'s `mod tests`, replace line 1141 with `let root = tmpdir(\"repo-file-set\");`, and drop both `remove_dir_all` calls (lines 1142 and 1157) to match the sibling modules' convention (no manual cleanup — `tempfile` owns uniqueness, `.keep()` is deliberate per the sibling copies)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/main.rs",
        "asserts": "repo_file_set_stays_raw_disk_including_gitignored_targets still passes unchanged — same gitignored-backing-target assertions, now against a tempfile-backed dir"
      }
    ],
    "acceptance": "main.rs's test module uses the same tmpdir(label) helper every sibling in-src test module carries; no hand-rolled env::temp_dir()+process::id()+manual remove_dir_all construction remains in src/; cargo test green.",
    "notes": "Drained from .flume/refactor/build-main-backing-tmpdir.md (observed at 96e947d, re-verified live at HEAD — src/main.rs:1141 unchanged since). Found while build verified TEST-SCAFFOLDING-CONSOLIDATE's acceptance bar: main.rs wasn't in that entry's files.edit (its one-off didn't match the counter+pid+label description), so this straggler is its own entry. Disjoint from every other open/blocked entry (none touch src/main.rs). scoped at 33c1f7f"
  },
  {
    "tag": "FRONTMATTER-DELIMITER-SCAN-CONSOLIDATE",
    "summary": "Consolidate the byte-identical closing-`---`-delimiter scan loop duplicated in frontmatter.rs's split_frontmatter and install.rs's frontmatter_inner into one home",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "TEST-SCAFFOLDING-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/frontmatter.rs",
          "description": "`split_frontmatter` (line 409) inlines the closing-delimiter scan (lines 417-426: `for line in rest.split_inclusive('\\n') { let content = line.strip_suffix('\\n')...; if content.trim_end() == \"---\" { ... } offset += line.len(); }`) after its own opening-delimiter check. Extract the scan into `pub(crate) fn closing_delimiter(rest: &str) -> Option<(&str, &str)>` returning `(matter, body)` slices of `rest`; `split_frontmatter` keeps its opening check (`raw.split_once('\\n')`, `first.trim_end() != \"---\"`) and delegates the rest to it."
        },
        {
          "path": "src/install.rs",
          "description": "Delete `frontmatter_inner` (line 1463) — its own doc comment admits it 'mirrors the delimiter detection the loaders use': a byte-for-byte copy of the same scan loop, returning only the matter half. Its three call sites — `project_modeline` (line 1412), `project_note` (line 1441), `placement_lines` (line 1481), each already holding `rest = source.strip_prefix(\"---\\n\")?` — use `crate::frontmatter::closing_delimiter(rest).map(|(matter, _)| matter)`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "modeline-insertion and managed-by-note projection (project_modeline/project_note/placement_lines-driven discovery-report behavior) is unchanged for frontmatter with and without a closing delimiter"
      },
      {
        "path": "src/frontmatter.rs",
        "asserts": "the in-source frontmatter split tests (with/without a closing `---`, with a false-positive `---`-looking body line) are unchanged"
      }
    ],
    "acceptance": "One closing-delimiter scanner (`crate::frontmatter::closing_delimiter`, `pub(crate)`); no duplicate scan loop remains in install.rs; `cargo test` green.",
    "notes": "Job 4 residue sweep at 425968d: both functions' doc comments admit they 'mirror' the same scan. gray_matter unused here — doesn't preserve raw bytes (pinned-semantics exception). blockedBy TEST-SCAFFOLDING-CONSOLIDATE (shares install.rs, now open, unshipped). install.rs refs re-verified at 220e9cd post plural-helper edit: frontmatter_inner->1463, project_modeline->1412, project_note->1441, placement_lines unchanged 1481. scoped at 220e9cd"
  },
  {
    "tag": "TEST-FIXTURE-HELPERS-CONSOLIDATE",
    "summary": "Consolidate 5 more copy-pasted integration-test builders (write_harness/write_skill twins, CheckRun/check_in, author_satisfies, tree_bytes) into tests/common",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "TEST-SCAFFOLDING-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/common/mod.rs",
          "description": "Already exists (WINDOWS-VENDOR-SYMLINK-JUNCTION shipped it; TEST-SCAFFOLDING-CONSOLIDATE, blocking this entry, further extends it with tmpdir/fixture) — extend it further. Add one shared `write_skill(root: &Path, name: &str, skill_md: &str)` — the byte-identical body duplicated as `write_harness` in tests/cli.rs:80, tests/session_start.rs:62, tests/reporters.rs:181 AND as `write_skill` in tests/coverage.rs:60, tests/memory_gate.rs:62, tests/requirement_roster.rs:66 (same body, two names, 6 sites — one function). Add one `check_in` covering the 3 exact-dup `CheckRun{ok,output}`+`check_in(root)` sites (tests/graph.rs:89,96; tests/coverage.rs:117,124; tests/requirement_roster.rs:165,172 — plain `temper check`) and the 2 near-dup github-reporter shapes (tests/gate_fail_loud.rs:85, `(Vec<String>, bool)` + extra args; tests/lock_declaration_rows.rs:770, `(bool, String)`) — one function parameterized by reporter/args, each caller adapting to its own return shape. Add one `author_satisfies` covering the 3-arg dup (tests/coverage.rs:69, tests/requirement_roster.rs:75) and the 4-arg `kind_dir`-parameterized dup (tests/graph.rs:180, tests/lock_declaration_rows.rs:850) as one function (3-arg callers pass a default `kind_dir`). Add one `tree_bytes(dir: &Path) -> BTreeMap<PathBuf, Vec<u8>>` backed by the sanctioned `walkdir` crate (already a direct Cargo.toml dependency, line 26) replacing the hand-rolled `fs::read_dir` stack walk triplicated at tests/bundle.rs:90, tests/install.rs:148, tests/emit.rs:54."
        },
        {
          "path": "tests/cli.rs",
          "description": "Delete the local `write_harness` (line 80, byte-identical to coverage.rs's `write_skill`); its call sites use `common::write_skill`."
        },
        {
          "path": "tests/session_start.rs",
          "description": "Delete the local `write_harness` (line 62); call sites use `common::write_skill`."
        },
        {
          "path": "tests/reporters.rs",
          "description": "Delete the local `write_harness` (line 181); call sites use `common::write_skill`."
        },
        {
          "path": "tests/graph.rs",
          "description": "Delete the local `write_harness` (line 77, 5-arg rule+skill variant) — keep its 2-line rule-write inline, delegate the skill half to `common::write_skill`; delete the local `CheckRun`/`check_in` (lines 89, 96); delete the local 4-arg `author_satisfies` (line 180); all three switch to `common`."
        },
        {
          "path": "tests/coverage.rs",
          "description": "Delete the local `write_skill` (line 60); delete the local `CheckRun`/`check_in` (lines 117, 124); delete the local 3-arg `author_satisfies` (line 69); switch to `common`."
        },
        {
          "path": "tests/requirement_roster.rs",
          "description": "Delete the local `write_skill` (line 66); delete the local `CheckRun`/`check_in` (lines 165, 172); delete the local 3-arg `author_satisfies` (line 75); switch to `common`."
        },
        {
          "path": "tests/memory_gate.rs",
          "description": "Delete the local `write_skill` (line 62); call sites use `common::write_skill`."
        },
        {
          "path": "tests/coverage_note.rs",
          "description": "Delete the local `write_skill` (line 48, generates its own name-derived skill_md body) — its caller builds the skill_md string itself, then calls `common::write_skill`, preserving today's generated frontmatter/body exactly."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "Delete the local `check_in` (line 770, github-reporter tuple shape) and the local 4-arg `author_satisfies` (line 850); switch to `common`."
        },
        {
          "path": "tests/gate_fail_loud.rs",
          "description": "Delete the local `check_in` (line 85, github-reporter + args tuple shape); switch to `common`, keeping each call site's existing `::`-line finding-count parsing."
        },
        {
          "path": "tests/bundle.rs",
          "description": "Delete the local `tree_bytes` (line 90, hand-rolled `fs::read_dir` stack walk); call sites use `common::tree_bytes`."
        },
        {
          "path": "tests/install.rs",
          "description": "Delete the local `tree_bytes` (line 148); call sites use `common::tree_bytes`."
        },
        {
          "path": "tests/emit.rs",
          "description": "Delete the local `tree_bytes` (line 54); call sites use `common::tree_bytes`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "rule+skill harness construction, check_in-driven assertions, and 4-arg author_satisfies-driven requirement/satisfies fixtures are unchanged after switching to the shared common helpers"
      },
      {
        "path": "tests/lock_declaration_rows.rs",
        "asserts": "github-reporter check_in output and 4-arg author_satisfies fixtures used by lock-row assertions are unchanged"
      },
      {
        "path": "tests/emit.rs",
        "asserts": "tree_bytes-based byte-for-byte directory comparisons (the double-emit and real-SDK-vendored tests) are unchanged after switching to the walkdir-backed shared helper"
      }
    ],
    "acceptance": "tests/common exports one write_skill, one check_in (reporter/args-parameterized), one author_satisfies (kind_dir-parameterized), and one walkdir-backed tree_bytes; no duplicate or near-duplicate definition of any of the four remains in the 13 edited test files; `cargo test` green.",
    "notes": "Found in job 4's residue sweep, same 'test scaffolding is a surface too' class as TEST-SCAFFOLDING-CONSOLIDATE but different symbols -- blockedBy it, same files (avoid two opens on one file). Re-verified live at d6c37a3: tests/common/mod.rs now exists (moved new->edit here); tests/install.rs's tree_bytes line corrected 180->148 and tests/emit.rs's 53->54 (both shifted by db34aea's sdk_root/vendor_sdk consolidation). All other cited line numbers still accurate. scoped at d6c37a3"
  },
  {
    "tag": "TEST-HELPER-DUPES-CONSOLIDATE(recordwriters)",
    "summary": "Consolidate 6 more copy-pasted test builders (write_requirements, write_rule, write_retired_manifest, write_lock, retired_manifest_name, clean_skill) into tests/common",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "TEST-FIXTURE-HELPERS-CONSOLIDATE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/common/mod.rs",
          "description": "Already exists, extended by every entry ahead of this one in the chain. Add six byte-identical-per-copy helpers this residue sweep found past the four TEST-FIXTURE-HELPERS-CONSOLIDATE already covers: `pub fn write_requirements(root: &Path, requirements: Vec<RequirementRow>)` (3 copies: tests/gate_fail_loud.rs:69, tests/requirement_roster.rs:262, tests/coverage.rs:103); `pub fn write_rule(root: &Path, name: &str)` (tests/coverage.rs:359, tests/requirement_roster.rs:96); `pub fn write_retired_manifest(root: &Path, contents: &str)` (tests/graph.rs:119, tests/requirement_roster.rs:215); `pub fn write_lock(root: &Path, declarations: Declarations)` (tests/graph.rs:127, tests/lock_declaration_rows.rs:940); `pub fn retired_manifest_name() -> String` (tests/requirement_roster.rs:208, tests/graph.rs:112); `pub fn clean_skill(name: &str) -> String` (tests/graph.rs:47, tests/requirement_roster.rs:50)."
        },
        {
          "path": "tests/gate_fail_loud.rs",
          "description": "Delete the local `write_requirements` (line 69); call sites use `common::write_requirements`."
        },
        {
          "path": "tests/requirement_roster.rs",
          "description": "Delete the local `write_requirements` (line 262), `write_rule` (line 96), `write_retired_manifest` (line 215), `retired_manifest_name` (line 208), `clean_skill` (line 50); call sites use `common::*`."
        },
        {
          "path": "tests/coverage.rs",
          "description": "Delete the local `write_requirements` (line 103) and `write_rule` (line 359); call sites use `common::*`."
        },
        {
          "path": "tests/graph.rs",
          "description": "Delete the local `write_retired_manifest` (line 119), `write_lock` (line 127), `retired_manifest_name` (line 112), `clean_skill` (line 47); call sites use `common::*`."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "Delete the local `write_lock` (line 940); call sites use `common::write_lock`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/requirement_roster.rs",
        "asserts": "requirement-roster fixtures built from write_requirements/write_rule/write_retired_manifest/retired_manifest_name/clean_skill render unchanged after switching to the shared common helpers"
      },
      {
        "path": "tests/graph.rs",
        "asserts": "graph resolution tests using write_lock/write_retired_manifest/clean_skill are unchanged"
      }
    ],
    "acceptance": "tests/common exports one write_requirements, write_rule, write_retired_manifest, write_lock, retired_manifest_name, clean_skill; no duplicate definition remains in gate_fail_loud.rs, requirement_roster.rs, coverage.rs, graph.rs, or lock_declaration_rows.rs; `cargo test` green.",
    "notes": "Found in job 4's residue sweep at ef72da3 -- six byte-identical helper groups past TEST-FIXTURE-HELPERS-CONSOLIDATE's scope (different symbols: requirement/rule/retired-manifest/lock writers, not write_skill/check_in/author_satisfies/tree_bytes). blockedBy TEST-FIXTURE-HELPERS-CONSOLIDATE: shares graph.rs/coverage.rs/requirement_roster.rs/lock_declaration_rows.rs/gate_fail_loud.rs/tests/common/mod.rs with it. scoped at ef72da3"
  },
  {
    "tag": "TEST-HELPER-DUPES-CONSOLIDATE(lockunitbuilders)",
    "summary": "Consolidate 9 more copy-pasted/near-duplicate test builders (unit/member/kind-facts/findings/satisfies/requirement helpers) and fold check_harness into check_in",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "TEST-HELPER-DUPES-CONSOLIDATE(recordwriters)"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "tests/common/mod.rs",
          "description": "Extend further (chain continues from the recordwriters slice). Add `pub fn surface_unit(member: &Member, member_doc: &str, dir: &Path) -> Unit` (tests/command_kind.rs:63, tests/agent_kind.rs:62, identical); `pub fn skill_member(name: &str, description: &str, body: &str) -> PayloadMember` (tests/lock_declaration_rows.rs:95, tests/emit.rs:123, identical); `pub fn rule_member(name: &str, paths: Option<&[&str]>, body: &str) -> PayloadMember` — the Option-taking superset (tests/emit.rs:109 today); tests/lock_declaration_rows.rs:108's non-Option 3-arg callers pass `Some(paths)`; `pub fn rule_kind_facts(...) -> KindFactRow` and `pub fn skill_kind_facts(...) -> KindFactRow`, each parameterized by the provider/registration values the 2-3 copies diverge on (tests/lock_declaration_rows.rs:67,50; tests/emit.rs:83,96; tests/acceptance.rs:137); `pub fn skill_surface_unit(skill: &Member, name: Option<&str>) -> Unit` — tests/acceptance.rs:65's no-name 1-arg callers pass `None` and keep deriving from `skill.id`, tests/extract_equivalence.rs:58's name-carrying callers pass `Some(name)`; `pub fn findings_for<'a>(findings: &'a [String], rule: &str) -> Vec<&'a String>` (tests/memory_gate.rs:100, tests/gate_fail_loud.rs:104, tests/coverage_note.rs:123, identical); `pub fn author_rule_satisfies(root: &Path, name: &str, requirements: &[&str])` (tests/coverage.rs:371, tests/requirement_roster.rs:109, identical); `pub fn requirement(name: &str, required: bool, kind: Option<&str>) -> RequirementRow` — the union of three shapes (tests/gate_fail_loud.rs:55 passes `false, None`; tests/coverage.rs:89 passes `required, None`; tests/requirement_roster.rs:221 passes `false, Some(kind)`). Fold `check_harness`'s `(Vec<String>, bool)` shape (tests/coverage_note.rs:104) into the `check_in` family TEST-FIXTURE-HELPERS-CONSOLIDATE already added — same job (run `temper check --reporter github`, parse `::`-prefixed lines), not a fifth near-duplicate helper."
        },
        {
          "path": "tests/command_kind.rs",
          "description": "Delete local `surface_unit` (line 63); call sites use `common::surface_unit`."
        },
        {
          "path": "tests/agent_kind.rs",
          "description": "Delete local `surface_unit` (line 62); call sites use `common::surface_unit`."
        },
        {
          "path": "tests/lock_declaration_rows.rs",
          "description": "Delete local `skill_member` (line 95), `rule_member` (line 108, 3-arg `&[&str]` shape — pass `Some(paths)`), `rule_kind_facts` (line 67), `skill_kind_facts` (line 50); call sites use `common::*`, each supplying its own provider/registration values."
        },
        {
          "path": "tests/emit.rs",
          "description": "Delete local `skill_member` (line 123), `rule_member` (line 109, already `Option`-shaped), `rule_kind_facts` (line 83), `skill_kind_facts` (line 96); call sites use `common::*`."
        },
        {
          "path": "tests/acceptance.rs",
          "description": "Delete local `skill_kind_facts` (line 137), `skill_surface_unit` (line 65, no-name 1-arg shape); call sites use `common::*`, passing `None` for the name param."
        },
        {
          "path": "tests/extract_equivalence.rs",
          "description": "Delete local `skill_surface_unit` (line 58, 2-arg name-carrying shape); call sites use `common::skill_surface_unit`, passing `Some(name)`."
        },
        {
          "path": "tests/memory_gate.rs",
          "description": "Delete local `findings_for` (line 100) and local `check_harness` (line 82, `Vec<String>` shape); call sites use `common::findings_for` and the shared `check_in` family, adapting to its return shape."
        },
        {
          "path": "tests/coverage_note.rs",
          "description": "Delete local `findings_for` (line 123) and local `check_harness` (line 104, `(Vec<String>, bool)` shape); call sites use `common::findings_for` and the shared `check_in` family."
        },
        {
          "path": "tests/gate_fail_loud.rs",
          "description": "Delete local `findings_for` (line 104) and local `requirement` (line 55, 1-arg shape — pass `false, None`); call sites use `common::*`."
        },
        {
          "path": "tests/coverage.rs",
          "description": "Delete local `author_rule_satisfies` (line 371) and local `requirement` (line 89, 2-arg `required`-carrying shape — pass `required, None`); call sites use `common::*`."
        },
        {
          "path": "tests/requirement_roster.rs",
          "description": "Delete local `author_rule_satisfies` (line 109) and local `requirement` (line 221, 2-arg `kind`-carrying shape — pass `false, Some(kind)`); call sites use `common::*`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/lock_declaration_rows.rs",
        "asserts": "lock-row fixtures built from skill_member/rule_member/rule_kind_facts/skill_kind_facts are unchanged after switching to the shared, parameterized common helpers"
      },
      {
        "path": "tests/requirement_roster.rs",
        "asserts": "author_rule_satisfies-driven and requirement()-driven roster scenarios are unchanged for every (required, kind) combination in use today"
      },
      {
        "path": "tests/coverage_note.rs",
        "asserts": "check_harness's success-bool-carrying scenarios are unchanged after folding into the shared check_in family"
      }
    ],
    "acceptance": "tests/common exports one surface_unit, skill_member, rule_member, rule_kind_facts, skill_kind_facts, skill_surface_unit, findings_for, author_rule_satisfies, requirement (each parameterized to its callers' full range), and check_harness folds into the existing check_in family; no duplicate or near-duplicate definition of any of these remains in the 11 edited test files; `cargo test` green.",
    "notes": "Found in job 4's residue sweep at ef72da3, second half of the pair filed with TEST-HELPER-DUPES-CONSOLIDATE(recordwriters). blockedBy it: shares lock_declaration_rows.rs/gate_fail_loud.rs/coverage.rs/requirement_roster.rs/tests/common/mod.rs with it. check_harness (memory_gate.rs:82, coverage_note.rs:104) was missed by TEST-FIXTURE-HELPERS-CONSOLIDATE's check_in scoping — folds in here rather than becoming a fifth near-duplicate. scoped at ef72da3"
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper`, single package, `(sdk-package-layout)`, now at 0.0.5 pending publish — SDK-VERSION-LOCKSTEP); channel 1's SDK foundation shipped. No `.github/workflows/release.yml` exists (only the `temper.yml` check job); root package.json is still the private flume manifest."
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (distribution.md)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, distribution.md). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (distribution.md 'Fail-loud invariant').",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm `@dtmd/temper`; ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. install.rs range-correctness shipped in SCAFFOLD-OUTPUT-VALID (^0.0.4); release owns only future re-pins."
  }
]
