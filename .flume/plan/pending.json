[
  {
    "tag": "TEMPER-TOML-ZERO",
    "summary": "Retire every remaining temper.toml reader — load_layer, AuthorLayer::load/authority/fold_local, the in-place [[member]] path, the consts — and sweep all refs to zero",
    "per": {
      "path": "specs/architecture/20-surface.md",
      "section": "The lock and drift — one vocabulary"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "Retire `AuthorLayer::load` (compose.rs:671)/`authority` (838)/`fold_local` (932), the in-place read path (`parse_inplace_member` 1449, `inplace_members` 847, `InPlaceMember` 120, the `[[member]]` array parse in `AuthorLayer::parse` 684), and the `layer_over` (881)/temper.toml plumbing left vestigial after GATE-CONTRACT-FROM-LOCK (the lock's `ClauseRow` now feeds `compose::effective`, which STAYS). The `AuthorLayer::parse`/`layer_over` unit tests (compose.rs:1620+) retire with the reader; the `ClauseRow`-driven `effective` tests (1818+) stay. Sweep the module's temper.toml doc references (18/322)."
        },
        {
          "path": "src/main.rs",
          "description": "Remove `TEMPER_TOML` (main.rs:50) + `TEMPER_LOCAL_TOML` (56) + every use: gate paths (269/274), Schema (305), explain load_layer (431), `load_layer` fn (587), gate (626), the session_start harness-join (615). Retiring `load_layer`/`AuthorLayer::load` cascades to the in-place consumers — `validate_inplace_kinds` (980), `inplace_unit` (1156), the explain `any_inplace` synthesis (470), the inplace arms (985/1068/1111) — all die with the reader. `authority_from_lock` (550) stays: it already reads the lock. Sweep remaining temper.toml doc comments."
        },
        {
          "path": "src/import.rs",
          "description": "Sweep the temper.toml doc reference (import.rs:6) to the lock vocabulary — the manifest filename is retired."
        },
        {
          "path": "src/bundle.rs",
          "description": "Rewrite the temper.toml mentions in bundle guidance strings (bundle.rs:109/134) to the lock/SDK vocabulary."
        },
        {
          "path": "src/roster.rs",
          "description": "Sweep the temper.toml doc comments (roster.rs:205/595) and retire the `AuthorLayer::parse`-over-`temper.toml` test helper (roster.rs:599) with the reader."
        },
        {
          "path": "src/read.rs",
          "description": "Rewrite the user-facing message (read.rs:1311) telling authors to declare `[requirement.<name>]` in temper.toml — requirements ride the lock/SDK program now, never a manifest."
        },
        {
          "path": "src/contract.rs",
          "description": "Sweep the temper.toml doc references (contract.rs:6/525) to the lock vocabulary."
        },
        {
          "path": "src/check.rs",
          "description": "Sweep the temper.toml doc references (check.rs:308/573)."
        },
        {
          "path": "src/graph.rs",
          "description": "Retire the temper.toml test-helper spellings (`AuthorLayer::parse` fragments, graph.rs:1022/1281) with the reader; sweep the doc references (graph.rs:1018/1277)."
        },
        {
          "path": "tests/requirement_roster.rs",
          "description": "Zero out every remaining temper.toml write/reference; a file by that name is inert (old temper). In-place `[[member]]` and any residual layer fixtures retire — assembly facts already ride golden locks."
        },
        {
          "path": "tests/acceptance.rs",
          "description": "Zero out remaining temper.toml references (post-migration residue)."
        },
        {
          "path": "tests/graph.rs",
          "description": "Zero out remaining temper.toml references."
        },
        {
          "path": "tests/reachable_gate.rs",
          "description": "Zero out remaining temper.toml references."
        },
        {
          "path": "tests/coverage.rs",
          "description": "Zero out remaining temper.toml references."
        },
        {
          "path": "tests/session_start.rs",
          "description": "Zero out remaining temper.toml references."
        },
        {
          "path": "tests/memory_gate.rs",
          "description": "Zero out remaining temper.toml references."
        },
        {
          "path": "tests/gate_fail_loud.rs",
          "description": "Zero out remaining temper.toml references."
        },
        {
          "path": "tests/install.rs",
          "description": "Zero out remaining temper.toml references."
        },
        {
          "path": "tests/cli.rs",
          "description": "Assert no verb reads or writes a temper.toml; a file by that name is inert (old temper, never current temper's input). Zero out remaining references."
        },
        {
          "path": "tests/reporters.rs",
          "description": "Zero out the remaining temper.toml reference."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/cli.rs",
        "asserts": "no verb reads or writes a temper.toml; a file by that name is inert (old temper, never current temper's input)"
      }
    ],
    "acceptance": "`rg -c 'temper\\.toml' src/ tests/` = 0; the manifest filename is retired entirely (20-surface: 'the name temper.toml retires with the manifest era entirely'). cargo test/clippy/fmt green.",
    "notes": "Chain terminal, now open (GATE-CONTRACT-FROM-LOCK shipped 08173bd: `compose::effective` reads lock `ClauseRow`, so the temper.toml layer feeds no contract). Pulls the reader + the in-place `[[member]]` path S5 kept, sweeps every doc/message/test ref to zero. Keep `effective`. Cascade reports (2)-(4) under `(inplace-lock-producer)` close here. 20 files (9 src + 11 test); schema.rs dropped — zero refs on disk."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, the engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/architecture/50-distribution.md",
      "section": "Three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper` 0.0.2, PR #4); channel 1's SDK foundation shipped"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (50-distribution)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, 50-distribution). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (50-distribution 'Fail-loud delivery').",
    "notes": "Release infra, mostly outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm (`@dtmd/temper`, published 0.0.2 07-06); ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. USPTO name screen due before launch."
  }
]
