[
  {
    "tag": "DISCOVERY-INFALLIBLE-RESULT-COLLAPSE",
    "summary": "The per-kind discovery scan unified onto the tolerant shared walk; `ImportError::ReadDir` is now unconstructable and six `discover_*` fns carry vacuous `Result` — collapse to bare collections",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/import.rs",
          "description": "The residue of the retired second discovery path. `read_entries` (the only `ImportError::ReadDir` constructor) was retired at 0bc0ee9 when the per-kind scan moved onto the shared walk's in-memory index; `ImportError::ReadDir` (variant at 142, in the enum at 138) is now unconstructable — grep finds no builder in src/ or tests/ — leaving `ImportError::Write` (153, built only at 660/668 by the roll-up writer) the sole live variant. Retire the `ReadDir` variant (this is a symbol removal within a surviving file, not a file retirement — the file stays, so it is an edit, not a `files.retire` entry). Then collapse the six vacuous-`Result` discovery fns to their bare collection: `discover_builtin` (206, `-> Result<Vec<PathBuf>, ImportError>`), `discover_nested_file` (252, `-> Result<Vec<NestedFileUnit>, ImportError>`), `declared_governed_paths` (304, `-> Result<BTreeSet<PathBuf>, ImportError>`), `discover_kind_files` (358), `discover_kind_units` (393), `scan_locus` (417) — none can `Err`: the shared walk swallows I/O via `walk.flatten()` (548, the settled tolerance posture since f419987) and `scan_locus`/`collect_glob` now read the in-memory index and touch no filesystem. Drop each internal `?`. `ImportError` (doc comment at 137, 'discovering or rolling up') is now Write-only — reword the doc to the writer alone, or rename the type to a writer-scoped error (a judgment call for the build; name it in the commit body). The inline `discover_builtin_*` tests (790+) drop their `.unwrap()`. Addresses re-verified at HEAD 1126033 (unmoved since 04b33de — the only commit in between, 1126033, touched `.flume/**` alone)."
        },
        {
          "path": "src/json_manifest.rs",
          "description": "The ripple past import.rs. The manifest error enum's `Discovery(#[from] crate::import::ImportError)` variant (316) becomes unconstructable once `discover_kind_files` (called at 431, currently `?`-ed) is infallible — json_manifest only reads members, never writes, so no `ImportError` reaches it. Drop the `?` at 431 and the now-dead `Discovery` variant (and its `#[from]`), or narrow the enum to whatever error the reader still raises. Re-verified at HEAD 1126033."
        },
        {
          "path": "src/main.rs",
          "description": "Two `?` sites drop when discovery is infallible: `discover_nested_file` (1353) and `discover_kind_files` (1372). After removing them, re-check whether main's error surface still names `ImportError` at all here; leave nothing wrapping a variant that can no longer flow. Re-verified at HEAD 1126033."
        },
        {
          "path": "src/install.rs",
          "description": "The `discover_builtin` call `?`-ed at 332 drops its `?`. Same re-check as main.rs — install never writes through this path, so no `ImportError` reaches it after the collapse. Re-verified at HEAD 1126033."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "the measure-first cost fixture still pins read_dir_count == 0 with walk_count/glob_compile_count unchanged — the collapse is signature-only, discovery behavior byte-identical"
      },
      {
        "path": "src/import.rs",
        "asserts": "the inline discover_builtin_* tests yield the same member sets with the infallible signatures (unwraps removed); no ImportError::ReadDir and no Result remain in the discovery read path"
      }
    ],
    "acceptance": "The discovery read path is infallible end to end — `discover_builtin`/`discover_nested_file`/`declared_governed_paths`/`discover_kind_files`/`discover_kind_units`/`scan_locus` return bare collections, `ImportError::ReadDir` is gone, and `ImportError` (or its writer-scoped rename) carries only the roll-up writer's failure. No behavior change (engineering.md, 'One job, one home': a retired second implementation's residue is subtracted, not carried). `cargo test` and `cargo clippy -D warnings` green.",
    "notes": "Drained from .flume/refactor/build-discovery-infallible-result-plumbing.md (observed 8b06146, cut 0bc0ee9). Pure subtraction, no 'Loud or nothing' fork — flatten() tolerance settled pre-SCAN (f419987), discovery was already infallible. Refile of 648fba5, reverted: its `files.retire` named a symbol string, not a path, tripping the fence gate. Fixed: ReadDir removal is now prose inside the import.rs edit, retire: []. scoped at 1126033."
  },
  {
    "tag": "FRONTMATTER-TEST-SYNTHETIC-KINDS",
    "summary": "frontmatter.rs's test fixtures pull real skill/rule Kind values via builtin_kind::definition — a provider dependency in a format adapter's tests; swap to synthetic test_support kinds (0040)",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Invariants — stated as absences"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/frontmatter.rs",
          "description": "Test module (466-478) imports `crate::builtin_kind` and its `skill_kind()`/`rule_kind()` helpers call `builtin_kind::definition(\"skill\"/\"rule\")` for real CustomKind values — the test-only `frontmatter → builtin_kind` edge architecture.md declares (0040, decision point 2). Swap both helpers to call a new test_support kind-builder instead, dropping the `crate::builtin_kind` import. The synthetic kinds must reproduce the exact shape the 8 tests below rely on: `skill_kind()` — `Format::YamlFrontmatter`, `UnitShape::Directory`, declared fields in order `name`, `description`, `license` (mirrors `builtin_kind::skill_extraction`, builtin_kind.rs:25-41's first three `Primitive::Field`s — the fixture's `allowed-tools`/`priority` are deliberately undeclared, asserted as sorted-after-declared unknown keys, so the builder must NOT declare them); `rule_kind()` — `Format::YamlFrontmatter`, `UnitShape::File`, one declared field `paths` (mirrors `builtin_kind::claude_code_rule`, builtin_kind.rs:187-190)."
        },
        {
          "path": "src/test_support.rs",
          "description": "Add the synthetic kind-builder decision 0040 names (currently one fn, `tmpdir` at 7) — a `pub(crate)` constructor(s) building a minimal `CustomKind` via `CustomKind::new(name, governs, extraction)` (kind.rs:747) for a directory-shaped and a file-shaped frontmatter fixture, parametrized by declared field names in order plus unit_shape, sufficient for frontmatter.rs's `skill_kind()`/`rule_kind()` to stop importing `builtin_kind`. Real governs/format details are test-arbitrary (`Member::from_source` takes an explicit path, never discovery), so only the extraction/unit_shape/format axes need to match production."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/frontmatter.rs",
        "asserts": "all 8 existing inline tests (directory_shape_ids_..., file_shape_ids_..., companions_scan_..., a_non_mapping_frontmatter_..., a_malformed_yaml_frontmatter_..., an_empty_frontmatter_..., a_paths_rule_lifts_...) pass unchanged against the synthetic kinds — same field order, same id derivation, same error behavior"
      }
    ],
    "acceptance": "src/frontmatter.rs's test module carries no `crate::builtin_kind` import; its fixtures build kinds through a `test_support` kind-builder instead. `cargo test` and `cargo clippy -D warnings` green.",
    "notes": "Decision 0040, point 2. Disjoint from every other queue entry's files (frontmatter.rs, test_support.rs touched nowhere else this tick). scoped at f404e48."
  },
  {
    "tag": "DRIFT-LOCK-ROW-WALK-CONSOLIDATION",
    "summary": "src/drift.rs walks the committed lock's declaration rows three separate times (read_prior_provenance, config_stale, emit_owned_targets) — consolidate to one shared raw-row walk",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "One job, one home"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "DISCOVERY-INFALLIBLE-RESULT-COLLAPSE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "Three fns duplicate the same 'walk the committed lock's declaration rows' job, each independently opening `lock.toml`, tolerant-parsing it as `DocumentMut`, ranging over every top-level `(kind, item)` pair, filtering to `item.as_array_of_tables()`, and pulling columns via `row.get(..).and_then(Item::as_str)`: `read_prior_provenance` (fn 1918, loop 1928-1946, requires name+source_path+emit_hash, returns `Vec<ProvenanceRow>`), `config_stale` (fn 2342, loop 2355-2384, same three columns, inlines the staleness diagnostic instead of returning rows), `emit_owned_targets` (fn 2614, loop 2624-2641, only name+source_path, returns `Vec<EmitOwnedEntry>`). Their doc comments already cross-reference each other's tolerant-absence behavior (1916-1917, 2611-2612) — the duplication is self-aware in the code but never consolidated. Add a private `fn walk_lock_rows(workspace_dir: &Path) -> Vec<RawLockRow>` (raw, all-`Option` columns: kind, name, source_path, emit_hash) that opens and parses the lock once and does the `(kind, item)`/`as_array_of_tables` walk once; have each of the three public fns `filter_map` over its rows instead of re-deriving the walk — `config_stale`'s and `emit_owned_targets`' looser column requirements fall out of a narrower filter each. Same-file fn-level unification — the home stays `drift.rs`, this is not a new module. Consumers (`src/install.rs`, `src/main.rs`, `tests/install.rs`) call only the three public fns — no signature change, no ripple. Re-verified at HEAD 7132213 (unmoved since b8fc7ca, when observed)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "the config_stale(...).is_empty() assertions (536, 607) stay green — the shared walk's config_stale filter/map preserves the same tolerant-absence behavior"
      },
      {
        "path": "tests/emit.rs",
        "asserts": "re_emitting_after_a_member_is_removed_leaves_a_hand_edited_projection_and_reports_drift and the_emit_report_distinguishes_reaped_from_drifted_orphan stay green — these exercise read_prior_provenance/emit_owned_targets through the reap path with byte-identical output"
      }
    ],
    "acceptance": "read_prior_provenance, config_stale, and emit_owned_targets all filter/map over one shared walk_lock_rows helper; no behavior change (engineering.md, 'One job, one home': a second implementation of one job is residue, subtracted rather than carried). `cargo test` and `cargo clippy -D warnings` green.",
    "notes": "Drained from .flume/refactor/plan-drift-lock-row-walk-triplication.md (observed b8fc7ca). Gate changed open -> blockedBy DISCOVERY-INFALLIBLE-RESULT-COLLAPSE: no direct file overlap, but PLACEMENT-MODULE-EXTRACTION/EXTRACT-FOUNDATION-BOUNDARY-RESTORE (0040) touch drift.rs downstream and share files with DISCOVERY too — one chain keeps every same-file pair from racing open (pending-entry.md). Re-verified unmoved at f404e48. scoped at f404e48."
  },
  {
    "tag": "DRIFT-EMIT-LOCK-PARSE-HOIST",
    "summary": "emit() parses lock.toml twice per run — read_prior_provenance (reap-diff) and read_declarations (layer-drop check) each independently open+parse it; hoist to one shared parse",
    "per": {
      "path": "specs/process/engineering.md",
      "section": "Cost scale is hoisted, and pinned by count"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "DRIFT-LOCK-ROW-WALK-CONSOLIDATION"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/drift.rs",
          "description": "`emit` (fn 901) reads and parses the workspace's `lock.toml` twice in one invocation: the reap-diff loop (1197) calls `read_prior_provenance` (fn 1918, opens+parses the lock at 1919-1925 fresh) and the layer-drop check (1244-1247) calls `read_declarations` (fn 3398, opens+parses the lock again at 3399-3407 via `parse_declarations` -> the private `declarations_from_doc` at 3439, which already takes a parsed `&DocumentMut`). Two independent reads+parses of the same file in one `emit()` run — the whole-input work the cost doctrine hoists once per run and shares, never recomputed per call site. Land after DRIFT-LOCK-ROW-WALK-CONSOLIDATION: that entry's `walk_lock_rows` establishes the doc-based-extractor shape this entry's provenance-row side should follow rather than re-deriving. Fix: parse the lock document once inside `emit()`, then derive the reap-diff provenance rows and the layer-drop nested-member counts from that one parsed `DocumentMut` — `declarations_from_doc` already accepts one; give the provenance side an equivalent doc-scoped extractor. Re-verified at HEAD c2eca2d (drift.rs unchanged since b8fc7ca)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/check_cost.rs",
        "asserts": "an emit run over a fixture whose lock.toml exercises both the reap-diff and layer-drop-check paths reads/parses lock.toml exactly once — a count pin, the same doctrine this file's discovery-walk/glob-compile pins already established, never a wall-clock threshold"
      }
    ],
    "acceptance": "emit() reads and parses the workspace's lock.toml exactly once per invocation; the reap-diff provenance rows and the layer-drop nested-member count both derive from that one parse. No behavior change (engineering.md, 'Cost scale is hoisted': whole-input work computes once per run and is shared, never recomputed per call site). `cargo test` and `cargo clippy -D warnings` green.",
    "notes": "Found sweeping `drift` (rotation's first subsystem). Distinct from DRIFT-LOCK-ROW-WALK-CONSOLIDATION: that entry unifies three check/install-time readers of provenance columns; this is emit()'s own double-parse of the whole lock document (provenance rows vs the `[declaration]` sub-table) — different row families, same file, same run. Serialized after it so this entry's doc-based provenance extractor sits beside `walk_lock_rows`, not duplicating its shape. scoped at c2eca2d."
  },
  {
    "tag": "PLACEMENT-MODULE-EXTRACTION",
    "summary": "0040's drift→install edge dissolves — the managed-metadata marker vocabulary moves from install.rs to a new flat `placement` pipeline module; drift and install both import it downward",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Invariants — stated as absences"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "DRIFT-EMIT-LOCK-PARSE-HOIST"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": "src/placement.rs",
          "description": "New flat pipeline-subsystem module (0040, decision point 1; architecture.md's pipeline codemap gains `placement`). Carries the recognizer vocabulary presently in src/install.rs: the three marker constants `MODELINE_MARKER` (156), `NOTE_MARKER` (145), `BANNER_MARKER` (151), plus `is_placement_comment` (1687-1690) and the pub(crate) `placement_lines` (1664-1682) — moved verbatim, `pub(crate)` preserved on `placement_lines` since `drift::emit_one` calls it across the module boundary. `GUARD_MARKER`/`GUARD_MATCHER`/`GUARD_COMMAND`/`GUARD_PATH_MATCH`/`NOTE_COMMENT`/`NOTE_BANNER` stay in install.rs — they are the guard's own detection constants and the placing wording, not the managed-metadata recognizer vocabulary this module owns. The inline test `placement_lines_round_trips_the_body_banner_of_a_frontmatterless_source` (install.rs:1892-1899) moves here with it."
        }
      ],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "Remove the three moved constants (145,151,156), `placement_lines` (1664-1682), and `is_placement_comment` (1687-1690) plus their moved test — replace with `use crate::placement::{MODELINE_MARKER, NOTE_MARKER, BANNER_MARKER};` (or `crate::placement::`-qualified calls). `project_modeline` (1574), `project_note` (1604), and `project_banner` (1632) keep their placing logic and wording (`NOTE_COMMENT` 161, `NOTE_BANNER` 169) but read the three markers off the new module — install imports the vocabulary downward, exactly as drift's emit does (0040)."
        },
        {
          "path": "src/drift.rs",
          "description": "`emit_one`'s placement read (2034, `install::placement_lines`) becomes `crate::placement::placement_lines`; the `use crate::install;` import (30) — drift.rs's only other reference to `install` is the prose-only doc pointer at 2603 (`install::matches_projection`, unrelated to the moved vocabulary; `matches_projection` stays in install, used by the guard) — drops once the real call moves, dissolving the `drift → install` edge architecture.md declares (0040). Leave the 2603 doc pointer as prose (no code dependency), rewording only if it reads stale once the import is gone; say which in the commit body."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/placement.rs",
        "asserts": "the moved placement_lines/is_placement_comment tests (round-trips a frontmatterless banner, empty on an unmarked body) pass unchanged from their new home"
      },
      {
        "path": "tests/install.rs",
        "asserts": "existing modeline/note/banner placement + config_stale/gate_installed coverage stays green — the move is location-only, no behavior change"
      }
    ],
    "acceptance": "`src/drift.rs` carries no `crate::install` import; the managed-metadata marker vocabulary lives in a new flat `src/placement.rs` (pipeline subsystem, architecture.md's codemap gains it); install imports the vocabulary downward for its own placing functions. `cargo test` and `cargo clippy -D warnings` green.",
    "notes": "Decision 0040, point 1. Serialized behind DRIFT-EMIT-LOCK-PARSE-HOIST (behind DRIFT-LOCK-ROW-WALK-CONSOLIDATION, behind DISCOVERY-INFALLIBLE-RESULT-COLLAPSE) for shared-file safety (pending-entry.md, 'Disjoint, or serialized'): this and EXTRACT-FOUNDATION-BOUNDARY-RESTORE both touch drift.rs, and DISCOVERY touches install.rs (here) and json_manifest.rs/main.rs (next) — one chain avoids two open entries racing a file. No functional dependency among the four. scoped at f404e48."
  },
  {
    "tag": "EXTRACT-FOUNDATION-BOUNDARY-RESTORE",
    "summary": "extract.rs's upward imports into drift/kind dissolve — lock-row lifters move to drift.rs, the manifest-collection grammar moves to json_manifest.rs; extract returns foundation-clean (0040)",
    "per": {
      "path": "specs/process/architecture.md",
      "section": "Invariants — stated as absences"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "PLACEMENT-MODULE-EXTRACTION"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/extract.rs",
          "description": "Move `nested_members_from_rows` (764-772) and `embedded_member_from_row` (778-798, takes `crate::drift::NestedMemberRow`) down to `src/drift.rs`, which defines the row type. Move the manifest-collection grammar's both faces — `manifest_members` (1015-1040, branches on `crate::kind::CollectionKeyPath` at 1022/1032), `entry_fields` (1048-1056), `enablement_member_fields` (1075-1080, reads `crate::kind::ENABLEMENT_FIELD`), `enablement_entry_value` (1092-1098), `hook_member_fields` (1107-1135), `hook_matcher_group` (1144-1163) — down to `src/json_manifest.rs`, which already imports `crate::extract` for this exact grammar (json_manifest.rs:33,386,398) and owns the format's read+write faces the same way `frontmatter.rs` owns its own (0040, decision point 3, 'a format owns its grammar's read and write faces'). Their inline tests (`nested_members_from_rows_matches_only_the_named_host_address`/`_lifts_leaves_and_one_layer_of_collections` ~1525-1541; `manifest_members_walk_each_entry_raw_and_skip_a_non_object`, `enablement_members_carry_the_scalar_value_and_re_render_identically`, `an_enablement_member_with_no_enabled_field_renders_the_documented_true`, `hook_members_decompose_the_matcher_group_array_and_re_nest_identically` ~1362-1450) move with their functions. After both moves extract.rs imports no sibling module — `use std::...`/`serde_json` only — restoring the module header's own longstanding, narrower claim (22-23, 'no dependency on crate::contract') as the full foundation invariant architecture.md states."
        },
        {
          "path": "src/drift.rs",
          "description": "Gains `nested_members_from_rows`/`embedded_member_from_row` from extract.rs, `pub(crate)` (matches today's visibility — `builtin_kind::features` at builtin_kind.rs:578 calls it across the module boundary as `crate::drift::nested_members_from_rows` once moved)."
        },
        {
          "path": "src/json_manifest.rs",
          "description": "Gains `manifest_members`/`entry_fields`/`enablement_member_fields`/`enablement_entry_value`/`hook_member_fields`/`hook_matcher_group` from extract.rs. Per the reconciliation decision 0040 names for the (now-superseded) EXTRACT-PRIVATE-COLLECTION-DECODERS entry: `manifest_members`, `entry_fields`, `hook_member_fields`, `enablement_member_fields` go fully private `fn` (called only in-module, by `manifest_members` at its own call site 386 and by each other) — none has a caller outside this file once the move lands; `enablement_entry_value`/`hook_matcher_group` stay `pub(crate)` since `crate::drift` calls them across the module boundary (drift.rs:1000,1014 — update the qualification from `crate::extract::` to `crate::json_manifest::`). Drop the now-local `extract::` qualification at the call site (386); the `crate::extract::{self, FeatureValue}` import (33) still needs `extract::json_to_feature` (398), so it narrows to `FeatureValue` plus a bare `extract` import for that one call — verify before narrowing."
        },
        {
          "path": "src/builtin_kind.rs",
          "description": "`features` (578, doc comment 561) calls `extract::nested_members_from_rows` — requalify to `crate::drift::nested_members_from_rows` (or `drift::` if already imported) once the fn moves; reword the doc pointer at 561 to match."
        },
        {
          "path": "src/kind.rs",
          "description": "`ENABLEMENT_FIELD`'s doc comment (214, '`crate::extract::manifest_members`') points at the pre-move location — reword to `crate::json_manifest::manifest_members`."
        },
        {
          "path": "src/main.rs",
          "description": "`nested_member_admissibility`'s doc comment (2068, '`extract::nested_members_from_rows`') points at the pre-move location — reword to name `drift::nested_members_from_rows`."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "src/drift.rs",
        "asserts": "the moved nested_members_from_rows tests (host-address matching, one-layer collection lift) pass unchanged from their new home"
      },
      {
        "path": "src/json_manifest.rs",
        "asserts": "the moved manifest-grammar tests (raw entry walk skipping non-objects, enablement scalar round-trip, no-enabled-field defaulting true, hook matcher-group decompose/re-nest) pass unchanged from their new home"
      },
      {
        "path": "tests/gauntlet.rs",
        "asserts": "the composed-corpus check/emit snapshot stays byte-identical — the moves are signature/location-only, no behavior change to any manifest or nested-member read"
      }
    ],
    "acceptance": "extract.rs imports no crate-internal sibling (foundation-clean); the lock-row lifters live in drift.rs and the manifest-collection grammar (both read and write faces) lives in json_manifest.rs, with manifest_members/entry_fields/hook_member_fields/enablement_member_fields private and enablement_entry_value/hook_matcher_group pub(crate) for drift — reconciling the narrower EXTRACT-PRIVATE-COLLECTION-DECODERS ask at the functions' new homes rather than at extract (0040, Consequences). architecture.md's foundation invariant holds without qualification. `cargo test` and `cargo clippy -D warnings` green.",
    "notes": "Decision 0040, point 3 — supersedes/retires EXTRACT-PRIVATE-COLLECTION-DECODERS: its enablement_member_fields/hook_member_fields private-fn ask folds into this entry's acceptance at the functions' new home, json_manifest.rs, per 0040's Consequences naming that reconciliation. Serialized behind PLACEMENT-MODULE-EXTRACTION for shared drift.rs edits (pending-entry.md) — also shares json_manifest.rs/main.rs with DISCOVERY-INFALLIBLE-RESULT-COLLAPSE, upstream in the chain. scoped at f404e48."
  },
  {
    "tag": "IMPORT-HOP-CAP-CITE",
    "summary": "`MAX_IMPORT_HOPS = 5` cites a memory doc that now reads four hops: the gate constant and its own source disagree, and the doc sentence cannot settle which counting is temper's",
    "per": {
      "path": "specs/process/spec-system.md",
      "section": "Form rules"
    },
    "gate": {
      "kind": "parked",
      "reason": "A human probe rules the hop semantics — the doc text cannot. Fetched live at 420da04 (code.claude.com/docs/en/memory, retrieved 2026-07-16): 'Imported files can recursively import other files, with a maximum depth of four hops.' Both readings survive that sentence: the root's own import is hop 1 and the chain caps at four edges (⇒ 4), or 'imported files ... recursively import' names a recursion four deep ATOP that first import (⇒ 5, today's constant). Only measuring the runtime's actual load depth decides it, the headless-testbed act that produced the `(mention-gate-containment)` evidence — never plan's to guess and never build's to fetch, because guessing here edits a live gate's semantics: a cap too low forges a dead-member finding on a member that loads, a cap too high suppresses a real one (intent.md invariant 2)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/graph.rs",
          "description": "The constant and the cite that justifies it. `MAX_IMPORT_HOPS = 5` (59) sits under a doc (55-58) asserting 'the `at-import` grammar is recursion-capped at five hops (code.claude.com/docs/en/memory, retrieved 2026-07-02)' — a number the live page does not say. temper's own indexing IS decidable on disk and needs no ruling: `live_members` (630) seeds every own-live member and runs `for _ in 0..MAX_IMPORT_HOPS` BFS rounds (657), one import edge per round, so the constant counts EDGES traversed from a live seed. What no reading of the disk settles is which edges the doc's 'four hops' counts — hence the park. Whatever the ruling: the cite is re-retrieved, re-dated, and re-worded to state the hop-counting it encodes, never left asserting a bare number its source does not carry. The two narration cites deferring to it — 528 ('hop-capped at [`MAX_IMPORT_HOPS`]') and 626-629 ('capped at [`MAX_IMPORT_HOPS`] hops (the `at-import` recursion depth the format documents)') — carry the same claim and move with it, as does the propagation comment at 652-655 ('capped at the format's import recursion depth'). 6667265 (mention-reachable, in the b85df4a..HEAD window) shifted every address after ~519 — the const (59) and its doc (55-58) sit above the edit and are unmoved; the later addresses re-read at HEAD, so every address carries."
        },
        {
          "path": "tests/graph.rs",
          "description": "`the_import_closure_is_hop_capped` (1407) already pins the boundary mechanically rather than in a comment — `r1..r5` rescued from a live seed, `r6` dead as the sole finding — so it encodes today's 5 in the one place a flip cannot silently miss, and it is the test that moves if the ruling is four. Its comment (1408-1412) spells 'capped at the format's five-hop import recursion' (1410) and carries the same claim as the constant's cite; re-word it to the ruled reading rather than leaving the fixture narrating the retired number. 6667265 (mention-reachable, in the b85df4a..HEAD window) added the mention-reachable fixture and shifted this test down; addresses re-read at HEAD, so they carry."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/graph.rs",
        "asserts": "the import closure rescues a chain exactly as deep as the ruled cap and no deeper — the boundary hop pinned at the ruled number, in the ruled counting"
      }
    ],
    "acceptance": "`MAX_IMPORT_HOPS`, its doc cite, and the two narration cites that defer to it all agree with the live docs, and each states which hop-counting it encodes (spec-system.md, 'Form rules': an external fact carries its source and retrieval date where it is enforced, verified against current docs, never encoded from memory). The boundary hop stays pinned by a test at the ruled number. `cargo test` green.",
    "notes": "Park re-tested at d1af9a5 and holds — nothing ruled the hop semantics; VERIFIER-TYPED's window touched graph.rs but not the const region (still 5 at graph.rs:59, cite still 2026-07-02 at 55-58). The park's subject is untouched, and may still be wrong. A build-side friction capture records the same disagreement; the human's to read, not plan's to drain. rescoped 4a33911."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Release infra outside src/; cargo gates can't validate npm/bundle publish. Park re-tested on disk at d1af9a5, holding on every clause: no v0.1 tag (only era tags), crate 0.1.0, release.yml still states the darwin + channel-3 deferral verbatim. `git diff 68fae5c..HEAD -- .github/` is empty. scoped b676656."
  }
]
