[
  {
    "tag": "COMMENT-STOCK-SWEEP",
    "summary": "Whole-tree comment sweep — prose spec recaps to pointer tags, delete era + compliance narration, retired-tree citations, and dangling pending-tag refs per rust.md's taxonomy; no behavior change",
    "per": {
      "path": ".claude/rules/rust.md",
      "section": "Style & structure"
    },
    "gate": {
      "kind": "deferred",
      "reason": "Whole-tree sweep — must run solo (a broad src/+tests/ comment edit races any concurrent entry). Promoted to a solo open entry once CHECK-LOCK-KIND-ROWS ships and the queue is otherwise empty; promotion re-greps to finalize the exact residue set (including tests/)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/builtin_kind.rs",
          "description": "The `kinds/<name>/KIND.md` curated-tree citations (`//!` :5-7, :26, :60, :89) and the \"any more\" era narration: drop the citations (the tree is slated for physical deletion, a human chore), convert prose spec recaps to terse pointer tags (file path only), delete era narration. Module `//!` header to ~10 lines."
        },
        {
          "path": "src/builtin.rs",
          "description": "Era narration the projection left in its own header: `//!` :10-11 (\"There is no package noun any more\") and :95 (\"the noun retired with the hand-written mirror\") — commit-body material, cut per rust.md's exit clause. Header to ~10 lines."
        },
        {
          "path": "src/bundle.rs",
          "description": "The `packages/` curated-tree reference (:79) — cite the clause `cite` field / spec path only, drop the retired-tree pointer."
        },
        {
          "path": "src/read.rs",
          "description": "Retired `[edge.<target>]`/`[edge.*]` document-clause references and the dangling `READ-EDGE-UNIFY` pending tag (`//!` :23, :65, and :391): the `[edge.*]` construct shipped/retired and the tag is not in the queue — delete both per rust.md's exit clause (dangling pending-tag reference + retired-noun comment)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "Zero quoted spec section titles in src/ or tests/ comments; zero era vocabulary from 90-spec-system's migration trail; no comment referencing a pending tag that isn't in the queue; no citation of the retired `kinds/`/`packages/` curated trees or `[edge.*]` document clauses; module `//!` headers ~10 lines; cargo gates green — no behavior change.",
    "notes": "Ceremony item (John 07-06). rust.md's exit clause cuts comments on contact for chain-touched files (builtin.rs by the projection; main.rs's KIND.md comments by CHECK-LOCK-KIND-ROWS); this sweep is the residue in files the chain leaves untouched. Deferred, not blockedBy: a whole-tree edit races every open entry — promoted to solo open once CHECK-LOCK-KIND-ROWS ships and the queue is otherwise empty; promotion re-greps the full residue set (incl. tests/ + any main.rs remainder)."
  },
  {
    "tag": "PACKAGING-CHANNELS",
    "summary": "Ship the three acquisition channels — SDK npm package, engine binary pinned via per-platform optionalDependencies + standalone assets, the plugin/bundle — fail-loud on a missing platform binary",
    "per": {
      "path": "specs/architecture/50-distribution.md",
      "section": "Three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "needs the per-platform engine-binary release workflow build-out + John's decide-at-release calls (optional Apple notarization for the standalone mac binary, USPTO name screen) before the mechanical wrappers validate end-to-end. No marketplace/signing creds exist — a marketplace is a git repo + marketplace.json, community submission a free web form gated by `claude plugin validate` (verified 2026-07-06, code.claude.com/docs); npm creds done 07-05. SDK scope resolved (`@dtmd/temper` 0.0.3, single package, `(sdk-package-layout)`); channel 1's SDK foundation shipped"
    },
    "dependsOnForks": [],
    "files": {
      "new": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Build the per-target release binaries and publish them to the npm platform packages (channel 2: the engine binary pinned by the SDK via per-platform `optionalDependencies`, plus standalone GitHub release assets), and produce/publish the plugin bundle with its `marketplace.json` (channel 3). Channel auto-detected. Distinct from any CI *check* job, which is a documented two-line user-authored job (`temper check` + `emit --frozen` byte-compare), not a generated file (50-distribution)."
        }
      ],
      "edit": [
        {
          "path": "package.json",
          "description": "The root `package.json` today is the *private* flume control-plane manifest (`temper-flume-harness`, `private: true`). The npm distribution entry needs platform-specific `optionalDependencies` pointing at per-target prebuilt-binary packages plus a launcher that resolves and execs the platform binary, erroring loudly if none is installed (fail-loud, 50-distribution). Reconcile with the flume manifest rather than clobbering it. NB distinct from sdk/package.json (`@dtmd/temper`, already published — the engine pin is declared there, once)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper via the SDK's pinned engine binary (per-platform optionalDependencies), standalone release assets, or the plugin/bundle (marketplace.json); a missing platform binary is an install error, never a silent skip (50-distribution 'Fail-loud delivery').",
    "notes": "Release infra, mostly outside src/; cargo gates can't validate npm/bundle publish; needs human creds. ch1 = SDK npm (`@dtmd/temper`, published 0.0.3); ch2 = engine binary via per-platform optionalDependencies + standalone; ch3 = plugin/bundle via `temper bundle` + marketplace.json. Parked on human release creds + the engine-binary workflow. USPTO name screen due before launch."
  }
]
