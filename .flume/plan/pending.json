[
  {
    "tag": "CHECK-WORKSPACE-DIR-RESOLVE",
    "summary": "`temper check <dir>` where `<dir>` carries `lock.toml` directly resolves whole — harness root is the parent — instead of half-gating the workspace as its own rootless corpus",
    "per": {
      "path": "specs/intent.md",
      "section": "Invariants"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "`harness_diagnostics` (645-652) resolves two cases today: `<path>/.temper` is a dir ⇒ `gate(authored, path)`, else `gate(path, path)`. The else branch is meant for an unrepresented raw root and swallows a workspace dir: `check .temper` loads the lock from `.temper/lock.toml` but walks `.temper` as the harness root, so declarations arrive with no corpus behind them (required requirements false-fire `requirement.unfilled`; coverage collapses to whatever module-adjacent prose matches a governs glob). Add the third case — `<path>/lock.toml` is a file ⇒ the path IS the workspace, so `gate(path, path.parent())` — and reconcile the `Command::Check` doc comment (88-105) plus the one-resolution comment (~246) with the three-way resolution."
        },
        {
          "path": "src/bundle.rs",
          "description": "Line 89 — the shipped plugin skill's prose teaches `temper check .temper` as the gate. Retarget to `temper check .` (the harness root), the spelling 208e8f8 flipped every authored surface to and `SESSION_START_COMMAND` (line 61) already carries."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/gate_fail_loud.rs",
        "asserts": "`check <workspace-dir>` (the dir carrying lock.toml) resolves the parent's corpus — members discovered, no false `requirement.unfilled` — the mirror of `an_adopted_root_resolves_its_own_lock_rather_than_half_gating` (41)"
      }
    ],
    "acceptance": "`temper check .temper` reports the same member count and requirement verdict as `temper check .` (10 members, no false `requirement.unfilled`); no path loads the lock from one place and discovers a corpus from another.",
    "notes": "Probe at f4e3c13: `check .temper` → 1 member + 2 false `requirement.unfilled`, exit 0; `check .` → 10 members, neither fires. Mirror of the pre-4256274 false-green. Rejected: refuse loudly naming the harness root — equally loud-or-nothing-clean, but John ruled resolve-whole (inbox, 07-16) and the dir carries lock.toml at its top, so there is nothing ambiguous to refuse over. scoped at f4e3c13."
  },
  {
    "tag": "INSTALL-REPRESENTED-SKIPS-THE-QUESTION",
    "summary": "`install` on a root already carrying `.temper/lock.toml` skips the represent question and converges on the lock, never defaulting non-interactive to session-start-only",
    "per": {
      "path": "specs/model/pipeline.md",
      "section": "Install"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "CHECK-WORKSPACE-DIR-RESOLVE"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/main.rs",
          "description": "The represent decision (396-410): `yes` ⇒ Yes, `no_represent` ⇒ No, else `ask_represent()` (618-630, which defaults an unattended terminal to No). A root whose `.temper/lock.toml` exists has answered the fork on disk, so neither the prompt nor its conservative default applies: detect it and take the represented path, placements following the lock's current contents. `--no-represent` against such a root asserts the false half of a settled fork — refuse as a usage error naming the lock, never silently place less than the lock justifies."
        },
        {
          "path": "src/install.rs",
          "description": "`REPRESENT_QUESTION` and its doc (177-178) carry the one-genuine-fork rule; state there that the question is asked only where the fork is live (no lock on disk). `render_discovery` — the report printed before the question — names the already-represented root, so a skipped question is visible rather than invisible state."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "a represented root (lock present), neither flag, no tty: takes the represented path — the lock's placements, no prompt; `--no-represent` against a represented root refuses loud"
      }
    ],
    "acceptance": "On this repo, non-interactive `temper install --dry-run` reports the 3 managed-by notes `check .` demands as `install.gate-installed` — the probe and the on-ramp converge — and install asks no question the lock has already answered.",
    "notes": "Probe at f4e3c13: `install . --dry-run` here prints the question, defaults No, and manages the session-start hook alone; `--yes --dry-run` scaffolds 0 members and places the 3 notes. So the yes-path already converges — the gap is that a settled fork is re-asked, then answered wrong when unattended. Serialized behind CHECK-WORKSPACE-DIR-RESOLVE: both edit src/main.rs. scoped at f4e3c13."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "The packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing), the plugin/bundle + marketplace.json, and the binary↔npm version lockstep at the v0.1 tag",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package; the v0.1 launch tag that flips the binary's self-reported crate version (0.1.0) into lockstep with npm's 0.0.x. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) + the SDK launcher/optionalDependencies + standalone tag assets shipped 07-11; the standalone-asset step is wired but unexercised until a tag fires."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json); the binary self-reports 0.1.0 in lockstep with the v0.1 npm cut (distribution.md: prebuilt binaries install on all three OSes; lockstep engine pin).",
    "notes": "Supersedes retired PACKAGING-CHANNELS (John, 07-11): both its file claims were dead — release.yml now exists (its `new` claim fails the refs gate) and the root-package.json launcher was ruled out (the launcher + optionalDependencies live on the SDK, distribution.md 'What ships'). Release infra outside src/; cargo gates can't validate npm/bundle publish. scoped at b676656."
  }
]
