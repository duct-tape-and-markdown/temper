[
  {
    "tag": "FRONTMATTER-LOAD-FAULT-AS-DIAGNOSTIC",
    "summary": "A member whose frontmatter fails to load (no name, malformed YAML) must become a collected error Diagnostic naming the file, never a thrown Err that aborts the whole check run.",
    "per": {
      "path": "specs/intent.md",
      "section": "Invariants"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/compose.rs",
          "description": "read_file_unit's YamlFrontmatter/None-format arm (502-530) calls frontmatter::Member::from_source_rooted with `?`, and resolve_kind_units's two file-discovery loops (606-654) call read_file_unit with `?` in turn — a FrontmatterError::{NoId,NoNamedFieldId,Malformed} on any one file aborts the whole kind's resolution via miette::Result. Catch those three variants at this boundary and collect an error-severity Diagnostic (naming the file and the fault) instead, continuing the loop over the remaining files; thread the diagnostics out (e.g. resolve_kind_units/kind_units_and_features return them alongside the units). Leave read_file_unit's other error paths (layout_unit, json_manifest, toml_document, and frontmatter's own Io/NotUtf8) on their existing hard-fail `?` — outside the verified gap (the probe's crashes were specifically frontmatter::{no_named_field_id,malformed})."
        },
        {
          "path": "src/gate.rs",
          "description": "kind_units_and_features is called from gate's builtin-kind loop (~140-159) and custom-kind loop (~224-233); once compose.rs's chain returns collected load-fault diagnostics alongside a kind's units, merge them into the run's `diagnostics` vec at both call sites."
        },
        {
          "path": "tests/gate_fail_loud.rs",
          "description": "a_malformed_frontmatter_block_fails_loud_naming_the_file (136-175) currently asserts the run aborts with zero github-format findings emitted (comment: 'aborts loud; it must not emit field findings') — that is the behavior this entry reverses. Rewrite: the run still fails (!run.ok), output still names the file and 'mapping', but `run.findings()` now carries a real finding naming the file rather than being empty. Add a second case: a harness with the malformed member plus one clean, unrelated member — the clean member must still be discovered and checked in the same run (the actual 4/9-harness crash the probe caught)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/gate_fail_loud.rs",
        "asserts": "a malformed or nameless member's load fault is a collected error Diagnostic naming the file, and every other member in the harness is still discovered and checked in the same run"
      }
    ],
    "acceptance": "check over a harness with one malformed/nameless member plus N clean ones exits non-zero, reports a finding naming the bad file, and still validates the N clean members instead of aborting the whole run.",
    "notes": "Verified on disk: compose.rs 502-530/606-654 propagate FrontmatterError via `?`, matching the probe's 4/9-harness crash. gate_fail_loud.rs:136 currently encodes today's abort-with-no-findings as deliberate Invariant-6 compliance; Invariant 6 is 'loud', not 'abort-first-file' — an error Diagnostic is still loud and matches rust.md's own Diagnostic-not-Err rule. scoped at 9d4ce81.",
    "observedFiles": [
      "src/compose.rs",
      "src/gate.rs",
      "tests/gate_fail_loud.rs"
    ]
  },
  {
    "tag": "GATE-INSTALLED-NAMES-FILES-SUPPRESS-UNADOPTED",
    "summary": "install.gate-installed fires as pure noise on any foreign harness with no .temper/, and even when it legitimately fires it counts un-noted files without naming them.",
    "per": {
      "path": "specs/distribution.md",
      "section": "The placements and their enforcement modes"
    },
    "gate": {
      "kind": "open"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "gate_installed (520-578): (1) it always calls evaluate_placements or place_settings_only regardless of whether `.temper/` exists on disk at all; place_settings_only (588) unconditionally checks whether .claude/settings.json has the hook wired, firing on any repo lacking it even with zero temper adoption. Add an early `if !temper_dir.is_dir() { return Vec::new(); }` before line 523. (2) the notes/modelines tally (534-568) collapses to a bare count in the rendered message (570-577); thread each un-noted InstallEntry's own `path` into the message instead of only counting outcomes."
        },
        {
          "path": "tests/install.rs",
          "description": "gate_installed_never_scaffolds_and_reflects_represented_vs_not (949) and its neighbors (945-976): add a case for a harness with no `.temper/` directory at all, asserting gate_installed is empty; update an existing drifted-notes case to assert the message names the specific un-noted file(s), not just a count."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "gate_installed is silent on a harness carrying no .temper/ directory, and names the specific un-noted file(s) when it fires on an adopted-but-drifted harness"
      }
    ],
    "acceptance": "Checking a plain foreign repo with no .temper/ never emits install.gate-installed; an adopted harness missing managed-by notes reports which files, not just a count.",
    "notes": "Verified: install.rs 520-529 gate_installed calls evaluate_placements/place_settings_only unconditionally; place_settings_only (588) checks settings.json's hook wiring with no adoption check at all. Count-only message at 570-577 (parts.join). Closes probe finding 8 and dogfood finding 1 together — same function. scoped at 9d4ce81."
  },
  {
    "tag": "HOOK-COMMAND-FAILS-LOUD-ON-MISSING-TEMPER",
    "summary": "The placed SessionStart/guard hooks invoke bare `temper ...`; with no binary on PATH they die command-not-found silently, so the gate can never report its own absence.",
    "per": {
      "path": "specs/distribution.md",
      "section": "The placements and their enforcement modes"
    },
    "gate": {
      "kind": "blockedBy",
      "tag": "GATE-INSTALLED-NAMES-FILES-SUPPRESS-UNADOPTED"
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "src/install.rs",
          "description": "SESSION_START_COMMAND (92) and GUARD_COMMAND (138) are bare `temper ...` strings Claude Code's hook runs through a shell. Add a PATH-resolvability guard ahead of the real invocation (e.g. a `command -v temper` prefix that fails loud, naming `temper` as the missing binary, before attempting to run it) so a fresh clone with no `temper` on PATH gets a visible, named failure rather than a silent shell command-not-found — per distribution.md's fail-loud invariant ('a placement that cannot run the engine errors, never silently skips')."
        },
        {
          "path": "tests/install.rs",
          "description": "~8 call sites assert the literal command strings verbatim (lines 78, 244, 281, 322, 440, 768, 920, 924) — update each to the new guarded form."
        },
        {
          "path": "tests/session_start.rs",
          "description": "Line 360 splits SESSION_START_COMMAND on whitespace into an argv array for a direct-exec repro; a compound shell-guarded command breaks that split. Re-drive it through an actual shell invocation (or the hook's real dispatch path) instead."
        },
        {
          "path": "tests/bundle.rs",
          "description": "Line 64 asserts the plugin-shipped hooks.json carries the literal SESSION_START_COMMAND string verbatim; update to the new guarded form."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [
      {
        "path": "tests/install.rs",
        "asserts": "the placed SessionStart/guard hook command, run with `temper` absent from PATH, exits non-zero and prints a message naming `temper` as the missing binary rather than a bare shell command-not-found"
      }
    ],
    "acceptance": "A fresh clone with the hook wired but no `temper` on PATH gets a named, visible failure instead of a silent no-op.",
    "notes": "Verified: install.rs 92/138 SESSION_START_COMMAND/GUARD_COMMAND are bare `temper ...`; ~8 tests across install.rs/session_start.rs/bundle.rs assert the literal strings — the honest ripple. Serialized behind GATE-INSTALLED-NAMES-FILES-SUPPRESS-UNADOPTED: both entries edit install.rs, and the pending-entry rule bars two open entries sharing a file. scoped at 9d4ce81."
  },
  {
    "tag": "PACKAGING-CHANNELS-REMAINDER",
    "summary": "Packaging remainder after the linux-x64+win32-x64 first cut: darwin binaries (Apple notarizing) and the plugin/bundle + marketplace.json channel (channel 3).",
    "per": {
      "path": "specs/distribution.md",
      "section": "What ships — three channels"
    },
    "gate": {
      "kind": "parked",
      "reason": "Human release actions: John's Apple notarizing for the darwin standalone binary + darwin optionalDependency package. Channel 3 (plugin/bundle + marketplace.json) is a git repo + marketplace.json + free community web form (verified 2026-07-06, code.claude.com/docs) — no signing creds — but rides the same release-workflow build-out. release.yml (channel 2, linux-x64+win32-x64) is now proven live, not merely wired: the v0.0.11 tag fired it and its publish smoke gate went green (b6dc0a3's commit body)."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": ".github/workflows/release.yml",
          "description": "Extend the existing release.yml (linux-x64 + win32-x64 first cut, the darwin deferral stated at lines 7-9) with the darwin matrix targets (aarch64/x86_64-apple-darwin) + an Apple notarization step, and a channel-3 step producing/publishing the `temper bundle` output with its marketplace.json. Distinct from the standalone tag-asset step, already wired (unexercised until a tag)."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A stranger acquires temper on all three OSes without a Rust toolchain (darwin binary notarized + pinned), or via the plugin/bundle (marketplace.json) — distribution.md's v0.1 launch gate: prebuilt binaries install on all three OSes.",
    "notes": "Park re-tested on disk at 01ba369: release.yml still states the darwin + channel-3 deferral verbatim (lines 7-9). The lockstep clause carried here since ac40c72/b676656 is resolved, dropped: da77c60 walked Cargo.toml 0.1.0→0.0.11 to match npm's cadence directly (no v0.1-tag flip), b6dc0a3 fixed the lockfile mismatch, and tag v0.0.11→da77c60 fired release.yml with a green smoke gate per its own commit body — channel 2 is proven live, not just wired. scoped 01ba369."
  },
  {
    "tag": "GUIDANCE-FIELD-DECLARATION-CHANNEL",
    "summary": "0045 also frees guidance onto a field declaration, no predicate — deferred: none of the six promoted built-in notes targets a single field, so there is no consumer yet",
    "per": {
      "path": "specs/model/contract.md",
      "section": "clause"
    },
    "gate": {
      "kind": "deferred",
      "reason": "0045's Consequences name field-declaration guidance beside kind-level, but the SDK's only per-field metadata today is a clause's `field:` target (predicate-bound) or `EdgeField` (edge-typed only) — no 'field declaration' list a bare advisory could attach to. None of the six promoted notes (GUIDANCE-BUILTIN-NOTES-PROMOTE) target a single field, inventing the mechanism now is speculative scope with no kind to exercise it ('An export earns its consumer', engineering.md). Revive when a field wants counsel no clause carries."
    },
    "dependsOnForks": [],
    "files": {
      "new": [],
      "edit": [
        {
          "path": "sdk/src/kind.ts",
          "description": "Anticipated: a field-name-keyed guidance fact on `KindFacts`, analogous to `EdgeField`'s field-name-keyed list (196) but for arbitrary fields — shape unset; no field in any built-in kind wants one yet."
        },
        {
          "path": "src/drift.rs",
          "description": "Anticipated: a `KindFactRow` column (or sibling row) carrying per-field guidance/cite, following the `to_table`/`from_table` pattern GUIDANCE-KIND-SDK-LOCK-FACT establishes for the kind-level pair."
        },
        {
          "path": "src/schema.rs",
          "description": "Anticipated: `emit()`'s per-property `description` (156-172) folds field-declaration guidance in alongside a clause's, the same 'join into one description' discipline `push_description` (316-329) already runs for several guided clauses on one field."
        },
        {
          "path": "src/read.rs",
          "description": "Anticipated: field-level narration extends `narrate_governing_contract` (374-402) or a sibling to print a field's declared guidance."
        }
      ],
      "retire": []
    },
    "schemaDelta": "none",
    "tests": [],
    "acceptance": "A field declaring `guidance` (no predicate) surfaces it via `schema` hover and `explain`, never gating — provable once a real kind needs one.",
    "notes": "0045 Consequences name field-declaration guidance beside kind-level; this waits for a driver, the same 'no consumer' bar prior deferred entries in this queue's history have held to. scoped at 946e303."
  }
]
