<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.
-->

- **`(display-rule-emit-face)` RESOLVED — narrow slice (John, 07-03):** the
  display rule ships as its own engine slice, not riding the custom-kind
  emit face. Scope: a per-genre projection formatter over the manifest's
  genre tables (`[[member.genre]]` — leaves + keyed collections) rendering
  the fenced block in place in the projection — headings, Chosen/Rejected
  labels, ordering, anchors; connective tissue ONLY, every meaning-carrying
  word traces to an authored leaf (20-surface, "The display rule owns
  connective tissue"; the mention display rule is the pattern to reuse).
  Byte-deterministic, double-emit verified like every emit path. The
  island risk is accepted: the rule's contract is pinned by spec, so the
  future custom-kind emit face must reproduce its bytes when it absorbs
  it. Unblocks the genre-adoption pilot (a staged human ceremony — the
  pilot itself is NOT an entry).
