<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.
-->

- KERNEL RECUT (specs: e842a32 + be8e1bf) replaced the whole corpus: eight
  nouns in `specs/model/`, decisions evicted to `specs/decisions/` (outside
  your read path — read `git show e842a32 -- specs/decisions/` ONCE while
  deriving). `specs/decisions/0001-kernel-recut.md` carries the retirement
  map and a "Consequences — work the model demands of code" list: that list
  is the seed of the next chain (embedded locus; one edge enumeration;
  requirement as a member kind + its prose-never-persisted fix; the
  satisfier-set kind-blind/kind-typed bug; hooks/permissions/MCP kinds; the
  scalar `Kind` enum rename). Derive from the new body text, cite new paths.
- Open-questions reconcile — the kernel resolves or transforms several
  standing forks; prune per the lifecycle rule: `(hook-kind-locus)` RESOLVED
  (hooks are embedded members — `specs/model/representation.md`, nesting/
  locus orthogonality); `(place-three-state-retire)` RESOLVED toward collapse
  (drift is "one comparison in one vocabulary... no merge model" —
  `specs/model/pipeline.md`, Drift); `(edge-representation-unify)` is no
  longer a fork — "every edge resolves into one enumeration" is ratified body
  text (`specs/model/contract.md`, edge): file it as an entry;
  `(genre-fence-format)` genre is retired vocabulary — the concept is now the
  nested kind template; re-key or drop. Every surviving record cites retired
  spec paths — refresh cites while pruning.
- PACKAGING-CHANNELS (parked): cite moved — `specs/distribution.md`,
  "What ships — three channels". Still parked on human release creds; full
  rewrite when audited.
