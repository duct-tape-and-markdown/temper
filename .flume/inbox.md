<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.
-->
- (cascade module-carriage round trip, 2026-07-05 — the full journey landed: harness.ts + rules.ts/skills.ts + prose assets, emit → 21 byte-stable projections + lock, install's gate now truthful and installed, registry 0.0.2 consumed) new findings:
- (cascade) the member schema-gate reads only root temper.toml, never the lock's declaration rows: with a fully-authored .temper/ (lock present, 21 members) and temper.toml removed, `check` reports "checked 0 members" exit 0 — the live external repro of the wave-end symptom GATE-READ-LOCK-DEMOLITION is parked on. Cascade keeps a regenerated temper.toml beside the workspace as a stopgap; that dual-artifact requirement should die with the demolition.
- (cascade, positive datum) lock-fingerprint drift detection works externally end-to-end: hand-edit to a projection → config.stale, correct member attribution, correct guidance. Emit double-runs byte-identical (same lock sha across consecutive emits).
- (cascade) SDK writeEmit conflates workspace and harness root: lock.toml and .claude/** projections both land under one targetDir; a real repo needs lock→.temper/, projections→root. Cascade authors its own 20-line emit script calling emit() — writeEmit should take the two dirs.
- (cascade) the Skill face types only description/license; user-invocable, disable-model-invocation, argument-hint, allowed-tools are documented Claude Code fields in live use (11/11 cascade skills carry at least one). orderedFields round-trips them at runtime; cascade widens the init type locally. The face should carry them.
- (cascade) `temper emit` (engine) on a lockless workspace renders temper::drift::lock_read as an error yet exits 0 — CI can't trust the verdict. Also init scaffolds no .temper/ at all (no lock, no schema/) — the "on-ramp" leaves every workspace verb dangling until the SDK or a hand-touch bootstraps it; this is the "init skipped the full round trip" frustration.
- (cascade, 2026-07-06 — first external CUSTOM KINDS field report; specs/** is now a temper projection) Nine `kind<T>()` spec kinds, an expect floor (max_lines advisory), and four requirements compiled clean into the lock's declaration families; emit projected the 74-member corpus byte-faithful (zero-diff lift) and refuses on a lost law-anchor satisfier. What the engine can't see: custom kinds are invisible to `check` both ways — in-place carriage rejects them ("in-place member names non-built-in kind — in-place carriage is built-in-kind only", and that error EXITS 0, same class as the emit lock_read exit-0), and the lock's declaration rows aren't read for the by-kind gate (the parked demolition). Cascade evaluates its own clauses via an SDK-side script until then; when the demolition ships, delete-my-script is the acceptance test an external user hands you.
- (cascade) projectionPath footgun: an any-depth glob (`**/*.md`) flattens every member to `<root>/<name>.md` — a nested member (specs/ui/components/x.md) projects to the wrong path silently. Cascade dodged it with one kind per directory; a locus that can't address its own members should refuse at kind construction, not misproject.
