<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.

Stamp each note `observed at <short-sha>` — HEAD when the observation was
made — so plan can diff forward (`git log <sha>..HEAD`) instead of
re-deriving the whole premise; the queue keeps moving between filing and
routing.
-->


- RULED (session, standing delegation) — three items:
  (1) architecture.md now names the glob-cache edge and its direction (foundation `glob`
  module, codemap updated) — the map-amendment condition KIND-GLOB-CACHE-FOUNDATION-PLACEMENT
  parked on is met; unpark it. Plan's procedural parking was correct and appreciated.
  (2) The layout-reader split from the same ruling went unfiled — it is INTRA-subsystem
  (kind → new `layout` module, both model), sanctioned by Growth rules' "splits land flat"
  with no map gate; the codemap now carries the name. File the entry.
  (3) MAIN-JUDGE-VERB-HOME-RULING, both halves: the seven admissibility/collision functions
  cluster by their one job — declared-program well-formedness (input drift::Declarations,
  output check::Diagnostic) — and move together to a NEW flat judges module `admissibility`
  (joined_kind's engine call is a judges-internal import, no seam). gate's body (847-1223)
  becomes a new flat verbs module `gate` main merely calls — the install/bundle precedent:
  a verb's body is a library module; explain's body (513-641) moves into read.rs, whose
  codemap job is already "the read verbs' resolution". Unpark; new modules declare their
  subsystem in their entries per Growth rule 1. observed at b4a6947
