<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.
-->

- 2026-07-04 (John, session): **the demolition wave is UNBLOCKED — "let's
  go."** John's ruling, verbatim intent: no further foundation ceremony is
  owed — the merged six-noun corpus (PR #2 + the alignment sweeps) IS the
  ratified foundation, and "check before clearing house" caution is not
  wanted: **git is the rollback story**; the per-tick gates (fmt, clippy,
  test, sdk) are the only technical gates. Plan should now file the full
  demolition + rebuild against the corpus as ordinary evidence-free-to-
  decompose chains, sequenced only by build order, e.g.: the lock grows its
  declaration rows; the gate's corpus becomes committed-artifacts-plus-lock;
  the SDK re-cuts to the corpus face (harness(), kind<T>(), clause values,
  needs, file()/text/blocks, one JSON pipe); the CLI collapses (explain
  lands; import/diff/session-start-verb/why/requirements/impact/context
  retire; temper guard lands); the manifest machinery retires
  (temper.toml-as-corpus, roster.toml/bindings.toml + this morning's reader,
  contract/ byte-parity corpus); KIND.md/PACKAGE.md parsing retires.
  - The dogfood self-gate is deactivated for the wave (chain.ts, this
    commit) per 90-spec-system's engine-wave provision; the dogfood's
    committed artifacts go stale by design. The session runs the wave-end
    confirmation and re-arms.
  - Fence-side moves stay the session's hands (they are outside build's
    writable paths, a mechanical fact, not a ceremony): once the new emit
    lands, the session migrates the .claude dogfood and then the specs
    corpus onto the SDK face and retires .temper/ document carriage.
