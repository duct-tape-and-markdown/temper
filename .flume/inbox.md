<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.

Stamp each note `observed at <short-sha>` — HEAD when the observation was
made — so plan can diff forward (`git log <sha>..HEAD`) instead of
re-deriving the whole premise; the queue keeps moving between filing and
routing.
-->

- Field (centercode, standards-as-layout-source prototype; **ruled —
  decision 0038, derive from the record**): one path, two claimants — a
  consumer kind's exact-path locus and skill's `supporting-doc` template
  both materialize `.claude/skills/jobs/conventions.md`, so a phantom
  `supporting-doc:conventions` twin rides beside the working layout member
  (coverage counts it, `explain` narrates three members, and
  `supporting-doc.degree` fires at 0 incoming on the phantom since the real
  edge targets the layout member). Ruling: a path a declared kind's locus
  claims leaves the template's discovery — exactly that path; the
  declaration-beats-presumption family's third instance. Declared-vs-
  declared stays the existing governs-collision refusal. observed at
  4cc3081

- Field (same prototype; **ruled — decision 0038, derive from the
  record**): a star-directory glob has no name-splice, so a layout-source
  kind cannot scale past one member — the SDK splice refuses
  `*/conventions.md` at root `.claude/skills`, and stem-derived identity
  would name every member `conventions`. Consumer capped the kind at one
  exact-path declaration per member. Ruling: single-`*` directory globs
  admitted with identity from the starred segment — `skill`'s own
  `UnitShape::Directory` identity rule generalized to every author's
  constructor (ownership, not privilege); identity source becomes the
  kind's declared fact, stem or starred segment, never inferred. observed
  at 4cc3081
