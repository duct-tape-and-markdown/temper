<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.
-->
- (from cascade adoption, 2026-07-05, first external harness) `install`/`guard` assume emit carriage universally: on an all-in-place harness (init → root `temper.toml`, no `.temper/`), install stamps the managed-by note "edit the owning .temper/ module and re-run temper emit, never this generated file" and `guard` emits ".claude/ is projected from the .temper/ surface" on every Write/Edit — but the files ARE the authored source. Actively misleads agent sessions (cascade's flume ticks would refuse legit harness edits); cascade reverted install, kept manifest+check. Per 20-surface guard binds *projections* — in-place members aren't. Placements/messages need carriage awareness.
- (cascade) phantom `agents-md.memory (1)` in manifest-mode `check` coverage; repo has no AGENTS.md anywhere (`--harness` mode correctly says 0). Root `temper.toml` has 21 members; manifest check reports 22.
- (cascade) `explain` reads a different seam than `check`: after init wrote root `temper.toml`, `temper explain member:CLAUDE` → "no member in the surface; import first" while bare `temper check` resolves 22 members in the same cwd.
- (cascade) install's schema modelines point at `.temper/schema/{skill,rule}.json` but nothing materializes them (`schema` prints to stdout; init writes no `.temper/`) — the keystroke gate dangles on an in-place harness.
