<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.
-->


- KIND-PROSE-RECONCILE: done by hand (kinds/ is curated territory — the fence
  revert was correct). Both KIND.md bodies now state the shipped reality
  (embedded, composed through the generic path, discovery off governs; the
  hand-coded extractors gone). Do not re-file.
