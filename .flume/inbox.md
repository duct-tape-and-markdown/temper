<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.

Stamp each note `observed at <short-sha>` — HEAD when the observation was
made — so plan can diff forward (`git log <sha>..HEAD`) instead of
re-deriving the whole premise; the queue keeps moving between filing and
routing.
-->

- RULED (session, standing delegation): the kind-cohesion capture — split the two
  clear-cut strangers, keep the algebra whole. The layout-document reader (Layout family,
  284-565, depends only on extract) extracts to a new flat `layout` module, model
  subsystem. The glob-compilation cache (compile_glob/GLOB_CACHE/pins, 1223-1284) is
  crate-wide leaf infra with three external callers by its own doc — it extracts to a new
  flat foundation module (`glob`), which the foundation invariant admits (globset-only,
  knows no harness). The extraction-primitive algebra STAYS in kind.rs: a kind's extractor
  composed from its data is the kind algebra's own job — splitting it would divide one
  concept over two homes, the disease not the cure. Delete the capture on filing.
  observed at 065c454
