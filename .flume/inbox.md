<!--
Inbox — external notes for the next `plan` tick to route into pending,
open-questions, or accepted debt. Humans append lines here; plan drains and
removes them each tick. Empty is the normal state.
-->

- DECLARED-FRONTMATTER-ADAPTER un-park condition met: the curated
  format/unit_shape lines are IN (kinds/skill, kinds/rule — see the packages:
  commit). Flip it open and run the swap. Also: specs/45-governance.md gained
  the Decision "coupling is a join — a one-way edge never obligates its
  target" (join-primacy; 05-model names `join`) — doctrine, no engine work
  expected beyond what the classed-corpus entries already cover; reconcile
  breadcrumbs only.
